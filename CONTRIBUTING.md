## Clanker

Use [crush](https://charm.land/crush/) with the `deepseek-v4-flash` model ([hyper](https://hyper.charm.land)).
Make sure you have the `carapace` skill.

1. ensure dockerd is running
2. tell to `add a scraper for https://some/repo/url`
