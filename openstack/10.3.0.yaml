# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: openstack
flags:
    --debug: Show tracebacks on errors.
    --insecure: Disable server certificate verification
    --log-file=: Specify a file to log output.
    --os-access-token-endpoint=: 'With v3oidcclientcredentials: OpenID Connect Provider Token Endpoint.'
    --os-access-token-type=: 'With v3oidcclientcredentials: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection.'
    --os-access-token=: 'With v3oidcaccesstoken: OAuth 2.0 Access Token'
    --os-application-credential-id=: 'With v3applicationcredential: Application credential ID'
    --os-application-credential-name=: 'With v3applicationcredential: Application credential name'
    --os-application-credential-secret=: 'With v3applicationcredential: Application credential auth secret'
    --os-auth-methods=: 'With v3multifactor: Methods to authenticate with.'
    --os-auth-type=: Select an authentication type.
    --os-auth-url=: 'With v3token: Authentication URL'
    --os-beta-command: Enable beta commands which are subject to change
    --os-cacert=: 'CA certificate bundle file (Env: OS_CACERT)'
    --os-cert=: 'Client certificate bundle file (Env: OS_CERT)'
    --os-client-id=: 'With v3oidcclientcredentials: OAuth 2.0 Client ID'
    --os-client-secret=: 'With v3oidcclientcredentials: OAuth 2.0 Client Secret'
    --os-cloud=: 'Cloud name in clouds.yaml (Env: OS_CLOUD)'
    --os-code-challenge-method=: 'With v3oidcdeviceauthz: PKCE Challenge Method (RFC 7636)'
    --os-code=: 'With v3oidcauthcode: OAuth 2.0 Authorization Code'
    --os-compute-api-version=: 'Compute API version, default=2.1 (Env: OS_COMPUTE_API_VERSION)'
    --os-default-domain-id=: 'With token: Optional domain ID to use with v3 and v2 parameters.'
    --os-default-domain-name=: 'With token: Optional domain name to use with v3 API and v2 parameters.'
    --os-default-domain=: Default domain ID, default=default.
    --os-device-authorization-endpoint=: 'With v3oidcdeviceauthz: OAuth 2.0 Device Authorization Endpoint.'
    --os-discovery-endpoint=: 'With v3oidcclientcredentials: OpenID Connect Discovery Document URL.'
    --os-domain-id=: 'With v3token: Domain ID to scope to'
    --os-domain-name=: 'With v3token: Domain name to scope to'
    --os-endpoint-override=: Use this API endpoint instead of the Service Catalog.
    --os-endpoint=: 'With none: The endpoint that will always be used'
    --os-identity-api-version=: 'Identity API version, default=3 (Env: OS_IDENTITY_API_VERSION)'
    --os-identity-provider=: 'With v3oidcclientcredentials: Identity Provider''s name'
    --os-idp-otp-key=: 'With v3oidcpassword: A key to be used in the Identity Provider access token endpoint to pass the OTP value.'
    --os-image-api-version=: 'Image API version, default=2 (Env: OS_IMAGE_API_VERSION)'
    --os-interface=: Select an interface type.
    --os-key=: 'Client certificate key file (Env: OS_KEY)'
    --os-login-timeout=: 'With v3websso: Seconds to wait for the user to finish authenticating in the browser before giving up.'
    --os-network-api-version=: 'Network API version, default=2.0 (Env: OS_NETWORK_API_VERSION)'
    --os-oauth2-client-id=: 'With v3oauth2mtlsclientcredential: Client credential ID for OAuth2.0 Mutual-TLS Authorization'
    --os-oauth2-client-secret=: 'With v3oauth2clientcredential: Client secret for OAuth2.0'
    --os-oauth2-endpoint=: 'With v3oauth2mtlsclientcredential: Endpoint for OAuth2.0 Mutual-TLS Authorization'
    --os-object-api-version=: 'Object API version, default=1 (Env: OS_OBJECT_API_VERSION)'
    --os-openid-scope=: 'With v3oidcclientcredentials: OpenID Connect scope that is requested from authorization server.'
    --os-passcode=: 'With v3totp: User''s TOTP passcode'
    --os-password=: 'With v2password: Password to use'
    --os-project-domain-id=: 'With v3token: Domain ID containing project'
    --os-project-domain-name=: 'With v3token: Domain name containing project'
    --os-project-id=: 'With v3token: Project ID to scope to'
    --os-project-name=: 'With v3token: Project name to scope to'
    --os-protocol=: 'With v3oidcclientcredentials: Protocol for federated plugin'
    --os-redirect-host=: 'With v3websso: Host the callback server listens on.'
    --os-redirect-port=: 'With v3websso: Port the callback server listens on.'
    --os-redirect-uri=: 'With v3oidcauthcode: OpenID Connect Redirect URL'
    --os-region-name=: 'Authentication region name (Env: OS_REGION_NAME)'
    --os-remote-project-domain-id=: Domain ID of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
    --os-remote-project-domain-name=: Domain name of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
    --os-remote-project-id=: Project ID when authenticating to a service provider if using Keystone-to-Keystone federation.
    --os-remote-project-name=: Project name when authenticating to a service provider if using Keystone-to-Keystone federation.
    --os-service-provider=: Authenticate with and perform the command on a service provider using Keystone-to-keystone federation.
    --os-share-api-version=: 'Shared File System API version, default=2.98 (version supported by both the client and the server) (Env: OS_SHARE_API_VERSION)'
    --os-system-scope=: 'With v3token: Scope for system operations'
    --os-tenant-id=: ==SUPPRESS==
    --os-tenant-name=: ==SUPPRESS==
    --os-token=: 'With v3token: Token to authenticate with'
    --os-trust-id=: 'With v3token: ID of the trust to use as a trustee use'
    --os-user-domain-id=: 'With v3totp: User''s domain ID'
    --os-user-domain-name=: 'With v3totp: User''s domain name'
    --os-user-id=: 'With v2password: User ID to login with'
    --os-username=: 'With v2password: Username to login with'
    --os-volume-api-version=: 'Volume API version, default=3 (Env: OS_VOLUME_API_VERSION)'
    --timing: Print API call timing info
    --verify: Verify server certificate (default)
    --version=: show program's version number and exit
    -q, --quiet: Suppress output except warnings and errors.
    -v, --verbose: Increase verbosity of output.
completion:
    flag:
        os-auth-type:
            - v3token
            - v3tokenlessauth
            - v2token
            - v3oidcclientcredentials
            - none
            - v2password
            - v3multifactor
            - v3oidcpassword
            - v3oidcaccesstoken
            - v3oidcauthcode
            - v3websso
            - v3oauth2mtlsclientcredential
            - v3oidcdeviceauthz
            - v3totp
            - v3applicationcredential
            - admin_token
            - v3oauth2clientcredential
            - http_basic
            - token
            - password
            - v3password
        os-interface:
            - admin
            - public
            - internal
        os-share-api-version:
            - "2.0"
            - "2.1"
            - "2.2"
            - "2.3"
            - "2.4"
            - "2.5"
            - "2.6"
            - "2.7"
            - "2.8"
            - "2.9"
            - "2.10"
            - "2.11"
            - "2.12"
            - "2.13"
            - "2.14"
            - "2.15"
            - "2.16"
            - "2.17"
            - "2.18"
            - "2.19"
            - "2.20"
            - "2.21"
            - "2.22"
            - "2.23"
            - "2.24"
            - "2.25"
            - "2.26"
            - "2.27"
            - "2.28"
            - "2.29"
            - "2.30"
            - "2.31"
            - "2.32"
            - "2.33"
            - "2.34"
            - "2.35"
            - "2.36"
            - "2.37"
            - "2.38"
            - "2.39"
            - "2.40"
            - "2.41"
            - "2.42"
            - "2.43"
            - "2.44"
            - "2.45"
            - "2.46"
            - "2.47"
            - "2.48"
            - "2.49"
            - "2.50"
            - "2.51"
            - "2.52"
            - "2.53"
            - "2.54"
            - "2.55"
            - "2.56"
            - "2.57"
            - "2.58"
            - "2.59"
            - "2.60"
            - "2.61"
            - "2.62"
            - "2.63"
            - "2.64"
            - "2.65"
            - "2.66"
            - "2.67"
            - "2.68"
            - "2.69"
            - "2.70"
            - "2.71"
            - "2.72"
            - "2.73"
            - "2.74"
            - "2.75"
            - "2.76"
            - "2.77"
            - "2.78"
            - "2.79"
            - "2.80"
            - "2.81"
            - "2.82"
            - "2.83"
            - "2.84"
            - "2.85"
            - "2.86"
            - "2.87"
            - "2.88"
            - "2.89"
            - "2.90"
            - "2.91"
            - "2.92"
            - "2.93"
            - "2.94"
            - "2.95"
            - "2.96"
            - "2.97"
            - "2.98"
commands:
    - name: ""
      flags:
        --debug: Show tracebacks on errors.
        --insecure: Disable server certificate verification
        --log-file=: Specify a file to log output.
        --os-access-token-endpoint=: 'With v3oidcclientcredentials: OpenID Connect Provider Token Endpoint.'
        --os-access-token-type=: 'With v3oidcclientcredentials: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection.'
        --os-access-token=: 'With v3oidcaccesstoken: OAuth 2.0 Access Token'
        --os-application-credential-id=: 'With v3applicationcredential: Application credential ID'
        --os-application-credential-name=: 'With v3applicationcredential: Application credential name'
        --os-application-credential-secret=: 'With v3applicationcredential: Application credential auth secret'
        --os-auth-methods=: 'With v3multifactor: Methods to authenticate with.'
        --os-auth-type=: Select an authentication type.
        --os-auth-url=: 'With v3token: Authentication URL'
        --os-beta-command: Enable beta commands which are subject to change
        --os-cacert=: 'CA certificate bundle file (Env: OS_CACERT)'
        --os-cert=: 'Client certificate bundle file (Env: OS_CERT)'
        --os-client-id=: 'With v3oidcclientcredentials: OAuth 2.0 Client ID'
        --os-client-secret=: 'With v3oidcclientcredentials: OAuth 2.0 Client Secret'
        --os-cloud=: 'Cloud name in clouds.yaml (Env: OS_CLOUD)'
        --os-code-challenge-method=: 'With v3oidcdeviceauthz: PKCE Challenge Method (RFC 7636)'
        --os-code=: 'With v3oidcauthcode: OAuth 2.0 Authorization Code'
        --os-compute-api-version=: 'Compute API version, default=2.1 (Env: OS_COMPUTE_API_VERSION)'
        --os-default-domain-id=: 'With token: Optional domain ID to use with v3 and v2 parameters.'
        --os-default-domain-name=: 'With token: Optional domain name to use with v3 API and v2 parameters.'
        --os-default-domain=: Default domain ID, default=default.
        --os-device-authorization-endpoint=: 'With v3oidcdeviceauthz: OAuth 2.0 Device Authorization Endpoint.'
        --os-discovery-endpoint=: 'With v3oidcclientcredentials: OpenID Connect Discovery Document URL.'
        --os-domain-id=: 'With v3token: Domain ID to scope to'
        --os-domain-name=: 'With v3token: Domain name to scope to'
        --os-endpoint-override=: Use this API endpoint instead of the Service Catalog.
        --os-endpoint=: 'With none: The endpoint that will always be used'
        --os-identity-api-version=: 'Identity API version, default=3 (Env: OS_IDENTITY_API_VERSION)'
        --os-identity-provider=: 'With v3oidcclientcredentials: Identity Provider''s name'
        --os-idp-otp-key=: 'With v3oidcpassword: A key to be used in the Identity Provider access token endpoint to pass the OTP value.'
        --os-image-api-version=: 'Image API version, default=2 (Env: OS_IMAGE_API_VERSION)'
        --os-interface=: Select an interface type.
        --os-key=: 'Client certificate key file (Env: OS_KEY)'
        --os-login-timeout=: 'With v3websso: Seconds to wait for the user to finish authenticating in the browser before giving up.'
        --os-network-api-version=: 'Network API version, default=2.0 (Env: OS_NETWORK_API_VERSION)'
        --os-oauth2-client-id=: 'With v3oauth2mtlsclientcredential: Client credential ID for OAuth2.0 Mutual-TLS Authorization'
        --os-oauth2-client-secret=: 'With v3oauth2clientcredential: Client secret for OAuth2.0'
        --os-oauth2-endpoint=: 'With v3oauth2mtlsclientcredential: Endpoint for OAuth2.0 Mutual-TLS Authorization'
        --os-object-api-version=: 'Object API version, default=1 (Env: OS_OBJECT_API_VERSION)'
        --os-openid-scope=: 'With v3oidcclientcredentials: OpenID Connect scope that is requested from authorization server.'
        --os-passcode=: 'With v3totp: User''s TOTP passcode'
        --os-password=: 'With v2password: Password to use'
        --os-project-domain-id=: 'With v3token: Domain ID containing project'
        --os-project-domain-name=: 'With v3token: Domain name containing project'
        --os-project-id=: 'With v3token: Project ID to scope to'
        --os-project-name=: 'With v3token: Project name to scope to'
        --os-protocol=: 'With v3oidcclientcredentials: Protocol for federated plugin'
        --os-redirect-host=: 'With v3websso: Host the callback server listens on.'
        --os-redirect-port=: 'With v3websso: Port the callback server listens on.'
        --os-redirect-uri=: 'With v3oidcauthcode: OpenID Connect Redirect URL'
        --os-region-name=: 'Authentication region name (Env: OS_REGION_NAME)'
        --os-remote-project-domain-id=: Domain ID of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
        --os-remote-project-domain-name=: Domain name of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
        --os-remote-project-id=: Project ID when authenticating to a service provider if using Keystone-to-Keystone federation.
        --os-remote-project-name=: Project name when authenticating to a service provider if using Keystone-to-Keystone federation.
        --os-service-provider=: Authenticate with and perform the command on a service provider using Keystone-to-keystone federation.
        --os-share-api-version=: 'Shared File System API version, default=2.98 (version supported by both the client and the server) (Env: OS_SHARE_API_VERSION)'
        --os-system-scope=: 'With v3token: Scope for system operations'
        --os-tenant-id=: ==SUPPRESS==
        --os-tenant-name=: ==SUPPRESS==
        --os-token=: 'With v3token: Token to authenticate with'
        --os-trust-id=: 'With v3token: ID of the trust to use as a trustee use'
        --os-user-domain-id=: 'With v3totp: User''s domain ID'
        --os-user-domain-name=: 'With v3totp: User''s domain name'
        --os-user-id=: 'With v2password: User ID to login with'
        --os-username=: 'With v2password: Username to login with'
        --os-volume-api-version=: 'Volume API version, default=3 (Env: OS_VOLUME_API_VERSION)'
        --timing: Print API call timing info
        --verify: Verify server certificate (default)
        --version=: show program's version number and exit
        -q, --quiet: Suppress output except warnings and errors.
        -v, --verbose: Increase verbosity of output.
      completion:
        flag:
            os-auth-type:
                - v3token
                - v3tokenlessauth
                - v2token
                - v3oidcclientcredentials
                - none
                - v2password
                - v3multifactor
                - v3oidcpassword
                - v3oidcaccesstoken
                - v3oidcauthcode
                - v3websso
                - v3oauth2mtlsclientcredential
                - v3oidcdeviceauthz
                - v3totp
                - v3applicationcredential
                - admin_token
                - v3oauth2clientcredential
                - http_basic
                - token
                - password
                - v3password
            os-interface:
                - admin
                - public
                - internal
            os-share-api-version:
                - "2.0"
                - "2.1"
                - "2.2"
                - "2.3"
                - "2.4"
                - "2.5"
                - "2.6"
                - "2.7"
                - "2.8"
                - "2.9"
                - "2.10"
                - "2.11"
                - "2.12"
                - "2.13"
                - "2.14"
                - "2.15"
                - "2.16"
                - "2.17"
                - "2.18"
                - "2.19"
                - "2.20"
                - "2.21"
                - "2.22"
                - "2.23"
                - "2.24"
                - "2.25"
                - "2.26"
                - "2.27"
                - "2.28"
                - "2.29"
                - "2.30"
                - "2.31"
                - "2.32"
                - "2.33"
                - "2.34"
                - "2.35"
                - "2.36"
                - "2.37"
                - "2.38"
                - "2.39"
                - "2.40"
                - "2.41"
                - "2.42"
                - "2.43"
                - "2.44"
                - "2.45"
                - "2.46"
                - "2.47"
                - "2.48"
                - "2.49"
                - "2.50"
                - "2.51"
                - "2.52"
                - "2.53"
                - "2.54"
                - "2.55"
                - "2.56"
                - "2.57"
                - "2.58"
                - "2.59"
                - "2.60"
                - "2.61"
                - "2.62"
                - "2.63"
                - "2.64"
                - "2.65"
                - "2.66"
                - "2.67"
                - "2.68"
                - "2.69"
                - "2.70"
                - "2.71"
                - "2.72"
                - "2.73"
                - "2.74"
                - "2.75"
                - "2.76"
                - "2.77"
                - "2.78"
                - "2.79"
                - "2.80"
                - "2.81"
                - "2.82"
                - "2.83"
                - "2.84"
                - "2.85"
                - "2.86"
                - "2.87"
                - "2.88"
                - "2.89"
                - "2.90"
                - "2.91"
                - "2.92"
                - "2.93"
                - "2.94"
                - "2.95"
                - "2.96"
                - "2.97"
                - "2.98"
      documentation:
        flag:
            debug: Show tracebacks on errors.
            insecure: Disable server certificate verification
            log-file: Specify a file to log output. Disabled by default.
            os-access-token: |-
                With v3oidcaccesstoken: OAuth 2.0 Access Token

                (Env: OS_ACCESS_TOKEN)
            os-access-token-endpoint: |-
                With v3oidcclientcredentials: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.
                With v3oidcpassword: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.
                With v3oidcauthcode: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.
                With v3oidcdeviceauthz: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.

                (Env: OS_ACCESS_TOKEN_ENDPOINT)
            os-access-token-type: |-
                With v3oidcclientcredentials: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"
                With v3oidcpassword: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"
                With v3oidcauthcode: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"
                With v3oidcdeviceauthz: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"

                (Env: OS_ACCESS_TOKEN_TYPE)
            os-application-credential-id: |-
                With v3applicationcredential: Application credential ID

                (Env: OS_APPLICATION_CREDENTIAL_ID)
            os-application-credential-name: |-
                With v3applicationcredential: Application credential name

                (Env: OS_APPLICATION_CREDENTIAL_NAME)
            os-application-credential-secret: |-
                With v3applicationcredential: Application credential auth secret

                (Env: OS_APPLICATION_CREDENTIAL_SECRET)
            os-auth-methods: |-
                With v3multifactor: Methods to authenticate with.

                (Env: OS_AUTH_METHODS)
            os-auth-type: 'Select an authentication type. Available types: v3token, v3tokenlessauth, v2token, v3oidcclientcredentials, none, v2password, v3multifactor, v3oidcpassword, v3oidcaccesstoken, v3oidcauthcode, v3websso, v3oauth2mtlsclientcredential, v3oidcdeviceauthz, v3totp, v3applicationcredential, admin_token, v3oauth2clientcredential, http_basic, token, password, v3password. Default: selected based on --os-username/--os-token (Env: OS_AUTH_TYPE)'
            os-auth-url: |-
                With v3token: Authentication URL
                With v3tokenlessauth: Authentication URL
                With v2token: Authentication URL
                With v3oidcclientcredentials: Authentication URL
                With v2password: Authentication URL
                With v3multifactor: Authentication URL
                With v3oidcpassword: Authentication URL
                With v3oidcaccesstoken: Authentication URL
                With v3oidcauthcode: Authentication URL
                With v3websso: Authentication URL
                With v3oauth2mtlsclientcredential: Authentication URL
                With v3oidcdeviceauthz: Authentication URL
                With v3totp: Authentication URL
                With v3applicationcredential: Authentication URL
                With v3oauth2clientcredential: Authentication URL
                With token: Authentication URL
                With password: Authentication URL
                With v3password: Authentication URL

                (Env: OS_AUTH_URL)
            os-beta-command: Enable beta commands which are subject to change
            os-cacert: 'CA certificate bundle file (Env: OS_CACERT)'
            os-cert: 'Client certificate bundle file (Env: OS_CERT)'
            os-client-id: |-
                With v3oidcclientcredentials: OAuth 2.0 Client ID
                With v3oidcpassword: OAuth 2.0 Client ID
                With v3oidcauthcode: OAuth 2.0 Client ID
                With v3oidcdeviceauthz: OAuth 2.0 Client ID

                (Env: OS_CLIENT_ID)
            os-client-secret: |-
                With v3oidcclientcredentials: OAuth 2.0 Client Secret
                With v3oidcpassword: OAuth 2.0 Client Secret
                With v3oidcauthcode: OAuth 2.0 Client Secret
                With v3oidcdeviceauthz: OAuth 2.0 Client Secret

                (Env: OS_CLIENT_SECRET)
            os-cloud: 'Cloud name in clouds.yaml (Env: OS_CLOUD)'
            os-code: |-
                With v3oidcauthcode: OAuth 2.0 Authorization Code

                (Env: OS_CODE)
            os-code-challenge-method: |-
                With v3oidcdeviceauthz: PKCE Challenge Method (RFC 7636)

                (Env: OS_CODE_CHALLENGE_METHOD)
            os-compute-api-version: 'Compute API version, default=2.1 (Env: OS_COMPUTE_API_VERSION)'
            os-default-domain: 'Default domain ID, default=default. (Env: OS_DEFAULT_DOMAIN)'
            os-default-domain-id: |-
                With token: Optional domain ID to use with v3 and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.
                With password: Optional domain ID to use with v3 and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.

                (Env: OS_DEFAULT_DOMAIN_ID)
            os-default-domain-name: |-
                With token: Optional domain name to use with v3 API and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.
                With password: Optional domain name to use with v3 API and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.

                (Env: OS_DEFAULT_DOMAIN_NAME)
            os-device-authorization-endpoint: |-
                With v3oidcdeviceauthz: OAuth 2.0 Device Authorization Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.

                (Env: OS_DEVICE_AUTHORIZATION_ENDPOINT)
            os-discovery-endpoint: |-
                With v3oidcclientcredentials: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration
                With v3oidcpassword: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration
                With v3oidcauthcode: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration
                With v3oidcdeviceauthz: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration

                (Env: OS_DISCOVERY_ENDPOINT)
            os-domain-id: |-
                With v3token: Domain ID to scope to
                With v3tokenlessauth: Domain ID to scope to
                With v3oidcclientcredentials: Domain ID to scope to
                With v3multifactor: Domain ID to scope to
                With v3oidcpassword: Domain ID to scope to
                With v3oidcaccesstoken: Domain ID to scope to
                With v3oidcauthcode: Domain ID to scope to
                With v3websso: Domain ID to scope to
                With v3oauth2mtlsclientcredential: Domain ID to scope to
                With v3oidcdeviceauthz: Domain ID to scope to
                With v3totp: Domain ID to scope to
                With v3applicationcredential: Domain ID to scope to
                With v3oauth2clientcredential: Domain ID to scope to
                With token: Domain ID to scope to
                With password: Domain ID to scope to
                With v3password: Domain ID to scope to

                (Env: OS_DOMAIN_ID)
            os-domain-name: |-
                With v3token: Domain name to scope to
                With v3tokenlessauth: Domain name to scope to
                With v3oidcclientcredentials: Domain name to scope to
                With v3multifactor: Domain name to scope to
                With v3oidcpassword: Domain name to scope to
                With v3oidcaccesstoken: Domain name to scope to
                With v3oidcauthcode: Domain name to scope to
                With v3websso: Domain name to scope to
                With v3oauth2mtlsclientcredential: Domain name to scope to
                With v3oidcdeviceauthz: Domain name to scope to
                With v3totp: Domain name to scope to
                With v3applicationcredential: Domain name to scope to
                With v3oauth2clientcredential: Domain name to scope to
                With token: Domain name to scope to
                With password: Domain name to scope to
                With v3password: Domain name to scope to

                (Env: OS_DOMAIN_NAME)
            os-endpoint: |-
                With none: The endpoint that will always be used
                With admin_token: The endpoint that will always be used
                With http_basic: The endpoint that will always be used

                (Env: OS_ENDPOINT)
            os-endpoint-override: Use this API endpoint instead of the Service Catalog. Defaults to env[OS_SHARED_FILE_SYSTEM_ENDPOINT_OVERRIDE].
            os-identity-api-version: 'Identity API version, default=3 (Env: OS_IDENTITY_API_VERSION)'
            os-identity-provider: |-
                With v3oidcclientcredentials: Identity Provider's name
                With v3oidcpassword: Identity Provider's name
                With v3oidcaccesstoken: Identity Provider's name
                With v3oidcauthcode: Identity Provider's name
                With v3websso: Identity Provider's name
                With v3oidcdeviceauthz: Identity Provider's name

                (Env: OS_IDENTITY_PROVIDER)
            os-idp-otp-key: |-
                With v3oidcpassword: A key to be used in the Identity Provider access token endpoint to pass the OTP value. E.g. totp

                (Env: OS_IDP_OTP_KEY)
            os-image-api-version: 'Image API version, default=2 (Env: OS_IMAGE_API_VERSION)'
            os-interface: 'Select an interface type. Valid interface types: [admin, public, internal]. default=public, (Env: OS_INTERFACE)'
            os-key: 'Client certificate key file (Env: OS_KEY)'
            os-login-timeout: |-
                With v3websso: Seconds to wait for the user to finish authenticating in the browser before giving up. Raise it when a first login or MFA prompt is likely to take longer than the default.

                (Env: OS_LOGIN_TIMEOUT)
            os-network-api-version: 'Network API version, default=2.0 (Env: OS_NETWORK_API_VERSION)'
            os-oauth2-client-id: |-
                With v3oauth2mtlsclientcredential: Client credential ID for OAuth2.0 Mutual-TLS Authorization
                With v3oauth2clientcredential: Client id for OAuth2.0

                (Env: OS_OAUTH2_CLIENT_ID)
            os-oauth2-client-secret: |-
                With v3oauth2clientcredential: Client secret for OAuth2.0

                (Env: OS_OAUTH2_CLIENT_SECRET)
            os-oauth2-endpoint: |-
                With v3oauth2mtlsclientcredential: Endpoint for OAuth2.0 Mutual-TLS Authorization
                With v3oauth2clientcredential: Endpoint for OAuth2.0

                (Env: OS_OAUTH2_ENDPOINT)
            os-object-api-version: 'Object API version, default=1 (Env: OS_OBJECT_API_VERSION)'
            os-openid-scope: |-
                With v3oidcclientcredentials: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.
                With v3oidcpassword: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.
                With v3oidcauthcode: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.
                With v3oidcdeviceauthz: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.

                (Env: OS_OPENID_SCOPE)
            os-passcode: |-
                With v3totp: User's TOTP passcode

                (Env: OS_PASSCODE)
            os-password: |-
                With v2password: Password to use
                With v3oidcpassword: Password
                With http_basic: User's password
                With password: User's password
                With v3password: User's password

                (Env: OS_PASSWORD)
            os-project-domain-id: |-
                With v3token: Domain ID containing project
                With v3tokenlessauth: Domain ID containing project
                With v3oidcclientcredentials: Domain ID containing project
                With v3multifactor: Domain ID containing project
                With v3oidcpassword: Domain ID containing project
                With v3oidcaccesstoken: Domain ID containing project
                With v3oidcauthcode: Domain ID containing project
                With v3websso: Domain ID containing project
                With v3oauth2mtlsclientcredential: Domain ID containing project
                With v3oidcdeviceauthz: Domain ID containing project
                With v3totp: Domain ID containing project
                With v3applicationcredential: Domain ID containing project
                With v3oauth2clientcredential: Domain ID containing project
                With token: Domain ID containing project
                With password: Domain ID containing project
                With v3password: Domain ID containing project

                (Env: OS_PROJECT_DOMAIN_ID)
            os-project-domain-name: |-
                With v3token: Domain name containing project
                With v3tokenlessauth: Domain name containing project
                With v3oidcclientcredentials: Domain name containing project
                With v3multifactor: Domain name containing project
                With v3oidcpassword: Domain name containing project
                With v3oidcaccesstoken: Domain name containing project
                With v3oidcauthcode: Domain name containing project
                With v3websso: Domain name containing project
                With v3oauth2mtlsclientcredential: Domain name containing project
                With v3oidcdeviceauthz: Domain name containing project
                With v3totp: Domain name containing project
                With v3applicationcredential: Domain name containing project
                With v3oauth2clientcredential: Domain name containing project
                With token: Domain name containing project
                With password: Domain name containing project
                With v3password: Domain name containing project

                (Env: OS_PROJECT_DOMAIN_NAME)
            os-project-id: |-
                With v3token: Project ID to scope to
                With v3tokenlessauth: Project ID to scope to
                With v3oidcclientcredentials: Project ID to scope to
                With v3multifactor: Project ID to scope to
                With v3oidcpassword: Project ID to scope to
                With v3oidcaccesstoken: Project ID to scope to
                With v3oidcauthcode: Project ID to scope to
                With v3websso: Project ID to scope to
                With v3oauth2mtlsclientcredential: Project ID to scope to
                With v3oidcdeviceauthz: Project ID to scope to
                With v3totp: Project ID to scope to
                With v3applicationcredential: Project ID to scope to
                With v3oauth2clientcredential: Project ID to scope to
                With token: Project ID to scope to
                With password: Project ID to scope to
                With v3password: Project ID to scope to

                (Env: OS_PROJECT_ID)
            os-project-name: |-
                With v3token: Project name to scope to
                With v3tokenlessauth: Project name to scope to
                With v3oidcclientcredentials: Project name to scope to
                With v3multifactor: Project name to scope to
                With v3oidcpassword: Project name to scope to
                With v3oidcaccesstoken: Project name to scope to
                With v3oidcauthcode: Project name to scope to
                With v3websso: Project name to scope to
                With v3oauth2mtlsclientcredential: Project name to scope to
                With v3oidcdeviceauthz: Project name to scope to
                With v3totp: Project name to scope to
                With v3applicationcredential: Project name to scope to
                With v3oauth2clientcredential: Project name to scope to
                With token: Project name to scope to
                With password: Project name to scope to
                With v3password: Project name to scope to

                (Env: OS_PROJECT_NAME)
            os-protocol: |-
                With v3oidcclientcredentials: Protocol for federated plugin
                With v3oidcpassword: Protocol for federated plugin
                With v3oidcaccesstoken: Protocol for federated plugin
                With v3oidcauthcode: Protocol for federated plugin
                With v3websso: Protocol for federated plugin
                With v3oidcdeviceauthz: Protocol for federated plugin

                (Env: OS_PROTOCOL)
            os-redirect-host: |-
                With v3websso: Host the callback server listens on. This must be a loopback address.

                (Env: OS_REDIRECT_HOST)
            os-redirect-port: |-
                With v3websso: Port the callback server listens on. The resulting callback URL, http://<redirect-host>:<redirect-port>/auth/websso/, must be listed in the identity service's [federation] trusted_dashboard option.

                (Env: OS_REDIRECT_PORT)
            os-redirect-uri: |-
                With v3oidcauthcode: OpenID Connect Redirect URL

                (Env: OS_REDIRECT_URI)
            os-region-name: 'Authentication region name (Env: OS_REGION_NAME)'
            os-remote-project-domain-id: Domain ID of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
            os-remote-project-domain-name: Domain name of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
            os-remote-project-id: Project ID when authenticating to a service provider if using Keystone-to-Keystone federation.
            os-remote-project-name: Project name when authenticating to a service provider if using Keystone-to-Keystone federation.
            os-service-provider: Authenticate with and perform the command on a service provider using Keystone-to-keystone federation. Must also specify the remote project option.
            os-share-api-version: 'Shared File System API version, default=2.98 (version supported by both the client and the server) (Env: OS_SHARE_API_VERSION)'
            os-system-scope: |-
                With v3token: Scope for system operations
                With v3oidcclientcredentials: Scope for system operations
                With v3multifactor: Scope for system operations
                With v3oidcpassword: Scope for system operations
                With v3oidcaccesstoken: Scope for system operations
                With v3oidcauthcode: Scope for system operations
                With v3websso: Scope for system operations
                With v3oauth2mtlsclientcredential: Scope for system operations
                With v3oidcdeviceauthz: Scope for system operations
                With v3totp: Scope for system operations
                With v3applicationcredential: Scope for system operations
                With v3oauth2clientcredential: Scope for system operations
                With token: Scope for system operations
                With password: Scope for system operations
                With v3password: Scope for system operations

                (Env: OS_SYSTEM_SCOPE)
            os-tenant-id: ==SUPPRESS==
            os-tenant-name: ==SUPPRESS==
            os-token: |-
                With v3token: Token to authenticate with
                With v2token: Token
                With admin_token: The token that will always be used
                With token: Token to authenticate with

                (Env: OS_TOKEN)
            os-trust-id: |-
                With v3token: ID of the trust to use as a trustee use
                With v2token: ID of the trust to use as a trustee use
                With v3oidcclientcredentials: ID of the trust to use as a trustee use
                With v2password: ID of the trust to use as a trustee use
                With v3multifactor: ID of the trust to use as a trustee use
                With v3oidcpassword: ID of the trust to use as a trustee use
                With v3oidcaccesstoken: ID of the trust to use as a trustee use
                With v3oidcauthcode: ID of the trust to use as a trustee use
                With v3websso: ID of the trust to use as a trustee use
                With v3oauth2mtlsclientcredential: ID of the trust to use as a trustee use
                With v3oidcdeviceauthz: ID of the trust to use as a trustee use
                With v3totp: ID of the trust to use as a trustee use
                With v3applicationcredential: ID of the trust to use as a trustee use
                With v3oauth2clientcredential: ID of the trust to use as a trustee use
                With token: ID of the trust to use as a trustee use
                With password: ID of the trust to use as a trustee use
                With v3password: ID of the trust to use as a trustee use

                (Env: OS_TRUST_ID)
            os-user-domain-id: |-
                With v3totp: User's domain ID
                With v3applicationcredential: User's domain ID
                With password: User's domain id
                With v3password: User's domain ID

                (Env: OS_USER_DOMAIN_ID)
            os-user-domain-name: |-
                With v3totp: User's domain name
                With v3applicationcredential: User's domain name
                With password: User's domain name
                With v3password: User's domain name

                (Env: OS_USER_DOMAIN_NAME)
            os-user-id: |-
                With v2password: User ID to login with
                With v3totp: User's user ID
                With v3applicationcredential: User's user ID
                With password: User id
                With v3password: User's user ID

                (Env: OS_USER_ID)
            os-username: |-
                With v2password: Username to login with
                With v3oidcpassword: Username
                With v3websso: Optional label used only to keep cached tokens for different SSO accounts apart. It is not used to authenticate; the identity is chosen in the browser. Set a distinct value per account, or leave it unset if only one account is used.
                With v3totp: User's username
                With v3applicationcredential: User's username
                With http_basic: Username
                With password: Username
                With v3password: User's username

                (Env: OS_USERNAME)
            os-volume-api-version: 'Volume API version, default=3 (Env: OS_VOLUME_API_VERSION)'
            quiet: Suppress output except warnings and errors.
            timing: Print API call timing info
            verbose: Increase verbosity of output. Can be repeated.
            verify: Verify server certificate (default)
            version: show program's version number and exit
    - name: access
      commands:
        - name: rule
          commands:
            - name: delete
              description: Delete access rule(s)
            - name: list
              description: List access rules
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: User whose access rules to list (name or ID)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    user: User whose access rules to list (name or ID)
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
            - name: show
              description: Display access rule details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: token
          commands:
            - name: create
              description: Create an access token
              flags:
                --consumer-key=!: Consumer key (required)
                --consumer-secret=!: Consumer secret (required)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --request-key=!: Request token to exchange for access token (required)
                --request-secret=!: Secret associated with <request-key> (required)
                --variable=: ==SUPPRESS==
                --verifier=!: Verifier associated with <request-key> (required)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    consumer-key: Consumer key (required)
                    consumer-secret: Consumer secret (required)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    request-key: Request token to exchange for access token (required)
                    request-secret: Secret associated with <request-key> (required)
                    variable: ==SUPPRESS==
                    verifier: Verifier associated with <request-key> (required)
    - name: address
      commands:
        - name: group
          commands:
            - name: create
              description: Create a new Address Group
              flags:
                --address=: IP address or CIDR (repeat option to set multiple addresses)
                --description=: New address group description
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    address: IP address or CIDR (repeat option to set multiple addresses)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: New address group description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete address group(s)
            - name: list
              description: List address groups
              flags:
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --name=: List only address groups with the specified name
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: List only address groups with the specified project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: List only address groups with the specified name
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: List only address groups with the specified project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set address group properties
              flags:
                --address=: IP address or CIDR (repeat option to set multiple addresses)
                --description=: Set address group description
                --extra-property=: Additional parameters can be passed using this property.
                --name=: Set address group name
              documentation:
                flag:
                    address: IP address or CIDR (repeat option to set multiple addresses)
                    description: Set address group description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    name: Set address group name
            - name: show
              description: Display address group details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset address group properties
              flags:
                --address=: IP address or CIDR (repeat option to unset multiple addresses)
              documentation:
                flag:
                    address: IP address or CIDR (repeat option to unset multiple addresses)
        - name: scope
          commands:
            - name: create
              description: Create a new Address Scope
              flags:
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --ip-version=: IP version (default is 4)
                --max-width=: Maximum display width, <1 to disable.
                --no-share: Do not share the address scope between projects (default)
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --share: Share the address scope between projects
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
                    ip-version:
                        - "4"
                        - "6"
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ip-version: IP version (default is 4)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    no-share: Do not share the address scope between projects (default)
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    share: Share the address scope between projects
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete address scope(s)
            - name: list
              description: List address scopes
              flags:
                --fit-width: Fit the table to the display width.
                --ip-version=: List only address scopes with the specified IP version networks (4 or 6)
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --name=: List only address scopes with the specified name
                --no-share: List only address scopes not shared between projects
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: List only address scopes with the specified project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --share: List only address scopes shared between projects
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    ip-version:
                        - "4"
                        - "6"
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ip-version: List only address scopes with the specified IP version networks (4 or 6)
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: List only address scopes with the specified name
                    no-share: List only address scopes not shared between projects
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: List only address scopes with the specified project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    share: List only address scopes shared between projects
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set address scope properties
              flags:
                --extra-property=: Additional parameters can be passed using this property.
                --name=: Set address scope name
                --no-share: Do not share the address scope between projects
                --share: Share the address scope between projects
              documentation:
                flag:
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    name: Set address scope name
                    no-share: Do not share the address scope between projects
                    share: Share the address scope between projects
            - name: show
              description: Display address scope details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: aggregate
      commands:
        - name: add
          commands:
            - name: host
              description: Add host to aggregate
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: cache
          commands:
            - name: image
              description: Request image caching for aggregate
        - name: create
          description: Create a new aggregate
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --property=: Property to add to this aggregate (repeat option to set multiple properties)
            --variable=: ==SUPPRESS==
            --zone=: Availability zone name
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                property: Property to add to this aggregate (repeat option to set multiple properties)
                variable: ==SUPPRESS==
                zone: Availability zone name
        - name: delete
          description: Delete existing aggregate(s)
        - name: list
          description: List all aggregates
          flags:
            --availability-zone=: Filter by availability zone name
            --fit-width: Fit the table to the display width.
            --host=: Filter by aggregates containing this host
            --long: List additional fields in output
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                availability-zone: Filter by availability zone name
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                host: Filter by aggregates containing this host
                long: List additional fields in output
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: remove
          commands:
            - name: host
              description: Remove host from aggregate
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: set
          description: Set aggregate properties
          flags:
            --name=: Set aggregate name
            --no-property: Remove all properties from <aggregate> (specify both --property and --no-property to overwrite the current properties)
            --property=: Property to set on <aggregate> (repeat option to set multiple properties)
            --zone=: Set availability zone name
          documentation:
            flag:
                name: Set aggregate name
                no-property: Remove all properties from <aggregate> (specify both --property and --no-property to overwrite the current properties)
                property: Property to set on <aggregate> (repeat option to set multiple properties)
                zone: Set availability zone name
        - name: show
          description: Display aggregate details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset aggregate properties
          flags:
            --property=: Property to remove from aggregate (repeat option to remove multiple properties)
          documentation:
            flag:
                property: Property to remove from aggregate (repeat option to remove multiple properties)
    - name: application
      commands:
        - name: credential
          commands:
            - name: create
              description: Create new application credential
              flags:
                --access-rules=: 'Either a string or file path containing a JSON-formatted list of access rules, each containing a request method, path, and service, for example ''[{"method": "GET", "path": "/v2.1/servers", "service": "compute"}]'''
                --description=: Application credential description
                --expiration=: Sets an expiration date for the application credential, format of YYYY-mm-ddTHH:MM:SS (if not provided, the application credential will not expire)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --restricted: Prohibit application credential from creating and deleting other application credentials and trusts (this is the default behavior)
                --role=: Roles to authorize (name or ID) (repeat option to set multiple values)
                --secret=: Secret to use for authentication (if not provided, one will be generated)
                --unrestricted: Enable application credential to create and delete other application credentials and trusts (this is potentially dangerous behavior and is disabled by default)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    access-rules: 'Either a string or file path containing a JSON-formatted list of access rules, each containing a request method, path, and service, for example ''[{"method": "GET", "path": "/v2.1/servers", "service": "compute"}]'''
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Application credential description
                    expiration: Sets an expiration date for the application credential, format of YYYY-mm-ddTHH:MM:SS (if not provided, the application credential will not expire)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    restricted: Prohibit application credential from creating and deleting other application credentials and trusts (this is the default behavior)
                    role: Roles to authorize (name or ID) (repeat option to set multiple values)
                    secret: Secret to use for authentication (if not provided, one will be generated)
                    unrestricted: Enable application credential to create and delete other application credentials and trusts (this is potentially dangerous behavior and is disabled by default)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete application credentials(s)
            - name: list
              description: List application credentials
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: User whose application credentials to list (name or ID)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    user: User whose application credentials to list (name or ID)
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
            - name: show
              description: Display application credential details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: availability
      commands:
        - name: zone
          commands:
            - name: list
              description: List availability zones and their status
              flags:
                --compute: List compute availability zones
                --fit-width: Fit the table to the display width.
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --network: List network availability zones
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --share: List share availability zones
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --volume: List volume availability zones
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    compute: List compute availability zones
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    network: List network availability zones
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    share: List share availability zones
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    volume: List volume availability zones
    - name: bgp
      commands:
        - name: dragent
          commands:
            - name: add
              commands:
                - name: speaker
                  description: Add a BGP speaker to a dynamic routing agent
            - name: list
              description: List dynamic routing agents
              flags:
                --bgp-speaker=: List dynamic routing agents hosting a BGP speaker (name or ID)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    bgp-speaker: List dynamic routing agents hosting a BGP speaker (name or ID)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: remove
              commands:
                - name: speaker
                  description: Removes a BGP speaker from a dynamic routing agent
        - name: peer
          commands:
            - name: create
              description: Create a BGP peer
              flags:
                --auth-type=: Authentication algorithm.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --password=: Authentication password
                --peer-ip=!: Peer IP address
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --remote-as=!: Peer AS number.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    auth-type:
                        - none
                        - md5
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    auth-type: 'Authentication algorithm. Supported algorithms: none (default), md5'
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    password: Authentication password
                    peer-ip: Peer IP address
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    remote-as: Peer AS number. (Integer in [1, 4294967295] is allowed)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete a BGP peer
            - name: list
              description: List BGP peers
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Update a BGP peer
              flags:
                --name=: Updated name of the BGP peer
                --password=: Updated authentication password
              documentation:
                flag:
                    name: Updated name of the BGP peer
                    password: Updated authentication password
            - name: show
              description: Show information for a BGP peer
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: speaker
          commands:
            - name: add
              commands:
                - name: network
                  description: Add a network to a BGP speaker
                - name: peer
                  description: Add a peer to a BGP speaker
            - name: create
              description: Create a BGP speaker
              flags:
                --advertise-floating-ip-host-routes: Enable the advertisement of floating IP host routes by the BGP speaker.
                --advertise-tenant-networks: Enable the advertisement of tenant network routes by the BGP speaker.
                --fit-width: Fit the table to the display width.
                --ip-version=: IP version for the BGP speaker (default is 4)
                --local-as=!: Local AS number.
                --max-width=: Maximum display width, <1 to disable.
                --no-advertise-floating-ip-host-routes: Disable the advertisement of floating IP host routes by the BGP speaker.
                --no-advertise-tenant-networks: Disable the advertisement of tenant network routes by the BGP speaker.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
                    ip-version:
                        - "4"
                        - "6"
              documentation:
                flag:
                    advertise-floating-ip-host-routes: Enable the advertisement of floating IP host routes by the BGP speaker. (default)
                    advertise-tenant-networks: Enable the advertisement of tenant network routes by the BGP speaker. (default)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ip-version: IP version for the BGP speaker (default is 4)
                    local-as: Local AS number. (Integer in [1, 4294967295] is allowed.)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    no-advertise-floating-ip-host-routes: Disable the advertisement of floating IP host routes by the BGP speaker.
                    no-advertise-tenant-networks: Disable the advertisement of tenant network routes by the BGP speaker.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete a BGP speaker
            - name: list
              description: List BGP speakers
              flags:
                --agent=: List BGP speakers hosted by an agent (ID only)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              commands:
                - name: advertised
                  commands:
                    - name: routes
                      description: List routes advertised
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
              documentation:
                flag:
                    agent: List BGP speakers hosted by an agent (ID only)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: remove
              commands:
                - name: network
                  description: Remove a network from a BGP speaker
                - name: peer
                  description: Remove a peer from a BGP speaker
            - name: set
              description: Set BGP speaker properties
              flags:
                --advertise-floating-ip-host-routes: Enable the advertisement of floating IP host routes by the BGP speaker.
                --advertise-tenant-networks: Enable the advertisement of tenant network routes by the BGP speaker.
                --name=: New name for the BGP speaker
                --no-advertise-floating-ip-host-routes: Disable the advertisement of floating IP host routes by the BGP speaker.
                --no-advertise-tenant-networks: Disable the advertisement of tenant network routes by the BGP speaker.
              documentation:
                flag:
                    advertise-floating-ip-host-routes: Enable the advertisement of floating IP host routes by the BGP speaker. (default)
                    advertise-tenant-networks: Enable the advertisement of tenant network routes by the BGP speaker. (default)
                    name: New name for the BGP speaker
                    no-advertise-floating-ip-host-routes: Disable the advertisement of floating IP host routes by the BGP speaker.
                    no-advertise-tenant-networks: Disable the advertisement of tenant network routes by the BGP speaker.
            - name: show
              description: Show a BGP speaker
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: bgpvpn
      commands:
        - name: create
          description: Create BGP VPN resource
          flags:
            --export-target=: Add Route Target to export list (repeat option for multiple Route Targets)
            --fit-width: Fit the table to the display width.
            --import-target=: Add Route Target to import list (repeat option for multiple Route Targets)
            --local-pref=: Default BGP LOCAL_PREF to use in route advertisementstowards this BGPVPN.
            --max-width=: Maximum display width, <1 to disable.
            --name=: Name of the BGP VPN
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Owner's project (name or ID)
            --route-distinguisher=: Add Route Distinguisher to the list of Route Distinguishers from which a Route Distinguishers will be picked from to advertise a VPN route (repeat option for multiple Route Distinguishers)
            --route-target=: Add Route Target to import/export list (repeat option for multiple Route Targets)
            --type=: 'BGP VPN type selection between IP VPN (l3) and Ethernet VPN (l2) (default: l3)'
            --variable=: ==SUPPRESS==
            --vni=: VXLAN Network Identifier to be used for this BGPVPN when a VXLAN encapsulation is used
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
                type:
                    - l2
                    - l3
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                export-target: Add Route Target to export list (repeat option for multiple Route Targets)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                import-target: Add Route Target to import list (repeat option for multiple Route Targets)
                local-pref: Default BGP LOCAL_PREF to use in route advertisementstowards this BGPVPN.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: Name of the BGP VPN
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Owner's project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                route-distinguisher: Add Route Distinguisher to the list of Route Distinguishers from which a Route Distinguishers will be picked from to advertise a VPN route (repeat option for multiple Route Distinguishers)
                route-target: Add Route Target to import/export list (repeat option for multiple Route Targets)
                type: 'BGP VPN type selection between IP VPN (l3) and Ethernet VPN (l2) (default: l3)'
                variable: ==SUPPRESS==
                vni: VXLAN Network Identifier to be used for this BGPVPN when a VXLAN encapsulation is used
        - name: delete
          description: Delete BGP VPN resource(s)
        - name: list
          description: List BGP VPN resources
          flags:
            --fit-width: Fit the table to the display width.
            --long: List additional fields in output
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Owner's project (name or ID)
            --property=: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                long: List additional fields in output
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: Owner's project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: network
          commands:
            - name: association
              commands:
                - name: create
                  description: Create a BGP VPN network association
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a BGP VPN network association(s) for a given BGP VPN
                - name: list
                  description: List BGP VPN network associations for a given BGP VPN
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --property=: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        property: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: show
                  description: Show information of a given BGP VPN network association
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
        - name: port
          commands:
            - name: association
              commands:
                - name: create
                  description: Create a BGP VPN port association
                  flags:
                    --advertise-fixed-ips: Fixed IPs of the port will be advertised to the BGP VPN (default)
                    --bgpvpn-route=: Add BGP VPN route for route leaking.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --no-advertise-fixed-ips: Fixed IPs of the port will not be advertised to the BGP VPN
                    --noindent: whether to disable indenting the JSON
                    --prefix-route=: Add prefix route in CIDR notation.
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        advertise-fixed-ips: Fixed IPs of the port will be advertised to the BGP VPN (default)
                        bgpvpn-route: Add BGP VPN route for route leaking. Optionally, can control the value of the BGP LOCAL_PREF of the routes that will be advertised (repeat option for multiple BGP VPN routes)
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        no-advertise-fixed-ips: Fixed IPs of the port will not be advertised to the BGP VPN
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        prefix-route: Add prefix route in CIDR notation. Optionally, can control the value of the BGP LOCAL_PREF of the routes that will be advertised (repeat option for multiple prefix routes)
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a BGP VPN port association(s) for a given BGP VPN
                - name: list
                  description: List BGP VPN port associations for a given BGP VPN
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --property=: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        property: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set BGP VPN port association properties
                  flags:
                    --advertise-fixed-ips: Fixed IPs of the port will be advertised to the BGP VPN
                    --bgpvpn-route=: Add BGP VPN route for route leaking.
                    --no-advertise-fixed-ips: Fixed IPs of the port will not be advertised to the BGP VPN
                    --no-bgpvpn-route: Empty BGP VPN route list
                    --no-prefix-route: Empty prefix route list
                    --prefix-route=: Add prefix route in CIDR notation.
                  documentation:
                    flag:
                        advertise-fixed-ips: Fixed IPs of the port will be advertised to the BGP VPN
                        bgpvpn-route: Add BGP VPN route for route leaking. Optionally, can control the value of the BGP LOCAL_PREF of the routes that will be advertised (repeat option for multiple BGP VPN routes)
                        no-advertise-fixed-ips: Fixed IPs of the port will not be advertised to the BGP VPN
                        no-bgpvpn-route: Empty BGP VPN route list
                        no-prefix-route: Empty prefix route list
                        prefix-route: Add prefix route in CIDR notation. Optionally, can control the value of the BGP LOCAL_PREF of the routes that will be advertised (repeat option for multiple prefix routes)
                - name: show
                  description: Show information of a given BGP VPN port association
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset BGP VPN port association properties
                  flags:
                    --advertise-fixed-ips: Fixed IPs of the port will not be advertised to the BGP VPN
                    --all-bgpvpn-routes: Empty BGP VPN route list
                    --all-prefix-routes: Empty prefix route list
                    --bgpvpn-route=: Remove BGP VPN route (repeat option for multiple BGP VPN routes)
                    --no-advertise-fixed-ips: Fixed IPs of the port will be advertised to the BGP VPN
                    --prefix-route=: Remove prefix route in CIDR notation (repeat option for multiple prefix routes)
                  documentation:
                    flag:
                        advertise-fixed-ips: Fixed IPs of the port will not be advertised to the BGP VPN
                        all-bgpvpn-routes: Empty BGP VPN route list
                        all-prefix-routes: Empty prefix route list
                        bgpvpn-route: Remove BGP VPN route (repeat option for multiple BGP VPN routes)
                        no-advertise-fixed-ips: Fixed IPs of the port will be advertised to the BGP VPN
                        prefix-route: Remove prefix route in CIDR notation (repeat option for multiple prefix routes)
        - name: router
          commands:
            - name: association
              commands:
                - name: create
                  description: Create a BGP VPN router association
                  flags:
                    --advertise_extra_routes: Routes will be advertised to the BGP VPN (default)
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --no-advertise_extra_routes: Routes from the router will not be advertised to the BGP VPN
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        advertise_extra_routes: Routes will be advertised to the BGP VPN (default)
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        no-advertise_extra_routes: Routes from the router will not be advertised to the BGP VPN
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a BGP VPN router association(s) for a given BGP VPN
                - name: list
                  description: List BGP VPN router associations for a given BGP VPN
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --property=: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        property: Filter property to apply on returned BGP VPNs (repeat to filter on multiple properties)
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set BGP VPN router association properties
                  flags:
                    --advertise_extra_routes: Routes will be advertised to the BGP VPN
                    --no-advertise_extra_routes: Routes from the router will not be advertised to the BGP VPN
                  documentation:
                    flag:
                        advertise_extra_routes: Routes will be advertised to the BGP VPN
                        no-advertise_extra_routes: Routes from the router will not be advertised to the BGP VPN
                - name: show
                  description: Show information of a given BGP VPN router association
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset BGP VPN router association properties
                  flags:
                    --advertise_extra_routes: Routes from the router will not be advertised to the BGP VPN
                    --no-advertise_extra_routes: Routes will be advertised to the BGP VPN
                  documentation:
                    flag:
                        advertise_extra_routes: Routes from the router will not be advertised to the BGP VPN
                        no-advertise_extra_routes: Routes will be advertised to the BGP VPN
        - name: set
          description: Set BGP VPN properties
          flags:
            --export-target=: Add Route Target to export list (repeat option for multiple Route Targets)
            --import-target=: Add Route Target to import list (repeat option for multiple Route Targets)
            --local-pref=: Default BGP LOCAL_PREF to use in route advertisementstowards this BGPVPN.
            --name=: Name of the BGP VPN
            --no-export-target: Empty export route target list
            --no-import-target: Empty import route target list
            --no-route-distinguisher: Empty route distinguisher list
            --no-route-target: Empty route target list
            --route-distinguisher=: Add Route Distinguisher to the list of Route Distinguishers from which a Route Distinguishers will be picked from to advertise a VPN route (repeat option for multiple Route Distinguishers)
            --route-target=: Add Route Target to import/export list (repeat option for multiple Route Targets)
            --vni=: VXLAN Network Identifier to be used for this BGPVPN when a VXLAN encapsulation is used
          documentation:
            flag:
                export-target: Add Route Target to export list (repeat option for multiple Route Targets)
                import-target: Add Route Target to import list (repeat option for multiple Route Targets)
                local-pref: Default BGP LOCAL_PREF to use in route advertisementstowards this BGPVPN.
                name: Name of the BGP VPN
                no-export-target: Empty export route target list
                no-import-target: Empty import route target list
                no-route-distinguisher: Empty route distinguisher list
                no-route-target: Empty route target list
                route-distinguisher: Add Route Distinguisher to the list of Route Distinguishers from which a Route Distinguishers will be picked from to advertise a VPN route (repeat option for multiple Route Distinguishers)
                route-target: Add Route Target to import/export list (repeat option for multiple Route Targets)
                vni: VXLAN Network Identifier to be used for this BGPVPN when a VXLAN encapsulation is used
        - name: show
          description: Show information of a given BGP VPN
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset BGP VPN properties
          flags:
            --all-export-target: Empty export route target list
            --all-import-target: Empty import route target list
            --all-route-distinguisher: Empty route distinguisher list
            --all-route-target: Empty route target list
            --export-target=: Remove Route Target from export list (repeat option for multiple Route Targets)
            --import-target=: Remove Route Target from import list (repeat option for multiple Route Targets)
            --local-pref=: Default BGP LOCAL_PREF to use in route advertisementstowards this BGPVPN.
            --route-distinguisher=: Remove Route Distinguisher from the list of Route Distinguishers from which a Route Distinguishers will be picked from to advertise a VPN route (repeat option for multiple Route Distinguishers)
            --route-target=: Remove Route Target from import/export list (repeat option for multiple Route Targets)
            --vni=: VXLAN Network Identifier to be used for this BGPVPN when a VXLAN encapsulation is used
          documentation:
            flag:
                all-export-target: Empty export route target list
                all-import-target: Empty import route target list
                all-route-distinguisher: Empty route distinguisher list
                all-route-target: Empty route target list
                export-target: Remove Route Target from export list (repeat option for multiple Route Targets)
                import-target: Remove Route Target from import list (repeat option for multiple Route Targets)
                local-pref: Default BGP LOCAL_PREF to use in route advertisementstowards this BGPVPN.
                route-distinguisher: Remove Route Distinguisher from the list of Route Distinguishers from which a Route Distinguishers will be picked from to advertise a VPN route (repeat option for multiple Route Distinguishers)
                route-target: Remove Route Target from import/export list (repeat option for multiple Route Targets)
                vni: VXLAN Network Identifier to be used for this BGPVPN when a VXLAN encapsulation is used
    - name: block
      commands:
        - name: storage
          commands:
            - name: cleanup
              description: Do block storage cleanup.
              flags:
                --binary=: Name of the service binary.
                --cluster=: Name of block storage cluster in which cleanup needs to be performed (name only)
                --disabled: Filter by disabled status.
                --down: Filter by down status.
                --enabled: Filter by enabled status.
                --fit-width: Fit the table to the display width.
                --host=: Host where the service resides.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --resource-id=: UUID of a resource to cleanup.
                --resource-type=: Type of resource to cleanup.
                --service-id=: The service ID field from the DB, not the UUID of the service.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --up: Filter by up status.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    resource-type:
                        - Volume
                        - Snapshot
              documentation:
                flag:
                    binary: Name of the service binary.
                    cluster: Name of block storage cluster in which cleanup needs to be performed (name only)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    disabled: Filter by disabled status.
                    down: Filter by down status. If this is set, services need to be down.
                    enabled: Filter by enabled status.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Host where the service resides. (name only)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    resource-id: UUID of a resource to cleanup.
                    resource-type: Type of resource to cleanup.
                    service-id: The service ID field from the DB, not the UUID of the service.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    up: Filter by up status. If this is set, services need to be up.
            - name: cluster
              commands:
                - name: list
                  description: List block storage clusters.
                  flags:
                    --binary=: Cluster binary.
                    --cluster=: Filter by cluster name, without backend will list all clustered services from the same cluster.
                    --disabled: Filter by disabled status.
                    --down: Filter by down status.
                    --enabled: Filter by enabled status.
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --num-down-hosts=: Filter by number of hosts that are down.
                    --num-hosts=: Filter by number of hosts in the cluster.
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    --up: Filter by up status.
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        binary: Cluster binary.
                        cluster: Filter by cluster name, without backend will list all clustered services from the same cluster.
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        disabled: Filter by disabled status.
                        down: Filter by down status.
                        enabled: Filter by enabled status.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        num-down-hosts: Filter by number of hosts that are down.
                        num-hosts: Filter by number of hosts in the cluster.
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                        up: Filter by up status.
                - name: set
                  description: Set block storage cluster properties.
                  flags:
                    --binary=: Name of binary to filter by; defaults to 'cinder-volume' (optional)
                    --disable: Disable cluster
                    --disable-reason=: Reason for disabling the cluster (should be used with --disable option)
                    --enable: Enable cluster
                  documentation:
                    flag:
                        binary: Name of binary to filter by; defaults to 'cinder-volume' (optional)
                        disable: Disable cluster
                        disable-reason: Reason for disabling the cluster (should be used with --disable option)
                        enable: Enable cluster
                - name: show
                  description: Show detailed information for a block storage cluster.
                  flags:
                    --binary=: Service binary.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        binary: Service binary.
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: log
              commands:
                - name: level
                  commands:
                    - name: list
                      description: List log levels of block storage service.
                      flags:
                        --fit-width: Fit the table to the display width.
                        --host=: List block storage service log level of specified host (name only)
                        --log-prefix=: Prefix for the log, e.g. 'sqlalchemy'
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --service=: List block storage service log level of the specified service (name only)
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                            service:
                                - None
                                - '*'
                                - cinder-api
                                - cinder-volume
                                - cinder-scheduler
                                - cinder-backup
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            host: List block storage service log level of specified host (name only)
                            log-prefix: Prefix for the log, e.g. 'sqlalchemy'
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            service: List block storage service log level of the specified service (name only)
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: set
                      description: Set log level of block storage service
                      flags:
                        --host=: Set block storage service log level of specified host (name only)
                        --log-prefix=: Prefix for the log, e.g. 'sqlalchemy'
                        --service=: Set block storage service log level of specified service (name only)
                      completion:
                        flag:
                            service:
                                - None
                                - '*'
                                - cinder-api
                                - cinder-volume
                                - cinder-scheduler
                                - cinder-backup
                      documentation:
                        flag:
                            host: Set block storage service log level of specified host (name only)
                            log-prefix: Prefix for the log, e.g. 'sqlalchemy'
                            service: Set block storage service log level of specified service (name only)
            - name: resource
              commands:
                - name: filter
                  commands:
                    - name: list
                      description: List block storage resource filters
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: show
                      description: Show filters for a block storage resource type
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
            - name: snapshot
              commands:
                - name: manageable
                  commands:
                    - name: list
                      description: List manageable snapshots.
                      flags:
                        --cluster=: Cinder cluster on which to list manageable snapshots.
                        --detailed=: ==SUPPRESS==
                        --fit-width: Fit the table to the display width.
                        --limit=: Maximum number of snapshots to return.
                        --long: List additional fields in output
                        --marker=: Begin returning snapshots that appear later in the snapshot list than that represented by this reference.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --offset=: Number of snapshots to skip after marker.
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        --sort=: Comma-separated list of sort keys and directions in the form of <key>[:<asc|desc>].
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            cluster: 'Cinder cluster on which to list manageable snapshots. Takes the form: cluster@backend-name#pool. (supported by --os-volume-api-version 3.17 or later)'
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            detailed: ==SUPPRESS==
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            limit: Maximum number of snapshots to return. Default=None.
                            long: List additional fields in output
                            marker: Begin returning snapshots that appear later in the snapshot list than that represented by this reference. This reference should be json like. Default=None.
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            offset: Number of snapshots to skip after marker. Default=None.
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort: 'Comma-separated list of sort keys and directions in the form of <key>[:<asc|desc>]. Valid keys: size, reference. Default=None.'
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
            - name: volume
              commands:
                - name: manageable
                  commands:
                    - name: list
                      description: List manageable volumes.
                      flags:
                        --cluster=: Cinder cluster on which to list manageable volumes.
                        --detailed=: ==SUPPRESS==
                        --fit-width: Fit the table to the display width.
                        --limit=: Maximum number of volumes to return.
                        --long: List additional fields in output
                        --marker=: Begin returning volumes that appear later in the volume list than that represented by this reference.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --offset=: Number of volumes to skip after marker.
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        --sort=: Comma-separated list of sort keys and directions in the form of <key>[:<asc|desc>].
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            cluster: 'Cinder cluster on which to list manageable volumes. Takes the form: cluster@backend-name#pool. (supported by --os-volume-api-version 3.17 or later)'
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            detailed: ==SUPPRESS==
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            limit: Maximum number of volumes to return. Default=None.
                            long: List additional fields in output
                            marker: Begin returning volumes that appear later in the volume list than that represented by this reference. This reference should be json like. Default=None.
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            offset: Number of volumes to skip after marker. Default=None.
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort: 'Comma-separated list of sort keys and directions in the form of <key>[:<asc|desc>]. Valid keys: size, reference. Default=None.'
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
    - name: cached
      commands:
        - name: image
          commands:
            - name: clear
              description: Clear all images from cache, queue or both
              flags:
                --cache: Clears all the cached images
                --queue: Clears all the queued images
              documentation:
                flag:
                    cache: Clears all the cached images
                    queue: Clears all the queued images
            - name: delete
              description: Delete image(s) from cache
            - name: list
              description: Get Cache State
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: queue
              description: Queue image(s) for caching.
    - name: catalog
      commands:
        - name: list
          description: List services in the service catalog
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: show
          description: Display service catalog details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: command
      commands:
        - name: list
          description: List recognized commands by group
          flags:
            --fit-width: Fit the table to the display width.
            --group=: 'Show commands filtered by a command group, for example: identity, volume, compute, image, network and other keywords'
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                group: 'Show commands filtered by a command group, for example: identity, volume, compute, image, network and other keywords'
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
    - name: complete
      description: print bash completion command
      flags:
        --name=: Command name to support with command completion
        --shell=: Shell being used.
      completion:
        flag:
            shell:
                - bash
                - none
      documentation:
        flag:
            name: Command name to support with command completion
            shell: 'Shell being used. Use none for data only (default: bash)'
    - name: compute
      commands:
        - name: agent
          commands:
            - name: create
              description: Create compute agent.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete compute agent(s).
            - name: list
              description: List compute agents.
              flags:
                --fit-width: Fit the table to the display width.
                --hypervisor=: Type of hypervisor
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    hypervisor: Type of hypervisor
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set compute agent properties.
              flags:
                --agent-version=: Version of the agent
                --md5hash=: MD5 hash of the agent
                --url=: URL of the agent
              documentation:
                flag:
                    agent-version: Version of the agent
                    md5hash: MD5 hash of the agent
                    url: URL of the agent
        - name: service
          commands:
            - name: delete
              description: Delete compute service(s)
            - name: list
              description: List compute services.
              flags:
                --fit-width: Fit the table to the display width.
                --host=: List services on specified host (name only)
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --service=: List only specified service binaries (name only).
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: List services on specified host (name only)
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    service: List only specified service binaries (name only). For example, ``nova-compute``, ``nova-conductor``, etc.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set compute service properties
              flags:
                --disable: Disable service
                --disable-reason=: Reason for disabling the service (in quotes).
                --down: Force down service.
                --enable: Enable service
                --up: Force up service.
              documentation:
                flag:
                    disable: Disable service
                    disable-reason: Reason for disabling the service (in quotes). Should be used with --disable option.
                    down: Force down service. Requires ``--os-compute-api-version`` 2.11 or greater.
                    enable: Enable service
                    up: Force up service. Requires ``--os-compute-api-version`` 2.11 or greater.
    - name: configuration
      commands:
        - name: show
          description: Display configuration details
          flags:
            --fit-width: Fit the table to the display width.
            --mask: Attempt to mask passwords (default)
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --unmask: Show password in clear text
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                mask: Attempt to mask passwords (default)
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                unmask: Show password in clear text
                variable: ==SUPPRESS==
    - name: consistency
      commands:
        - name: group
          commands:
            - name: add
              commands:
                - name: volume
                  description: Add volume(s) to consistency group
            - name: create
              description: Create new consistency group.
              flags:
                --availability-zone=: Availability zone for this consistency group (not available if creating consistency group from source)
                --consistency-group-snapshot=: ==SUPPRESS==
                --consistency-group-source=: ==SUPPRESS==
                --description=: Description of this consistency group
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --snapshot=: Existing consistency group snapshot (name or ID)
                --source=: Existing consistency group (name or ID)
                --variable=: ==SUPPRESS==
                --volume-type=: Volume type of this consistency group (name or ID)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    availability-zone: Availability zone for this consistency group (not available if creating consistency group from source)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    consistency-group-snapshot: ==SUPPRESS==
                    consistency-group-source: ==SUPPRESS==
                    description: Description of this consistency group
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    snapshot: Existing consistency group snapshot (name or ID)
                    source: Existing consistency group (name or ID)
                    variable: ==SUPPRESS==
                    volume-type: Volume type of this consistency group (name or ID)
            - name: delete
              description: Delete consistency group(s).
              flags:
                --force: Allow delete in state other than error or available
              documentation:
                flag:
                    force: Allow delete in state other than error or available
            - name: list
              description: List consistency groups.
              flags:
                --all-projects: Show details for all projects.
                --fit-width: Fit the table to the display width.
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all-projects: Show details for all projects. Admin only. (defaults to False)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: remove
              commands:
                - name: volume
                  description: Remove volume(s) from consistency group
            - name: set
              description: Set consistency group properties
              flags:
                --description=: New consistency group description
                --name=: New consistency group name
              documentation:
                flag:
                    description: New consistency group description
                    name: New consistency group name
            - name: show
              description: Display consistency group details.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: snapshot
              commands:
                - name: create
                  description: Create new consistency group snapshot.
                  flags:
                    --consistency-group=: Consistency group to snapshot (name or ID) (default to be the same as <snapshot-name>)
                    --description=: Description of this consistency group snapshot
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        consistency-group: Consistency group to snapshot (name or ID) (default to be the same as <snapshot-name>)
                        description: Description of this consistency group snapshot
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete consistency group snapshot(s).
                - name: list
                  description: List consistency group snapshots.
                  flags:
                    --all-projects: Show detail for all projects (admin only) (defaults to False)
                    --consistency-group=: Filters results by a consistency group (name or ID)
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    --status=: Filters results by a status ("available", "error", "creating", "deleting" or "error_deleting")
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                        status:
                            - available
                            - error
                            - creating
                            - deleting
                            - error_deleting
                  documentation:
                    flag:
                        all-projects: Show detail for all projects (admin only) (defaults to False)
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        consistency-group: Filters results by a consistency group (name or ID)
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                        status: Filters results by a status ("available", "error", "creating", "deleting" or "error_deleting")
                - name: show
                  description: Display consistency group snapshot details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
    - name: console
      commands:
        - name: connection
          commands:
            - name: show
              description: Show server's remote console connection information
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: log
          commands:
            - name: show
              description: Show server's console output
              flags:
                --lines=: Number of lines to display from the end of the log (default=all)
              documentation:
                flag:
                    lines: Number of lines to display from the end of the log (default=all)
        - name: url
          commands:
            - name: show
              description: Show server's remote console URL
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --mks: Show WebMKS console URL
                --noindent: whether to disable indenting the JSON
                --novnc: Show noVNC console URL (default)
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --rdp: Show RDP console URL
                --serial: Show serial console URL
                --spice: Show SPICE console URL
                --spice-direct: Show SPICE direct protocol native console URL
                --variable=: ==SUPPRESS==
                --xvpvnc: Show xvpvnc console URL
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    mks: Show WebMKS console URL
                    noindent: whether to disable indenting the JSON
                    novnc: Show noVNC console URL (default)
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    rdp: Show RDP console URL
                    serial: Show serial console URL
                    spice: Show SPICE console URL
                    spice-direct: Show SPICE direct protocol native console URL
                    variable: ==SUPPRESS==
                    xvpvnc: Show xvpvnc console URL
    - name: consumer
      commands:
        - name: create
          description: Create new consumer
          flags:
            --description=: New consumer description
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: New consumer description
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete consumer(s)
        - name: list
          description: List consumers
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: set
          description: Set consumer properties
          flags:
            --description=: New consumer description
          documentation:
            flag:
                description: New consumer description
        - name: show
          description: Display consumer details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: container
      commands:
        - name: create
          description: Create new container
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --public: Make the container publicly accessible
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --storage-policy=: Specify a particular storage policy to use.
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                public: Make the container publicly accessible
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                storage-policy: Specify a particular storage policy to use.
        - name: delete
          description: Delete container
          flags:
            -r, --recursive: Recursively delete objects and container
          documentation:
            flag:
                recursive: Recursively delete objects and container
        - name: list
          description: List containers
          flags:
            --all: List all containers (default is 10000)
            --end-marker=: End anchor for paging
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: Filter list using <prefix>
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all: List all containers (default is 10000)
                column: specify the column(s) to include, can be repeated to show multiple columns
                end-marker: End anchor for paging
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: Filter list using <prefix>
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: save
          description: Save container contents locally
        - name: set
          description: Set container properties
          flags:
            --property=!: Set a property on this container (repeat option to set multiple properties)
          documentation:
            flag:
                property: Set a property on this container (repeat option to set multiple properties)
        - name: show
          description: Display container details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset container properties
          flags:
            --property=!: Property to remove from container (repeat option to remove multiple properties)
          documentation:
            flag:
                property: Property to remove from container (repeat option to remove multiple properties)
    - name: credential
      commands:
        - name: create
          description: Create new credential
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project=: Project which limits the scope of the credential (name or ID)
            --type=: 'New credential type: cert, ec2, totp and so on'
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Project which limits the scope of the credential (name or ID)
                type: 'New credential type: cert, ec2, totp and so on'
                variable: ==SUPPRESS==
        - name: delete
          description: Delete credential(s)
        - name: list
          description: List credentials
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --type=: 'Filter credentials by type: cert, ec2, totp and so on'
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Filter credentials by <user> (name or ID)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                type: 'Filter credentials by type: cert, ec2, totp and so on'
                user: Filter credentials by <user> (name or ID)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: set
          description: Set credential properties
          flags:
            --data=!: New credential data
            --project=: Project which limits the scope of the credential (name or ID)
            --type=!: 'New credential type: cert, ec2, totp and so on'
            --user=!: User that owns the credential (name or ID)
          documentation:
            flag:
                data: New credential data
                project: Project which limits the scope of the credential (name or ID)
                type: 'New credential type: cert, ec2, totp and so on'
                user: User that owns the credential (name or ID)
        - name: show
          description: Display credential details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: default
      commands:
        - name: security
          commands:
            - name: group
              commands:
                - name: rule
                  commands:
                    - name: create
                      description: Add a new security group rule to the default security group template.
                      flags:
                        --description=: Set default security group rule description
                        --dst-port=: 'Destination port, may be a single port or a starting and ending port range: 137:139.'
                        --egress: Rule will apply to outgoing network traffic
                        --ethertype=: 'Ethertype of network traffic (IPv4, IPv6; default: based on IP protocol)'
                        --extra-property=: Additional parameters can be passed using this property.
                        --fit-width: Fit the table to the display width.
                        --for-custom-sg: Set this default security group rule to be used in all custom security groups created manually by users
                        --for-default-sg: Set this default security group rule to be used in all default security groups created automatically for each project
                        --icmp-code=: ICMP code for ICMP IP protocols
                        --icmp-type=: ICMP type for ICMP IP protocols
                        --ingress: Rule will apply to incoming network traffic (default)
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --protocol=: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv66-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                        --remote-address-group=: Remote address group (ID)
                        --remote-group=: Remote security group (ID)
                        --remote-ip=: 'Remote IP address block (may use CIDR notation; default for IPv4 rule: 0.0.0.0/0, default for IPv6 rule: ::/0)'
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            ethertype:
                                - IPv4
                                - IPv6
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            description: Set default security group rule description
                            dst-port: 'Destination port, may be a single port or a starting and ending port range: 137:139. Required for IP protocols TCP and UDP. Ignored for ICMP IP protocols.'
                            egress: Rule will apply to outgoing network traffic
                            ethertype: 'Ethertype of network traffic (IPv4, IPv6; default: based on IP protocol)'
                            extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            for-custom-sg: Set this default security group rule to be used in all custom security groups created manually by users
                            for-default-sg: Set this default security group rule to be used in all default security groups created automatically for each project
                            format: the output format, defaults to table
                            icmp-code: ICMP code for ICMP IP protocols
                            icmp-type: ICMP type for ICMP IP protocols
                            ingress: Rule will apply to incoming network traffic (default)
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            protocol: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv66-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                            remote-address-group: Remote address group (ID)
                            remote-group: Remote security group (ID)
                            remote-ip: 'Remote IP address block (may use CIDR notation; default for IPv4 rule: 0.0.0.0/0, default for IPv6 rule: ::/0)'
                            variable: ==SUPPRESS==
                    - name: delete
                      description: Remove security group rule(s) from the default security group template.
                    - name: list
                      description: List security group rules used for new default security groups.
                      flags:
                        --egress: List only default rules which will be applied to outgoing network traffic
                        --ethertype=: List default rules by the Ethertype (IPv4 or IPv6)
                        --fit-width: Fit the table to the display width.
                        --ingress: List only default rules which will be applied to incoming network traffic
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --protocol=: 'List only default rules with the specified IP protocol (ah, dhcp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            egress: List only default rules which will be applied to outgoing network traffic
                            ethertype: List default rules by the Ethertype (IPv4 or IPv6)
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            ingress: List only default rules which will be applied to incoming network traffic
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            protocol: 'List only default rules with the specified IP protocol (ah, dhcp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: show
                      description: Show a security group rule used for new default security groups.
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
    - name: domain
      commands:
        - name: create
          description: Create new domain
          flags:
            --description=: New domain description
            --disable: Disable domain
            --enable: Enable domain (default)
            --fit-width: Fit the table to the display width.
            --immutable: Make resource immutable.
            --max-width=: Maximum display width, <1 to disable.
            --no-immutable: Make resource mutable (default)
            --noindent: whether to disable indenting the JSON
            --or-show: Return existing domain
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: New domain description
                disable: Disable domain
                enable: Enable domain (default)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                immutable: Make resource immutable. An immutable project may not be deleted or modified except to remove the immutable flag
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                no-immutable: Make resource mutable (default)
                noindent: whether to disable indenting the JSON
                or-show: Return existing domain
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete domain(s)
        - name: list
          description: List domains
          flags:
            --enabled: The domains that are enabled will be returned
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --name=: The domain name
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                enabled: The domains that are enabled will be returned
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: The domain name
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: set
          description: Set domain properties
          flags:
            --description=: New domain description
            --disable: Disable domain
            --enable: Enable domain
            --immutable: Make resource immutable.
            --name=: New domain name
            --no-immutable: Make resource mutable (default)
          documentation:
            flag:
                description: New domain description
                disable: Disable domain
                enable: Enable domain
                immutable: Make resource immutable. An immutable project may not be deleted or modified except to remove the immutable flag
                name: New domain name
                no-immutable: Make resource mutable (default)
        - name: show
          description: Display domain details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: ec2
      commands:
        - name: credentials
          commands:
            - name: create
              description: Create EC2 credentials
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: 'Create credentials in project (name or ID; default: current authenticated project)'
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: 'Create credentials for user (name or ID; default: current authenticated user)'
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: 'Create credentials in project (name or ID; default: current authenticated project)'
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    user: 'Create credentials for user (name or ID; default: current authenticated user)'
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete EC2 credentials
              flags:
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: Delete credentials for user (name or ID)
              documentation:
                flag:
                    user: Delete credentials for user (name or ID)
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
            - name: list
              description: List EC2 credentials
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: Filter list by user (name or ID)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    user: Filter list by user (name or ID)
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
            - name: show
              description: Display EC2 credentials details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: Show credentials for user (name or ID)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    user: Show credentials for user (name or ID)
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
                    variable: ==SUPPRESS==
    - name: endpoint
      commands:
        - name: add
          commands:
            - name: project
              description: Associate a project to an endpoint
              flags:
                --project-domain=: Domain the project belongs to (name or ID).
              documentation:
                flag:
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
        - name: create
          description: Create new endpoint
          flags:
            --disable: Disable endpoint
            --enable: Enable endpoint (default)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --region=: New endpoint region ID
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                disable: Disable endpoint
                enable: Enable endpoint (default)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                region: New endpoint region ID
                variable: ==SUPPRESS==
        - name: delete
          description: Delete endpoint(s)
        - name: group
          commands:
            - name: add
              commands:
                - name: project
                  description: Add a project to an endpoint group
                  flags:
                    --project-domain=: Domain the project belongs to (name or ID).
                  documentation:
                    flag:
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
            - name: create
              description: Create new endpoint group
              flags:
                --description=: Description of the endpoint group
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the endpoint group
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete endpoint group(s)
            - name: list
              description: List endpoint groups
              flags:
                --domain=: Domain owning <project> (name or ID)
                --endpointgroup=: Endpoint Group (name or ID)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project=: Project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    domain: Domain owning <project> (name or ID)
                    endpointgroup: Endpoint Group (name or ID)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Project (name or ID)
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: remove
              commands:
                - name: project
                  description: Remove project from endpoint group
                  flags:
                    --project-domain=: Domain the project belongs to (name or ID).
                  documentation:
                    flag:
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
            - name: set
              description: Set endpoint group properties
              flags:
                --description=: New endpoint group description
                --filters=: Filename that contains a new set of filters
                --name=: New endpoint group name
              documentation:
                flag:
                    description: New endpoint group description
                    filters: Filename that contains a new set of filters
                    name: New endpoint group name
            - name: show
              description: Display endpoint group details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: list
          description: List endpoints
          flags:
            --endpoint=: Endpoint to list filters
            --fit-width: Fit the table to the display width.
            --interface=: Filter by interface type (admin, public or internal)
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Project to list filters (name or ID)
            --quote=: when to include quotes, defaults to nonnumeric
            --region=: Filter by region ID
            --service=: Filter by service (type, name or ID)
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                interface:
                    - admin
                    - public
                    - internal
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                endpoint: Endpoint to list filters
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                interface: Filter by interface type (admin, public or internal)
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: Project to list filters (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                quote: when to include quotes, defaults to nonnumeric
                region: Filter by region ID
                service: Filter by service (type, name or ID)
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: remove
          commands:
            - name: project
              description: Dissociate a project from an endpoint
              flags:
                --project-domain=: Domain the project belongs to (name or ID).
              documentation:
                flag:
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
        - name: set
          description: Set endpoint properties
          flags:
            --disable: Disable endpoint
            --enable: Enable endpoint
            --interface=: New endpoint interface type (admin, public or internal)
            --region=: New endpoint region ID
            --service=: New endpoint service (name or ID)
            --url=: New endpoint URL
          completion:
            flag:
                interface:
                    - admin
                    - public
                    - internal
          documentation:
            flag:
                disable: Disable endpoint
                enable: Enable endpoint
                interface: New endpoint interface type (admin, public or internal)
                region: New endpoint region ID
                service: New endpoint service (name or ID)
                url: New endpoint URL
        - name: show
          description: Display endpoint details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: extension
      commands:
        - name: list
          description: List API extensions
          flags:
            --compute: List extensions for the Compute API
            --fit-width: Fit the table to the display width.
            --identity: List extensions for the Identity API (only supported by v2)
            --long: List additional fields in output
            --max-width=: Maximum display width, <1 to disable.
            --network: List extensions for the Network API
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --volume: List extensions for the Block Storage API
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                compute: List extensions for the Compute API
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                identity: List extensions for the Identity API (only supported by v2)
                long: List additional fields in output
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                network: List extensions for the Network API
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                volume: List extensions for the Block Storage API
        - name: show
          description: Show API extension
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: federation
      commands:
        - name: domain
          commands:
            - name: list
              description: List accessible domains
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
        - name: project
          commands:
            - name: list
              description: List accessible projects
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
        - name: protocol
          commands:
            - name: create
              description: Create new federation protocol
              flags:
                --fit-width: Fit the table to the display width.
                --identity-provider=!: Identity provider that will support the new federation  protocol (name or ID) (required)
                --mapping=!: Mapping that is to be used (name or ID) (required)
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    identity-provider: Identity provider that will support the new federation  protocol (name or ID) (required)
                    mapping: Mapping that is to be used (name or ID) (required)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete federation protocol(s)
              flags:
                --identity-provider=!: Identity provider that supports <federation-protocol> (name or ID) (required)
              documentation:
                flag:
                    identity-provider: Identity provider that supports <federation-protocol> (name or ID) (required)
            - name: list
              description: List federation protocols
              flags:
                --fit-width: Fit the table to the display width.
                --identity-provider=!: Identity provider to list (name or ID) (required)
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    identity-provider: Identity provider to list (name or ID) (required)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set federation protocol properties
              flags:
                --identity-provider=!: Identity provider that supports <federation-protocol> (name or ID) (required)
                --mapping=: Mapping that is to be used (name or ID)
              documentation:
                flag:
                    identity-provider: Identity provider that supports <federation-protocol> (name or ID) (required)
                    mapping: Mapping that is to be used (name or ID)
            - name: show
              description: Display federation protocol details
              flags:
                --fit-width: Fit the table to the display width.
                --identity-provider=!: Identity provider that supports <federation-protocol> (name or ID) (required)
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    identity-provider: Identity provider that supports <federation-protocol> (name or ID) (required)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: firewall
      commands:
        - name: group
          commands:
            - name: create
              description: Create a new firewall group
              flags:
                --description=: Description of the firewall group
                --disable: Disable firewall group
                --egress-firewall-policy=: Egress firewall policy (name or ID)
                --enable: Enable firewall group
                --fit-width: Fit the table to the display width.
                --ingress-firewall-policy=: Ingress firewall policy (name or ID)
                --max-width=: Maximum display width, <1 to disable.
                --name=: (Deprecated, please pass name as a positional argument) Name for the firewall group
                --no-egress-firewall-policy: Detach egress firewall policy from the firewall group
                --no-ingress-firewall-policy: Detach ingress firewall policy from the firewall group
                --no-port: Detach all port from the firewall group
                --no-share: Restrict use of the firewall group to the current project
                --noindent: whether to disable indenting the JSON
                --port=: Port(s) (name or ID) to apply firewall group.
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --share: Share the firewall group to be used in all projects (by default, it is restricted to be used by the current project).
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the firewall group
                    disable: Disable firewall group
                    egress-firewall-policy: Egress firewall policy (name or ID)
                    enable: Enable firewall group
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ingress-firewall-policy: Ingress firewall policy (name or ID)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: (Deprecated, please pass name as a positional argument) Name for the firewall group
                    no-egress-firewall-policy: Detach egress firewall policy from the firewall group
                    no-ingress-firewall-policy: Detach ingress firewall policy from the firewall group
                    no-port: Detach all port from the firewall group
                    no-share: Restrict use of the firewall group to the current project
                    noindent: whether to disable indenting the JSON
                    port: Port(s) (name or ID) to apply firewall group.  This option can be repeated
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    share: Share the firewall group to be used in all projects (by default, it is restricted to be used by the current project).
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete firewall group(s)
            - name: list
              description: List firewall groups
              flags:
                --fit-width: Fit the table to the display width.
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: policy
              commands:
                - name: add
                  commands:
                    - name: rule
                      description: Insert a rule into a given firewall policy
                      flags:
                        --insert-after=: Insert the new rule after this existing rule  (name or ID)
                        --insert-before=: Insert the new rule before this existing rule  (name or ID)
                      documentation:
                        flag:
                            insert-after: Insert the new rule after this existing rule  (name or ID)
                            insert-before: Insert the new rule before this existing rule  (name or ID)
                - name: create
                  description: Create a new firewall policy
                  flags:
                    --audited: Enable auditing for the policy
                    --description=: Description of the firewall policy
                    --firewall-rule=: Firewall rule(s) to apply (name or ID)
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --no-audited: Disable auditing for the policy
                    --no-firewall-rule: Unset all firewall rules from firewall policy
                    --no-share: Restrict use of the firewall policy to the current project
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --share: Share the firewall policy to be used in all projects (by default, it is restricted to be used by the current project).
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        audited: Enable auditing for the policy
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description of the firewall policy
                        firewall-rule: Firewall rule(s) to apply (name or ID)
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        no-audited: Disable auditing for the policy
                        no-firewall-rule: Unset all firewall rules from firewall policy
                        no-share: Restrict use of the firewall policy to the current project
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        share: Share the firewall policy to be used in all projects (by default, it is restricted to be used by the current project).
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete firewall policy(s)
                - name: list
                  description: List firewall policies
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: remove
                  commands:
                    - name: rule
                      description: Remove a rule from a given firewall policy
                - name: set
                  description: Set firewall policy properties
                  flags:
                    --audited: Enable auditing for the policy
                    --description=: Description of the firewall policy
                    --firewall-rule=: Firewall rule(s) to apply (name or ID)
                    --name=: Name for the firewall policy
                    --no-audited: Disable auditing for the policy
                    --no-firewall-rule: Remove all firewall rules from firewall policy
                    --no-share: Restrict use of the firewall policy to the current project
                    --share: Share the firewall policy to be used in all projects (by default, it is restricted to be used by the current project).
                  documentation:
                    flag:
                        audited: Enable auditing for the policy
                        description: Description of the firewall policy
                        firewall-rule: Firewall rule(s) to apply (name or ID)
                        name: Name for the firewall policy
                        no-audited: Disable auditing for the policy
                        no-firewall-rule: Remove all firewall rules from firewall policy
                        no-share: Restrict use of the firewall policy to the current project
                        share: Share the firewall policy to be used in all projects (by default, it is restricted to be used by the current project).
                - name: show
                  description: Display firewall policy details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset firewall policy properties
                  flags:
                    --all-firewall-rule: Remove all firewall rules from the firewall policy
                    --audited: Disable auditing for the policy
                    --firewall-rule=: Remove firewall rule(s) from the firewall policy (name or ID)
                    --share: (Deprecated) Use "firewall policy set --no-share" instead.
                  documentation:
                    flag:
                        all-firewall-rule: Remove all firewall rules from the firewall policy
                        audited: Disable auditing for the policy
                        firewall-rule: Remove firewall rule(s) from the firewall policy (name or ID)
                        share: (Deprecated) Use "firewall policy set --no-share" instead. Restrict use of the firewall policy to the current project
            - name: rule
              commands:
                - name: create
                  description: Create a new firewall rule
                  flags:
                    --action=: Action for the firewall rule
                    --description=: Description of the firewall rule
                    --destination-firewall-group=: Destination firewall group (name or ID)
                    --destination-ip-address=: Destination IP address or subnet
                    --destination-port=: Destination port number or range(integer in [1, 65535] or range like 123:456)
                    --disable-rule: Disable this rule
                    --enable-rule: Enable this rule (default is enabled)
                    --fit-width: Fit the table to the display width.
                    --ip-version=: Set IP version 4 or 6 (default is 4)
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: (Deprecated, please pass name as a positional argument) Name of the firewall rule
                    --no-destination-firewall-group: No associated destination firewall group
                    --no-destination-ip-address: Detach destination IP address
                    --no-destination-port: Detach destination port number or range
                    --no-share: Restrict use of the firewall rule to the current project
                    --no-source-firewall-group: No associated source firewall group
                    --no-source-ip-address: Detach source IP address
                    --no-source-port: Detach source port number or range
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --protocol=: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                    --share: Share the firewall rule to be used in all projects (by default, it is restricted to be used by the current project).
                    --source-firewall-group=: Source firewall group (name or ID)
                    --source-ip-address=: Source IP address or subnet
                    --source-port=: Source port number or range (integer in [1, 65535] or range like 123:456)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        action:
                            - allow
                            - deny
                            - reject
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                        ip-version:
                            - "4"
                            - "6"
                  documentation:
                    flag:
                        action: Action for the firewall rule
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description of the firewall rule
                        destination-firewall-group: Destination firewall group (name or ID)
                        destination-ip-address: Destination IP address or subnet
                        destination-port: Destination port number or range(integer in [1, 65535] or range like 123:456)
                        disable-rule: Disable this rule
                        enable-rule: Enable this rule (default is enabled)
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        ip-version: Set IP version 4 or 6 (default is 4)
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: (Deprecated, please pass name as a positional argument) Name of the firewall rule
                        no-destination-firewall-group: No associated destination firewall group
                        no-destination-ip-address: Detach destination IP address
                        no-destination-port: Detach destination port number or range
                        no-share: Restrict use of the firewall rule to the current project
                        no-source-firewall-group: No associated source firewall group
                        no-source-ip-address: Detach source IP address
                        no-source-port: Detach source port number or range
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        protocol: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                        share: Share the firewall rule to be used in all projects (by default, it is restricted to be used by the current project).
                        source-firewall-group: Source firewall group (name or ID)
                        source-ip-address: Source IP address or subnet
                        source-port: Source port number or range (integer in [1, 65535] or range like 123:456)
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete firewall rule(s)
                - name: list
                  description: List firewall rules that belong to a given tenant
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set firewall rule properties
                  flags:
                    --action=: Action for the firewall rule
                    --description=: Description of the firewall rule
                    --destination-firewall-group=: Destination firewall group (name or ID)
                    --destination-ip-address=: Destination IP address or subnet
                    --destination-port=: Destination port number or range(integer in [1, 65535] or range like 123:456)
                    --disable-rule: Disable this rule
                    --enable-rule: Enable this rule (default is enabled)
                    --ip-version=: Set IP version 4 or 6 (default is 4)
                    --name=: Name of the firewall rule
                    --no-destination-firewall-group: No associated destination firewall group
                    --no-destination-ip-address: Detach destination IP address
                    --no-destination-port: Detach destination port number or range
                    --no-share: Restrict use of the firewall rule to the current project
                    --no-source-firewall-group: No associated source firewall group
                    --no-source-ip-address: Detach source IP address
                    --no-source-port: Detach source port number or range
                    --protocol=: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                    --share: Share the firewall rule to be used in all projects (by default, it is restricted to be used by the current project).
                    --source-firewall-group=: Source firewall group (name or ID)
                    --source-ip-address=: Source IP address or subnet
                    --source-port=: Source port number or range (integer in [1, 65535] or range like 123:456)
                  completion:
                    flag:
                        action:
                            - allow
                            - deny
                            - reject
                        ip-version:
                            - "4"
                            - "6"
                  documentation:
                    flag:
                        action: Action for the firewall rule
                        description: Description of the firewall rule
                        destination-firewall-group: Destination firewall group (name or ID)
                        destination-ip-address: Destination IP address or subnet
                        destination-port: Destination port number or range(integer in [1, 65535] or range like 123:456)
                        disable-rule: Disable this rule
                        enable-rule: Enable this rule (default is enabled)
                        ip-version: Set IP version 4 or 6 (default is 4)
                        name: Name of the firewall rule
                        no-destination-firewall-group: No associated destination firewall group
                        no-destination-ip-address: Detach destination IP address
                        no-destination-port: Detach destination port number or range
                        no-share: Restrict use of the firewall rule to the current project
                        no-source-firewall-group: No associated source firewall group
                        no-source-ip-address: Detach source IP address
                        no-source-port: Detach source port number or range
                        protocol: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                        share: Share the firewall rule to be used in all projects (by default, it is restricted to be used by the current project).
                        source-firewall-group: Source firewall group (name or ID)
                        source-ip-address: Source IP address or subnet
                        source-port: Source port number or range (integer in [1, 65535] or range like 123:456)
                - name: show
                  description: Display firewall rule details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset firewall rule properties
                  flags:
                    --destination-firewall-group: Destination firewall group (name or ID)
                    --destination-ip-address: Destination IP address or subnet
                    --destination-port: Destination port number or range(integer in [1, 65535] or range like 123:456)
                    --enable-rule: (Deprecated) Use "firewall rule set --disable-rule" instead.
                    --share: (Deprecated) Use "firewall rule set --no-share" instead.
                    --source-firewall-group: Source firewall group (name or ID)
                    --source-ip-address: Source IP address or subnet
                    --source-port: Source port number or range(integer in [1, 65535] or range like 123:456)
                  documentation:
                    flag:
                        destination-firewall-group: Destination firewall group (name or ID)
                        destination-ip-address: Destination IP address or subnet
                        destination-port: Destination port number or range(integer in [1, 65535] or range like 123:456)
                        enable-rule: (Deprecated) Use "firewall rule set --disable-rule" instead. Disable this rule
                        share: (Deprecated) Use "firewall rule set --no-share" instead. Restrict use of the firewall rule to the current project
                        source-firewall-group: Source firewall group (name or ID)
                        source-ip-address: Source IP address or subnet
                        source-port: Source port number or range(integer in [1, 65535] or range like 123:456)
            - name: set
              description: Set firewall group properties
              flags:
                --description=: Description of the firewall group
                --disable: Disable firewall group
                --egress-firewall-policy=: Egress firewall policy (name or ID)
                --enable: Enable firewall group
                --ingress-firewall-policy=: Ingress firewall policy (name or ID)
                --name=: Name for the firewall group
                --no-egress-firewall-policy: Detach egress firewall policy from the firewall group
                --no-ingress-firewall-policy: Detach ingress firewall policy from the firewall group
                --no-port: Detach all port from the firewall group
                --no-share: Restrict use of the firewall group to the current project
                --port=: Port(s) (name or ID) to apply firewall group.
                --share: Share the firewall group to be used in all projects (by default, it is restricted to be used by the current project).
              documentation:
                flag:
                    description: Description of the firewall group
                    disable: Disable firewall group
                    egress-firewall-policy: Egress firewall policy (name or ID)
                    enable: Enable firewall group
                    ingress-firewall-policy: Ingress firewall policy (name or ID)
                    name: Name for the firewall group
                    no-egress-firewall-policy: Detach egress firewall policy from the firewall group
                    no-ingress-firewall-policy: Detach ingress firewall policy from the firewall group
                    no-port: Detach all port from the firewall group
                    no-share: Restrict use of the firewall group to the current project
                    port: Port(s) (name or ID) to apply firewall group.  This option can be repeated
                    share: Share the firewall group to be used in all projects (by default, it is restricted to be used by the current project).
            - name: show
              description: Display firewall group details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset firewall group properties
              flags:
                --all-port: Remove all ports for this firewall group
                --egress-firewall-policy: Egress firewall policy (name or ID) to delete
                --enable: (Deprecated) Use "firewall group set --disable" instead.
                --ingress-firewall-policy: Ingress firewall policy (name or ID) to delete
                --port=: Port(s) (name or ID) to apply firewall group.
                --share: (Deprecated) Use "firewall group set --no-share" instead.
              documentation:
                flag:
                    all-port: Remove all ports for this firewall group
                    egress-firewall-policy: Egress firewall policy (name or ID) to delete
                    enable: (Deprecated) Use "firewall group set --disable" instead. Disable firewall group
                    ingress-firewall-policy: Ingress firewall policy (name or ID) to delete
                    port: Port(s) (name or ID) to apply firewall group.  This option can be repeated
                    share: (Deprecated) Use "firewall group set --no-share" instead. Restrict use of the firewall group to the current project
    - name: flavor
      commands:
        - name: create
          description: Create new flavor
          flags:
            --description=: Description for the flavor.(Supported
            --disk=: Disk size in GB (default 0G)
            --ephemeral=: Ephemeral disk size in GB (default 0G)
            --fit-width: Fit the table to the display width.
            --id=: Unique flavor ID
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --private: Flavor is not available to other projects
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Allow <project> to access private flavor (name or ID) (Must be used with --private option)
            --property=: Property to add for this flavor (repeat option to set multiple properties)
            --public: Flavor is available to other projects (default)
            --ram=: Memory size in MB (default 256M)
            --rxtx-factor=: RX/TX factor
            --swap=: Additional swap space size in MB (default 0M)
            --variable=: ==SUPPRESS==
            --vcpus=: Number of vcpus (default 1)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Description for the flavor.(Supported by API versions '2.55' - '2.latest'
                disk: Disk size in GB (default 0G)
                ephemeral: Ephemeral disk size in GB (default 0G)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                id: Unique flavor ID
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                private: Flavor is not available to other projects
                project: Allow <project> to access private flavor (name or ID) (Must be used with --private option)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Property to add for this flavor (repeat option to set multiple properties)
                public: Flavor is available to other projects (default)
                ram: Memory size in MB (default 256M)
                rxtx-factor: RX/TX factor
                swap: Additional swap space size in MB (default 0M)
                variable: ==SUPPRESS==
                vcpus: Number of vcpus (default 1)
        - name: delete
          description: Delete flavor(s)
        - name: list
          description: List flavors
          flags:
            --all: List all flavors, whether public or private
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --min-disk=: Filters the flavors by a minimum disk space, in GiB.
            --min-ram=: Filters the flavors by a minimum RAM, in MiB.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --private: List only private flavors
            --public: List only public flavors (default)
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all: List all flavors, whether public or private
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                min-disk: Filters the flavors by a minimum disk space, in GiB.
                min-ram: Filters the flavors by a minimum RAM, in MiB.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                private: List only private flavors
                public: List only public flavors (default)
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: set
          description: Set flavor properties
          flags:
            --description=: Set description for the flavor.(Supported
            --no-property: Remove all properties from this flavor (specify both --no-property and --property to remove the current properties before setting new properties.)
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Set flavor access to project (name or ID) (admin only)
            --property=: Property to add or modify for this flavor (repeat option to set multiple properties)
          documentation:
            flag:
                description: Set description for the flavor.(Supported by API versions '2.55' - '2.latest'
                no-property: Remove all properties from this flavor (specify both --no-property and --property to remove the current properties before setting new properties.)
                project: Set flavor access to project (name or ID) (admin only)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Property to add or modify for this flavor (repeat option to set multiple properties)
        - name: show
          description: Display flavor details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset flavor properties
          flags:
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Remove flavor access from project (name or ID) (admin only)
            --property=: Property to remove from flavor (repeat option to unset multiple properties)
          documentation:
            flag:
                project: Remove flavor access from project (name or ID) (admin only)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Property to remove from flavor (repeat option to unset multiple properties)
    - name: floating
      commands:
        - name: ip
          commands:
            - name: create
              description: Create floating IP
              flags:
                --description=: Set floating IP description
                --dns-domain=: Set DNS domain for this floating IP
                --dns-name=: Set DNS name for this floating IP
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --fixed-ip-address=: Fixed IP address mapped to the floating IP
                --floating-ip-address=: Floating IP address
                --max-width=: Maximum display width, <1 to disable.
                --no-tag: No tags associated with the floating IP
                --noindent: whether to disable indenting the JSON
                --port=: Port to be associated with the floating IP (name or ID)
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --qos-policy=: Attach QoS policy to the floating IP (name or ID)
                --subnet=: Subnet on which you want to create the floating IP (name or ID)
                --tag=: Tag to be added to the floating IP (repeat option to set multiple tags)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Set floating IP description
                    dns-domain: Set DNS domain for this floating IP
                    dns-name: Set DNS name for this floating IP
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    fixed-ip-address: Fixed IP address mapped to the floating IP
                    floating-ip-address: Floating IP address
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    no-tag: No tags associated with the floating IP
                    noindent: whether to disable indenting the JSON
                    port: Port to be associated with the floating IP (name or ID)
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    qos-policy: Attach QoS policy to the floating IP (name or ID)
                    subnet: Subnet on which you want to create the floating IP (name or ID)
                    tag: Tag to be added to the floating IP (repeat option to set multiple tags)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete floating IP(s)
            - name: list
              description: List floating IP(s)
              flags:
                --any-tags=: List floating IP which have any given tag(s) (Comma-separated list of tags)
                --fit-width: Fit the table to the display width.
                --fixed-ip-address=: List only floating IP(s) with the specified fixed IP address
                --floating-ip-address=: List only floating IP(s) with the specified floating IP address
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --network=: List only floating IP(s) with the specified network (name or ID) (repeat option to fiter on multiple networks)
                --noindent: whether to disable indenting the JSON
                --not-any-tags=: Exclude floating IP which have any given tag(s) (Comma-separated list of tags)
                --not-tags=: Exclude floating IP which have all given tag(s) (Comma-separated list of tags)
                --port=: List only floating IP(s) with the specified port (name or ID) (repeat option to fiter on multiple ports)
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: List only floating IP(s) with the specified project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --router=: List only floating IP(s) with the specified router (name or ID) (repeat option to fiter on multiple routers)
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --status=: List only floating IP(s) with the specified status ('ACTIVE', 'DOWN')
                --tags=: List floating IP which have all given tag(s) (Comma-separated list of tags)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    status:
                        - ACTIVE
                        - DOWN
              documentation:
                flag:
                    any-tags: List floating IP which have any given tag(s) (Comma-separated list of tags)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    fixed-ip-address: List only floating IP(s) with the specified fixed IP address
                    floating-ip-address: List only floating IP(s) with the specified floating IP address
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    network: List only floating IP(s) with the specified network (name or ID) (repeat option to fiter on multiple networks)
                    noindent: whether to disable indenting the JSON
                    not-any-tags: Exclude floating IP which have any given tag(s) (Comma-separated list of tags)
                    not-tags: Exclude floating IP which have all given tag(s) (Comma-separated list of tags)
                    port: List only floating IP(s) with the specified port (name or ID) (repeat option to fiter on multiple ports)
                    print-empty: Print empty table if there is no data to show.
                    project: List only floating IP(s) with the specified project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    router: List only floating IP(s) with the specified router (name or ID) (repeat option to fiter on multiple routers)
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    status: List only floating IP(s) with the specified status ('ACTIVE', 'DOWN')
                    tags: List floating IP which have all given tag(s) (Comma-separated list of tags)
            - name: port
              commands:
                - name: forwarding
                  commands:
                    - name: create
                      description: Create floating IP port forwarding
                      flags:
                        --description=: Text to describe/contextualize the use of the port forwarding configuration
                        --external-protocol-port=!: The protocol port number of the port forwarding's floating IP address
                        --extra-property=: Additional parameters can be passed using this property.
                        --fit-width: Fit the table to the display width.
                        --internal-ip-address=!: The fixed IPv4 address of the network port associated to the floating IP port forwarding
                        --internal-protocol-port=!: The protocol port number of the network port fixed IPv4 address associated to the floating IP port forwarding
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --port=!: The name or ID of the network port associated to the floating IP port forwarding
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --protocol=!: 'The protocol used in the floating IP port forwarding, for instance: TCP, UDP'
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            description: Text to describe/contextualize the use of the port forwarding configuration
                            external-protocol-port: The protocol port number of the port forwarding's floating IP address
                            extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            internal-ip-address: The fixed IPv4 address of the network port associated to the floating IP port forwarding
                            internal-protocol-port: The protocol port number of the network port fixed IPv4 address associated to the floating IP port forwarding
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            port: The name or ID of the network port associated to the floating IP port forwarding
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            protocol: 'The protocol used in the floating IP port forwarding, for instance: TCP, UDP'
                            variable: ==SUPPRESS==
                    - name: delete
                      description: Delete floating IP port forwarding
                    - name: list
                      description: List floating IP port forwarding
                      flags:
                        --external-protocol-port=: List only floating IP port forwardings with the specified external protocol port number
                        --fit-width: Fit the table to the display width.
                        --limit=: The maximum number of entries to return per page.
                        --marker=: The first position in the collection to return results from.
                        --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --port=: List only floating IP port forwardings with the specified internal network port (name or ID)
                        --print-empty: Print empty table if there is no data to show.
                        --protocol=: List only floating IP port forwardings with the specified protocol number
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            external-protocol-port: List only floating IP port forwardings with the specified external protocol port number
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                            marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                            max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            port: List only floating IP port forwardings with the specified internal network port (name or ID)
                            print-empty: Print empty table if there is no data to show.
                            protocol: List only floating IP port forwardings with the specified protocol number
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: set
                      description: Set floating IP Port Forwarding Properties
                      flags:
                        --description=: Text to describe/contextualize the use of the port forwarding configuration
                        --external-protocol-port=: The TCP/UDP/other protocol port number of the port forwarding's floating IP address
                        --extra-property=: Additional parameters can be passed using this property.
                        --internal-ip-address=: The fixed IPv4 address of the network port associated to the floating IP port forwarding
                        --internal-protocol-port=: The TCP/UDP/other protocol port number of the network port fixed IPv4 address associated to the floating IP port forwarding
                        --port=: The ID of the network port associated to the floating IP port forwarding
                        --protocol=: The IP protocol used in the floating IP port forwarding
                      completion:
                        flag:
                            protocol:
                                - tcp
                                - udp
                      documentation:
                        flag:
                            description: Text to describe/contextualize the use of the port forwarding configuration
                            external-protocol-port: The TCP/UDP/other protocol port number of the port forwarding's floating IP address
                            extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                            internal-ip-address: The fixed IPv4 address of the network port associated to the floating IP port forwarding
                            internal-protocol-port: The TCP/UDP/other protocol port number of the network port fixed IPv4 address associated to the floating IP port forwarding
                            port: The ID of the network port associated to the floating IP port forwarding
                            protocol: The IP protocol used in the floating IP port forwarding
                    - name: show
                      description: Display floating IP Port Forwarding details
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
            - name: set
              description: Set floating IP Properties
              flags:
                --description=: Set floating IP description
                --extra-property=: Additional parameters can be passed using this property.
                --fixed-ip-address=: Fixed IP of the port (required only if port has multiple IPs)
                --no-qos-policy: Remove the QoS policy attached to the floating IP
                --no-tag: Clear tags associated with the floating IP.
                --port=: Associate the floating IP with port (name or ID)
                --qos-policy=: Attach QoS policy to the floating IP (name or ID)
                --tag=: Tag to be added to the floating IP (repeat option to set multiple tags)
              documentation:
                flag:
                    description: Set floating IP description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fixed-ip-address: Fixed IP of the port (required only if port has multiple IPs)
                    no-qos-policy: Remove the QoS policy attached to the floating IP
                    no-tag: Clear tags associated with the floating IP. Specify both --tag and --no-tag to overwrite current tags
                    port: Associate the floating IP with port (name or ID)
                    qos-policy: Attach QoS policy to the floating IP (name or ID)
                    tag: Tag to be added to the floating IP (repeat option to set multiple tags)
            - name: show
              description: Display floating IP details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset floating IP Properties
              flags:
                --all-tag: Clear all tags associated with the floating IP
                --extra-property=: Additional parameters can be passed using this property.
                --port: Disassociate any port associated with the floating IP
                --qos-policy: Remove the QoS policy attached to the floating IP
                --tag=: Tag to be removed from the floating IP (repeat option to remove multiple tags)
              documentation:
                flag:
                    all-tag: Clear all tags associated with the floating IP
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    port: Disassociate any port associated with the floating IP
                    qos-policy: Remove the QoS policy attached to the floating IP
                    tag: Tag to be removed from the floating IP (repeat option to remove multiple tags)
    - name: group
      commands:
        - name: add
          commands:
            - name: user
              description: Add user to group
              flags:
                --group-domain=: Domain the group belongs to (name or ID).
                --user-domain=: Domain the user belongs to (name or ID).
              documentation:
                flag:
                    group-domain: Domain the group belongs to (name or ID). This can be used in case collisions between group names exist.
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: contains
          commands:
            - name: user
              description: Check user membership in group
              flags:
                --group-domain=: Domain the group belongs to (name or ID).
                --user-domain=: Domain the user belongs to (name or ID).
              documentation:
                flag:
                    group-domain: Domain the group belongs to (name or ID). This can be used in case collisions between group names exist.
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: create
          description: Create new group
          flags:
            --description=: New group description
            --domain=: Domain to contain new group (name or ID)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --or-show: Return existing group
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: New group description
                domain: Domain to contain new group (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                or-show: Return existing group
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete group(s)
          flags:
            --domain=: Domain containing group(s) (name or ID)
          documentation:
            flag:
                domain: Domain containing group(s) (name or ID)
        - name: list
          description: List groups
          flags:
            --domain=: Filter group list by <domain> (name or ID)
            --fit-width: Fit the table to the display width.
            --long: List additional fields in output
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Filter group list by <user> (name or ID)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                domain: Filter group list by <domain> (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                long: List additional fields in output
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                user: Filter group list by <user> (name or ID)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: remove
          commands:
            - name: user
              description: Remove user from group
              flags:
                --group-domain=: Domain the group belongs to (name or ID).
                --user-domain=: Domain the user belongs to (name or ID).
              documentation:
                flag:
                    group-domain: Domain the group belongs to (name or ID). This can be used in case collisions between group names exist.
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: set
          description: Set group properties
          flags:
            --description=: New group description
            --domain=: Domain containing <group> (name or ID)
            --name=: New group name
          documentation:
            flag:
                description: New group description
                domain: Domain containing <group> (name or ID)
                name: New group name
        - name: show
          description: Display group details
          flags:
            --domain=: Domain containing <group> (name or ID)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                domain: Domain containing <group> (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: help
      description: print detailed help for another command
    - name: host
      commands:
        - name: list
          description: 'DEPRECATED: List hosts'
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --zone=: Only return hosts in the availability zone
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                zone: Only return hosts in the availability zone
        - name: set
          description: 'DEPRECATED: Set host properties'
          flags:
            --disable: Disable the host
            --disable-maintenance: Disable maintenance mode for the host
            --enable: Enable the host
            --enable-maintenance: Enable maintenance mode for the host
          documentation:
            flag:
                disable: Disable the host
                disable-maintenance: Disable maintenance mode for the host
                enable: Enable the host
                enable-maintenance: Enable maintenance mode for the host
        - name: show
          description: 'DEPRECATED: Display host details'
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
    - name: hypervisor
      commands:
        - name: list
          description: List hypervisors
          flags:
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --matching=: Filter hypervisors using <hostname> substringHypervisor Type and Host IP are not returned when using microversion 2.52 or lower
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                matching: Filter hypervisors using <hostname> substringHypervisor Type and Host IP are not returned when using microversion 2.52 or lower
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: show
          description: Display hypervisor details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: stats
          commands:
            - name: show
              description: Display hypervisor stats details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: identity
      commands:
        - name: provider
          commands:
            - name: create
              description: Create new identity provider
              flags:
                --authorization-ttl=: Time to keep the role assignments for users authenticating via this identity provider.
                --description=: New identity provider description
                --disable: Disable the identity provider
                --domain=: Domain to associate with the identity provider.
                --enable: Enable identity provider (default)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --remote-id-file=: Name of a file that contains many remote IDs to associate with the identity provider, one per line
                --remote-id=: Remote IDs to associate with the Identity Provider (repeat option to provide multiple values)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    authorization-ttl: Time to keep the role assignments for users authenticating via this identity provider. When not provided, global default configured in the Identity service will be used. Available since Identity API version 3.14 (Ussuri).
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: New identity provider description
                    disable: Disable the identity provider
                    domain: Domain to associate with the identity provider. If not specified, a domain will be created automatically. (Name or ID)
                    enable: Enable identity provider (default)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    remote-id: Remote IDs to associate with the Identity Provider (repeat option to provide multiple values)
                    remote-id-file: Name of a file that contains many remote IDs to associate with the identity provider, one per line
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete identity provider(s)
            - name: list
              description: List identity providers
              flags:
                --enabled: List only enabled identity providers
                --fit-width: Fit the table to the display width.
                --id=: Filter identity providers by ID
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    enabled: List only enabled identity providers
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    id: Filter identity providers by ID
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set identity provider properties
              flags:
                --authorization-ttl=: Time to keep the role assignments for users authenticating via this identity provider.
                --description=: Set identity provider description
                --disable: Disable the identity provider
                --enable: Enable the identity provider
                --remote-id-file=: Name of a file that contains many remote IDs to associate with the identity provider, one per line
                --remote-id=: Remote IDs to associate with the Identity Provider (repeat option to provide multiple values)
              documentation:
                flag:
                    authorization-ttl: Time to keep the role assignments for users authenticating via this identity provider. Available since Identity API version 3.14 (Ussuri).
                    description: Set identity provider description
                    disable: Disable the identity provider
                    enable: Enable the identity provider
                    remote-id: Remote IDs to associate with the Identity Provider (repeat option to provide multiple values)
                    remote-id-file: Name of a file that contains many remote IDs to associate with the identity provider, one per line
            - name: show
              description: Display identity provider details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: image
      commands:
        - name: add
          commands:
            - name: project
              description: Associate project with image
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    variable: ==SUPPRESS==
        - name: create
          description: Create/upload an image
          flags:
            --checksum=: ==SUPPRESS==
            --community: Image is accessible by all users but does not appear in the default image list of any user except the owner (requires --os-image-api-version 2.5 or later)
            --container-format=: Image container format.
            --copy-from=: ==SUPPRESS==
            --disk-format=: Image disk format.
            --file=: Upload image from local file
            --fit-width: Fit the table to the display width.
            --force: Force image creation if volume is in use (only meaningful with --volume)
            --id=: Image ID to reserve
            --import: Force the use of glance image import instead of direct upload
            --location=: ==SUPPRESS==
            --max-width=: Maximum display width, <1 to disable.
            --min-disk=: Minimum disk size needed to boot image, in gigabytes
            --min-ram=: Minimum RAM size needed to boot image, in megabytes
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --private: Image is only accessible by the owner (default until --os-image-api-version 2.5)
            --progress: Show upload progress bar (ignored if passing data via stdin)
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Set an alternate project on this image (name or ID)
            --property=: Set a property on this image (repeat option to set multiple properties)
            --protected: Prevent image from being deleted
            --public: Image is accessible and visible to all users
            --shared: Image is only accessible by the owner and image members (requires --os-image-api-version 2.5 or later) (default since --os-image-api-version 2.5)
            --sign-cert-id=: The specified certificate UUID is a reference to the certificate in the key manager that corresponds to the public key and is used for signature validation.
            --sign-key-path=: Sign the image using the specified private key.
            --size=: Size of image data in bytes.
            --store=: ==SUPPRESS==
            --tag=: Set a tag on this image (repeat option to set multiple tags)
            --unprotected: Allow image to be deleted (default)
            --variable=: ==SUPPRESS==
            --volume=: Create image from a volume
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                container-format:
                    - ami
                    - ari
                    - aki
                    - bare
                    - docker
                    - ova
                    - ovf
                disk-format:
                    - ami
                    - ari
                    - aki
                    - vhd
                    - vmdk
                    - raw
                    - qcow2
                    - vhdx
                    - vdi
                    - iso
                    - ploop
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                checksum: ==SUPPRESS==
                column: specify the column(s) to include, can be repeated to show multiple columns
                community: Image is accessible by all users but does not appear in the default image list of any user except the owner (requires --os-image-api-version 2.5 or later)
                container-format: 'Image container format. The supported options are: ami, ari, aki, bare, docker, ova, ovf. The default format is: bare'
                copy-from: ==SUPPRESS==
                disk-format: 'Image disk format. The supported options are: ami, ari, aki, vhd, vmdk, raw, qcow2, vhdx, vdi, iso, ploop. The default format is: raw'
                file: Upload image from local file
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                force: Force image creation if volume is in use (only meaningful with --volume)
                format: the output format, defaults to table
                id: Image ID to reserve
                import: Force the use of glance image import instead of direct upload
                location: ==SUPPRESS==
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                min-disk: Minimum disk size needed to boot image, in gigabytes
                min-ram: Minimum RAM size needed to boot image, in megabytes
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                private: Image is only accessible by the owner (default until --os-image-api-version 2.5)
                progress: Show upload progress bar (ignored if passing data via stdin)
                project: Set an alternate project on this image (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Set a property on this image (repeat option to set multiple properties)
                protected: Prevent image from being deleted
                public: Image is accessible and visible to all users
                shared: Image is only accessible by the owner and image members (requires --os-image-api-version 2.5 or later) (default since --os-image-api-version 2.5)
                sign-cert-id: The specified certificate UUID is a reference to the certificate in the key manager that corresponds to the public key and is used for signature validation. Only use in combination with --sign-key-path
                sign-key-path: Sign the image using the specified private key. Only use in combination with --sign-cert-id
                size: Size of image data in bytes. Providing this can improve upload performance.
                store: ==SUPPRESS==
                tag: Set a tag on this image (repeat option to set multiple tags)
                unprotected: Allow image to be deleted (default)
                variable: ==SUPPRESS==
                volume: Create image from a volume
        - name: delete
          description: Delete image(s)
          flags:
            --store=: Store to delete image(s) from.
          documentation:
            flag:
                store: Store to delete image(s) from.
        - name: import
          description: Initiate the image import process.
          flags:
            --all-stores: Make image available to all stores (either '--store' or '--all-stores' required with the 'copy-image' import method)
            --allow-failure: When uploading to multiple stores, indicate that the import should be continue should any of the uploads fail.
            --disallow-failure: When uploading to multiple stores, indicate that the import should be reverted should any of the uploads fail.
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --method=: Import method used for image import process.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --remote-image=: The image of remote glance (ID only) to be imported (only valid with the 'glance-download' import method)
            --remote-region=: The remote Glance region to download the image from (only valid with the 'glance-download' import method)
            --remote-service-interface=: The remote Glance service interface to use when importing images (only valid with the 'glance-download' import method)
            --store=:
                description: Backend store to upload image to (specify multiple times to upload to multiple stores) (either '--store' or '--all-stores' required with the 'copy-image' import method)
                nargs: -1
            --uri=: URI to download the external image (only valid with the 'web-download' import method)
            --variable=: ==SUPPRESS==
            --wait: Wait for operation to complete
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
                method:
                    - glance-direct
                    - web-download
                    - glance-download
                    - copy-image
          commands:
            - name: info
              description: Show available import methods
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
          documentation:
            flag:
                all-stores: Make image available to all stores (either '--store' or '--all-stores' required with the 'copy-image' import method)
                allow-failure: When uploading to multiple stores, indicate that the import should be continue should any of the uploads fail. Only usable with --stores or --all-stores
                column: specify the column(s) to include, can be repeated to show multiple columns
                disallow-failure: When uploading to multiple stores, indicate that the import should be reverted should any of the uploads fail. Only usable with --stores or --all-stores
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                method: Import method used for image import process. Not all deployments will support all methods. The 'glance-direct' method (default) requires images be first staged using the 'image-stage' command.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                remote-image: The image of remote glance (ID only) to be imported (only valid with the 'glance-download' import method)
                remote-region: The remote Glance region to download the image from (only valid with the 'glance-download' import method)
                remote-service-interface: The remote Glance service interface to use when importing images (only valid with the 'glance-download' import method)
                store: Backend store to upload image to (specify multiple times to upload to multiple stores) (either '--store' or '--all-stores' required with the 'copy-image' import method)
                uri: URI to download the external image (only valid with the 'web-download' import method)
                variable: ==SUPPRESS==
                wait: Wait for operation to complete
        - name: list
          description: List available images
          flags:
            --all: List all images
            --community: List only community images (requires --os-image-api-version 2.5 or later)
            --fit-width: Fit the table to the display width.
            --hidden: List hidden images
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --member-status=: Filter images based on member status.
            --name=: Filter images based on name.
            --noindent: whether to disable indenting the JSON
            --page-size=: ==SUPPRESS==
            --print-empty: Print empty table if there is no data to show.
            --private: List only private images
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Search by project (admin only) (name or ID)
            --property=: Filter output based on property (repeat option to filter on multiple properties)
            --public: List only public images
            --quote=: when to include quotes, defaults to nonnumeric
            --shared: List only shared images (requires --os-image-api-version 2.5 or later)
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --sort=: 'Sort output by selected keys and directions (asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
            --status=: Filter images based on status.
            --tag=: Filter images based on tag.
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                member-status:
                    - accepted
                    - pending
                    - rejected
                    - all
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all: List all images
                column: specify the column(s) to include, can be repeated to show multiple columns
                community: List only community images (requires --os-image-api-version 2.5 or later)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                hidden: List hidden images
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                member-status: 'Filter images based on member status. The supported options are: accepted, pending, rejected, all. '
                name: Filter images based on name.
                noindent: whether to disable indenting the JSON
                page-size: ==SUPPRESS==
                print-empty: Print empty table if there is no data to show.
                private: List only private images
                project: Search by project (admin only) (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Filter output based on property (repeat option to filter on multiple properties)
                public: List only public images
                quote: when to include quotes, defaults to nonnumeric
                shared: List only shared images (requires --os-image-api-version 2.5 or later)
                sort: 'Sort output by selected keys and directions (asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                status: Filter images based on status.
                tag: Filter images based on tag. (repeat option to filter on multiple tags)
        - name: member
          commands:
            - name: get
              description: Show a particular project associated with image
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    variable: ==SUPPRESS==
            - name: list
              description: List projects associated with image
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
        - name: metadef
          commands:
            - name: namespace
              commands:
                - name: create
                  description: Create a metadef namespace
                  flags:
                    --description=: A description of the namespace
                    --display-name=: A user-friendly name for the namespace.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --private: Set namespace visibility 'private'
                    --protected: Prevent metadef namespace from being deleted
                    --public: Set namespace visibility 'public'
                    --unprotected: Allow metadef namespace to be deleted (default)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: A description of the namespace
                        display-name: A user-friendly name for the namespace.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        private: Set namespace visibility 'private'
                        protected: Prevent metadef namespace from being deleted
                        public: Set namespace visibility 'public'
                        unprotected: Allow metadef namespace to be deleted (default)
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete metadef namespace
                - name: list
                  description: List metadef namespaces
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --resource-types=: filter resource types
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    --visibility=: filter on visibility
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        resource-types: filter resource types
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                        visibility: filter on visibility
                - name: set
                  description: Set metadef namespace properties
                  flags:
                    --description=: Set the description of the namespace
                    --display-name=: Set a user-friendly name for the namespace.
                    --private: Metadef namespace is inaccessible to the public (default)
                    --protected: Prevent metadef namespace from being deleted
                    --public: Metadef namespace is accessible to the public
                    --tag=: Set a tag on this metadef namespace (repeat option to set multiple tags)
                    --unprotected: Allow metadef namespace to be deleted (default)
                  documentation:
                    flag:
                        description: Set the description of the namespace
                        display-name: Set a user-friendly name for the namespace.
                        private: Metadef namespace is inaccessible to the public (default)
                        protected: Prevent metadef namespace from being deleted
                        public: Metadef namespace is accessible to the public
                        tag: Set a tag on this metadef namespace (repeat option to set multiple tags)
                        unprotected: Allow metadef namespace to be deleted (default)
                - name: show
                  description: Show a metadef namespace
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset metadef namespace tags
                  flags:
                    --all-tags: Unset all metadef tags
                    --tag=: Unset a tag on this metadef namespace (repeat option to unset multiple tags)
                  documentation:
                    flag:
                        all-tags: Unset all metadef tags
                        tag: Unset a tag on this metadef namespace (repeat option to unset multiple tags)
            - name: object
              commands:
                - name: create
                  description: Create a metadef object
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --namespace=: Metadef namespace to create the object in (name)
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        namespace: Metadef namespace to create the object in (name)
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete metadata definitions object(s)
                - name: list
                  description: List metadef objects inside a specific namespace.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: property
                  commands:
                    - name: show
                      description: Describe a specific metadata definitions property inside an object.
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
                - name: show
                  description: Show a particular metadef object
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: update
                  description: Update a metadef object
                  flags:
                    --name=: New name of the object
                  documentation:
                    flag:
                        name: New name of the object
            - name: property
              commands:
                - name: create
                  description: Create a metadef property
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=!: Internal name of the property
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --schema=!: Valid JSON schema of the property
                    --title=!: Property name displayed to the user
                    --type=!: Property type
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: Internal name of the property
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        schema: Valid JSON schema of the property
                        title: Property name displayed to the user
                        type: Property type
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete metadef propert(ies)
                - name: list
                  description: List metadef properties
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Update metadef namespace property
                  flags:
                    --name=: Internal name of the property
                    --schema=: Valid JSON schema of the property
                    --title=: Property name displayed to the user
                    --type=: Property type
                  documentation:
                    flag:
                        name: Internal name of the property
                        schema: Valid JSON schema of the property
                        title: Property name displayed to the user
                        type: Property type
                - name: show
                  description: Show a particular metadef property
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: resource
              commands:
                - name: type
                  commands:
                    - name: association
                      commands:
                        - name: create
                          description: Create metadef resource type association
                          flags:
                            --fit-width: Fit the table to the display width.
                            --max-width=: Maximum display width, <1 to disable.
                            --noindent: whether to disable indenting the JSON
                            --prefix=: add a prefix to all variable names
                            --print-empty: Print empty table if there is no data to show.
                            --properties-target=: Some resource types allow more than one key/value pair per instance.
                            --variable=: ==SUPPRESS==
                            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                            -f, --format=: the output format, defaults to table
                          completion:
                            flag:
                                format:
                                    - json
                                    - shell
                                    - table
                                    - value
                                    - yaml
                          documentation:
                            flag:
                                column: specify the column(s) to include, can be repeated to show multiple columns
                                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                                format: the output format, defaults to table
                                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                                noindent: whether to disable indenting the JSON
                                prefix: add a prefix to all variable names
                                print-empty: Print empty table if there is no data to show.
                                properties-target: Some resource types allow more than one key/value pair per instance.
                                variable: ==SUPPRESS==
                        - name: delete
                          description: Delete metadef resource type association
                          flags:
                            --force: Force delete the resource type association if thenamespace is protected
                          documentation:
                            flag:
                                force: Force delete the resource type association if thenamespace is protected
                        - name: list
                          description: List metadef resource type associations
                          flags:
                            --fit-width: Fit the table to the display width.
                            --max-width=: Maximum display width, <1 to disable.
                            --noindent: whether to disable indenting the JSON
                            --print-empty: Print empty table if there is no data to show.
                            --quote=: when to include quotes, defaults to nonnumeric
                            --sort-ascending: sort the column(s) in ascending order
                            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            --sort-descending: sort the column(s) in descending order
                            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                            -f, --format=: the output format, defaults to table
                          completion:
                            flag:
                                format:
                                    - csv
                                    - json
                                    - table
                                    - value
                                    - yaml
                                quote:
                                    - all
                                    - minimal
                                    - none
                                    - nonnumeric
                          documentation:
                            flag:
                                column: specify the column(s) to include, can be repeated to show multiple columns
                                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                                format: the output format, defaults to table
                                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                                noindent: whether to disable indenting the JSON
                                print-empty: Print empty table if there is no data to show.
                                quote: when to include quotes, defaults to nonnumeric
                                sort-ascending: sort the column(s) in ascending order
                                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                                sort-descending: sort the column(s) in descending order
                    - name: list
                      description: List metadef resource types
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
        - name: remove
          commands:
            - name: project
              description: Disassociate project with image
              flags:
                --project-domain=: Domain the project belongs to (name or ID).
              documentation:
                flag:
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
        - name: save
          description: Save an image locally
          flags:
            --chunk-size=: 'Size in bytes to read from the wire and buffer at one time (default: 1024)'
            --file=: 'Downloaded image save filename (default: stdout)'
          documentation:
            flag:
                chunk-size: 'Size in bytes to read from the wire and buffer at one time (default: 1024)'
                file: 'Downloaded image save filename (default: stdout)'
        - name: set
          description: Set image properties
          flags:
            --accept: Accept the image membership for either the project indicated by '--project', if provided, or the current user's project
            --activate: Activate the image
            --architecture=: Operating system architecture
            --community: Image is accessible by all users but does not appear in the default image list of any user except the owner (requires --os-image-api-version 2.5 or later)
            --container-format=: Image container format.
            --deactivate: Deactivate the image
            --disk-format=: Image disk format.
            --hidden: Hide the image
            --instance-id=: ID of server instance used to create this image
            --instance-uuid=: ==SUPPRESS==
            --kernel-id=: ID of kernel image used to boot this disk image
            --min-disk=: Minimum disk size needed to boot image, in gigabytes
            --min-ram=: Minimum RAM size needed to boot image, in megabytes
            --name=: New image name
            --os-distro=: Operating system distribution name
            --os-version=: Operating system distribution version
            --pending: Reset the image membership to 'pending'
            --private: Image is only accessible by the owner (default until --os-image-api-version 2.5)
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Set an alternate project on this image (name or ID)
            --property=: Set a property on this image (repeat option to set multiple properties)
            --protected: Prevent image from being deleted
            --public: Image is accessible and visible to all users
            --ramdisk-id=: ID of ramdisk image used to boot this disk image
            --reject: Reject the image membership for either the project indicated by '--project', if provided, or the current user's project
            --shared: Image is only accessible by the owner and image members (requires --os-image-api-version 2.5 or later) (default since --os-image-api-version 2.5)
            --tag=: Set a tag on this image (repeat option to set multiple tags)
            --unhidden: Unhide the image
            --unprotected: Allow image to be deleted (default)
            --visibility=: ==SUPPRESS==
          completion:
            flag:
                container-format:
                    - ami
                    - ari
                    - aki
                    - bare
                    - docker
                    - ova
                    - ovf
                disk-format:
                    - ami
                    - ari
                    - aki
                    - vhd
                    - vmdk
                    - raw
                    - qcow2
                    - vhdx
                    - vdi
                    - iso
                    - ploop
          documentation:
            flag:
                accept: Accept the image membership for either the project indicated by '--project', if provided, or the current user's project
                activate: Activate the image
                architecture: Operating system architecture
                community: Image is accessible by all users but does not appear in the default image list of any user except the owner (requires --os-image-api-version 2.5 or later)
                container-format: 'Image container format. The supported options are: ami, ari, aki, bare, docker, ova, ovf'
                deactivate: Deactivate the image
                disk-format: 'Image disk format. The supported options are: ami, ari, aki, vhd, vmdk, raw, qcow2, vhdx, vdi, iso, ploop'
                hidden: Hide the image
                instance-id: ID of server instance used to create this image
                instance-uuid: ==SUPPRESS==
                kernel-id: ID of kernel image used to boot this disk image
                min-disk: Minimum disk size needed to boot image, in gigabytes
                min-ram: Minimum RAM size needed to boot image, in megabytes
                name: New image name
                os-distro: Operating system distribution name
                os-version: Operating system distribution version
                pending: Reset the image membership to 'pending'
                private: Image is only accessible by the owner (default until --os-image-api-version 2.5)
                project: Set an alternate project on this image (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Set a property on this image (repeat option to set multiple properties)
                protected: Prevent image from being deleted
                public: Image is accessible and visible to all users
                ramdisk-id: ID of ramdisk image used to boot this disk image
                reject: Reject the image membership for either the project indicated by '--project', if provided, or the current user's project
                shared: Image is only accessible by the owner and image members (requires --os-image-api-version 2.5 or later) (default since --os-image-api-version 2.5)
                tag: Set a tag on this image (repeat option to set multiple tags)
                unhidden: Unhide the image
                unprotected: Allow image to be deleted (default)
                visibility: ==SUPPRESS==
        - name: show
          description: Display image details
          flags:
            --fit-width: Fit the table to the display width.
            --human-readable: Print image size in a human-friendly format.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                human-readable: Print image size in a human-friendly format.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: stage
          description: Upload data for a specific image to staging.
          flags:
            --file=: Local file that contains disk image to be uploaded.
            --progress: Show upload progress bar (ignored if passing data via stdin)
            --size=: Size of image data in bytes.
          documentation:
            flag:
                file: Local file that contains disk image to be uploaded. Alternatively, images can be passed via stdin.
                progress: Show upload progress bar (ignored if passing data via stdin)
                size: Size of image data in bytes. Providing this can improve upload performance.
        - name: stores
          commands:
            - name: list
              description: Get available backends (only valid with Multi-Backend support)
              flags:
                --detail: Shows details of stores (admin only) (requires --os-image-api-version 2.15 or later)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    detail: Shows details of stores (admin only) (requires --os-image-api-version 2.15 or later)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
        - name: task
          commands:
            - name: list
              description: List tasks
              flags:
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --sort-dir=: 'Sort output by selected keys and directions (asc or desc) (default: name:desc) (multiple keys and directions can be specified separated by comma)'
                --sort-key=: 'Sorts the response by one of the following attributes: created_at, expires_at, id, status, type, updated_at.'
                --status=: Filter tasks based on status.
                --type=: Filters the response by a task type.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    status:
                        - pending
                        - processing
                        - success
                        - failure
                    type:
                        - import
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    sort-dir: 'Sort output by selected keys and directions (asc or desc) (default: name:desc) (multiple keys and directions can be specified separated by comma)'
                    sort-key: 'Sorts the response by one of the following attributes: created_at, expires_at, id, status, type, updated_at. (default is created_at) (multiple keys and directions can be specified separated by comma)'
                    status: Filter tasks based on status.
                    type: Filters the response by a task type.
            - name: show
              description: Display task details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: unset
          description: Unset image tags and properties
          flags:
            --property=: Unset a property on this image (repeat option to unset multiple properties)
            --tag=: Unset a tag on this image (repeat option to unset multiple tags)
          documentation:
            flag:
                property: Unset a property on this image (repeat option to unset multiple properties)
                tag: Unset a tag on this image (repeat option to unset multiple tags)
    - name: implied
      commands:
        - name: role
          commands:
            - name: create
              description: Creates an association between prior and implied roles
              flags:
                --fit-width: Fit the table to the display width.
                --implied-role=!: <role> (name or ID) implied by another role
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    implied-role: <role> (name or ID) implied by another role
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Deletes an association between prior and implied roles
              flags:
                --implied-role=!: <role> (name or ID) implied by another role
              documentation:
                flag:
                    implied-role: <role> (name or ID) implied by another role
            - name: list
              description: List implied roles
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
    - name: ip
      commands:
        - name: availability
          commands:
            - name: list
              description: List IP availability for network
              flags:
                --fit-width: Fit the table to the display width.
                --ip-version=: List only IP availability with the specified IP version networks (4 or 6, default is 4)
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: List only IP availability with the specified project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    ip-version:
                        - "4"
                        - "6"
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ip-version: List only IP availability with the specified IP version networks (4 or 6, default is 4)
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: List only IP availability with the specified project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show network IP availability details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: keypair
      commands:
        - name: create
          description: Create new public or private key for server ssh access
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --private-key=: Filename for private key to save.
            --public-key=: Filename for public key to add.
            --type=: Keypair type (supported by --os-compute-api-version 2.2 or above)
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: The owner of the keypair (admin only) (name or ID) (supported by --os-compute-api-version 2.10 or above)
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
                type:
                    - ssh
                    - x509
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                private-key: Filename for private key to save. If not used, print private key in console.
                public-key: Filename for public key to add. If not used, generates a private key in ssh-ed25519 format. To generate keys in other formats, including the legacy ssh-rsa format, you must use an external tool such as ssh-keygen and specify this argument.
                type: Keypair type (supported by --os-compute-api-version 2.2 or above)
                user: The owner of the keypair (admin only) (name or ID) (supported by --os-compute-api-version 2.10 or above)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete public or private key(s)
          flags:
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: The owner of the keypair.
          documentation:
            flag:
                user: The owner of the keypair. (admin only) (name or ID). Requires ``--os-compute-api-version`` 2.10 or greater.
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: list
          description: List key fingerprints
          flags:
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Show keypairs for all users associated with project (admin only) (name or ID).
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Show keypairs for another user (admin only) (name or ID).
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: Show keypairs for all users associated with project (admin only) (name or ID). Requires ``--os-compute-api-version`` 2.10 or greater.
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                user: Show keypairs for another user (admin only) (name or ID). Requires ``--os-compute-api-version`` 2.10 or greater.
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: show
          description: Display key details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --public-key: Show only bare public key paired with the generated key
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: The owner of the keypair.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                public-key: Show only bare public key paired with the generated key
                user: The owner of the keypair. (admin only) (name or ID). Requires ``--os-compute-api-version`` 2.10 or greater.
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
                variable: ==SUPPRESS==
    - name: limit
      commands:
        - name: create
          description: Create a limit
          flags:
            --description=: Description of the limit
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=!: Project to associate the resource limit to
            --region=: Region for the limit to affect.
            --resource-limit=!: The resource limit for the project to assume
            --service=!: Service responsible for the resource to limit
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Description of the limit
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Project to associate the resource limit to
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                region: Region for the limit to affect.
                resource-limit: The resource limit for the project to assume
                service: Service responsible for the resource to limit
                variable: ==SUPPRESS==
        - name: delete
          description: Delete a limit
        - name: list
          description: List limits
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: List resource limits associated with project
            --quote=: when to include quotes, defaults to nonnumeric
            --region=: Region for the registered limit to affect.
            --resource-name=: The name of the resource to limit
            --service=: Service responsible for the resource to limit
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: List resource limits associated with project
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                quote: when to include quotes, defaults to nonnumeric
                region: Region for the registered limit to affect.
                resource-name: The name of the resource to limit
                service: Service responsible for the resource to limit
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: set
          description: Update information about a limit
          flags:
            --description=: Description of the limit
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --resource-limit=: The resource limit for the project to assume
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Description of the limit
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                resource-limit: The resource limit for the project to assume
                variable: ==SUPPRESS==
        - name: show
          description: Display limit details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: limits
      commands:
        - name: show
          description: Show compute and block storage limits
          flags:
            --absolute: Show absolute limits
            --domain=: ==SUPPRESS==
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Show limits for a specific project (name or ID) (only valid with --absolute)
            --quote=: when to include quotes, defaults to nonnumeric
            --rate: Show rate limits.
            --reserved: Include reservations count (only valid with --absolute)
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                absolute: Show absolute limits
                column: specify the column(s) to include, can be repeated to show multiple columns
                domain: ==SUPPRESS==
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: Show limits for a specific project (name or ID) (only valid with --absolute)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                quote: when to include quotes, defaults to nonnumeric
                rate: Show rate limits. This is not supported by the compute service since the 12.0.0 (Liberty) release and is only supported by the block storage service when the rate-limiting middleware is enabled. It is therefore a no-op in most deployments.
                reserved: Include reservations count (only valid with --absolute)
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
    - name: local
      commands:
        - name: ip
          commands:
            - name: association
              commands:
                - name: create
                  description: Create Local IP Association
                  flags:
                    --fit-width: Fit the table to the display width.
                    --fixed-ip=: Fixed IP for Local IP Association
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        fixed-ip: Fixed IP for Local IP Association
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete Local IP association(s)
                - name: list
                  description: List Local IP Associations
                  flags:
                    --fit-width: Fit the table to the display width.
                    --fixed-ip=: List only local IP associations with the specified fixed IP
                    --fixed-port=: List only local IP assocations with the specified fixed IP port (name or ID)
                    --host=: List only local IP associations with the specified host
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        fixed-ip: List only local IP associations with the specified fixed IP
                        fixed-port: List only local IP assocations with the specified fixed IP port (name or ID)
                        format: the output format, defaults to table
                        host: List only local IP associations with the specified host
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
            - name: create
              description: Create Local IP
              flags:
                --description=: Description for Local IP
                --fit-width: Fit the table to the display width.
                --ip-mode=: IP mode to use for Local IP
                --local-ip-address=: IP address or CIDR for Local IP
                --local-port=: Port to allocate Local IP from (name or ID)
                --max-width=: Maximum display width, <1 to disable.
                --name=: New Local IP name
                --network=: Network to allocate Local IP from (name or ID)
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description for Local IP
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ip-mode: IP mode to use for Local IP
                    local-ip-address: IP address or CIDR for Local IP
                    local-port: Port to allocate Local IP from (name or ID)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: New Local IP name
                    network: Network to allocate Local IP from (name or ID)
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete Local IP(s)
            - name: list
              description: List Local IPs
              flags:
                --fit-width: Fit the table to the display width.
                --ip-mode=: List only local IP(s) with the specified IP mode
                --local-ip-address=: List only local IP(s) with the specified IP address
                --local-port=: List only local IP(s) with the specified port (name or ID)
                --max-width=: Maximum display width, <1 to disable.
                --name=: List only local IP(s) with the specified name
                --network=: List only local IP(s) with the specified network (name or ID)
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: List only local IP(s) with the specified project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ip-mode: List only local IP(s) with the specified IP mode
                    local-ip-address: List only local IP(s) with the specified IP address
                    local-port: List only local IP(s) with the specified port (name or ID)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: List only local IP(s) with the specified name
                    network: List only local IP(s) with the specified network (name or ID)
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: List only local IP(s) with the specified project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set Local IP properties
              flags:
                --description=: Set Local IP description
                --name=: Set local IP name
              documentation:
                flag:
                    description: Set Local IP description
                    name: Set local IP name
            - name: show
              description: Display Local IP details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: mapping
      commands:
        - name: create
          description: Create new mapping
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --rules=!: Filename that contains a set of mapping rules (required)
            --schema-version=: The federated attribute mapping schema version.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                rules: Filename that contains a set of mapping rules (required)
                schema-version: The federated attribute mapping schema version. The default value on the client side is 'None'; however, that will lead the backend to set the default according to 'attribute_mapping_default_schema_version' option.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete mapping(s)
        - name: list
          description: List mappings
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: set
          description: Set mapping properties
          flags:
            --rules=: Filename that contains a new set of mapping rules
            --schema-version=: The federated attribute mapping schema version.
          documentation:
            flag:
                rules: Filename that contains a new set of mapping rules
                schema-version: The federated attribute mapping schema version. The default value on the client side is 'None'; however, that will lead the backend to set the default according to 'attribute_mapping_default_schema_version' option.
        - name: show
          description: Display mapping details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: module
      commands:
        - name: list
          description: List module versions
          flags:
            --all: Show all modules that have version information
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                all: Show all modules that have version information
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: network
      commands:
        - name: agent
          commands:
            - name: add
              commands:
                - name: network
                  description: Add network to an agent
                  flags:
                    --dhcp: Add network to a DHCP agent
                  documentation:
                    flag:
                        dhcp: Add network to a DHCP agent
                - name: router
                  description: Add router to an agent
                  flags:
                    --ha-chassis-priority=: HA Chassis priority, ranging from [0, 32767].
                    --l3: Add router to an L3 agent
                  documentation:
                    flag:
                        ha-chassis-priority: HA Chassis priority, ranging from [0, 32767]. Only used with --l3 and for ML2/OVN L3 agents
                        l3: Add router to an L3 agent
            - name: delete
              description: Delete network agent(s)
            - name: list
              description: List network agents
              flags:
                --agent-type=: List only agents with the specified agent type.
                --fit-width: Fit the table to the display width.
                --host=: List only agents running on the specified host
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --network=: List agents hosting the specified network (name or ID)
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --router=: List agents hosting the specified router (name or ID)
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    agent-type:
                        - bgp
                        - dhcp
                        - open-vswitch
                        - linux-bridge
                        - ofa
                        - l3
                        - loadbalancer
                        - metering
                        - metadata
                        - macvtap
                        - nic
                        - baremetal
                        - ovn-controller
                        - ovn-controller-gateway
                        - ovn-metadata
                        - ovn-agent
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    agent-type: 'List only agents with the specified agent type. The supported agent types are: bgp,dhcp,open-vswitch,linux-bridge,ofa,l3,loadbalancer,metering,metadata,macvtap,nic,baremetal,ovn-controller,ovn-controller-gateway,ovn-metadata,ovn-agent.'
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: List only agents running on the specified host
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    network: List agents hosting the specified network (name or ID)
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    router: List agents hosting the specified router (name or ID)
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: remove
              commands:
                - name: network
                  description: Remove network from an agent.
                  flags:
                    --dhcp: Remove network from DHCP agent
                  documentation:
                    flag:
                        dhcp: Remove network from DHCP agent
                - name: router
                  description: Remove router from an agent
                  flags:
                    --l3: Remove router from an L3 agent
                  documentation:
                    flag:
                        l3: Remove router from an L3 agent
            - name: router
              commands:
                - name: set
                  description: Set properties of a router associated to an agent
                  flags:
                    --ha-chassis-priority=!: HA Chassis priority, ranging from [0, 32767].
                  documentation:
                    flag:
                        ha-chassis-priority: HA Chassis priority, ranging from [0, 32767]. Only used with --l3 and for ML2/OVN L3 agents
            - name: set
              description: Set network agent properties
              flags:
                --description=: Set network agent description
                --disable: Disable network agent
                --enable: Enable network agent
              documentation:
                flag:
                    description: Set network agent description
                    disable: Disable network agent
                    enable: Enable network agent
            - name: show
              description: Display network agent details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: auto
          commands:
            - name: allocated
              commands:
                - name: topology
                  commands:
                    - name: create
                      description: Create the  auto allocated topology for project
                      flags:
                        --check-resources: Validate the requirements for auto allocated topology.
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --or-show: If topology exists returns the topology's information (default)
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --project-domain=: Domain the project belongs to (name or ID).
                        --project=: Return the auto allocated topology for a given project.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            check-resources: Validate the requirements for auto allocated topology. Does not return a topology.
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            or-show: If topology exists returns the topology's information (default)
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            project: Return the auto allocated topology for a given project. Default is current project.
                            project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                            variable: ==SUPPRESS==
                    - name: delete
                      description: Delete auto allocated topology for project
                      flags:
                        --project-domain=: Domain the project belongs to (name or ID).
                        --project=: Delete auto allocated topology for a given project.
                      documentation:
                        flag:
                            project: Delete auto allocated topology for a given project. Default is the current project.
                            project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
        - name: create
          description: Create new network
          flags:
            --availability-zone-hint=: Availability Zone in which to create this network (Network Availability Zone extension required, repeat option to set multiple availability zones)
            --default: Specify if this network should be used as the default external network
            --description=: Set network description
            --disable: Disable network
            --disable-port-security: Disable port security by default for ports created on this network
            --dns-domain=: Set DNS domain for this network (requires DNS integration extension)
            --enable: Enable network (default)
            --enable-port-security: Enable port security by default for ports created on this network (default)
            --external: The network has an external routing facility that is not managed by Neutron and can be used.
            --extra-property=: Additional parameters can be passed using this property.
            --fit-width: Fit the table to the display width.
            --internal: Opposite of '--external' (default)
            --max-width=: Maximum display width, <1 to disable.
            --mtu=: Set network mtu
            --no-default: Do not use the network as the default external network (default)
            --no-pvlan: Disable Private VLAN for the network (PVLAN extension required)
            --no-qinq-vlan: Disable VLAN QinQ (S-Tag ethtype 0x8a88) for the network
            --no-share: Do not share the network between projects
            --no-tag: No tags associated with the network
            --no-transparent-vlan: Do not make the network VLAN transparent
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Owner's project (name or ID)
            --provider-network-type=: The physical mechanism by which the virtual network is implemented.
            --provider-physical-network=: Name of the physical network over which the virtual network is implemented
            --provider-segment=: VLAN ID for VLAN networks or Tunnel ID for GENEVE/GRE/VXLAN networks
            --pvlan: Enable Private VLAN for the network (PVLAN extension required)
            --qinq-vlan: Enable VLAN QinQ (S-Tag ethtype 0x8a88) for the network
            --qos-policy=: QoS policy to attach to this network (name or ID)
            --share: Share the network between projects
            --tag=: Tag to be added to the network (repeat option to set multiple tags)
            --transparent-vlan: Make the network VLAN transparent
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                availability-zone-hint: Availability Zone in which to create this network (Network Availability Zone extension required, repeat option to set multiple availability zones)
                column: specify the column(s) to include, can be repeated to show multiple columns
                default: Specify if this network should be used as the default external network
                description: Set network description
                disable: Disable network
                disable-port-security: Disable port security by default for ports created on this network
                dns-domain: Set DNS domain for this network (requires DNS integration extension)
                enable: Enable network (default)
                enable-port-security: Enable port security by default for ports created on this network (default)
                external: 'The network has an external routing facility that is not managed by Neutron and can be used. For example: openstack router set --external-gateway NETWORK (external-net extension required)'
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                internal: Opposite of '--external' (default)
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                mtu: Set network mtu
                no-default: Do not use the network as the default external network (default)
                no-pvlan: Disable Private VLAN for the network (PVLAN extension required)
                no-qinq-vlan: Disable VLAN QinQ (S-Tag ethtype 0x8a88) for the network
                no-share: Do not share the network between projects
                no-tag: No tags associated with the network
                no-transparent-vlan: Do not make the network VLAN transparent
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Owner's project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                provider-network-type: 'The physical mechanism by which the virtual network is implemented. For example: flat, geneve, gre, local, vlan or vxlan.'
                provider-physical-network: Name of the physical network over which the virtual network is implemented
                provider-segment: VLAN ID for VLAN networks or Tunnel ID for GENEVE/GRE/VXLAN networks
                pvlan: Enable Private VLAN for the network (PVLAN extension required)
                qinq-vlan: Enable VLAN QinQ (S-Tag ethtype 0x8a88) for the network
                qos-policy: QoS policy to attach to this network (name or ID)
                share: Share the network between projects
                tag: Tag to be added to the network (repeat option to set multiple tags)
                transparent-vlan: Make the network VLAN transparent
                variable: ==SUPPRESS==
        - name: delete
          description: Delete network(s)
        - name: flavor
          commands:
            - name: add
              commands:
                - name: profile
                  description: Add a service profile to a network flavor
            - name: create
              description: Create new network flavor
              flags:
                --description=: Description for the flavor
                --disable: Disable the flavor
                --enable: Enable the flavor (default)
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --service-type=!: Service type to which the flavor applies.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description for the flavor
                    disable: Disable the flavor
                    enable: Enable the flavor (default)
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    service-type: 'Service type to which the flavor applies. For example: VPN (See openstack network service provider list for loaded examples.)'
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete network flavors
            - name: list
              description: List network flavors
              flags:
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: profile
              commands:
                - name: create
                  description: Create new network flavor profile
                  flags:
                    --description=: Description for the flavor profile
                    --disable: Disable the flavor profile
                    --driver=: Python module path to driver.
                    --enable: Enable the flavor profile
                    --extra-property=: Additional parameters can be passed using this property.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --metainfo=: Metainfo for the flavor profile.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description for the flavor profile
                        disable: Disable the flavor profile
                        driver: Python module path to driver. This becomes required if --metainfo is missing and vice-versa.
                        enable: Enable the flavor profile
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        metainfo: Metainfo for the flavor profile. This becomes required if --driver is missing and vice-versa.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete network flavor profile
                - name: list
                  description: List network flavor profile(s)
                  flags:
                    --fit-width: Fit the table to the display width.
                    --limit=: The maximum number of entries to return per page.
                    --marker=: The first position in the collection to return results from.
                    --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                        marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                        max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set network flavor profile properties
                  flags:
                    --description=: Description for the flavor profile
                    --disable: Disable the flavor profile
                    --driver=: Python module path to driver.
                    --enable: Enable the flavor profile
                    --extra-property=: Additional parameters can be passed using this property.
                    --metainfo=: Metainfo for the flavor profile.
                  documentation:
                    flag:
                        description: Description for the flavor profile
                        disable: Disable the flavor profile
                        driver: Python module path to driver. This becomes required if --metainfo is missing and vice-versa.
                        enable: Enable the flavor profile
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        metainfo: Metainfo for the flavor profile. This becomes required if --driver is missing and vice-versa.
                - name: show
                  description: Display network flavor profile details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: remove
              commands:
                - name: profile
                  description: Remove service profile from network flavor
            - name: set
              description: Set network flavor properties
              flags:
                --description=: Set network flavor description
                --disable: Disable network flavor
                --enable: Enable network flavor
                --extra-property=: Additional parameters can be passed using this property.
                --name=: Set flavor name
              documentation:
                flag:
                    description: Set network flavor description
                    disable: Disable network flavor
                    enable: Enable network flavor
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    name: Set flavor name
            - name: show
              description: Display network flavor details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: l3
          commands:
            - name: conntrack
              commands:
                - name: helper
                  commands:
                    - name: create
                      description: Create a new L3 conntrack helper
                      flags:
                        --fit-width: Fit the table to the display width.
                        --helper=!: The netfilter conntrack helper module
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --port=!: The network port for the netfilter conntrack target rule
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --protocol=!: The network protocol for the netfilter conntrack target rule
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            helper: The netfilter conntrack helper module
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            port: The network port for the netfilter conntrack target rule
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            protocol: The network protocol for the netfilter conntrack target rule
                            variable: ==SUPPRESS==
                    - name: delete
                      description: Delete L3 conntrack helper
                    - name: list
                      description: List L3 conntrack helpers
                      flags:
                        --fit-width: Fit the table to the display width.
                        --helper=: List only helpers using the specified netfilter conntrack helper module
                        --limit=: The maximum number of entries to return per page.
                        --marker=: The first position in the collection to return results from.
                        --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --port=: List only helpers with the specified network port for the netfilter conntrack target rule (name or ID)
                        --print-empty: Print empty table if there is no data to show.
                        --protocol=: List only helpers with the specified network protocol for the netfilter conntrack target rule
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            helper: List only helpers using the specified netfilter conntrack helper module
                            limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                            marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                            max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            port: List only helpers with the specified network port for the netfilter conntrack target rule (name or ID)
                            print-empty: Print empty table if there is no data to show.
                            protocol: List only helpers with the specified network protocol for the netfilter conntrack target rule
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: set
                      description: Set L3 conntrack helper properties
                      flags:
                        --helper=: The netfilter conntrack helper module
                        --port=: The network port for the netfilter conntrack target rule
                        --protocol=: The network protocol for the netfilter conntrack target rule
                      documentation:
                        flag:
                            helper: The netfilter conntrack helper module
                            port: The network port for the netfilter conntrack target rule
                            protocol: The network protocol for the netfilter conntrack target rule
                    - name: show
                      description: Display L3 conntrack helper details
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
        - name: list
          description: List networks
          flags:
            --agent=: List only networks hosted the specified agent (ID only)
            --any-tags=: List networks which have any given tag(s) (Comma-separated list of tags)
            --disable: List only disabled networks
            --enable: List only enabled networks
            --external: List only external networks
            --fit-width: Fit the table to the display width.
            --internal: List only internal networks
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --name=: List only networks with the specified name
            --no-pvlan: List only networks with PVLAN disabled
            --no-share: List only networks not shared between projects
            --noindent: whether to disable indenting the JSON
            --not-any-tags=: Exclude networks which have any given tag(s) (Comma-separated list of tags)
            --not-tags=: Exclude networks which have all given tag(s) (Comma-separated list of tags)
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: List only networks with the specified project (name or ID)
            --provider-network-type=: List only networks with the specified physical mechanisms.
            --provider-physical-network=: List only networks with the specified physical network name
            --provider-segment=: List only networks with the specified provider segment ID (VLAN ID for VLAN networks or Tunnel ID for GENEVE/GRE/VXLAN networks)
            --pvlan: List only networks with PVLAN enabled
            --quote=: when to include quotes, defaults to nonnumeric
            --share: List only networks shared between projects
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --status=: List only networks with the specified status ('ACTIVE', 'BUILD', 'DOWN', 'ERROR')
            --tags=: List networks which have all given tag(s) (Comma-separated list of tags)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                provider-network-type:
                    - flat
                    - geneve
                    - gre
                    - local
                    - vlan
                    - vxlan
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
                status:
                    - ACTIVE
                    - BUILD
                    - DOWN
                    - ERROR
          documentation:
            flag:
                agent: List only networks hosted the specified agent (ID only)
                any-tags: List networks which have any given tag(s) (Comma-separated list of tags)
                column: specify the column(s) to include, can be repeated to show multiple columns
                disable: List only disabled networks
                enable: List only enabled networks
                external: List only external networks
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                internal: List only internal networks
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: List only networks with the specified name
                no-pvlan: List only networks with PVLAN disabled
                no-share: List only networks not shared between projects
                noindent: whether to disable indenting the JSON
                not-any-tags: Exclude networks which have any given tag(s) (Comma-separated list of tags)
                not-tags: Exclude networks which have all given tag(s) (Comma-separated list of tags)
                print-empty: Print empty table if there is no data to show.
                project: List only networks with the specified project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                provider-network-type: 'List only networks with the specified physical mechanisms. The supported options are: flat, geneve, gre, local, vlan and vxlan.'
                provider-physical-network: List only networks with the specified physical network name
                provider-segment: List only networks with the specified provider segment ID (VLAN ID for VLAN networks or Tunnel ID for GENEVE/GRE/VXLAN networks)
                pvlan: List only networks with PVLAN enabled
                quote: when to include quotes, defaults to nonnumeric
                share: List only networks shared between projects
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                status: List only networks with the specified status ('ACTIVE', 'BUILD', 'DOWN', 'ERROR')
                tags: List networks which have all given tag(s) (Comma-separated list of tags)
        - name: meter
          commands:
            - name: create
              description: Create network meter
              flags:
                --description=: Description for meter
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --no-share: Do not share meter between projects
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --share: Share meter between projects
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description for meter
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    no-share: Do not share meter between projects
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    share: Share meter between projects
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete network meter
            - name: list
              description: List network meters
              flags:
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: rule
              commands:
                - name: create
                  description: Create a new meter rule
                  flags:
                    --destination-ip-prefix=: The destination IP prefix to associate with this rule
                    --egress: Apply rule to outgoing network traffic
                    --exclude: Exclude remote IP prefix from traffic count
                    --extra-property=: Additional parameters can be passed using this property.
                    --fit-width: Fit the table to the display width.
                    --include: Include remote IP prefix from traffic count (default)
                    --ingress: Apply rule to incoming network traffic (default)
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --remote-ip-prefix=: The remote IP prefix to associate with this rule
                    --source-ip-prefix=: The source IP prefix to associate with this rule
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        destination-ip-prefix: The destination IP prefix to associate with this rule
                        egress: Apply rule to outgoing network traffic
                        exclude: Exclude remote IP prefix from traffic count
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        include: Include remote IP prefix from traffic count (default)
                        ingress: Apply rule to incoming network traffic (default)
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        remote-ip-prefix: The remote IP prefix to associate with this rule
                        source-ip-prefix: The source IP prefix to associate with this rule
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete meter rule(s)
                - name: list
                  description: List meter rules
                  flags:
                    --fit-width: Fit the table to the display width.
                    --limit=: The maximum number of entries to return per page.
                    --marker=: The first position in the collection to return results from.
                    --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                        marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                        max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: show
                  description: Display meter rules details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: show
              description: Show network meter
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: qos
          commands:
            - name: policy
              commands:
                - name: create
                  description: Create a QoS policy
                  flags:
                    --default: Set this as a default network QoS policy
                    --description=: Description of the QoS policy
                    --extra-property=: Additional parameters can be passed using this property.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --no-default: Set this as a non-default network QoS policy
                    --no-share: Make the QoS policy not accessible by other projects (default)
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --share: Make the QoS policy accessible by other projects
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        default: Set this as a default network QoS policy
                        description: Description of the QoS policy
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        no-default: Set this as a non-default network QoS policy
                        no-share: Make the QoS policy not accessible by other projects (default)
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        share: Make the QoS policy accessible by other projects
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete Qos Policy(s)
                - name: list
                  description: List QoS policies
                  flags:
                    --fit-width: Fit the table to the display width.
                    --limit=: The maximum number of entries to return per page.
                    --marker=: The first position in the collection to return results from.
                    --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                    --max-width=: Maximum display width, <1 to disable.
                    --no-share: List only QoS policies not shared between projects
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: List only QoS policies with the specified project (name or ID)
                    --quote=: when to include quotes, defaults to nonnumeric
                    --share: List only QoS policies shared between projects
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                        marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                        max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        no-share: List only QoS policies not shared between projects
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        project: List only QoS policies with the specified project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        quote: when to include quotes, defaults to nonnumeric
                        share: List only QoS policies shared between projects
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set QoS policy properties
                  flags:
                    --default: Set this as a default network QoS policy
                    --description=: Description of the QoS policy
                    --extra-property=: Additional parameters can be passed using this property.
                    --name=: Set QoS policy name
                    --no-default: Set this as a non-default network QoS policy
                    --no-share: Make the QoS policy not accessible by other projects
                    --share: Make the QoS policy accessible by other projects
                  documentation:
                    flag:
                        default: Set this as a default network QoS policy
                        description: Description of the QoS policy
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        name: Set QoS policy name
                        no-default: Set this as a non-default network QoS policy
                        no-share: Make the QoS policy not accessible by other projects
                        share: Make the QoS policy accessible by other projects
                - name: show
                  description: Display QoS policy details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: rule
              commands:
                - name: create
                  description: Create new Network QoS rule
                  flags:
                    --any: Any traffic direction from the project point of view.
                    --dscp-mark=: 'DSCP mark: value can be 0, even numbers from 8-56, excluding 42, 44, 50, 52, and 54'
                    --egress: Egress traffic direction from the project point of view
                    --extra-property=: Additional parameters can be passed using this property.
                    --fit-width: Fit the table to the display width.
                    --ingress: Ingress traffic direction from the project point of view
                    --max-burst-kbits=: Maximum burst in kilobits, 0 or not specified means automatic, which is 80%% of the bandwidth limit, which works for typical TCP traffic.
                    --max-kbps=: Maximum bandwidth in kbps
                    --max-width=: Maximum display width, <1 to disable.
                    --min-kbps=: Minimum guaranteed bandwidth in kbps
                    --min-kpps=: Minimum guaranteed packet rate in kpps
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --type=!: QoS rule type (minimum-bandwidth, minimum-packet-rate, dscp-marking, bandwidth-limit)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                        type:
                            - minimum-bandwidth
                            - minimum-packet-rate
                            - dscp-marking
                            - bandwidth-limit
                  documentation:
                    flag:
                        any: Any traffic direction from the project point of view. Can be used only with minimum packet rate rule.
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        dscp-mark: 'DSCP mark: value can be 0, even numbers from 8-56, excluding 42, 44, 50, 52, and 54'
                        egress: Egress traffic direction from the project point of view
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        ingress: Ingress traffic direction from the project point of view
                        max-burst-kbits: Maximum burst in kilobits, 0 or not specified means automatic, which is 80%% of the bandwidth limit, which works for typical TCP traffic. For details check the QoS user workflow.
                        max-kbps: Maximum bandwidth in kbps
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        min-kbps: Minimum guaranteed bandwidth in kbps
                        min-kpps: Minimum guaranteed packet rate in kpps
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        type: QoS rule type (minimum-bandwidth, minimum-packet-rate, dscp-marking, bandwidth-limit)
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete Network QoS rule
                - name: list
                  description: List Network QoS rules
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set Network QoS rule properties
                  flags:
                    --any: Any traffic direction from the project point of view.
                    --dscp-mark=: 'DSCP mark: value can be 0, even numbers from 8-56, excluding 42, 44, 50, 52, and 54'
                    --egress: Egress traffic direction from the project point of view
                    --extra-property=: Additional parameters can be passed using this property.
                    --ingress: Ingress traffic direction from the project point of view
                    --max-burst-kbits=: Maximum burst in kilobits, 0 or not specified means automatic, which is 80%% of the bandwidth limit, which works for typical TCP traffic.
                    --max-kbps=: Maximum bandwidth in kbps
                    --min-kbps=: Minimum guaranteed bandwidth in kbps
                    --min-kpps=: Minimum guaranteed packet rate in kpps
                  documentation:
                    flag:
                        any: Any traffic direction from the project point of view. Can be used only with minimum packet rate rule.
                        dscp-mark: 'DSCP mark: value can be 0, even numbers from 8-56, excluding 42, 44, 50, 52, and 54'
                        egress: Egress traffic direction from the project point of view
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        ingress: Ingress traffic direction from the project point of view
                        max-burst-kbits: Maximum burst in kilobits, 0 or not specified means automatic, which is 80%% of the bandwidth limit, which works for typical TCP traffic. For details check the QoS user workflow.
                        max-kbps: Maximum bandwidth in kbps
                        min-kbps: Minimum guaranteed bandwidth in kbps
                        min-kpps: Minimum guaranteed packet rate in kpps
                - name: show
                  description: Display Network QoS rule details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: type
                  commands:
                    - name: list
                      description: List QoS rule types
                      flags:
                        --all-rules: List all QoS rule types implemented in Neutron QoS driver
                        --all-supported: List all the QoS rule types supported by any loaded mechanism drivers (the union of all sets of supported rules)
                        --fit-width: Fit the table to the display width.
                        --limit=: The maximum number of entries to return per page.
                        --marker=: The first position in the collection to return results from.
                        --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            all-rules: List all QoS rule types implemented in Neutron QoS driver
                            all-supported: List all the QoS rule types supported by any loaded mechanism drivers (the union of all sets of supported rules)
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                            marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                            max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: show
                      description: Show details about supported QoS rule type
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
        - name: rbac
          commands:
            - name: create
              description: Create network RBAC policy
              flags:
                --action=!: Action for the RBAC policy ("access_as_external" or "access_as_shared")
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: The owner project (name or ID)
                --target-all-projects: Allow creating RBAC policy for all projects
                --target-project-domain=: Domain the target project belongs to (name or ID).
                --target-project=: The project to which the RBAC policy will be enforced (name or ID)
                --type=!: Type of the object that RBAC policy affects ("address_group", "address_scope", "security_group", "subnetpool", "qos_policy" or "network")
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    action:
                        - access_as_external
                        - access_as_shared
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
                    type:
                        - address_group
                        - address_scope
                        - security_group
                        - subnetpool
                        - qos_policy
                        - network
              documentation:
                flag:
                    action: Action for the RBAC policy ("access_as_external" or "access_as_shared")
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: The owner project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    target-all-projects: Allow creating RBAC policy for all projects
                    target-project: The project to which the RBAC policy will be enforced (name or ID)
                    target-project-domain: Domain the target project belongs to (name or ID). This can be used in case collisions between project names exist.
                    type: Type of the object that RBAC policy affects ("address_group", "address_scope", "security_group", "subnetpool", "qos_policy" or "network")
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete network RBAC policy(s)
            - name: list
              description: List network RBAC policies
              flags:
                --action=: List only network RBAC policies with the specified action ("access_as_external" or "access_as_shared")
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --target-project=: List only network RBAC policies with the specified target project (name or ID)
                --type=: List only network RBAC policies with the specified object type ("address_group", "address_scope", "security_group", "subnetpool", "qos_policy" or "network")
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    action:
                        - access_as_external
                        - access_as_shared
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    type:
                        - address_group
                        - address_scope
                        - security_group
                        - subnetpool
                        - qos_policy
                        - network
              documentation:
                flag:
                    action: List only network RBAC policies with the specified action ("access_as_external" or "access_as_shared")
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    target-project: List only network RBAC policies with the specified target project (name or ID)
                    type: List only network RBAC policies with the specified object type ("address_group", "address_scope", "security_group", "subnetpool", "qos_policy" or "network")
            - name: set
              description: Set network RBAC policy properties
              flags:
                --extra-property=: Additional parameters can be passed using this property.
                --target-project-domain=: Domain the target project belongs to (name or ID).
                --target-project=: The project to which the RBAC policy will be enforced (name or ID)
              documentation:
                flag:
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    target-project: The project to which the RBAC policy will be enforced (name or ID)
                    target-project-domain: Domain the target project belongs to (name or ID). This can be used in case collisions between project names exist.
            - name: show
              description: Display network RBAC policy details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: segment
          commands:
            - name: create
              description: Create new network segment
              flags:
                --description=: Network segment description
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --network-type=!: Network type of this network segment (flat, geneve, gre, local, vlan or vxlan)
                --network=!: Network this network segment belongs to (name or ID)
                --noindent: whether to disable indenting the JSON
                --physical-network=: Physical network name of this network segment
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --segment=: Segment identifier for this network segment which is based on the network type, VLAN ID for vlan network type and tunnel ID for geneve, gre and vxlan network types
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
                    network-type:
                        - flat
                        - geneve
                        - gre
                        - local
                        - vlan
                        - vxlan
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Network segment description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    network: Network this network segment belongs to (name or ID)
                    network-type: Network type of this network segment (flat, geneve, gre, local, vlan or vxlan)
                    noindent: whether to disable indenting the JSON
                    physical-network: Physical network name of this network segment
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    segment: Segment identifier for this network segment which is based on the network type, VLAN ID for vlan network type and tunnel ID for geneve, gre and vxlan network types
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete network segment(s)
            - name: list
              description: List network segments
              flags:
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --network=: List only network segments associated with the specified network (name or ID)
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    network: List only network segments associated with the specified network (name or ID)
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: range
              commands:
                - name: create
                  description: Create new network segment range
                  flags:
                    --extra-property=: Additional parameters can be passed using this property.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --maximum=!: Maximum segment identifier for this network segment range which is based on the network type, VLAN ID for vlan network type and tunnel ID for geneve, gre and vxlan network types
                    --minimum=!: Minimum segment identifier for this network segment range which is based on the network type, VLAN ID for vlan network type and tunnel ID for geneve, gre and vxlan network types
                    --network-type=!: Network type of this network segment range (geneve, gre, vlan or vxlan)
                    --noindent: whether to disable indenting the JSON
                    --physical-network=: Physical network name of this network segment range
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --private: Network segment range is assigned specifically to the project
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Network segment range owner (name or ID).
                    --shared: Network segment range is shared with other projects
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                        network-type:
                            - geneve
                            - gre
                            - vlan
                            - vxlan
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        maximum: Maximum segment identifier for this network segment range which is based on the network type, VLAN ID for vlan network type and tunnel ID for geneve, gre and vxlan network types
                        minimum: Minimum segment identifier for this network segment range which is based on the network type, VLAN ID for vlan network type and tunnel ID for geneve, gre and vxlan network types
                        network-type: Network type of this network segment range (geneve, gre, vlan or vxlan)
                        noindent: whether to disable indenting the JSON
                        physical-network: Physical network name of this network segment range
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        private: Network segment range is assigned specifically to the project
                        project: Network segment range owner (name or ID). Optional when the segment range is shared.
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        shared: Network segment range is shared with other projects
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete network segment range(s)
                - name: list
                  description: List network segment ranges
                  flags:
                    --available: List only network segment ranges that have available segments
                    --fit-width: Fit the table to the display width.
                    --limit=: The maximum number of entries to return per page.
                    --long: List additional fields in output
                    --marker=: The first position in the collection to return results from.
                    --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    --unavailable: List only network segment ranges without available segments
                    --unused: List only network segment ranges that have segments not in use
                    --used: List only network segment ranges that have segments in use
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        available: List only network segment ranges that have available segments
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                        long: List additional fields in output
                        marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                        max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                        unavailable: List only network segment ranges without available segments
                        unused: List only network segment ranges that have segments not in use
                        used: List only network segment ranges that have segments in use
                - name: set
                  description: Set network segment range properties
                  flags:
                    --extra-property=: Additional parameters can be passed using this property.
                    --maximum=: Set network segment range maximum segment identifier
                    --minimum=: Set network segment range minimum segment identifier
                    --name=: Set network segment name
                  documentation:
                    flag:
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        maximum: Set network segment range maximum segment identifier
                        minimum: Set network segment range minimum segment identifier
                        name: Set network segment name
                - name: show
                  description: Display network segment range details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: set
              description: Set network segment properties
              flags:
                --description=: Set network segment description
                --extra-property=: Additional parameters can be passed using this property.
                --name=: Set network segment name
              documentation:
                flag:
                    description: Set network segment description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    name: Set network segment name
            - name: show
              description: Display network segment details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: service
          commands:
            - name: provider
              commands:
                - name: list
                  description: List Service Providers
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
        - name: set
          description: Set network properties
          flags:
            --default: Set the network as the default external network
            --description=: Set network description
            --disable: Disable network
            --disable-port-security: Disable port security by default for ports created on this network
            --dns-domain=: Set DNS domain for this network (requires DNS integration extension)
            --enable: Enable network
            --enable-port-security: Enable port security by default for ports created on this network
            --external: The network has an external routing facility that is not managed by Neutron and can be used.
            --extra-property=: Additional parameters can be passed using this property.
            --internal: Opposite of '--external'
            --mtu=: Set network mtu
            --name=: Set network name
            --no-default: Do not use the network as the default external network
            --no-pvlan: Disable Private VLAN for the network (Default).
            --no-qos-policy: Remove the QoS policy attached to this network
            --no-share: Do not share the network between projects
            --no-tag: Clear tags associated with the network.
            --provider-network-type=: The physical mechanism by which the virtual network is implemented.
            --provider-physical-network=: Name of the physical network over which the virtual network is implemented
            --provider-segment=: VLAN ID for VLAN networks or Tunnel ID for GENEVE/GRE/VXLAN networks
            --pvlan: Enable Private VLAN for the network.
            --qos-policy=: QoS policy to attach to this network (name or ID)
            --share: Share the network between projects
            --tag=: Tag to be added to the network (repeat option to set multiple tags)
          documentation:
            flag:
                default: Set the network as the default external network
                description: Set network description
                disable: Disable network
                disable-port-security: Disable port security by default for ports created on this network
                dns-domain: Set DNS domain for this network (requires DNS integration extension)
                enable: Enable network
                enable-port-security: Enable port security by default for ports created on this network
                external: 'The network has an external routing facility that is not managed by Neutron and can be used. For example: openstack router set --external-gateway NETWORK (external-net extension required).'
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                internal: Opposite of '--external'
                mtu: Set network mtu
                name: Set network name
                no-default: Do not use the network as the default external network
                no-pvlan: Disable Private VLAN for the network (Default). PVLAN extension required.
                no-qos-policy: Remove the QoS policy attached to this network
                no-share: Do not share the network between projects
                no-tag: Clear tags associated with the network. Specify both --tag and --no-tag to overwrite current tags
                provider-network-type: 'The physical mechanism by which the virtual network is implemented. For example: flat, geneve, gre, local, vlan or vxlan.'
                provider-physical-network: Name of the physical network over which the virtual network is implemented
                provider-segment: VLAN ID for VLAN networks or Tunnel ID for GENEVE/GRE/VXLAN networks
                pvlan: Enable Private VLAN for the network. PVLAN extension required.
                qos-policy: QoS policy to attach to this network (name or ID)
                share: Share the network between projects
                tag: Tag to be added to the network (repeat option to set multiple tags)
        - name: show
          description: Show network details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: subport
          commands:
            - name: list
              description: List all subports for a given network trunk
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --trunk=!: List only subports belonging to this trunk (name or ID)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    trunk: List only subports belonging to this trunk (name or ID)
        - name: trunk
          commands:
            - name: create
              description: Create a network trunk for a given project
              flags:
                --description=: A description of the trunk
                --disable: Disable trunk
                --enable: Enable trunk (default)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --parent-port=!: Parent port belonging to this trunk (name or ID)
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --subport=: Subport to add.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: A description of the trunk
                    disable: Disable trunk
                    enable: Enable trunk (default)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    parent-port: Parent port belonging to this trunk (name or ID)
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    subport: Subport to add. Subport is of form 'port=<name or ID>,segmentation-type=<segmentation-type>,segmentation-id=<segmentation-ID>' (repeat option to add multiple subports)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete a given network trunk
            - name: list
              description: List all network trunks
              flags:
                --fit-width: Fit the table to the display width.
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set network trunk properties
              flags:
                --description=: A description of the trunk
                --disable: Disable trunk
                --enable: Enable trunk
                --name=: Set trunk name
                --subport=: Subport to add.
              documentation:
                flag:
                    description: A description of the trunk
                    disable: Disable trunk
                    enable: Enable trunk
                    name: Set trunk name
                    subport: Subport to add. Subport is of form 'port=<name or ID>,segmentation-type=<segmentation-type>,segmentation-id=<segmentation-ID>' (repeat option to add multiple subports)
            - name: show
              description: Show information of a given network trunk
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: subport
              commands:
                - name: add
                  description: Add a subport to a given network trunk
                  flags:
                    --segmentation-id=: Segmentation ID of the subport
                    --segmentation-type=: Segmentation type of the subport, e.g. 'vlan'
                  documentation:
                    flag:
                        segmentation-id: Segmentation ID of the subport
                        segmentation-type: Segmentation type of the subport, e.g. 'vlan'
                - name: list
                  description: List all subports for a given network trunk
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: remove
                  description: Remove subport(s) from a given network trunk
            - name: unset
              description: Unset subports from a given network trunk
              flags:
                --subport=!: Subport to unset (name or ID of the port) (repeat option to unset multiple subports)
              documentation:
                flag:
                    subport: Subport to unset (name or ID of the port) (repeat option to unset multiple subports)
        - name: unset
          description: Unset network properties
          flags:
            --all-tag: Clear all tags associated with the network
            --extra-property=: Additional parameters can be passed using this property.
            --tag=: Tag to be removed from the network (repeat option to remove multiple tags)
          documentation:
            flag:
                all-tag: Clear all tags associated with the network
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                tag: Tag to be removed from the network (repeat option to remove multiple tags)
    - name: object
      commands:
        - name: create
          description: Upload object to container
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --name=: Upload a file and rename it.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: Upload a file and rename it. Can only be used when uploading a single object
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: delete
          description: Delete object from container
        - name: list
          description: List objects
          flags:
            --all: List all objects in container (default is 10000)
            --delimiter=: Roll up items with <delimiter>
            --end-marker=: End anchor for paging
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: Filter list using <prefix>
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all: List all objects in container (default is 10000)
                column: specify the column(s) to include, can be repeated to show multiple columns
                delimiter: Roll up items with <delimiter>
                end-marker: End anchor for paging
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: Filter list using <prefix>
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: save
          description: Save object locally
          flags:
            --file=: Destination filename (defaults to object name); using '-' as the filename will print the file to stdout
          documentation:
            flag:
                file: Destination filename (defaults to object name); using '-' as the filename will print the file to stdout
        - name: set
          description: Set object properties
          flags:
            --property=!: Set a property on this object (repeat option to set multiple properties)
          documentation:
            flag:
                property: Set a property on this object (repeat option to set multiple properties)
        - name: show
          description: Display object details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: store
          commands:
            - name: account
              commands:
                - name: set
                  description: Set account properties
                  flags:
                    --property=!: Set a property on this account (repeat option to set multiple properties)
                  documentation:
                    flag:
                        property: Set a property on this account (repeat option to set multiple properties)
                - name: show
                  description: Display account details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset account properties
                  flags:
                    --property=!: Property to remove from account (repeat option to remove multiple properties)
                  documentation:
                    flag:
                        property: Property to remove from account (repeat option to remove multiple properties)
        - name: unset
          description: Unset object properties
          flags:
            --property=!: Property to remove from object (repeat option to remove multiple properties)
          documentation:
            flag:
                property: Property to remove from object (repeat option to remove multiple properties)
    - name: policy
      commands:
        - name: create
          description: Create new policy
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --type=: New MIME type of the policy rules file (defaults to application/json)
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                type: New MIME type of the policy rules file (defaults to application/json)
                variable: ==SUPPRESS==
        - name: delete
          description: Delete policy(s)
        - name: list
          description: List policies
          flags:
            --fit-width: Fit the table to the display width.
            --long: List additional fields in output
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                long: List additional fields in output
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: set
          description: Set policy properties
          flags:
            --rules=: New serialized policy rules file
            --type=: New MIME type of the policy rules file
          documentation:
            flag:
                rules: New serialized policy rules file
                type: New MIME type of the policy rules file
        - name: show
          description: Display policy details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: port
      commands:
        - name: create
          description: Create a new port
          flags:
            --allowed-address=: 'Add allowed-address pair associated with this port: ip-address=<ip-address>[,mac-address=<mac-address>] (repeat option to set multiple allowed-address pairs)'
            --binding-profile=: Custom data to be passed as binding:profile.
            --description=: Description of this port
            --device-owner=: Device owner of this port.
            --device-profile=: Port device profile
            --device=: Port device ID
            --disable: Disable port
            --disable-port-security: Disable port security for this port
            --disable-uplink-status-propagation: Disable uplink status propagation
            --dns-domain=: Set DNS domain to this port (requires dns_domain extension for ports)
            --dns-name=: Set DNS name for this port (requires DNS integration extension)
            --enable: Enable port (default)
            --enable-port-security: Enable port security for this port (default)
            --enable-uplink-status-propagation: Enable uplink status propagation (default)
            --extra-dhcp-option=: 'Extra DHCP options to be assigned to this port: name=<name>[,value=<value>,ip-version={4,6}] (repeat option to set multiple extra DHCP options)'
            --extra-property=: Additional parameters can be passed using this property.
            --fit-width: Fit the table to the display width.
            --fixed-ip=: 'Desired IP and/or subnet for this port (name or ID): subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
            --hardware-offload-type=: Hardware offload type this port will request when attached to the network backend
            --hint=: Port hints as ALIAS=VALUE or as JSON.
            --host=: Allocate port on host <host-id> (ID only)
            --mac-address=: MAC address of this port
            --max-width=: Maximum display width, <1 to disable.
            --network=!: Network this port belongs to (name or ID)
            --no-fixed-ip: No IP or subnet set for this port
            --no-security-group: Associate no security groups with this port
            --no-tag: No tags associated with the port
            --noindent: whether to disable indenting the JSON
            --not-trusted: Set port to be not trusted.
            --numa-policy-legacy: NUMA affinity policy using legacy mode to schedule this port
            --numa-policy-preferred: NUMA affinity policy preferred to schedule this port
            --numa-policy-required: NUMA affinity policy required to schedule this port
            --numa-policy-socket: NUMA affinity policy socket to schedule this port
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Owner's project (name or ID)
            --pvlan-community=: Set PVLAN community name for this port.
            --pvlan-type=: Set Private VLAN type for this port.
            --qos-policy=: Attach QoS policy to this port (name or ID)
            --security-group=: Security group to associate with this port (name or ID) (repeat option to set multiple security groups)
            --tag=: Tag to be added to the port (repeat option to set multiple tags)
            --trusted: Set port to be trusted.
            --variable=: ==SUPPRESS==
            --vnic-type=: 'VNIC type for this port (accelerator-direct | accelerator-direct-physical | direct | direct-physical | macvtap | normal | baremetal | virtio-forwarder | smart-nic | vdpa | remote-managed) (default: normal)'
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
                pvlan-type:
                    - promiscuous
                    - isolated
                    - community
                vnic-type:
                    - accelerator-direct
                    - accelerator-direct-physical
                    - direct
                    - direct-physical
                    - macvtap
                    - normal
                    - baremetal
                    - virtio-forwarder
                    - smart-nic
                    - vdpa
                    - remote-managed
          documentation:
            flag:
                allowed-address: 'Add allowed-address pair associated with this port: ip-address=<ip-address>[,mac-address=<mac-address>] (repeat option to set multiple allowed-address pairs)'
                binding-profile: Custom data to be passed as binding:profile. Data may be passed as <key>=<value> or JSON (repeat option to set multiple binding:profile data).
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Description of this port
                device: Port device ID
                device-owner: Device owner of this port. This is the entity that uses the port (for example, network:dhcp).
                device-profile: Port device profile
                disable: Disable port
                disable-port-security: Disable port security for this port
                disable-uplink-status-propagation: Disable uplink status propagation
                dns-domain: Set DNS domain to this port (requires dns_domain extension for ports)
                dns-name: Set DNS name for this port (requires DNS integration extension)
                enable: Enable port (default)
                enable-port-security: Enable port security for this port (default)
                enable-uplink-status-propagation: Enable uplink status propagation (default)
                extra-dhcp-option: 'Extra DHCP options to be assigned to this port: name=<name>[,value=<value>,ip-version={4,6}] (repeat option to set multiple extra DHCP options)'
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                fixed-ip: 'Desired IP and/or subnet for this port (name or ID): subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
                format: the output format, defaults to table
                hardware-offload-type: Hardware offload type this port will request when attached to the network backend
                hint: 'Port hints as ALIAS=VALUE or as JSON. Valid hint aliases/values: ovs-tx-steering=thread, ovs-tx-steering=hash. Valid JSON values are as specified by the Neutron API. (requires port-hints extension) (requires port-hint-ovs-tx-steering extension for alias: ovs-tx-steering) (repeat option to set multiple hints).'
                host: Allocate port on host <host-id> (ID only)
                mac-address: MAC address of this port
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                network: Network this port belongs to (name or ID)
                no-fixed-ip: No IP or subnet set for this port
                no-security-group: Associate no security groups with this port
                no-tag: No tags associated with the port
                noindent: whether to disable indenting the JSON
                not-trusted: Set port to be not trusted. This will be populated into the 'binding:profile' dictionary and passed to the services which expect it in this dictionary (for example, Nova).
                numa-policy-legacy: NUMA affinity policy using legacy mode to schedule this port
                numa-policy-preferred: NUMA affinity policy preferred to schedule this port
                numa-policy-required: NUMA affinity policy required to schedule this port
                numa-policy-socket: NUMA affinity policy socket to schedule this port
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Owner's project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                pvlan-community: 'Set PVLAN community name for this port. Only applies when pvlan-type is ''community''. Requires PVLAN service plugin. Default: None.'
                pvlan-type: 'Set Private VLAN type for this port. Requires PVLAN service plugin. Default: promiscuous.'
                qos-policy: Attach QoS policy to this port (name or ID)
                security-group: Security group to associate with this port (name or ID) (repeat option to set multiple security groups)
                tag: Tag to be added to the port (repeat option to set multiple tags)
                trusted: Set port to be trusted. This will be populated into the 'binding:profile' dictionary and passed to the services which expect it in this dictionary (for example, Nova).
                variable: ==SUPPRESS==
                vnic-type: 'VNIC type for this port (accelerator-direct | accelerator-direct-physical | direct | direct-physical | macvtap | normal | baremetal | virtio-forwarder | smart-nic | vdpa | remote-managed) (default: normal)'
        - name: delete
          description: Delete port(s)
        - name: list
          description: List ports
          flags:
            --any-tags=: List ports which have any given tag(s) (Comma-separated list of tags)
            --device-id=: List only ports with the specified device ID
            --device-owner=: List only ports with the specified device owner.
            --fit-width: Fit the table to the display width.
            --fixed-ip=: 'Desired IP and/or subnet for filtering ports (name or ID): subnet=<subnet>,ip-address=<ip-address>,ip-substring=<ip-substring> (repeat option to filter multiple fixed IP addresses)'
            --host=: List only ports bound to this host ID
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --mac-address=: List only ports with the specified MAC address
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --name=: List only ports with the specified name
            --network=: List only ports connected to this network (name or ID)
            --no-pvlan: List only ports with PVLAN disabled
            --noindent: whether to disable indenting the JSON
            --not-any-tags=: Exclude ports which have any given tag(s) (Comma-separated list of tags)
            --not-tags=: Exclude ports which have all given tag(s) (Comma-separated list of tags)
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: List only ports with the specified project (name or ID)
            --pvlan: List only ports with PVLAN enabled
            --pvlan-community=: List only ports within a specified PVLAN community
            --pvlan-type=: List only ports with the specified PVLAN type
            --quote=: when to include quotes, defaults to nonnumeric
            --router=: List only ports attached to this router (name or ID)
            --security-group=: List only ports associated with this security group
            --server=: List only ports attached to this server (name or ID)
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --status=: List only ports with the specified status ('ACTIVE', 'BUILD', 'DOWN', 'ERROR')
            --tags=: List ports which have all given tag(s) (Comma-separated list of tags)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
                status:
                    - ACTIVE
                    - BUILD
                    - DOWN
                    - ERROR
          documentation:
            flag:
                any-tags: List ports which have any given tag(s) (Comma-separated list of tags)
                column: specify the column(s) to include, can be repeated to show multiple columns
                device-id: List only ports with the specified device ID
                device-owner: List only ports with the specified device owner. This is the entity that uses the port (for example, network:dhcp).
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                fixed-ip: 'Desired IP and/or subnet for filtering ports (name or ID): subnet=<subnet>,ip-address=<ip-address>,ip-substring=<ip-substring> (repeat option to filter multiple fixed IP addresses)'
                format: the output format, defaults to table
                host: List only ports bound to this host ID
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                mac-address: List only ports with the specified MAC address
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: List only ports with the specified name
                network: List only ports connected to this network (name or ID)
                no-pvlan: List only ports with PVLAN disabled
                noindent: whether to disable indenting the JSON
                not-any-tags: Exclude ports which have any given tag(s) (Comma-separated list of tags)
                not-tags: Exclude ports which have all given tag(s) (Comma-separated list of tags)
                print-empty: Print empty table if there is no data to show.
                project: List only ports with the specified project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                pvlan: List only ports with PVLAN enabled
                pvlan-community: List only ports within a specified PVLAN community
                pvlan-type: List only ports with the specified PVLAN type
                quote: when to include quotes, defaults to nonnumeric
                router: List only ports attached to this router (name or ID)
                security-group: List only ports associated with this security group
                server: List only ports attached to this server (name or ID)
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                status: List only ports with the specified status ('ACTIVE', 'BUILD', 'DOWN', 'ERROR')
                tags: List ports which have all given tag(s) (Comma-separated list of tags)
        - name: set
          description: Set port properties
          flags:
            --allowed-address=: 'Add allowed-address pair associated with this port: ip-address=<ip-address>[,mac-address=<mac-address>] (repeat option to set multiple allowed-address pairs)'
            --binding-profile=: Custom data to be passed as binding:profile.
            --data-plane-status=: Set data plane status of this port (ACTIVE | DOWN).
            --description=: Description of this port
            --device-owner=: Device owner of this port.
            --device=: Port device ID
            --disable: Disable port
            --disable-port-security: Disable port security for this port
            --disable-uplink-status-propagation: Disable uplink status propagation
            --dns-domain=: Set DNS domain to this port (requires dns_domain extension for ports)
            --dns-name=: Set DNS name for this port (requires DNS integration extension)
            --enable: Enable port
            --enable-port-security: Enable port security for this port
            --enable-uplink-status-propagation: Enable uplink status propagation
            --extra-dhcp-option=: 'Extra DHCP options to be assigned to this port: name=<name>[,value=<value>,ip-version={4,6}] (repeat option to set multiple extra DHCP options)'
            --extra-property=: Additional parameters can be passed using this property.
            --fixed-ip=: 'Desired IP and/or subnet for this port (name or ID): subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
            --hint=: Port hints as ALIAS=VALUE or as JSON.
            --host=: Allocate port on host <host-id> (ID only)
            --mac-address=: MAC address of this port (admin only)
            --name=: Set port name
            --no-allowed-address: Clear existing allowed-address pairs associated with this port.
            --no-binding-profile: Clear existing information of binding:profile.
            --no-fixed-ip: Clear existing information of fixed IP addresses.
            --no-security-group: Clear existing security groups associated with this port
            --no-tag: Clear tags associated with the port.
            --not-trusted: Set port to be not trusted.
            --numa-policy-legacy: NUMA affinity policy using legacy mode to schedule this port
            --numa-policy-preferred: NUMA affinity policy preferred to schedule this port
            --numa-policy-required: NUMA affinity policy required to schedule this port
            --numa-policy-socket: NUMA affinity policy socket to schedule this port
            --pvlan-community=: Set PVLAN community name for this port.
            --pvlan-type=: Set Private VLAN type for this port.
            --qos-policy=: Attach QoS policy to this port (name or ID)
            --security-group=: Security group to associate with this port (name or ID) (repeat option to set multiple security groups)
            --tag=: Tag to be added to the port (repeat option to set multiple tags)
            --trusted: Set port to be trusted.
            --vnic-type=: 'VNIC type for this port (accelerator-direct | accelerator-direct-physical | direct | direct-physical | macvtap | normal | baremetal | virtio-forwarder | smart-nic | vdpa | remote-managed) (default: normal)'
          completion:
            flag:
                data-plane-status:
                    - ACTIVE
                    - DOWN
                pvlan-type:
                    - promiscuous
                    - isolated
                    - community
                vnic-type:
                    - accelerator-direct
                    - accelerator-direct-physical
                    - direct
                    - direct-physical
                    - macvtap
                    - normal
                    - baremetal
                    - virtio-forwarder
                    - smart-nic
                    - vdpa
                    - remote-managed
          documentation:
            flag:
                allowed-address: 'Add allowed-address pair associated with this port: ip-address=<ip-address>[,mac-address=<mac-address>] (repeat option to set multiple allowed-address pairs)'
                binding-profile: Custom data to be passed as binding:profile. Data may be passed as <key>=<value> or JSON (repeat option to set multiple binding:profile data).
                data-plane-status: Set data plane status of this port (ACTIVE | DOWN). Unset it to None with the 'port unset' command (requires data plane status extension).
                description: Description of this port
                device: Port device ID
                device-owner: Device owner of this port. This is the entity that uses the port (for example, network:dhcp).
                disable: Disable port
                disable-port-security: Disable port security for this port
                disable-uplink-status-propagation: Disable uplink status propagation
                dns-domain: Set DNS domain to this port (requires dns_domain extension for ports)
                dns-name: Set DNS name for this port (requires DNS integration extension)
                enable: Enable port
                enable-port-security: Enable port security for this port
                enable-uplink-status-propagation: Enable uplink status propagation
                extra-dhcp-option: 'Extra DHCP options to be assigned to this port: name=<name>[,value=<value>,ip-version={4,6}] (repeat option to set multiple extra DHCP options)'
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                fixed-ip: 'Desired IP and/or subnet for this port (name or ID): subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
                hint: 'Port hints as ALIAS=VALUE or as JSON. Valid hint aliases/values: ovs-tx-steering=thread, ovs-tx-steering=hash. Valid JSON values are as specified by the Neutron API. (requires port-hints extension) (requires port-hint-ovs-tx-steering extension for alias: ovs-tx-steering) (repeat option to set multiple hints).'
                host: Allocate port on host <host-id> (ID only)
                mac-address: MAC address of this port (admin only)
                name: Set port name
                no-allowed-address: Clear existing allowed-address pairs associated with this port. Specify both --allowed-address and --no-allowed-address to overwrite the current allowed-address pairs.
                no-binding-profile: Clear existing information of binding:profile. Specify both --binding-profile and --no-binding-profile to overwrite the current binding:profile information.
                no-fixed-ip: Clear existing information of fixed IP addresses. Specify both --fixed-ip and --no-fixed-ip to overwrite the current fixed IP addresses.
                no-security-group: Clear existing security groups associated with this port
                no-tag: Clear tags associated with the port. Specify both --tag and --no-tag to overwrite current tags
                not-trusted: Set port to be not trusted. This will be populated into the 'binding:profile' dictionary and passed to the services which expect it in this dictionary (for example, Nova).
                numa-policy-legacy: NUMA affinity policy using legacy mode to schedule this port
                numa-policy-preferred: NUMA affinity policy preferred to schedule this port
                numa-policy-required: NUMA affinity policy required to schedule this port
                numa-policy-socket: NUMA affinity policy socket to schedule this port
                pvlan-community: 'Set PVLAN community name for this port. Only applies when pvlan-type is ''community''. Requires PVLAN service plugin. Default: None.'
                pvlan-type: 'Set Private VLAN type for this port. Requires PVLAN service plugin. Default: promiscuous.'
                qos-policy: Attach QoS policy to this port (name or ID)
                security-group: Security group to associate with this port (name or ID) (repeat option to set multiple security groups)
                tag: Tag to be added to the port (repeat option to set multiple tags)
                trusted: Set port to be trusted. This will be populated into the 'binding:profile' dictionary and passed to the services which expect it in this dictionary (for example, Nova).
                vnic-type: 'VNIC type for this port (accelerator-direct | accelerator-direct-physical | direct | direct-physical | macvtap | normal | baremetal | virtio-forwarder | smart-nic | vdpa | remote-managed) (default: normal)'
        - name: show
          description: Display port details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset port properties
          flags:
            --all-tag: Clear all tags associated with the port
            --allowed-address=: 'Desired allowed-address pair which should be removed from this port: ip-address=<ip-address>[,mac-address=<mac-address>] (repeat option to unset multiple allowed-address pairs)'
            --binding-profile=: Desired key which should be removed from binding:profile (repeat option to unset multiple binding:profile keys)
            --data-plane-status: Clear existing data plane status information
            --device: Clear device ID for the port.
            --device-owner: Clear device owner for the port.
            --extra-property=: Additional parameters can be passed using this property.
            --fixed-ip=: 'Desired IP and/or subnet which should be removed from this port (name or ID): subnet=<subnet>,ip-address=<ip-address> (repeat option to unset multiple fixed IP addresses)'
            --hints: Clear hints for the port
            --host: Clear host binding for the port
            --numa-policy: Clear existing NUMA affinity policy
            --pvlan-community: Clear PVLAN community name for the port.
            --qos-policy: Remove the QoS policy attached to the port
            --security-group=: Security group which should be removed this port (name or ID) (repeat option to unset multiple security groups)
            --tag=: Tag to be removed from the port (repeat option to remove multiple tags)
          documentation:
            flag:
                all-tag: Clear all tags associated with the port
                allowed-address: 'Desired allowed-address pair which should be removed from this port: ip-address=<ip-address>[,mac-address=<mac-address>] (repeat option to unset multiple allowed-address pairs)'
                binding-profile: Desired key which should be removed from binding:profile (repeat option to unset multiple binding:profile keys)
                data-plane-status: Clear existing data plane status information
                device: Clear device ID for the port.
                device-owner: Clear device owner for the port.
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                fixed-ip: 'Desired IP and/or subnet which should be removed from this port (name or ID): subnet=<subnet>,ip-address=<ip-address> (repeat option to unset multiple fixed IP addresses)'
                hints: Clear hints for the port
                host: Clear host binding for the port
                numa-policy: Clear existing NUMA affinity policy
                pvlan-community: Clear PVLAN community name for the port.
                qos-policy: Remove the QoS policy attached to the port
                security-group: Security group which should be removed this port (name or ID) (repeat option to unset multiple security groups)
                tag: Tag to be removed from the port (repeat option to remove multiple tags)
    - name: project
      commands:
        - name: cleanup
          description: Clean resources associated with a project
          flags:
            --auth-project: Delete resources of the project used to authenticate
            --auto-approve: Delete resources without asking for confirmation
            --created-before=: Only delete resources created before the given time
            --dry-run: List a project's resources but do not delete them
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Project to clean (name or ID)
            --skip-resource=: Skip cleanup of specific resource (repeat if necessary)
            --updated-before=: Only delete resources updated before the given time
          documentation:
            flag:
                auth-project: Delete resources of the project used to authenticate
                auto-approve: Delete resources without asking for confirmation
                created-before: Only delete resources created before the given time
                dry-run: List a project's resources but do not delete them
                project: Project to clean (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                skip-resource: Skip cleanup of specific resource (repeat if necessary)
                updated-before: Only delete resources updated before the given time
        - name: create
          description: Create new project
          flags:
            --description=: Project description
            --disable: Disable project
            --domain=: Domain owning the project (name or ID)
            --enable: Enable project
            --fit-width: Fit the table to the display width.
            --immutable: Make resource immutable.
            --max-width=: Maximum display width, <1 to disable.
            --no-immutable: Make resource mutable (default)
            --noindent: whether to disable indenting the JSON
            --or-show: Return existing project
            --parent=: Parent of the project (name or ID)
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --property=: Add a property to <name> (repeat option to set multiple properties)
            --tag=: Tag to be added to the project (repeat option to set multiple tags)
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Project description
                disable: Disable project
                domain: Domain owning the project (name or ID)
                enable: Enable project
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                immutable: Make resource immutable. An immutable project may not be deleted or modified except to remove the immutable flag
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                no-immutable: Make resource mutable (default)
                noindent: whether to disable indenting the JSON
                or-show: Return existing project
                parent: Parent of the project (name or ID)
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                property: Add a property to <name> (repeat option to set multiple properties)
                tag: Tag to be added to the project (repeat option to set multiple tags)
                variable: ==SUPPRESS==
        - name: delete
          description: Delete project(s).
          flags:
            --domain=: Domain owning <project> (name or ID)
          documentation:
            flag:
                domain: Domain owning <project> (name or ID)
        - name: list
          description: List projects
          flags:
            --disabled: List only disabled projects
            --domain=: Filter projects by <domain> (name or ID)
            --enabled: List only enabled projects
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --my-projects: List projects for the authenticated user.
            --noindent: whether to disable indenting the JSON
            --not-tags-any=: Exclude projects which have any given tag(s) (Comma-separated list of tags)
            --not-tags=: Exclude projects which have all given tag(s) (Comma-separated list of tags)
            --parent=: Filter projects whose parent is <parent> (name or ID)
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --sort=: 'Sort output by selected keys and directions (asc or desc) (default: asc), repeat this option to specify multiple keys and directions.'
            --tags-any=: List projects which have any given tag(s) (Comma-separated list of tags)
            --tags=: List projects which have all given tag(s) (Comma-separated list of tags)
            --user=: Filter projects by <user> (name or ID)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                disabled: List only disabled projects
                domain: Filter projects by <domain> (name or ID)
                enabled: List only enabled projects
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                my-projects: List projects for the authenticated user. Supersedes other filters.
                noindent: whether to disable indenting the JSON
                not-tags: Exclude projects which have all given tag(s) (Comma-separated list of tags)
                not-tags-any: Exclude projects which have any given tag(s) (Comma-separated list of tags)
                parent: Filter projects whose parent is <parent> (name or ID)
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort: 'Sort output by selected keys and directions (asc or desc) (default: asc), repeat this option to specify multiple keys and directions.'
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                tags: List projects which have all given tag(s) (Comma-separated list of tags)
                tags-any: List projects which have any given tag(s) (Comma-separated list of tags)
                user: Filter projects by <user> (name or ID)
        - name: set
          description: Set project properties
          flags:
            --clear-tags: Clear tags associated with the project.
            --description=: Set project description
            --disable: Disable project
            --domain=: Domain owning <project> (name or ID)
            --enable: Enable project
            --immutable: Make resource immutable.
            --name=: Set project name
            --no-immutable: Make resource mutable (default)
            --property=: Set a property on <project> (repeat option to set multiple properties)
            --remove-tag=: Tag to be deleted from the project (repeat option to delete multiple tags)
            --tag=: Tag to be added to the project (repeat option to set multiple tags)
          documentation:
            flag:
                clear-tags: Clear tags associated with the project. Specify both --tag and --clear-tags to overwrite current tags
                description: Set project description
                disable: Disable project
                domain: Domain owning <project> (name or ID)
                enable: Enable project
                immutable: Make resource immutable. An immutable project may not be deleted or modified except to remove the immutable flag
                name: Set project name
                no-immutable: Make resource mutable (default)
                property: Set a property on <project> (repeat option to set multiple properties)
                remove-tag: Tag to be deleted from the project (repeat option to delete multiple tags)
                tag: Tag to be added to the project (repeat option to set multiple tags)
        - name: show
          description: Display project details
          flags:
            --children: Show project's subtree (children) as a list
            --domain=: Domain owning <project> (name or ID)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --parents: Show the project's parents as a list
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                children: Show project's subtree (children) as a list
                column: specify the column(s) to include, can be repeated to show multiple columns
                domain: Domain owning <project> (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                parents: Show the project's parents as a list
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset project properties
          flags:
            --property=: Unset a project property (repeat option to unset multiple properties)
          documentation:
            flag:
                property: Unset a project property (repeat option to unset multiple properties)
    - name: quota
      commands:
        - name: delete
          description: Delete configured quota for a project and revert to defaults.
          flags:
            --all: Delete project quotas for all services (default)
            --compute: Delete compute quotas for the project (including network quotas when using nova-network)
            --network: Delete network quotas for the project
            --volume: Delete volume quotas for the project
          documentation:
            flag:
                all: Delete project quotas for all services (default)
                compute: Delete compute quotas for the project (including network quotas when using nova-network)
                network: Delete network quotas for the project
                volume: Delete volume quotas for the project
        - name: list
          description: List quotas for all projects with non-default quota values.
          flags:
            --compute: List compute quota
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --network: List network quota
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --volume: List volume quota
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                compute: List compute quota
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                network: List network quota
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                volume: List volume quota
        - name: set
          description: Set quotas for project or class
          flags:
            --backup-gigabytes=: New value for the backup-gigabytes quota
            --backups=: New value for the backups quota
            --check-limit: ==SUPPRESS==
            --class: '**Deprecated** Set quotas for <class>.'
            --cores=: New value for the cores quota
            --default: Set default quotas for <project>
            --floating-ips=: New value for the floating-ips quota
            --force: Force quota update (only supported by compute and network)
            --gigabytes=: New value for the gigabytes quota
            --groups=: New value for the groups quota
            --injected-file-size=: New value for the injected-file-size quota
            --injected-files=: New value for the injected-files quota
            --injected-path-size=: New value for the injected-path-size quota
            --instances=: New value for the instances quota
            --key-pairs=: New value for the key-pairs quota
            --networks=: New value for the networks quota
            --no-force: Do not force quota update (only supported by compute and network) (default)
            --per-volume-gigabytes=: New value for the per-volume-gigabytes quota
            --ports=: New value for the ports quota
            --properties=: New value for the properties quota
            --ram=: New value for the ram quota
            --rbac-policies=: New value for the rbac-policies quota
            --router-routes=: New value for the router-routes quota
            --routers=: New value for the routers quota
            --secgroup-rules=: New value for the secgroup-rules quota
            --secgroups=: New value for the secgroups quota
            --server-group-members=: New value for the server-group-members quota
            --server-groups=: New value for the server-groups quota
            --snapshots=: New value for the snapshots quota
            --subnetpools=: New value for the subnetpools quota
            --subnets=: New value for the subnets quota
            --volume-type=: Set quotas for a specific <volume-type>
            --volumes=: New value for the volumes quota
          documentation:
            flag:
                backup-gigabytes: New value for the backup-gigabytes quota
                backups: New value for the backups quota
                check-limit: ==SUPPRESS==
                class: '**Deprecated** Set quotas for <class>. Deprecated as quota classes were never fully implemented and only the default class is supported. (compute and volume only)'
                cores: New value for the cores quota
                default: Set default quotas for <project>
                floating-ips: New value for the floating-ips quota
                force: Force quota update (only supported by compute and network)
                gigabytes: New value for the gigabytes quota
                groups: New value for the groups quota
                injected-file-size: New value for the injected-file-size quota
                injected-files: New value for the injected-files quota
                injected-path-size: New value for the injected-path-size quota
                instances: New value for the instances quota
                key-pairs: New value for the key-pairs quota
                networks: New value for the networks quota
                no-force: Do not force quota update (only supported by compute and network) (default)
                per-volume-gigabytes: New value for the per-volume-gigabytes quota
                ports: New value for the ports quota
                properties: New value for the properties quota
                ram: New value for the ram quota
                rbac-policies: New value for the rbac-policies quota
                router-routes: New value for the router-routes quota
                routers: New value for the routers quota
                secgroup-rules: New value for the secgroup-rules quota
                secgroups: New value for the secgroups quota
                server-group-members: New value for the server-group-members quota
                server-groups: New value for the server-groups quota
                snapshots: New value for the snapshots quota
                subnetpools: New value for the subnetpools quota
                subnets: New value for the subnets quota
                volume-type: Set quotas for a specific <volume-type>
                volumes: New value for the volumes quota
        - name: show
          description: Show quotas for project or class.
          flags:
            --all: Show quotas for all services
            --compute: Show compute quota
            --default: Show default quotas for <project>
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --network: Show network quota
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --usage: Show details about quotas usage
            --volume: Show volume quota
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all: Show quotas for all services
                column: specify the column(s) to include, can be repeated to show multiple columns
                compute: Show compute quota
                default: Show default quotas for <project>
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                network: Show network quota
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                usage: Show details about quotas usage
                volume: Show volume quota
    - name: region
      commands:
        - name: create
          description: Create new region
          flags:
            --description=: New region description
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --parent-region=: Parent region ID
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: New region description
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                parent-region: Parent region ID
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete region(s)
        - name: list
          description: List regions
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --parent-region=: Filter by parent region ID
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                parent-region: Filter by parent region ID
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: set
          description: Set region properties
          flags:
            --description=: New region description
            --parent-region=: New parent region ID
          documentation:
            flag:
                description: New region description
                parent-region: New parent region ID
        - name: show
          description: Display region details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: registered
      commands:
        - name: limit
          commands:
            - name: create
              description: Create a registered limit
              flags:
                --default-limit=!: The default limit for the resources to assume (required)
                --description=: Description of the registered limit
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --region=: Region for the registered limit to affect
                --service=!: Service responsible for the resource to limit (required) (name or ID)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    default-limit: The default limit for the resources to assume (required)
                    description: Description of the registered limit
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    region: Region for the registered limit to affect
                    service: Service responsible for the resource to limit (required) (name or ID)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete a registered limit
            - name: list
              description: List registered limits
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --region=: Region for the limit to affect.
                --resource-name=: The name of the resource to limit
                --service=: Service responsible for the resource to limit (name or ID)
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    region: Region for the limit to affect.
                    resource-name: The name of the resource to limit
                    service: Service responsible for the resource to limit (name or ID)
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Update information about a registered limit
              flags:
                --default-limit=: The default limit for the resources to assume
                --description=: Description to update of the registered limit
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --region=: Region for the registered limit to affect.
                --resource-name=: Resource to be updated responsible for the resource to limit.
                --service=: Service to be updated responsible for the resource to limit (name or ID).
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    default-limit: The default limit for the resources to assume
                    description: Description to update of the registered limit
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    region: Region for the registered limit to affect. Either --service, --resource-name or --region must be different than existing value otherwise it will be duplicate entry
                    resource-name: Resource to be updated responsible for the resource to limit. Either --service, --resource-name or --region must be different than existing value otherwise it will be duplicate entry
                    service: Service to be updated responsible for the resource to limit (name or ID). Either --service, --resource-name or --region must be different than existing value otherwise it will be duplicate entry
                    variable: ==SUPPRESS==
            - name: show
              description: Display registered limit details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: request
      commands:
        - name: token
          commands:
            - name: authorize
              description: Authorize a request token
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --request-key=!: Request token to authorize (ID only) (required)
                --role=!: Roles to authorize (name or ID) (repeat option to set multiple values) (required)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    request-key: Request token to authorize (ID only) (required)
                    role: Roles to authorize (name or ID) (repeat option to set multiple values) (required)
                    variable: ==SUPPRESS==
            - name: create
              description: Create a request token
              flags:
                --consumer-key=!: Consumer key (required)
                --consumer-secret=!: Consumer secret (required)
                --domain=: Domain owning <project> (name or ID)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project=!: Project that consumer wants to access (name or ID) (required)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    consumer-key: Consumer key (required)
                    consumer-secret: Consumer secret (required)
                    domain: Domain owning <project> (name or ID)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Project that consumer wants to access (name or ID) (required)
                    variable: ==SUPPRESS==
    - name: role
      commands:
        - name: add
          description: Adds a role assignment to a user or group on the system, a domain, or a project
          flags:
            --domain=: Include <domain> (name or ID)
            --group-domain=: Domain the group belongs to (name or ID).
            --group=: Include <group> (name or ID)
            --inherited: Specifies if the role grant is inheritable to the sub projects
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Include <project> (name or ID)
            --role-domain=: Domain the role belongs to (name or ID).
            --system=: Include <system> (all)
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Include <user> (name or ID)
          documentation:
            flag:
                domain: Include <domain> (name or ID)
                group: Include <group> (name or ID)
                group-domain: Domain the group belongs to (name or ID). This can be used in case collisions between group names exist.
                inherited: Specifies if the role grant is inheritable to the sub projects
                project: Include <project> (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                role-domain: Domain the role belongs to (name or ID). This must be specified when the name of a domain specific role is used.
                system: Include <system> (all)
                user: Include <user> (name or ID)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: assignment
          commands:
            - name: list
              description: List role assignments
              flags:
                --auth-project: Only list assignments for the project to which the authenticated user's token is scoped
                --auth-user: Only list assignments for the authenticated user
                --domain=: Domain to filter (name or ID)
                --effective: Returns only effective role assignments
                --fit-width: Fit the table to the display width.
                --group-domain=: Domain the group belongs to (name or ID).
                --group=: Group to filter (name or ID)
                --inherited: Specifies if the role grant is inheritable to the sub projects
                --max-width=: Maximum display width, <1 to disable.
                --names: Display names instead of IDs
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Project to filter (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --role-domain=: Domain the role belongs to (name or ID).
                --role=: Role to filter (name or ID)
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --system=: Filter based on system role assignments
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: User to filter (name or ID)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    auth-project: Only list assignments for the project to which the authenticated user's token is scoped
                    auth-user: Only list assignments for the authenticated user
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    domain: Domain to filter (name or ID)
                    effective: Returns only effective role assignments
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    group: Group to filter (name or ID)
                    group-domain: Domain the group belongs to (name or ID). This can be used in case collisions between group names exist.
                    inherited: Specifies if the role grant is inheritable to the sub projects
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    names: Display names instead of IDs
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Project to filter (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    role: Role to filter (name or ID)
                    role-domain: Domain the role belongs to (name or ID). This must be specified when the name of a domain specific role is used.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    system: Filter based on system role assignments
                    user: User to filter (name or ID)
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: create
          description: Create new role
          flags:
            --description=: Add description about the role
            --domain=: Domain the role belongs to (name or ID)
            --fit-width: Fit the table to the display width.
            --immutable: Make resource immutable.
            --max-width=: Maximum display width, <1 to disable.
            --no-immutable: Make resource mutable (default)
            --noindent: whether to disable indenting the JSON
            --or-show: Return existing role
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Add description about the role
                domain: Domain the role belongs to (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                immutable: Make resource immutable. An immutable project may not be deleted or modified except to remove the immutable flag
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                no-immutable: Make resource mutable (default)
                noindent: whether to disable indenting the JSON
                or-show: Return existing role
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete role(s)
          flags:
            --domain=: Domain the role belongs to (name or ID)
          documentation:
            flag:
                domain: Domain the role belongs to (name or ID)
        - name: list
          description: List roles
          flags:
            --domain=: Include <domain> (name or ID)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                domain: Include <domain> (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: remove
          description: 'Removes a role assignment from system/domain/project : user/group'
          flags:
            --domain=: Include <domain> (name or ID)
            --group-domain=: Domain the group belongs to (name or ID).
            --group=: Include <group> (name or ID)
            --inherited: Specifies if the role grant is inheritable to the sub projects
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Include <project> (name or ID)
            --role-domain=: Domain the role belongs to (name or ID).
            --system=: Include <system> (all)
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Include <user> (name or ID)
          documentation:
            flag:
                domain: Include <domain> (name or ID)
                group: Include <group> (name or ID)
                group-domain: Domain the group belongs to (name or ID). This can be used in case collisions between group names exist.
                inherited: Specifies if the role grant is inheritable to the sub projects
                project: Include <project> (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                role-domain: Domain the role belongs to (name or ID). This must be specified when the name of a domain specific role is used.
                system: Include <system> (all)
                user: Include <user> (name or ID)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: set
          description: Set role properties
          flags:
            --description=: Add description about the role
            --domain=: Domain the role belongs to (name or ID)
            --immutable: Make resource immutable.
            --name=: Set role name
            --no-immutable: Make resource mutable (default)
          documentation:
            flag:
                description: Add description about the role
                domain: Domain the role belongs to (name or ID)
                immutable: Make resource immutable. An immutable project may not be deleted or modified except to remove the immutable flag
                name: Set role name
                no-immutable: Make resource mutable (default)
        - name: show
          description: Display role details
          flags:
            --domain=: Domain the role belongs to (name or ID)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                domain: Domain the role belongs to (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: router
      commands:
        - name: add
          commands:
            - name: gateway
              description: Add router gateway
              flags:
                --fit-width: Fit the table to the display width.
                --fixed-ip=: 'Desired IP and/or subnet (name or ID) on external gateway: subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    fixed-ip: 'Desired IP and/or subnet (name or ID) on external gateway: subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: port
              description: Add a port to a router
            - name: route
              description: Add extra static routes to a router's routing table.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --route=: Add extra static route to the router.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    route: 'Add extra static route to the router. destination: destination subnet (in CIDR notation), gateway: nexthop IP address. Repeat option to add multiple routes. Trying to add a route that is already present (exactly, including destination and nexthop) in the routing table is allowed and is considered a successful operation.'
                    variable: ==SUPPRESS==
            - name: subnet
              description: Add a subnet to a router
              flags:
                --advertise-host: Mark the subnet's prefixes to be advertised as host routes within the router's EVPN VNI.
              documentation:
                flag:
                    advertise-host: Mark the subnet's prefixes to be advertised as host routes within the router's EVPN VNI. Only valid for EVPN routers.
        - name: create
          description: Create a new router
          flags:
            --auto-evpn-vni: Associate the router with an EVPN using an auto-assigned VNI.
            --availability-zone-hint=: Availability Zone in which to create this router (Router Availability Zone extension required, repeat option to set multiple availability zones)
            --centralized: Create a centralized router
            --description=: Set router description
            --disable: Disable router
            --disable-default-route-bfd: Disable BFD sessions for default routes inferred from the external gateway port subnets for this router
            --disable-default-route-ecmp: Add default route only for first gateway port
            --disable-ndp-proxy: Disable IPv6 NDP proxy on external gateway
            --disable-snat: Disable Source NAT on external gateway
            --distributed: Create a distributed router
            --enable: Enable router (default)
            --enable-default-route-bfd: Enable BFD sessions for default routes inferred from the external gateway port subnets for this router
            --enable-default-route-ecmp: Add ECMP default routes if multiple are available via different gateway ports
            --enable-ndp-proxy: Enable IPv6 NDP proxy on external gateway
            --enable-snat: Enable Source NAT on external gateway
            --evpn-vni=: Associate the router with an EVPN identified by a VNI.
            --external-gateway=: External Network used as router's gateway (name or ID) (repeat option to set multiple gateways per router if the L3 service plugin in use supports it)
            --extra-property=: Additional parameters can be passed using this property.
            --fit-width: Fit the table to the display width.
            --fixed-ip=: 'Desired IP and/or subnet (name or ID) on external gateway: subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
            --flavor-id=: ==SUPPRESS==
            --flavor=: Associate the router to a flavor (by name or ID
            --ha: Create a highly available router
            --max-width=: Maximum display width, <1 to disable.
            --no-ha: Create a legacy router
            --no-tag: No tags associated with the router
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Owner's project (name or ID)
            --qos-policy=: Attach QoS policy to router gateway IPs
            --tag=: Tag to be added to the router (repeat option to set multiple tags)
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                auto-evpn-vni: Associate the router with an EVPN using an auto-assigned VNI.
                availability-zone-hint: Availability Zone in which to create this router (Router Availability Zone extension required, repeat option to set multiple availability zones)
                centralized: Create a centralized router
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Set router description
                disable: Disable router
                disable-default-route-bfd: Disable BFD sessions for default routes inferred from the external gateway port subnets for this router
                disable-default-route-ecmp: Add default route only for first gateway port
                disable-ndp-proxy: Disable IPv6 NDP proxy on external gateway
                disable-snat: Disable Source NAT on external gateway
                distributed: Create a distributed router
                enable: Enable router (default)
                enable-default-route-bfd: Enable BFD sessions for default routes inferred from the external gateway port subnets for this router
                enable-default-route-ecmp: Add ECMP default routes if multiple are available via different gateway ports
                enable-ndp-proxy: Enable IPv6 NDP proxy on external gateway
                enable-snat: Enable Source NAT on external gateway
                evpn-vni: Associate the router with an EVPN identified by a VNI.
                external-gateway: External Network used as router's gateway (name or ID) (repeat option to set multiple gateways per router if the L3 service plugin in use supports it)
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                fixed-ip: 'Desired IP and/or subnet (name or ID) on external gateway: subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
                flavor: Associate the router to a flavor (by name or ID
                flavor-id: ==SUPPRESS==
                format: the output format, defaults to table
                ha: Create a highly available router
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                no-ha: Create a legacy router
                no-tag: No tags associated with the router
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Owner's project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                qos-policy: Attach QoS policy to router gateway IPs
                tag: Tag to be added to the router (repeat option to set multiple tags)
                variable: ==SUPPRESS==
        - name: delete
          description: Delete router(s)
        - name: list
          description: List routers
          flags:
            --agent=: List only routers hosted by the specified agent (ID only)
            --any-tags=: List routers which have any given tag(s) (Comma-separated list of tags)
            --disable: List disabled routers
            --enable: List enabled routers
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --name=: List routers according to their name
            --noindent: whether to disable indenting the JSON
            --not-any-tags=: Exclude routers which have any given tag(s) (Comma-separated list of tags)
            --not-tags=: Exclude routers which have all given tag(s) (Comma-separated list of tags)
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: List only routers with the specified project (name or ID)
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --tags=: List routers which have all given tag(s) (Comma-separated list of tags)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                agent: List only routers hosted by the specified agent (ID only)
                any-tags: List routers which have any given tag(s) (Comma-separated list of tags)
                column: specify the column(s) to include, can be repeated to show multiple columns
                disable: List disabled routers
                enable: List enabled routers
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: List routers according to their name
                noindent: whether to disable indenting the JSON
                not-any-tags: Exclude routers which have any given tag(s) (Comma-separated list of tags)
                not-tags: Exclude routers which have all given tag(s) (Comma-separated list of tags)
                print-empty: Print empty table if there is no data to show.
                project: List only routers with the specified project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                tags: List routers which have all given tag(s) (Comma-separated list of tags)
        - name: ndp
          commands:
            - name: proxy
              commands:
                - name: create
                  description: Create NDP proxy
                  flags:
                    --description=: Text to describe/contextualize the use of the NDP proxy configuration
                    --fit-width: Fit the table to the display width.
                    --ip-address=: The IPv6 address that is to be proxied.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: New NDP proxy name
                    --noindent: whether to disable indenting the JSON
                    --port=!: The name or ID of the network port associated to the NDP proxy
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Text to describe/contextualize the use of the NDP proxy configuration
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        ip-address: The IPv6 address that is to be proxied. In case the port has multiple addresses assigned, use this option to select which address is to be used.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: New NDP proxy name
                        noindent: whether to disable indenting the JSON
                        port: The name or ID of the network port associated to the NDP proxy
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete NDP proxy
                - name: list
                  description: List NDP proxies
                  flags:
                    --fit-width: Fit the table to the display width.
                    --ip-address=: List only NDP proxies associated with the specified IPv6 address
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: List only NDP proxies with the specified name
                    --noindent: whether to disable indenting the JSON
                    --port=: List only NDP proxies associated with the specified port (name or ID)
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: List only NDP proxies with the specified project (name or ID)
                    --quote=: when to include quotes, defaults to nonnumeric
                    --router=: List only NDP proxies associated with the specifed router (name or ID)
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        ip-address: List only NDP proxies associated with the specified IPv6 address
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: List only NDP proxies with the specified name
                        noindent: whether to disable indenting the JSON
                        port: List only NDP proxies associated with the specified port (name or ID)
                        print-empty: Print empty table if there is no data to show.
                        project: List only NDP proxies with the specified project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        quote: when to include quotes, defaults to nonnumeric
                        router: List only NDP proxies associated with the specifed router (name or ID)
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set NDP proxy properties
                  flags:
                    --description=: Text to describe/contextualize the use of the NDP proxy configuration
                    --name=: Set NDP proxy name
                  documentation:
                    flag:
                        description: Text to describe/contextualize the use of the NDP proxy configuration
                        name: Set NDP proxy name
                - name: show
                  description: Display NDP proxy details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
        - name: remove
          commands:
            - name: gateway
              description: Remove router gateway
              flags:
                --fit-width: Fit the table to the display width.
                --fixed-ip=: 'IP and/or subnet (name or ID) on the external gateway which is used to identify a particular gateway if multiple are attached to the same network: subnet=<subnet>,ip-address=<ip-address>'
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    fixed-ip: 'IP and/or subnet (name or ID) on the external gateway which is used to identify a particular gateway if multiple are attached to the same network: subnet=<subnet>,ip-address=<ip-address>'
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: port
              description: Remove a port from a router
            - name: route
              description: Remove extra static routes from a router's routing table.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --route=: Remove extra static route from the router.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    route: 'Remove extra static route from the router. destination: destination subnet (in CIDR notation), gateway: nexthop IP address. Repeat option to remove multiple routes. Trying to remove a route that is already missing (fully, including destination and nexthop) from the routing table is allowed and is considered a successful operation.'
                    variable: ==SUPPRESS==
            - name: subnet
              description: Remove a subnet from a router
        - name: set
          description: Set router properties
          flags:
            --centralized: Set router to centralized mode (disabled router only)
            --description=: Set router description
            --disable: Disable router
            --disable-default-route-bfd: Disable BFD sessions for default routes inferred from the external gateway port subnets for this router
            --disable-default-route-ecmp: Add default route only for first gateway port
            --disable-ndp-proxy: Disable IPv6 NDP proxy on external gateway
            --disable-snat: Disable Source NAT on external gateway
            --distributed: Set router to distributed mode (disabled router only)
            --enable: Enable router
            --enable-default-route-bfd: Enable BFD sessions for default routes inferred from the external gateway port subnets for this router
            --enable-default-route-ecmp: Add ECMP default routes if multiple are available via different gateway ports
            --enable-ndp-proxy: Enable IPv6 NDP proxy on external gateway
            --enable-snat: Enable Source NAT on external gateway
            --external-gateway=: External Network used as router's gateway (name or ID) (repeat option to set multiple gateways per router if the L3 service plugin in use supports it).
            --extra-property=: Additional parameters can be passed using this property.
            --fixed-ip=: 'Desired IP and/or subnet (name or ID) on external gateway: subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
            --ha: Set the router as highly available (disabled router only)
            --name=: Set router name
            --no-ha: Clear high availability attribute of the router (disabled router only)
            --no-qos-policy: Remove QoS policy from router gateway IPs
            --no-route: Clear routes associated with the router.
            --no-tag: Clear tags associated with the router.
            --qos-policy=: Attach QoS policy to router gateway IPs
            --route=: Add routes to the router.
            --tag=: Tag to be added to the router (repeat option to set multiple tags)
          documentation:
            flag:
                centralized: Set router to centralized mode (disabled router only)
                description: Set router description
                disable: Disable router
                disable-default-route-bfd: Disable BFD sessions for default routes inferred from the external gateway port subnets for this router
                disable-default-route-ecmp: Add default route only for first gateway port
                disable-ndp-proxy: Disable IPv6 NDP proxy on external gateway
                disable-snat: Disable Source NAT on external gateway
                distributed: Set router to distributed mode (disabled router only)
                enable: Enable router
                enable-default-route-bfd: Enable BFD sessions for default routes inferred from the external gateway port subnets for this router
                enable-default-route-ecmp: Add ECMP default routes if multiple are available via different gateway ports
                enable-ndp-proxy: Enable IPv6 NDP proxy on external gateway
                enable-snat: Enable Source NAT on external gateway
                external-gateway: External Network used as router's gateway (name or ID) (repeat option to set multiple gateways per router if the L3 service plugin in use supports it).
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                fixed-ip: 'Desired IP and/or subnet (name or ID) on external gateway: subnet=<subnet>,ip-address=<ip-address> (repeat option to set multiple fixed IP addresses)'
                ha: Set the router as highly available (disabled router only)
                name: Set router name
                no-ha: Clear high availability attribute of the router (disabled router only)
                no-qos-policy: Remove QoS policy from router gateway IPs
                no-route: Clear routes associated with the router. Specify both --route and --no-route to overwrite current value of routes.
                no-tag: Clear tags associated with the router. Specify both --tag and --no-tag to overwrite current tags
                qos-policy: Attach QoS policy to router gateway IPs
                route: 'Add routes to the router. destination: destination subnet (in CIDR notation) gateway: nexthop IP address (repeat option to add multiple routes). This is deprecated in favor of ''router add/remove route'' since it is prone to race conditions between concurrent clients when not used together with --no-route to overwrite the current value of ''routes''.'
                tag: Tag to be added to the router (repeat option to set multiple tags)
        - name: show
          description: Display router details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset router properties
          flags:
            --all-tag: Clear all tags associated with the router
            --external-gateway: Remove external gateway information from the router
            --extra-property=: Additional parameters can be passed using this property.
            --qos-policy: Remove QoS policy from router gateway IPs
            --route=: Routes to be removed from the router.
            --tag=: Tag to be removed from the router (repeat option to remove multiple tags)
          documentation:
            flag:
                all-tag: Clear all tags associated with the router
                external-gateway: Remove external gateway information from the router
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                qos-policy: Remove QoS policy from router gateway IPs
                route: 'Routes to be removed from the router. destination: destination subnet (in CIDR notation) gateway: nexthop IP address (repeat option to unset multiple routes)'
                tag: Tag to be removed from the router (repeat option to remove multiple tags)
    - name: security
      commands:
        - name: group
          commands:
            - name: create
              description: Create a new security group
              flags:
                --description=: Security group description
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --no-tag: No tags associated with the security group
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --stateful: Security group is stateful (default)
                --stateless: Security group is stateless
                --tag=: Tag to be added to the security group (repeat option to set multiple tags)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Security group description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    no-tag: No tags associated with the security group
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    stateful: Security group is stateful (default)
                    stateless: Security group is stateless
                    tag: Tag to be added to the security group (repeat option to set multiple tags)
                    variable: ==SUPPRESS==
            - name: default
              commands:
                - name: statefulness
                  commands:
                    - name: create
                      description: Create a default statefulness setting for security groups
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --project-domain=: Domain the project belongs to (name or ID).
                        --project=: Apply the setting to this project (name or ID).
                        --stateful: Set default statefulness to stateful
                        --stateless: Set default statefulness to stateless
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            project: Apply the setting to this project (name or ID). If not specified, the setting applies system-wide
                            project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                            stateful: Set default statefulness to stateful
                            stateless: Set default statefulness to stateless
                            variable: ==SUPPRESS==
                    - name: delete
                      description: Delete security group default statefulness setting(s)
                    - name: list
                      description: List security group default statefulness settings
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --project-domain=: Domain the project belongs to (name or ID).
                        --project=: List only settings for this project (name or ID)
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            project: List only settings for this project (name or ID)
                            project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: set
                      description: Update a security group default statefulness setting
                      flags:
                        --stateful: Set default statefulness to stateful
                        --stateless: Set default statefulness to stateless
                      documentation:
                        flag:
                            stateful: Set default statefulness to stateful
                            stateless: Set default statefulness to stateless
                    - name: show
                      description: Show a security group default statefulness setting
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
            - name: delete
              description: Delete security group(s)
            - name: list
              description: List security groups
              flags:
                --any-tags=: List security group which have any given tag(s) (Comma-separated list of tags)
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --no-share: List only security groups not shared between projects
                --noindent: whether to disable indenting the JSON
                --not-any-tags=: Exclude security group which have any given tag(s) (Comma-separated list of tags)
                --not-tags=: Exclude security group which have all given tag(s) (Comma-separated list of tags)
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: List only security groups with the specified project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --share: List only security groups shared between projects
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --tags=: List security group which have all given tag(s) (Comma-separated list of tags)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    any-tags: List security group which have any given tag(s) (Comma-separated list of tags)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    no-share: List only security groups not shared between projects
                    noindent: whether to disable indenting the JSON
                    not-any-tags: Exclude security group which have any given tag(s) (Comma-separated list of tags)
                    not-tags: Exclude security group which have all given tag(s) (Comma-separated list of tags)
                    print-empty: Print empty table if there is no data to show.
                    project: List only security groups with the specified project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    share: List only security groups shared between projects
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    tags: List security group which have all given tag(s) (Comma-separated list of tags)
            - name: rule
              commands:
                - name: create
                  description: Create a new security group rule
                  flags:
                    --description=: Set security group rule description
                    --dst-port=: 'Destination port, may be a single port or a starting and ending port range: 137:139.'
                    --egress: Rule applies to outgoing network traffic
                    --ethertype=: 'Ethertype of network traffic (IPv4, IPv6; default: based on IP protocol)'
                    --extra-property=: Additional parameters can be passed using this property.
                    --fit-width: Fit the table to the display width.
                    --icmp-code=: ICMP code for ICMP IP protocols
                    --icmp-type=: ICMP type for ICMP IP protocols
                    --ingress: Rule applies to incoming network traffic (default)
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --protocol=: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                    --remote-address-group=: Remote address group (name or ID)
                    --remote-group=: Remote security group (name or ID)
                    --remote-ip=: 'Remote IP address block (may use CIDR notation; default for IPv4 rule: 0.0.0.0/0, default for IPv6 rule: ::/0)'
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        ethertype:
                            - IPv4
                            - IPv6
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Set security group rule description
                        dst-port: 'Destination port, may be a single port or a starting and ending port range: 137:139. Required for IP protocols TCP and UDP. Ignored for ICMP IP protocols.'
                        egress: Rule applies to outgoing network traffic
                        ethertype: 'Ethertype of network traffic (IPv4, IPv6; default: based on IP protocol)'
                        extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        icmp-code: ICMP code for ICMP IP protocols
                        icmp-type: ICMP type for ICMP IP protocols
                        ingress: Rule applies to incoming network traffic (default)
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        protocol: 'IP protocol (ah, dccp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                        remote-address-group: Remote address group (name or ID)
                        remote-group: Remote security group (name or ID)
                        remote-ip: 'Remote IP address block (may use CIDR notation; default for IPv4 rule: 0.0.0.0/0, default for IPv6 rule: ::/0)'
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete security group rule(s)
                - name: list
                  description: List security group rules
                  flags:
                    --egress: List only rules applied to outgoing network traffic
                    --ethertype=: List only rules with the specified Ethertype (IPv4 or IPv6)
                    --fit-width: Fit the table to the display width.
                    --ingress: List only rules applied to incoming network traffic
                    --limit=: The maximum number of entries to return per page.
                    --long: '**Deprecated** This argument is no longer needed'
                    --marker=: The first position in the collection to return results from.
                    --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: List only rules with the specified project (name or ID)
                    --protocol=: 'List only rules with the specified IP protocol (ah, dhcp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        egress: List only rules applied to outgoing network traffic
                        ethertype: List only rules with the specified Ethertype (IPv4 or IPv6)
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        ingress: List only rules applied to incoming network traffic
                        limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                        long: '**Deprecated** This argument is no longer needed'
                        marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                        max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        project: List only rules with the specified project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        protocol: 'List only rules with the specified IP protocol (ah, dhcp, egp, esp, gre, icmp, igmp, ipv6-encap, ipv6-frag, ipv6-icmp, ipv6-nonxt, ipv6-opts, ipv6-route, ospf, pgm, rsvp, sctp, tcp, udp, udplite, vrrp and integer representations [0-255] or any; default: any (all protocols))'
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: show
                  description: Display security group rule details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: set
              description: Set security group properties
              flags:
                --description=: New security group description
                --extra-property=: Additional parameters can be passed using this property.
                --name=: New security group name
                --no-tag: Clear tags associated with the security group.
                --stateful: Security group is stateful (default)
                --stateless: Security group is stateless
                --tag=: Tag to be added to the security group (repeat option to set multiple tags)
              documentation:
                flag:
                    description: New security group description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    name: New security group name
                    no-tag: Clear tags associated with the security group. Specify both --tag and --no-tag to overwrite current tags
                    stateful: Security group is stateful (default)
                    stateless: Security group is stateless
                    tag: Tag to be added to the security group (repeat option to set multiple tags)
            - name: show
              description: Display security group details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset security group properties
              flags:
                --all-tag: Clear all tags associated with the security group
                --tag=: Tag to be removed from the security group (repeat option to remove multiple tags)
              documentation:
                flag:
                    all-tag: Clear all tags associated with the security group
                    tag: Tag to be removed from the security group (repeat option to remove multiple tags)
    - name: server
      commands:
        - name: add
          commands:
            - name: fixed
              commands:
                - name: ip
                  description: Add fixed IP address to server
                  flags:
                    --fit-width: Fit the table to the display width.
                    --fixed-ip-address=: Requested fixed IP address
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --tag=: Tag for the attached interface.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        fixed-ip-address: Requested fixed IP address
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        tag: Tag for the attached interface. (supported by --os-compute-api-version 2.49 or above)
                        variable: ==SUPPRESS==
            - name: floating
              commands:
                - name: ip
                  description: Add floating IP address to server
                  flags:
                    --fixed-ip-address=: Fixed IP address to associate with this floating IP address.
                  documentation:
                    flag:
                        fixed-ip-address: Fixed IP address to associate with this floating IP address. The first server port containing the fixed IP address will be used
            - name: network
              description: Add network to server
              flags:
                --tag=: Tag for the attached interface.
              documentation:
                flag:
                    tag: Tag for the attached interface. (supported by --os-compute-api-version 2.49 or above)
            - name: port
              description: Add port to server
              flags:
                --tag=: Tag for the attached interface (supported by --os-compute-api-version 2.49 or later)
              documentation:
                flag:
                    tag: Tag for the attached interface (supported by --os-compute-api-version 2.49 or later)
            - name: security
              commands:
                - name: group
                  description: Add security group(s) to server
            - name: share
              description: Add a share to a server.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --tag=: Optional tag used to mount the share, if not provided the share uuid is used as tag by default
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    tag: Optional tag used to mount the share, if not provided the share uuid is used as tag by default
                    variable: ==SUPPRESS==
            - name: volume
              description: Add volume to server.
              flags:
                --device=: Server internal device name for volume
                --disable-delete-on-termination: Do not delete the volume when the server is destroyed (supported by --os-compute-api-version 2.79 or above)
                --enable-delete-on-termination: Delete the volume when the server is destroyed (supported by --os-compute-api-version 2.79 or above)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --tag=: Tag for the attached volume (supported by --os-compute-api-version 2.49 or above)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    device: Server internal device name for volume
                    disable-delete-on-termination: Do not delete the volume when the server is destroyed (supported by --os-compute-api-version 2.79 or above)
                    enable-delete-on-termination: Delete the volume when the server is destroyed (supported by --os-compute-api-version 2.79 or above)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    tag: Tag for the attached volume (supported by --os-compute-api-version 2.49 or above)
                    variable: ==SUPPRESS==
        - name: backup
          commands:
            - name: create
              description: Create a server backup image
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: 'Name of the backup image (default: server name)'
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --rotate=: 'Number of backups to keep (default: 1)'
                --type=: 'Used to populate the backup_type property of the backup image (default: empty)'
                --variable=: ==SUPPRESS==
                --wait: Wait for backup image create to complete
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: 'Name of the backup image (default: server name)'
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    rotate: 'Number of backups to keep (default: 1)'
                    type: 'Used to populate the backup_type property of the backup image (default: empty)'
                    variable: ==SUPPRESS==
                    wait: Wait for backup image create to complete
        - name: create
          description: Create a new server
          flags:
            --auto-network=: Automatically allocate a network to the server.
            --availability-zone=: Select an availability zone for the server.
            --block-device-mapping=: '**Deprecated** Create a block device on the server.'
            --block-device=: Create a block device on the server.
            --boot-from-volume=: When used in conjunction with the ``--image`` or ``--image-property`` option, this option automatically creates a block device mapping with a boot index of 0 and tells the compute service to create a volume of the given size (in GB) from the specified image and use it as the root disk of the server.
            --config-drive=: '**Deprecated** Use specified volume as the config drive, or ''True'' to use an ephemeral drive.'
            --delete-on-termination: Delete the boot volume automatically when the server is deleted (only valid with --boot-from-volume).
            --description=: Set description for the server (supported by --os-compute-api-version 2.19 or above)
            --ephemeral=: Create and attach a local ephemeral block device of <size> GiB and format it to <format>.
            --file=: File(s) to inject into image before boot (repeat option to set multiple files) (supported by --os-compute-api-version 2.57 or below)
            --fit-width: Fit the table to the display width.
            --flavor=!: Create server with this flavor (name or ID)
            --hint=: Hints for the scheduler
            --host=: Requested host to create servers.
            --hostname=: Hostname configured for the server in the metadata service.
            --hypervisor-hostname=: Requested hypervisor hostname to create servers.
            --image-property=: Create server using the image that matches the specified property.
            --image=: Create server boot disk from this image (name or ID)
            --key-name=: Keypair to inject into this server
            --max-width=: Maximum display width, <1 to disable.
            --max=: Maximum number of servers to launch (default=1)
            --min=: Minimum number of servers to launch (default=1)
            --network=: Create a NIC on the server and connect it to network.
            --nic=: Create a NIC on the server.
            --no-config-drive: Disable config drive.
            --no-network=: Do not attach a network to the server.
            --no-security-group: Do not associate a security group with ports attached to this server.
            --noindent: whether to disable indenting the JSON
            --password=: Set the password to this server.
            --port=: Create a NIC on the server and connect it to port.
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --property=: Set a property on this server (repeat option to set multiple values)
            --security-group=: Security group to associate with ports attached to this server (name or ID) (repeat option to set multiple groups)
            --server-group=: Server group to create the server within (this is an alias for '--hint group=<server-group-id>')
            --snapshot=: Create server using this snapshot as the boot disk (name or ID)
            --swap=: Create and attach a local swap block device of <swap_size> MiB.
            --tag=: Tags for the server.
            --trusted-image-cert=: Trusted image certificate IDs used to validate certificates during the image signature verification process.
            --use-config-drive: Enable config drive.
            --user-data=: User data file to serve from the metadata server
            --variable=: ==SUPPRESS==
            --volume=: Create server using this volume as the boot disk (name or ID)
            --wait: Wait for build to complete
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                auto-network: Automatically allocate a network to the server. This is the default network allocation policy. This is a wrapper for the '--nic auto' option that provides a simple syntax for enabling automatic configuration of network connectivity for a new server. For more advanced use cases, refer to the '--nic' parameter. (supported by --os-compute-api-version 2.37 or above)
                availability-zone: Select an availability zone for the server. Host and node are optional parameters. Availability zone in the format <zone-name>:<host-name>:<node-name>, <zone-name>::<node-name>, <zone-name>:<host-name> or <zone-name>
                block-device: |-
                    Create a block device on the server.
                    Either a path to a JSON file or a CSV-serialized string describing the block device mapping.
                    The following keys are accepted for both:
                    uuid=<uuid>: UUID of the volume, snapshot or ID (required if using source image, snapshot or volume),
                    source_type=<source_type>: source type (one of: image, snapshot, volume, blank),
                    destination_type=<destination_type>: destination type (one of: volume, local) (optional),
                    disk_bus=<disk_bus>: device bus (one of: uml, lxc, virtio, ...) (optional),
                    device_type=<device_type>: device type (one of: disk, cdrom, etc. (optional),
                    device_name=<device_name>: name of the device (optional),
                    volume_size=<volume_size>: size of the block device in MiB (for swap) or GiB (for everything else) (optional),
                    guest_format=<guest_format>: format of device (optional),
                    boot_index=<boot_index>: index of disk used to order boot disk (required for volume-backed instances),
                    delete_on_termination=<true|false>: whether to delete the volume upon deletion of server (optional),
                    tag=<tag>: device metadata tag (optional),
                    volume_type=<volume_type>: type of volume to create (name or ID) when source if blank, image or snapshot and dest is volume (optional)
                block-device-mapping: |-
                    **Deprecated** Create a block device on the server.
                    Block device mapping in the format
                    <dev-name>=<id>:<type>:<size(GB)>:<delete-on-terminate>
                    <dev-name>: block device name, like: vdb, xvdc (required)
                    <id>: Name or ID of the volume, volume snapshot or image (required)
                    <type>: volume, snapshot or image; default: volume (optional)
                    <size(GB)>: volume size if create from image or snapshot (optional)
                    <delete-on-terminate>: true or false; default: false (optional)
                    Replaced by --block-device
                boot-from-volume: When used in conjunction with the ``--image`` or ``--image-property`` option, this option automatically creates a block device mapping with a boot index of 0 and tells the compute service to create a volume of the given size (in GB) from the specified image and use it as the root disk of the server. The root volume will not be deleted when the server is deleted. This option is mutually exclusive with the ``--volume`` and ``--snapshot`` options.
                column: specify the column(s) to include, can be repeated to show multiple columns
                config-drive: '**Deprecated** Use specified volume as the config drive, or ''True'' to use an ephemeral drive. Replaced by ''--use-config-drive''.'
                delete-on-termination: Delete the boot volume automatically when the server is deleted (only valid with --boot-from-volume).
                description: Set description for the server (supported by --os-compute-api-version 2.19 or above)
                ephemeral: Create and attach a local ephemeral block device of <size> GiB and format it to <format>.
                file: File(s) to inject into image before boot (repeat option to set multiple files) (supported by --os-compute-api-version 2.57 or below)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                flavor: Create server with this flavor (name or ID)
                format: the output format, defaults to table
                hint: Hints for the scheduler
                host: Requested host to create servers. (admin only) (supported by --os-compute-api-version 2.74 or above)
                hostname: Hostname configured for the server in the metadata service. If unset, a hostname will be automatically generated from the server name. A utility such as cloud-init is required to propagate the hostname in the metadata service to the guest OS itself (hostnames supported by --os-compute-api-version 2.90 or above, FQDNs supported by --os-compute-api-version 2.94 or above)
                hypervisor-hostname: Requested hypervisor hostname to create servers. (admin only) (supported by --os-compute-api-version 2.74 or above)
                image: Create server boot disk from this image (name or ID)
                image-property: Create server using the image that matches the specified property. Property must match exactly one property.
                key-name: Keypair to inject into this server
                max: Maximum number of servers to launch (default=1)
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                min: Minimum number of servers to launch (default=1)
                network: Create a NIC on the server and connect it to network. Specify option multiple times to create multiple NICs. This is a wrapper for the '--nic net-id=<network>' parameter that provides simple syntax for the standard use case of connecting a new server to a given network. For more advanced use cases, refer to the '--nic' parameter.
                nic: |-
                    Create a NIC on the server.
                    NIC in the format:
                    net-id=<net-uuid>: attach NIC to network with this UUID,
                    port-id=<port-uuid>: attach NIC to port with this UUID,
                    v4-fixed-ip=<ip-addr>: IPv4 fixed address for NIC (optional),
                    v6-fixed-ip=<ip-addr>: IPv6 fixed address for NIC (optional),
                    tag: interface metadata tag (optional) (supported by --os-compute-api-version 2.43 or above),
                    none: (v2.37+) no network is attached,
                    auto: (v2.37+) the compute service will automatically allocate a network.

                    Specify option multiple times to create multiple NICs.
                    Specifying a --nic of auto or none cannot be used with any other --nic value.
                    Either net-id or port-id must be provided, but not both.
                no-config-drive: Disable config drive.
                no-network: Do not attach a network to the server. This is a wrapper for the '--nic none' option that provides a simple syntax for disabling network connectivity for a new server. For more advanced use cases, refer to the '--nic' parameter. (supported by --os-compute-api-version 2.37 or above)
                no-security-group: Do not associate a security group with ports attached to this server. This does not affect the security groups associated with pre-existing ports.
                noindent: whether to disable indenting the JSON
                password: Set the password to this server. This option requires cloud support.
                port: Create a NIC on the server and connect it to port. Specify option multiple times to create multiple NICs. This is a wrapper for the '--nic port-id=<port>' parameter that provides simple syntax for the standard use case of connecting a new server to a given port. For more advanced use cases, refer to the '--nic' parameter.
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                property: Set a property on this server (repeat option to set multiple values)
                security-group: Security group to associate with ports attached to this server (name or ID) (repeat option to set multiple groups)
                server-group: Server group to create the server within (this is an alias for '--hint group=<server-group-id>')
                snapshot: |-
                    Create server using this snapshot as the boot disk (name or ID)
                    This option automatically creates a block device mapping with a boot index of 0. On many hypervisors (libvirt/kvm for example) this will be device vda. Do not create a duplicate mapping using --block-device-mapping for this volume.
                swap: Create and attach a local swap block device of <swap_size> MiB.
                tag: Tags for the server. Specify multiple times to add multiple tags. (supported by --os-compute-api-version 2.52 or above)
                trusted-image-cert: Trusted image certificate IDs used to validate certificates during the image signature verification process. May be specified multiple times to pass multiple trusted image certificate IDs. (supported by --os-compute-api-version 2.63 or above)
                use-config-drive: Enable config drive.
                user-data: User data file to serve from the metadata server
                variable: ==SUPPRESS==
                volume: |-
                    Create server using this volume as the boot disk (name or ID)
                    This option automatically creates a block device mapping with a boot index of 0. On many hypervisors (libvirt/kvm for example) this will be device vda. Do not create a duplicate mapping using --block-device-mapping for this volume.
                wait: Wait for build to complete
        - name: delete
          description: Delete server(s)
          flags:
            --all-projects: Delete server(s) in another project by name (admin only)(can be specified using the ALL_PROJECTS envvar)
            --force: Force delete server(s)
            --wait: Wait for delete to complete
          documentation:
            flag:
                all-projects: Delete server(s) in another project by name (admin only)(can be specified using the ALL_PROJECTS envvar)
                force: Force delete server(s)
                wait: Wait for delete to complete
        - name: dump
          commands:
            - name: create
              description: Create a dump file in server(s)
        - name: evacuate
          description: Evacuate a server to a different host.
          flags:
            --fit-width: Fit the table to the display width.
            --host=: Set the preferred host on which to rebuild the evacuated server.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --password=: Set the password on the evacuated instance.
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --shared-storage: Indicate that the instance is on shared storage.
            --variable=: ==SUPPRESS==
            --wait: Wait for evacuation to complete
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                host: Set the preferred host on which to rebuild the evacuated server. The host will be validated by the scheduler. (supported by --os-compute-api-version 2.29 or above)
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                password: Set the password on the evacuated instance. This option is mutually exclusive with the --shared-storage option. This option requires cloud support.
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                shared-storage: Indicate that the instance is on shared storage. This will be auto-calculated with --os-compute-api-version 2.14 and greater and should not be used with later microversions. This option is mutually exclusive with the --password option
                variable: ==SUPPRESS==
                wait: Wait for evacuation to complete
        - name: event
          commands:
            - name: list
              description: List recent events of a server.
              flags:
                --changes-before=: List only server events changed earlier or equal to a certain point of time.
                --changes-since=: List only server events changed later or equal to a certain point of time.
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    changes-before: List only server events changed earlier or equal to a certain point of time. The provided time should be an ISO 8061 formatted time, e.g. ``2016-03-04T06:27:59Z``. (supported with --os-compute-api-version 2.66 or above)
                    changes-since: List only server events changed later or equal to a certain point of time. The provided time should be an ISO 8061 formatted time, e.g. ``2016-03-04T06:27:59Z``. (supported with --os-compute-api-version 2.58 or above)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show server event details.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: group
          commands:
            - name: create
              description: Create a new server group.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --policy=: Add a policy to <name> Specify --os-compute-api-version 2.15 or higher for the 'soft-affinity' or 'soft-anti-affinity' policy.
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --rule=: A rule for the policy.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
                    policy:
                        - affinity
                        - anti-affinity
                        - soft-affinity
                        - soft-anti-affinity
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    policy: Add a policy to <name> Specify --os-compute-api-version 2.15 or higher for the 'soft-affinity' or 'soft-anti-affinity' policy.
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    rule: A rule for the policy. Currently, only the 'max_server_per_host' rule is supported for the 'anti-affinity' policy.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete existing server group(s).
            - name: list
              description: List all server groups.
              flags:
                --all-projects: Display information from all projects (admin only)
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --offset=: The (zero-based) offset of the first item in the collection to return.
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all-projects: Display information from all projects (admin only)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the maximum value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    offset: The (zero-based) offset of the first item in the collection to return.
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Display server group details.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: image
          commands:
            - name: create
              description: Create a new server disk image from an existing server
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: 'Name of new disk image (default: server name)'
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --property=: Set a new property to meta_data.json on the metadata server (repeat option to set multiple values)
                --variable=: ==SUPPRESS==
                --wait: Wait for operation to complete
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: 'Name of new disk image (default: server name)'
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    property: Set a new property to meta_data.json on the metadata server (repeat option to set multiple values)
                    variable: ==SUPPRESS==
                    wait: Wait for operation to complete
        - name: list
          description: List servers
          flags:
            --all-projects: Include all projects (admin only) (can be specified using the ALL_PROJECTS envvar)
            --availability-zone=: Search by availability zone (admin only before microversion 2.83)
            --changes-before=: List only servers changed before a certain point of time.
            --changes-since=: List only servers changed after a certain point of time.
            --config-drive: Only display servers with a config drive attached (admin only before microversion 2.83)
            --deleted: Only display deleted servers (admin only)
            --fit-width: Fit the table to the display width.
            --flavor=: Search by flavor (name or ID)
            --host=: Search by hostname
            --image=: Search by image (name or ID)
            --instance-name=: Regular expression to match instance name (admin only)
            --ip=: Regular expression to match IP addresses
            --ip6=: Regular expression to match IPv6 addresses.
            --key-name=: Search by keypair name (admin only before microversion 2.83)
            --limit=: The maximum number of entries to return per page.
            --locked: Only display locked servers (supported by --os-compute-api-version 2.73 or above)
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --name-lookup-one-by-one: When looking up flavor and image names, look them up one by one as needed instead of all together (default).
            --name=: Regular expression to match names
            --no-config-drive: Only display servers without a config drive attached (admin only before microversion 2.83)
            --noindent: whether to disable indenting the JSON
            --not-tags=: Only list servers without the specified tag.
            --power-state=: Search by power_state value (admin only before microversion 2.83)
            --print-empty: Print empty table if there is no data to show.
            --progress=: Search by progress value (%%) (admin only before microversion 2.83)
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Search by project (admin only) (name or ID)
            --quote=: when to include quotes, defaults to nonnumeric
            --reservation-id=: Only return instances that match the reservation
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --status=: Search by server status
            --tags=: Only list servers with the specified tag.
            --task-state=: Search by task_state value (admin only before microversion 2.83)
            --unlocked: Only display unlocked servers (supported by --os-compute-api-version 2.73 or above)
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Search by user (name or ID) (admin only before microversion 2.83)
            --vm-state=: Search by vm_state value (admin only before microversion 2.83)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
            -n, --no-name-lookup: Skip flavor and image name lookup.
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                power-state:
                    - pending
                    - running
                    - paused
                    - shutdown
                    - crashed
                    - suspended
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
                status:
                    - ACTIVE
                    - BUILD
                    - DELETED
                    - ERROR
                    - HARD_REBOOT
                    - MIGRATING
                    - PASSWORD
                    - PAUSED
                    - REBOOT
                    - REBUILD
                    - RESCUE
                    - RESIZE
                    - REVERT_RESIZE
                    - SHELVED
                    - SHELVED_OFFLOADED
                    - SHUTOFF
                    - SOFT_DELETED
                    - SUSPENDED
                    - VERIFY_RESIZE
                task-state:
                    - block_device_mapping
                    - deleting
                    - image_backup
                    - image_pending_upload
                    - image_snapshot
                    - image_snapshot_pending
                    - image_uploading
                    - migrating
                    - networking
                    - pausing
                    - powering-off
                    - powering-on
                    - rebooting
                    - reboot_pending
                    - reboot_started
                    - reboot_pending_hard
                    - reboot_started_hard
                    - rebooting_hard
                    - rebuilding
                    - rebuild_block_device_mapping
                    - rebuild_spawning
                    - rescuing
                    - resize_confirming
                    - resize_finish
                    - resize_migrated
                    - resize_migrating
                    - resize_prep
                    - resize_reverting
                    - restoring
                    - resuming
                    - scheduling
                    - shelving
                    - shelving_image_pending_upload
                    - shelving_image_uploading
                    - shelving_offloading
                    - soft-deleting
                    - spawning
                    - suspending
                    - updating_password
                    - unpausing
                    - unrescuing
                    - unshelving
                vm-state:
                    - active
                    - building
                    - deleted
                    - error
                    - paused
                    - stopped
                    - suspended
                    - rescued
                    - resized
                    - shelved
                    - shelved_offloaded
                    - soft-delete
          documentation:
            flag:
                all-projects: Include all projects (admin only) (can be specified using the ALL_PROJECTS envvar)
                availability-zone: Search by availability zone (admin only before microversion 2.83)
                changes-before: List only servers changed before a certain point of time. The provided time should be an ISO 8061 formatted time (e.g., 2016-03-05T06:27:59Z). (supported by --os-compute-api-version 2.66 or above)
                changes-since: List only servers changed after a certain point of time. The provided time should be an ISO 8061 formatted time (e.g., 2016-03-04T06:27:59Z).
                column: specify the column(s) to include, can be repeated to show multiple columns
                config-drive: Only display servers with a config drive attached (admin only before microversion 2.83)
                deleted: Only display deleted servers (admin only)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                flavor: Search by flavor (name or ID)
                format: the output format, defaults to table
                host: Search by hostname
                image: Search by image (name or ID)
                instance-name: Regular expression to match instance name (admin only)
                ip: Regular expression to match IP addresses
                ip6: Regular expression to match IPv6 addresses. Note that this option only applies for non-admin users when using ``--os-compute-api-version`` 2.5 or greater.
                key-name: Search by keypair name (admin only before microversion 2.83)
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                locked: Only display locked servers (supported by --os-compute-api-version 2.73 or above)
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: Regular expression to match names
                name-lookup-one-by-one: When looking up flavor and image names, look them up one by one as needed instead of all together (default). Mutually exclusive with "--no-name-lookup|-n" option.
                no-config-drive: Only display servers without a config drive attached (admin only before microversion 2.83)
                no-name-lookup: Skip flavor and image name lookup. Mutually exclusive with "--name-lookup-one-by-one" option.
                noindent: whether to disable indenting the JSON
                not-tags: Only list servers without the specified tag. Specify multiple times to filter on multiple tags. (supported by --os-compute-api-version 2.26 or above)
                power-state: Search by power_state value (admin only before microversion 2.83)
                print-empty: Print empty table if there is no data to show.
                progress: Search by progress value (%%) (admin only before microversion 2.83)
                project: Search by project (admin only) (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                quote: when to include quotes, defaults to nonnumeric
                reservation-id: Only return instances that match the reservation
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                status: Search by server status
                tags: Only list servers with the specified tag. Specify multiple times to filter on multiple tags. (supported by --os-compute-api-version 2.26 or above)
                task-state: Search by task_state value (admin only before microversion 2.83)
                unlocked: Only display unlocked servers (supported by --os-compute-api-version 2.73 or above)
                user: Search by user (name or ID) (admin only before microversion 2.83)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
                vm-state: Search by vm_state value (admin only before microversion 2.83)
        - name: lock
          description: Lock server(s)
          flags:
            --reason=: Reason for locking the server(s) (supported by --os-compute-api-version 2.73 or above)
          documentation:
            flag:
                reason: Reason for locking the server(s) (supported by --os-compute-api-version 2.73 or above)
        - name: migrate
          description: Migrate server to different host.
          flags:
            --block-migration: Perform a block live migration (auto-configured from --os-compute-api-version 2.25)
            --disk-overcommit: Allow disk over-commit on the destination host (supported with --os-compute-api-version 2.24 or below)
            --host=: Migrate the server to the specified host.
            --live-migration: Live migrate the server; use the ``--host`` option to specify a target host for the migration which will be validated by the scheduler
            --no-disk-overcommit: Do not over-commit disk on the destination host (default) (supported with --os-compute-api-version 2.24 or below)
            --shared-migration: Perform a shared live migration (default before --os-compute-api-version 2.25, auto after)
            --wait: Wait for migrate to complete
          commands:
            - name: confirm
              description: 'DEPRECATED: Use ''server migration confirm'' instead.'
            - name: revert
              description: 'DEPRECATED: Use ''server migration revert'' instead.'
          documentation:
            flag:
                block-migration: Perform a block live migration (auto-configured from --os-compute-api-version 2.25)
                disk-overcommit: Allow disk over-commit on the destination host (supported with --os-compute-api-version 2.24 or below)
                host: Migrate the server to the specified host. (supported with --os-compute-api-version 2.30 or above when used with the --live-migration option) (supported with --os-compute-api-version 2.56 or above when used without the --live-migration option)
                live-migration: Live migrate the server; use the ``--host`` option to specify a target host for the migration which will be validated by the scheduler
                no-disk-overcommit: Do not over-commit disk on the destination host (default) (supported with --os-compute-api-version 2.24 or below)
                shared-migration: Perform a shared live migration (default before --os-compute-api-version 2.25, auto after)
                wait: Wait for migrate to complete
        - name: migration
          commands:
            - name: abort
              description: Cancel an ongoing live migration.
            - name: confirm
              description: Confirm server migration.
            - name: force
              commands:
                - name: complete
                  description: Force an ongoing live migration to complete.
            - name: list
              description: List server migrations
              flags:
                --changes-before=: List only migrations changed earlier or equal to a certain point of time.
                --changes-since=: List only migrations changed later or equal to a certain point of time.
                --fit-width: Fit the table to the display width.
                --host=: Filter migrations by source or destination host
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter migrations by project (name or ID) (supported with --os-compute-api-version 2.80 or above)
                --quote=: when to include quotes, defaults to nonnumeric
                --server=: Filter migrations by server (name or ID)
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --status=: Filter migrations by status
                --type=: Filter migrations by type
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: Filter migrations by user (name or ID) (supported with --os-compute-api-version 2.80 or above)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    type:
                        - evacuation
                        - live-migration
                        - cold-migration
                        - resize
              documentation:
                flag:
                    changes-before: List only migrations changed earlier or equal to a certain point of time. The provided time should be an ISO 8061 formatted time, e.g. ``2016-03-04T06:27:59Z``. (supported with --os-compute-api-version 2.66 or above)
                    changes-since: List only migrations changed later or equal to a certain point of time. The provided time should be an ISO 8061 formatted time, e.g. ``2016-03-04T06:27:59Z``. (supported with --os-compute-api-version 2.59 or above)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Filter migrations by source or destination host
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Filter migrations by project (name or ID) (supported with --os-compute-api-version 2.80 or above)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    server: Filter migrations by server (name or ID)
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    status: Filter migrations by status
                    type: Filter migrations by type
                    user: Filter migrations by user (name or ID) (supported with --os-compute-api-version 2.80 or above)
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
            - name: revert
              description: Revert server migration.
            - name: show
              description: Show an in-progress live migration for a given server.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: pause
          description: Pause server(s)
        - name: reboot
          description: Perform a hard or soft server reboot
          flags:
            --hard: Perform a hard reboot
            --soft: Perform a soft reboot
            --wait: Wait for reboot to complete
          documentation:
            flag:
                hard: Perform a hard reboot
                soft: Perform a soft reboot
                wait: Wait for reboot to complete
        - name: rebuild
          description: Rebuild server
          flags:
            --description=: Set a new description on the rebuilt server (supported by --os-compute-api-version 2.19 or above)
            --fit-width: Fit the table to the display width.
            --hostname=: Hostname configured for the server in the metadata service.
            --image=: Recreate server from the specified image (name or ID).
            --key-name=: Set the key name of key pair on the rebuilt server.
            --key-unset: ==SUPPRESS==
            --max-width=: Maximum display width, <1 to disable.
            --name=: Set the new name of the rebuilt server
            --no-key-name: Unset the key name of key pair on the rebuilt server.
            --no-preserve-ephemeral: Do not preserve the default ephemeral storage partition on rebuild.
            --no-reimage-boot-volume: Do not rebuild a volume-backed server.
            --no-trusted-image-certs: Remove any existing trusted image certificates from the server.
            --no-user-data: Remove existing user data when rebuilding server.
            --noindent: whether to disable indenting the JSON
            --password=: Set the password on the rebuilt server.
            --prefix=: add a prefix to all variable names
            --preserve-ephemeral: Preserve the default ephemeral storage partition on rebuild.
            --print-empty: Print empty table if there is no data to show.
            --property=: Set a new property on the rebuilt server (repeat option to set multiple values)
            --reimage-boot-volume: Rebuild a volume-backed server.
            --trusted-image-cert=: Trusted image certificate IDs used to validate certificates during the image signature verification process.
            --user-data=: Add a new user data file to the rebuilt server.
            --variable=: ==SUPPRESS==
            --wait: Wait for rebuild to complete
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Set a new description on the rebuilt server (supported by --os-compute-api-version 2.19 or above)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                hostname: Hostname configured for the server in the metadata service. A separate utility running in the guest is required to propagate changes to this value to the guest OS itself (hostnames supported by --os-compute-api-version 2.90 or above, FQDNs supported by --os-compute-api-version 2.94 or above)
                image: Recreate server from the specified image (name or ID). Defaults to the currently used one.
                key-name: Set the key name of key pair on the rebuilt server. Cannot be specified with the --key-unset option. (supported by --os-compute-api-version 2.54 or above)
                key-unset: ==SUPPRESS==
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: Set the new name of the rebuilt server
                no-key-name: Unset the key name of key pair on the rebuilt server. Cannot be specified with the --key-name option. (supported by --os-compute-api-version 2.54 or above)
                no-preserve-ephemeral: Do not preserve the default ephemeral storage partition on rebuild.
                no-reimage-boot-volume: Do not rebuild a volume-backed server. (supported by --os-compute-api-version 2.93 or above)
                no-trusted-image-certs: Remove any existing trusted image certificates from the server. Cannot be specified with the --trusted-certs option. (supported by --os-compute-api-version 2.63 or above)
                no-user-data: Remove existing user data when rebuilding server. Cannot be specified with the --user-data option. (supported by --os-compute-api-version 2.57 or above)
                noindent: whether to disable indenting the JSON
                password: Set the password on the rebuilt server. This option requires cloud support.
                prefix: add a prefix to all variable names
                preserve-ephemeral: Preserve the default ephemeral storage partition on rebuild.
                print-empty: Print empty table if there is no data to show.
                property: Set a new property on the rebuilt server (repeat option to set multiple values)
                reimage-boot-volume: Rebuild a volume-backed server. This will wipe the root volume data and overwrite it with the provided image. Defaults to False. (supported by --os-compute-api-version 2.93 or above)
                trusted-image-cert: Trusted image certificate IDs used to validate certificates during the image signature verification process. May be specified multiple times to pass multiple trusted image certificate IDs. Cannot be specified with the --no-trusted-certs option. (supported by --os-compute-api-version 2.63 or above)
                user-data: Add a new user data file to the rebuilt server. Cannot be specified with the --no-user-data option. (supported by --os-compute-api-version 2.57 or above)
                variable: ==SUPPRESS==
                wait: Wait for rebuild to complete
        - name: remove
          commands:
            - name: fixed
              commands:
                - name: ip
                  description: Remove fixed IP address from server
            - name: floating
              commands:
                - name: ip
                  description: Remove floating IP address from server
            - name: network
              description: Remove all ports of a network from server
            - name: port
              description: Remove port from server
            - name: security
              commands:
                - name: group
                  description: Remove security group from server
            - name: share
              description: Remove a share from a server.
            - name: volume
              description: Remove volume from server.
        - name: rescue
          description: Put server in rescue mode.
          flags:
            --image=: Image (name or ID) to use for the rescue mode (defaults to the currently used one)
            --password=: Set the password on the rescued instance (requires cloud support)
          documentation:
            flag:
                image: Image (name or ID) to use for the rescue mode (defaults to the currently used one)
                password: Set the password on the rescued instance (requires cloud support)
        - name: resize
          description: Scale server to a new flavor.
          flags:
            --confirm: '**Deprecated** Confirm server resize is complete.'
            --flavor=: Resize server to specified flavor
            --revert: '**Deprecated** Restore server state before resize.'
            --wait: Wait for resize to complete
          commands:
            - name: confirm
              description: Confirm server resize.
            - name: revert
              description: Revert server resize.
          documentation:
            flag:
                confirm: '**Deprecated** Confirm server resize is complete. Replaced by the ''openstack server resize confirm'' and ''openstack server migration confirm'' commands'
                flavor: Resize server to specified flavor
                revert: '**Deprecated** Restore server state before resize. Replaced by the ''openstack server resize revert'' and ''openstack server migration revert'' commands'
                wait: Wait for resize to complete
        - name: restore
          description: Restore server(s)
        - name: resume
          description: Resume server(s)
        - name: set
          description: Set server properties
          flags:
            --auto-approve: Allow server state override without asking for confirmation
            --description=: New server description (supported by --os-compute-api-version 2.19 or above)
            --hostname=: Hostname configured for the server in the metadata service.
            --name=: New server name
            --no-password: Clear the admin password for the server from the metadata service; note that this action does not actually change the server password
            --password=: Set the server password.
            --pinned-availability-zone=: Pin the server to the given availability zone.
            --property=: Property to add/change for this server (repeat option to set multiple properties)
            --root-password: ==SUPPRESS==
            --state=: New server state.**WARNING** Resetting the state is intended to work around servers stuck in an intermediate state, such as deleting.
            --tag=: Tag for the server.
          completion:
            flag:
                state:
                    - active
                    - error
          documentation:
            flag:
                auto-approve: Allow server state override without asking for confirmation
                description: New server description (supported by --os-compute-api-version 2.19 or above)
                hostname: Hostname configured for the server in the metadata service. A separate utility running in the guest is required to propagate changes to this value to the guest OS itself (hostnames supported by --os-compute-api-version 2.90 or above, FQDNs supported by --os-compute-api-version 2.94 or above)
                name: New server name
                no-password: Clear the admin password for the server from the metadata service; note that this action does not actually change the server password
                password: Set the server password. This option requires cloud support.
                pinned-availability-zone: Pin the server to the given availability zone. The server must currently be in the given zone. To unpin, use "server unset --pinned-availability-zone" (supported by --os-compute-api-version 2.104 or above)
                property: Property to add/change for this server (repeat option to set multiple properties)
                root-password: ==SUPPRESS==
                state: New server state.**WARNING** Resetting the state is intended to work around servers stuck in an intermediate state, such as deleting. If the server is in an error state then this is almost never the correct command to run and you should prefer hard reboot where possible. In particular, if the server is in an error state due to a move operation, setting the state can result in instances that are no longer usable. Proceed with caution. (admin only)
                tag: Tag for the server. Specify multiple times to add multiple tags. (supported by --os-compute-api-version 2.26 or above)
        - name: share
          commands:
            - name: list
              description: List all the shares attached to a server.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show detail of a share attachment to a server.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: shelve
          description: Shelve and optionally offload server(s).
          flags:
            --offload: Remove the shelved server(s) from the host (admin only).
            --wait: Wait for shelve and/or offload operation to complete
          documentation:
            flag:
                offload: Remove the shelved server(s) from the host (admin only). Invoking this option on an unshelved server(s) will result in the server being shelved first
                wait: Wait for shelve and/or offload operation to complete
        - name: show
          description: Show server details.
          flags:
            --diagnostics: Display server diagnostics information
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --topology: Include topology information in the output (supported by --os-compute-api-version 2.78 or above)
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                diagnostics: Display server diagnostics information
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                topology: Include topology information in the output (supported by --os-compute-api-version 2.78 or above)
                variable: ==SUPPRESS==
        - name: ssh
          description: SSH to server
          flags:
            --address-type=: Use other IP address (public, private, etc)
            --private: Use private IP address
            --public: Use public IP address
            "-4": Use only IPv4 addresses
            "-6": Use only IPv6 addresses
            -i, --identity=: ==SUPPRESS==
            -l, --login=: ==SUPPRESS==
            -o, --option=: ==SUPPRESS==
            -p, --port=: ==SUPPRESS==
            -v: ==SUPPRESS==
          documentation:
            flag:
                "4": Use only IPv4 addresses
                "6": Use only IPv6 addresses
                address-type: Use other IP address (public, private, etc)
                identity: ==SUPPRESS==
                login: ==SUPPRESS==
                option: ==SUPPRESS==
                port: ==SUPPRESS==
                private: Use private IP address
                public: Use public IP address
                v: ==SUPPRESS==
        - name: start
          description: Start server(s)
          flags:
            --all-projects: Start server(s) in another project by name (admin only) (can be specified using the ALL_PROJECTS envvar)
          documentation:
            flag:
                all-projects: Start server(s) in another project by name (admin only) (can be specified using the ALL_PROJECTS envvar)
        - name: stop
          description: Stop server(s)
          flags:
            --all-projects: Stop server(s) in another project by name (admin only) (can be specified using the ALL_PROJECTS envvar)
          documentation:
            flag:
                all-projects: Stop server(s) in another project by name (admin only) (can be specified using the ALL_PROJECTS envvar)
        - name: suspend
          description: Suspend server(s)
        - name: unlock
          description: Unlock server(s)
        - name: unpause
          description: Unpause server(s)
        - name: unrescue
          description: Restore server from rescue mode
        - name: unset
          description: Unset server properties and tags
          flags:
            --all-properties: Remove all properties
            --all-tags: Remove all tags (supported by --os-compute-api-version 2.26 or above)
            --description: Unset server description (supported by --os-compute-api-version 2.19 or above)
            --pinned-availability-zone: Unpin the server from its availability zone (supported by --os-compute-api-version 2.104 or above)
            --property=: Property key to remove from server (repeat option to remove multiple values)
            --tag=: Tag to remove from the server.
          documentation:
            flag:
                all-properties: Remove all properties
                all-tags: Remove all tags (supported by --os-compute-api-version 2.26 or above)
                description: Unset server description (supported by --os-compute-api-version 2.19 or above)
                pinned-availability-zone: Unpin the server from its availability zone (supported by --os-compute-api-version 2.104 or above)
                property: Property key to remove from server (repeat option to remove multiple values)
                tag: Tag to remove from the server. Specify multiple times to remove multiple tags. (supported by --os-compute-api-version 2.26 or above)
        - name: unshelve
          description: Unshelve server(s)
          flags:
            --availability-zone=: Name of the availability zone in which to unshelve a SHELVED_OFFLOADED server (supported by --os-compute-api-version 2.77 or above)
            --host=: Name of the destination host in which to unshelve a SHELVED_OFFLOADED server (supported by --os-compute-api-version 2.91 or above)
            --no-availability-zone: Unpin the availability zone of a SHELVED_OFFLOADED server.
            --wait: Wait for unshelve operation to complete
          documentation:
            flag:
                availability-zone: Name of the availability zone in which to unshelve a SHELVED_OFFLOADED server (supported by --os-compute-api-version 2.77 or above)
                host: Name of the destination host in which to unshelve a SHELVED_OFFLOADED server (supported by --os-compute-api-version 2.91 or above)
                no-availability-zone: Unpin the availability zone of a SHELVED_OFFLOADED server. Server will be unshelved on a host without availability zone constraint (supported by --os-compute-api-version 2.91 or above)
                wait: Wait for unshelve operation to complete
        - name: volume
          commands:
            - name: list
              description: List all the volumes attached to a server.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Update a volume attachment on the server.
              flags:
                --delete-on-termination: Delete the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
                --preserve-on-termination: Preserve the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
              documentation:
                flag:
                    delete-on-termination: Delete the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
                    preserve-on-termination: Preserve the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
            - name: update
              description: 'DEPRECATED: Use ''server volume set'' instead.'
              flags:
                --delete-on-termination: Delete the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
                --preserve-on-termination: Preserve the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
              documentation:
                flag:
                    delete-on-termination: Delete the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
                    preserve-on-termination: Preserve the volume when the server is destroyed (supported by --os-compute-api-version 2.85 or above)
    - name: service
      commands:
        - name: create
          description: Create new service
          flags:
            --description=: New service description
            --disable: Disable service
            --enable: Enable service (default)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --name=: New service name
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: New service description
                disable: Disable service
                enable: Enable service (default)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: New service name
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete service(s)
        - name: list
          description: List services
          flags:
            --fit-width: Fit the table to the display width.
            --long: List additional fields in output
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                long: List additional fields in output
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: provider
          commands:
            - name: create
              description: Create new service provider
              flags:
                --auth-url=!: Authentication URL of remote federated service provider (required)
                --description=: New service provider description
                --disable: Disable the service provider
                --enable: Enable the service provider (default)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --service-provider-url=!: A service URL where SAML assertions are being sent (required)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    auth-url: Authentication URL of remote federated service provider (required)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: New service provider description
                    disable: Disable the service provider
                    enable: Enable the service provider (default)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    service-provider-url: A service URL where SAML assertions are being sent (required)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete service provider(s)
            - name: list
              description: List service providers
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set service provider properties
              flags:
                --auth-url=: New Authentication URL of remote federated service provider
                --description=: New service provider description
                --disable: Disable the service provider
                --enable: Enable the service provider
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --service-provider-url=: New service provider URL, where SAML assertions are sent
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    auth-url: New Authentication URL of remote federated service provider
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: New service provider description
                    disable: Disable the service provider
                    enable: Enable the service provider
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    service-provider-url: New service provider URL, where SAML assertions are sent
                    variable: ==SUPPRESS==
            - name: show
              description: Display service provider details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: set
          description: Set service properties
          flags:
            --description=: New service description
            --disable: Disable service
            --enable: Enable service
            --name=: New service name
            --type=: New service type (compute, image, identity, volume, etc)
          documentation:
            flag:
                description: New service description
                disable: Disable service
                enable: Enable service
                name: New service name
                type: New service type (compute, image, identity, volume, etc)
        - name: show
          description: Display service details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: share
      commands:
        - name: abandon
          description: Abandon a share
          flags:
            --wait: Wait until share is abandoned
          documentation:
            flag:
                wait: Wait until share is abandoned
        - name: access
          commands:
            - name: create
              description: Create new share access rule
              flags:
                --access-level=: Share access level ("rw" and "ro" access levels are supported).
                --fit-width: Fit the table to the display width.
                --lock-deletion: When enabled, a 'delete' lock will be placed against the rule and the rule cannot be deleted while the lock exists.
                --lock-reason=: Reason for locking the access rule.
                --lock-visibility: Whether the sensitive fields of the access rule redacted to other users.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --properties=:
                    description: Space separated list of key=value pairs of properties.
                    nargs: -1
                --variable=: ==SUPPRESS==
                --wait: Wait for share access rule creation.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    access-level:
                        - rw
                        - ro
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    access-level: Share access level ("rw" and "ro" access levels are supported). Defaults to rw.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    lock-deletion: When enabled, a 'delete' lock will be placed against the rule and the rule cannot be deleted while the lock exists. Only available with API version >= 2.82.
                    lock-reason: Reason for locking the access rule. Should only be provided alongside a deletion or visibility lock. Only available with API version >= 2.82.
                    lock-visibility: Whether the sensitive fields of the access rule redacted to other users. Only available with API version >= 2.82.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    properties: 'Space separated list of key=value pairs of properties. OPTIONAL: Default=None. Available only for API microversion >= 2.45.'
                    variable: ==SUPPRESS==
                    wait: Wait for share access rule creation.
            - name: delete
              description: Delete a share access rule
              flags:
                --unrestrict: Seek access rule deletion despite restrictions.
                --wait: Wait for share access rule deletion
              documentation:
                flag:
                    unrestrict: Seek access rule deletion despite restrictions. Only available with API version >= 2.82.
                    wait: Wait for share access rule deletion
            - name: list
              description: List share access rule
              flags:
                --access-key=: Filter access rules by the access key.
                --access-level=: Filter access rules by the access level.
                --access-to=: Filter access rules by the access to field.
                --access-type=: Filter access rules by the access type.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --properties=:
                    description: Filters results by properties (key=value).
                    nargs: -1
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    access-key: Filter access rules by the access key.
                    access-level: Filter access rules by the access level.
                    access-to: Filter access rules by the access to field.
                    access-type: Filter access rules by the access type.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    properties: 'Filters results by properties (key=value). OPTIONAL: Default=None. Available only for API microversion >= 2.45'
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set properties to share access rule.
              flags:
                --access-level=: Share access level ("rw" and "ro" access levels are supported) to set.
                --property=: Set a property to this share access rule.
              completion:
                flag:
                    access-level:
                        - rw
                        - ro
              documentation:
                flag:
                    access-level: Share access level ("rw" and "ro" access levels are supported) to set.
                    property: Set a property to this share access rule. (Repeat option to set multiple properties) Available only for API microversion >= 2.45.
            - name: show
              description: Display a share access rule.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset properties of share access rule.
              flags:
                --property=: Remove property from share access rule.
              documentation:
                flag:
                    property: Remove property from share access rule. (Repeat option to remove multiple properties) Available only for API microversion >= 2.45.
        - name: adopt
          description: Adopt a share
          flags:
            --description=: Optional share description.
            --driver-options=:
                description: Optional driver options as key=value pairs (Default=None).
                nargs: -1
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --mount-point-name=: Optional custom export location.
            --name=: Optional share name.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --public: Level of visibility for share.
            --share-server-id=: Share server associated with share when using a share type with "driver_handles_share_servers" extra_spec set to True.
            --share-type=: Optional share type assigned to share.
            --variable=: ==SUPPRESS==
            --wait: Wait until share is adopted
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Optional share description. (Default=None)
                driver-options: Optional driver options as key=value pairs (Default=None).
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                mount-point-name: Optional custom export location. Available only for microversion >= 2.92. (Default=None)
                name: Optional share name. (Default=None)
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                public: Level of visibility for share. Defines whether other projects are able to see it or not. Available only for microversion >= 2.8. (Default=False)
                share-server-id: Share server associated with share when using a share type with "driver_handles_share_servers" extra_spec set to True. Available only for microversion >= 2.49. (Default=None)
                share-type: Optional share type assigned to share. (Default=None)
                variable: ==SUPPRESS==
                wait: Wait until share is adopted
        - name: availability
          commands:
            - name: zone
              commands:
                - name: list
                  description: List all availability zones
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
        - name: backup
          commands:
            - name: create
              description: Create a backup of the given share
              flags:
                --backup-options=: Backup driver option key=value pairs (Optional, Default=None).
                --description=: Optional share backup description.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Optional share backup name.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    backup-options: Backup driver option key=value pairs (Optional, Default=None).
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Optional share backup description. (Default=None).
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Optional share backup name. (Default=None).
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete one or more share backups
              flags:
                --wait: Wait for share backup deletion
              documentation:
                flag:
                    wait: Wait for share backup deletion
            - name: list
              description: List share backups
              flags:
                --description=: Filter results by description.
                --description~=: 'Filter results matching a share backup description '
                --detail=: Show detailed information about share backups.
                --fit-width: Fit the table to the display width.
                --limit=: Limit the number of backups returned.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Filter results by name.
                --name~=: 'Filter results matching a share backup name pattern. '
                --noindent: whether to disable indenting the JSON
                --offset=: Start position of backup records listing.
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --share=: Name or ID of the share to list backups for.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --sort_dir=: Sort direction, available values are ('asc', 'desc').
                --sort_key=: Key to be sorted, available keys are ('id', 'status', 'size', 'share_id', 'progress', 'restore_progress', 'name', 'host', 'topic', 'project_id').
                --status=: Filter results by status.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Filter results by description. Default=None.
                    description~: 'Filter results matching a share backup description '
                    detail: Show detailed information about share backups.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: Limit the number of backups returned. Default=None.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Filter results by name. Default=None.
                    name~: 'Filter results matching a share backup name pattern. '
                    noindent: whether to disable indenting the JSON
                    offset: Start position of backup records listing.
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    share: Name or ID of the share to list backups for.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    sort_dir: 'Sort direction, available values are (''asc'', ''desc''). OPTIONAL: Default=None.'
                    sort_key: Key to be sorted, available keys are ('id', 'status', 'size', 'share_id', 'progress', 'restore_progress', 'name', 'host', 'topic', 'project_id'). Default=None.
                    status: Filter results by status. Default=None.
            - name: restore
              description: Attempt to restore share backup
              flags:
                --target-share=: share to restore backup to.
                --wait: Wait for restore conclusion
              documentation:
                flag:
                    target-share: share to restore backup to. Source share if none supplied
                    wait: Wait for restore conclusion
            - name: set
              description: Set share backup properties
              flags:
                --description=: Set a description to the backup.
                --name=: Set a name to the backup.
                --status=: Assign a status to the backup(Admin only).
              completion:
                flag:
                    status:
                        - available
                        - error
                        - creating
                        - deleting
                        - restoring
              documentation:
                flag:
                    description: Set a description to the backup.
                    name: Set a name to the backup.
                    status: 'Assign a status to the backup(Admin only). Options include : available, error, creating, deleting, restoring.'
            - name: show
              description: Show details of a backup
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset share backup properties
              flags:
                --description: Unset a description to the backup.
                --name: Unset a name to the backup.
              documentation:
                flag:
                    description: Unset a description to the backup.
                    name: Unset a name to the backup.
        - name: create
          description: Create new share
          flags:
            --availability-zone=: Availability zone in which share should be created.
            --description=: Optional share description.
            --encryption-key-ref=: Set encryption key reference i.e. UUID of the secret stored in the key manager.
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --mount-point-name=: Optional custom export location.
            --name=: Optional share name.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --property=: Set a property to this share (repeat option to set multiple properties)
            --public=: Level of visibility for share.
            --scheduler-hint=: Set Scheduler hints for the share as key=value pairs, possible keys are same_host, different_host.
            --share-group=: Optional share group name or ID in which to create the share.
            --share-network=: Optional network info ID or name.
            --share-type=: The share type to create the share with.
            --snapshot-id=: Optional snapshot ID to create the share from.
            --variable=: ==SUPPRESS==
            --wait: Wait for share creation
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                availability-zone: Availability zone in which share should be created.
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Optional share description. (Default=None)
                encryption-key-ref: Set encryption key reference i.e. UUID of the secret stored in the key manager. Available for microversion >= 2.90
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                mount-point-name: Optional custom export location. Available for microversion >= 2.84
                name: Optional share name. (Default=None)
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                property: Set a property to this share (repeat option to set multiple properties)
                public: Level of visibility for share. Defines whether other tenants are able to see it or not. (Default = False)
                scheduler-hint: Set Scheduler hints for the share as key=value pairs, possible keys are same_host, different_host. (repeat option to set multiple hints)
                share-group: Optional share group name or ID in which to create the share. (Default=None).
                share-network: Optional network info ID or name.
                share-type: The share type to create the share with. If not specified, unless creating from a snapshot, the default share type will be used. Note that share creation will fail if no share type is specified and no default share type is configured by the administrator.
                snapshot-id: Optional snapshot ID to create the share from. (Default=None)
                variable: ==SUPPRESS==
                wait: Wait for share creation
        - name: delete
          description: Delete a share
          flags:
            --force: Attempt forced removal of share(s), regardless of state (defaults to False)
            --share-group=: Optional share group (name or ID) which contains the share
            --soft: Soft delete one or more shares.
            --wait: Wait for share deletion
          documentation:
            flag:
                force: Attempt forced removal of share(s), regardless of state (defaults to False)
                share-group: Optional share group (name or ID) which contains the share
                soft: Soft delete one or more shares.
                wait: Wait for share deletion
        - name: export
          commands:
            - name: location
              commands:
                - name: list
                  description: List export location of a share
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set an export location property.
                  flags:
                    --property=: Set a property to this export location (repeat option to set multiple properties).
                  documentation:
                    flag:
                        property: Set a property to this export location (repeat option to set multiple properties). Available only for microversion >= 2.87.
                - name: show
                  description: Show export location of a share
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset a share export location property
                  flags:
                    --property=: Remove a property from export location (repeat option to remove multiple properties).
                  documentation:
                    flag:
                        property: Remove a property from export location (repeat option to remove multiple properties). Available only for microversion >= 2.87.
        - name: group
          commands:
            - name: create
              description: Create new share group
              flags:
                --availability-zone=: Optional availability zone in which group should be created
                --description=: Share group description.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Share group name
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --share-group-type=: Share group type name or ID of the share group to be created.
                --share-network=: Specify share network name or id
                --share-types=:
                    description: Name or ID of share type(s).
                    nargs: -1
                --source-share-group-snapshot=: Share group snapshot name or ID to create the share group from.
                --variable=: ==SUPPRESS==
                --wait: Wait for share group creation
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    availability-zone: Optional availability zone in which group should be created
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Share group description.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Share group name
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    share-group-type: Share group type name or ID of the share group to be created.
                    share-network: Specify share network name or id
                    share-types: Name or ID of share type(s).
                    source-share-group-snapshot: Share group snapshot name or ID to create the share group from.
                    variable: ==SUPPRESS==
                    wait: Wait for share group creation
            - name: delete
              description: Delete one or more share groups
              flags:
                --force: Attempt to force delete the share group (Default=False) (Admin only).
                --wait: Wait for share group to delete
              documentation:
                flag:
                    force: Attempt to force delete the share group (Default=False) (Admin only).
                    wait: Wait for share group to delete
            - name: list
              description: List share groups
              flags:
                --all-projects: Display share groups from all projects (Admin only).
                --description=: Filter results by description.
                --description~=: Filter results matching a share group description pattern.
                --fit-width: Fit the table to the display width.
                --host=: Filter results by host.
                --limit=: Limit the number of share groups returned
                --marker=: The last share group ID of the previous page
                --max-width=: Maximum display width, <1 to disable.
                --name=: Filter results by name.
                --name~=: Filter results matching a share group name pattern.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter results by project name or ID.
                --quote=: when to include quotes, defaults to nonnumeric
                --share-group-type=: 'Filter results by a share group type ID or name that was used for share group creation. '
                --share-network=: 'Filter results by share-network name or ID. '
                --share-server=: Filter results by share server ID.
                --snapshot=: 'Filter results by share group snapshot name or ID that was used to create the share group. '
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --sort=: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                --status=: Filter results by status.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all-projects: Display share groups from all projects (Admin only).
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Filter results by description. Available only for microversion >= 2.36.
                    description~: 'Filter results matching a share group description pattern. Available only for microversion >= 2.36. '
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Filter results by host.
                    limit: Limit the number of share groups returned
                    marker: The last share group ID of the previous page
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Filter results by name.
                    name~: 'Filter results matching a share group name pattern. Available only for microversion >= 2.36. '
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: 'Filter results by project name or ID. Useful with set key ''--all-projects''. '
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    share-group-type: 'Filter results by a share group type ID or name that was used for share group creation. '
                    share-network: 'Filter results by share-network name or ID. '
                    share-server: Filter results by share server ID.
                    snapshot: 'Filter results by share group snapshot name or ID that was used to create the share group. '
                    sort: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    status: Filter results by status.
            - name: set
              description: Explicitly set share group status
              flags:
                --description=: Share group description.
                --name=: New name for the share group.
                --status=: Explicitly update the status of a share group (Admin  only).
              documentation:
                flag:
                    description: Share group description. (Default=None)
                    name: New name for the share group. (Default=None)
                    status: 'Explicitly update the status of a share group (Admin  only). Examples include: available, error, creating, deleting, error_deleting.'
            - name: show
              description: Show details about a share group
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: snapshot
              commands:
                - name: create
                  description: Create a share group snapshot of the given share group
                  flags:
                    --description=: Optional share group snapshot description.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: Optional share group snapshot name.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    --wait: Wait for share group snapshot creation
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Optional share group snapshot description. (Default=None)
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: Optional share group snapshot name. (Default=None)
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                        wait: Wait for share group snapshot creation
                - name: delete
                  description: Delete one or more share group snapshot
                  flags:
                    --force: Attempt to force delete the share group snapshot(s) (Default=False) (Admin only).
                    --wait: Wait for share group snapshot deletion
                  documentation:
                    flag:
                        force: Attempt to force delete the share group snapshot(s) (Default=False) (Admin only).
                        wait: Wait for share group snapshot deletion
                - name: list
                  description: List share group snapshots
                  flags:
                    --all-projects: Display information from all projects (Admin only).
                    --detailed: 'Show detailed information about share group snapshot. '
                    --fit-width: Fit the table to the display width.
                    --limit=: Limit the number of share groups returned
                    --marker=: The last share group snapshot ID of the previous page
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: Filter results by name.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --share-group=: Filter results by share group name or ID.
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    --sort=: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                    --status=: Filter results by status.
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        all-projects: Display information from all projects (Admin only).
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        detailed: 'Show detailed information about share group snapshot. '
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        limit: Limit the number of share groups returned
                        marker: The last share group snapshot ID of the previous page
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: Filter results by name.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        share-group: Filter results by share group name or ID.
                        sort: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                        status: Filter results by status.
                - name: members
                  commands:
                    - name: list
                      description: List members of share group snapshot
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set share group snapshot properties
                  flags:
                    --description=: Set a description to the snapshot.
                    --name=: Set a name to the snapshot.
                    --status=: Explicitly set the state of a share group snapshot(Admin only).
                  completion:
                    flag:
                        status:
                            - available
                            - error
                            - creating
                            - deleting
                            - error_deleting
                  documentation:
                    flag:
                        description: Set a description to the snapshot.
                        name: Set a name to the snapshot.
                        status: 'Explicitly set the state of a share group snapshot(Admin only). Options include : available, error, creating, deleting, error_deleting.'
                - name: show
                  description: Show details about a share group snapshot
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset a share group snapshot property
                  flags:
                    --description: Unset share group snapshot description.
                    --name: Unset share group snapshot name.
                  documentation:
                    flag:
                        description: Unset share group snapshot description.
                        name: Unset share group snapshot name.
            - name: type
              commands:
                - name: access
                  commands:
                    - name: create
                      description: Allow a project to access a share group type (Admin only).
                      flags:
                        --project-domain=: Domain the project belongs to (name or ID).
                      documentation:
                        flag:
                            project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    - name: delete
                      description: Deny a project to access a share group type (Admin only).
                      flags:
                        --project-domain=: Domain the project belongs to (name or ID).
                      documentation:
                        flag:
                            project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    - name: list
                      description: Get access list for share group type (Admin only).
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                - name: create
                  description: Create new share group type
                  flags:
                    --fit-width: Fit the table to the display width.
                    --group-specs=:
                        description: Share Group type extra specs by key and value.
                        nargs: -1
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --public=: Make type accessible to the public (default true).
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        group-specs: 'Share Group type extra specs by key and value. OPTIONAL: Default=None. Example: --group-specs consistent_snapshot_support=host.'
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        public: Make type accessible to the public (default true).
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a share group type
                - name: list
                  description: List share types
                  flags:
                    --all: Display all share group types whether public or private.
                    --fit-width: Fit the table to the display width.
                    --group-specs=:
                        description: Filter share group types with group specs (key=value).
                        nargs: -1
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        all: Display all share group types whether public or private. Default=False. (Admin only)
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        group-specs: Filter share group types with group specs (key=value).
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set share group type properties
                  flags:
                    --group-specs=:
                        description: Extra specs key and value of share group type that will be used for share type creation.
                        nargs: -1
                  documentation:
                    flag:
                        group-specs: 'Extra specs key and value of share group type that will be used for share type creation. OPTIONAL: Default=None. Example: --group-specs consistent-snapshot-support=True'
                - name: show
                  description: Show share group types
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset share group type extra specs
            - name: unset
              description: Unset a share group property
              flags:
                --description: Unset share group description.
                --name: Unset share group name.
              documentation:
                flag:
                    description: Unset share group description.
                    name: Unset share group name.
        - name: instance
          commands:
            - name: delete
              description: Forces the deletion of a share instance
              flags:
                --wait: Wait for share instance deletion.
              documentation:
                flag:
                    wait: Wait for share instance deletion.
            - name: export
              commands:
                - name: location
                  commands:
                    - name: list
                      description: List share instance export locations
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: show
                      description: Show export location for a share instance.
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
            - name: list
              description: List share instances
              flags:
                --export-location=: Export location to list instances for.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --share=: Name or ID of the share to list instances for.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    export-location: Export location to list instances for.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    share: Name or ID of the share to list instances for.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Explicitly reset share instance status
              flags:
                --status=: Indicate which state to assign the instance.
              documentation:
                flag:
                    status: 'Indicate which state to assign the instance. Options are: available, error, creating, deleting,error_deleting, migrating, migrating_to, server_migrating.'
            - name: show
              description: Show share instance
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: limits
          commands:
            - name: show
              description: Show a list of share limits for a user.
              flags:
                --absolute: Get the absolute limits for the user
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --rate: Get the API rate limits for the user
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    absolute: Get the absolute limits for the user
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    rate: Get the API rate limits for the user
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
        - name: list
          description: List shares
          flags:
            --all-projects: Include all projects (admin only)
            --availability-zone=: Filter shares by their availability zone.
            --description~=: Filter results matching a share description pattern.Available only for microversion >= 2.36.
            --encryption-key-ref=: Filter shares by their encryption key ref.
            --export-location=: Filter shares by export location id or path.
            --extra-spec=: Filter shares with extra specs (key=value) of the share type that they belong to.
            --fit-width: Fit the table to the display width.
            --host=: Filter shares belonging to a given host (admin only)
            --limit=: Maximum number of shares to display
            --long: List additional fields in output
            --marker=: The last share ID of the previous page
            --max-width=: Maximum display width, <1 to disable.
            --name=: Filter shares by share name
            --name~=: Filter results matching a share name pattern.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Filter shares by project (name or ID) (admin only)
            --property=: Filter shares having a given metadata key=value property (repeat option to filter by multiple properties)
            --public: Include public shares
            --quote=: when to include quotes, defaults to nonnumeric
            --share-group=: Filter shares belonging to a given share group
            --share-network=: Filter shares exported on a given share network
            --share-server=: Filter shares exported via a given share server (admin only)
            --share-type=: Filter shares of a given share type
            --snapshot=: Filter shares by snapshot name or id.
            --soft-deleted: Get shares in recycle bin.
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --sort=: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
            --status=: Filter shares by status
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Filter results by user (name or ID) (admin only)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all-projects: Include all projects (admin only)
                availability-zone: Filter shares by their availability zone. Available for microversion >= 2.97
                column: specify the column(s) to include, can be repeated to show multiple columns
                description~: Filter results matching a share description pattern.Available only for microversion >= 2.36.
                encryption-key-ref: Filter shares by their encryption key ref. Available for microversion >= 2.90
                export-location: Filter shares by export location id or path. Available only for microversion >= 2.35
                extra-spec: Filter shares with extra specs (key=value) of the share type that they belong to. (repeat option to filter by multiple extra specs)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                host: Filter shares belonging to a given host (admin only)
                limit: Maximum number of shares to display
                long: List additional fields in output
                marker: The last share ID of the previous page
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: Filter shares by share name
                name~: Filter results matching a share name pattern. Available only for microversion >= 2.36.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: Filter shares by project (name or ID) (admin only)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: Filter shares having a given metadata key=value property (repeat option to filter by multiple properties)
                public: Include public shares
                quote: when to include quotes, defaults to nonnumeric
                share-group: Filter shares belonging to a given share group
                share-network: Filter shares exported on a given share network
                share-server: Filter shares exported via a given share server (admin only)
                share-type: Filter shares of a given share type
                snapshot: Filter shares by snapshot name or id.
                soft-deleted: Get shares in recycle bin. If this parameter is set to True (Default=False), only shares in the recycle bin will be displayed. Available only for microversion >= 2.69.
                sort: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                status: Filter shares by status
                user: Filter results by user (name or ID) (admin only)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: lock
          commands:
            - name: create
              description: Lock a resource action from occurring on a resource
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --reason=: Reason for the resource lock.
                --resource_action=: Action to lock on the resource (default="delete")
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    reason: Reason for the resource lock.
                    resource_action: Action to lock on the resource (default="delete")
                    variable: ==SUPPRESS==
            - name: delete
              description: Remove one or more resource locks
            - name: list
              description: Lists all resource locks
              flags:
                --all-projects: Filter resource locks for all projects.
                --before=: Filter resource locks created before given date.
                --context=: Filter resource locks by context.
                --detailed=: Show detailed information about filtered resource locks.
                --fit-width: Fit the table to the display width.
                --id=: Filter resource locks by ID.
                --limit=: Number of resource locks to list.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --offset=: Starting position of resource lock records in a paginated list.
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter resource locks for specific project by name or ID, combine with --all-projects (Admin only).
                --quote=: when to include quotes, defaults to nonnumeric
                --resource_action=: Filter resource locks by resource action.
                --resource_id=: Filter resource locks for a resource by ID, specify --resource-type to look up by name.
                --resource_type=: Filter resource locks by type of resource.
                --since=: Filter resource locks created since given date.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --sort_dir=: Sort direction, available values are ('asc', 'desc').
                --sort_key=: Key to be sorted, available keys are ('id', 'created_at', 'updated_at', 'resource_id', 'resource_type', 'resource_action', 'lock_reason').
                --user-domain=: Domain the user belongs to (name or ID).
                --user=: Filter resource locks for specific user by name or ID, combine with --all-projects to search across projects (Admin only).
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    context:
                        - user
                        - admin
                        - service
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    sort_dir:
                        - asc
                        - desc
                    sort_key:
                        - id
                        - created_at
                        - updated_at
                        - resource_id
                        - resource_type
                        - resource_action
                        - lock_reason
              documentation:
                flag:
                    all-projects: Filter resource locks for all projects. (Admin only).
                    before: 'Filter resource locks created before given date. The date format must be conforming to ISO8601. '
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    context: Filter resource locks by context.
                    detailed: Show detailed information about filtered resource locks.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    id: Filter resource locks by ID. Default=None.
                    limit: Number of resource locks to list. (Default=None)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    offset: Starting position of resource lock records in a paginated list.
                    print-empty: Print empty table if there is no data to show.
                    project: Filter resource locks for specific project by name or ID, combine with --all-projects (Admin only).
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    resource_action: Filter resource locks by resource action.
                    resource_id: Filter resource locks for a resource by ID, specify --resource-type to look up by name.
                    resource_type: Filter resource locks by type of resource.
                    since: 'Filter resource locks created since given date. The date format must be conforming to ISO8601. '
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    sort_dir: 'Sort direction, available values are (''asc'', ''desc''). OPTIONAL: Default=None.'
                    sort_key: Key to be sorted, available keys are ('id', 'created_at', 'updated_at', 'resource_id', 'resource_type', 'resource_action', 'lock_reason'). Default=None.
                    user: Filter resource locks for specific user by name or ID, combine with --all-projects to search across projects (Admin only).
                    user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
            - name: set
              description: Update resource lock properties
              flags:
                --reason=: Reason for the resource lock
                --resource_action=: Resource action to set in the resource lock
              documentation:
                flag:
                    reason: Reason for the resource lock
                    resource_action: Resource action to set in the resource lock
            - name: show
              description: Show details about a resource lock
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Remove resource lock properties
              flags:
                --reason: Unset the lock reason.
              documentation:
                flag:
                    reason: Unset the lock reason. (Default=False)
        - name: message
          commands:
            - name: delete
              description: Remove one or more messages
            - name: list
              description: Lists all messages
              flags:
                --action-id=: Filters results by action id.
                --before=: Return only user messages created before given date.
                --detail-id=: Filters results by detail id.
                --fit-width: Fit the table to the display width.
                --limit=: Maximum number of messages to return.
                --max-width=: Maximum display width, <1 to disable.
                --message-level=: Filters results by the message level.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --request-id=: Filters results by request id.
                --resource-id=: Filters results by a resource uuid.
                --resource-type=: Filters results by a resource type.
                --since=: Return only user messages created since given date.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    action-id: Filters results by action id. Default=None.
                    before: Return only user messages created before given date. The date format must be conforming to ISO8601. Available only for microversion >= 2.52.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    detail-id: Filters results by detail id. Default=None.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: Maximum number of messages to return. (Default=None)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    message-level: 'Filters results by the message level. Default=None. Example: "openstack message list --message-level ERROR".'
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    request-id: Filters results by request id. Default=None.
                    resource-id: Filters results by a resource uuid. Default=None.
                    resource-type: 'Filters results by a resource type. Default=None. Example: "openstack message list --resource-type share"'
                    since: Return only user messages created since given date. The date format must be conforming to ISO8601. Available only for microversion >= 2.52.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show details about a message
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: migration
          commands:
            - name: cancel
              description: Cancels migration of a given share when copying
            - name: complete
              description: Completes migration for a given share
            - name: show
              description: Gets migration progress of a given share when copying
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: start
              description: Migrates share to a new host.
              flags:
                --force-host-assisted-migration=: Enforces the use of the host-assisted migration approach, which bypasses driver optimizations.
                --new-share-network=: Specify the new share network for the share.
                --new-share-type=: Specify the new share type for the share.
                --nondisruptive=!: Enforces migration to be nondisruptive.
                --preserve-metadata=!: Enforces migration to preserve all file metadata when moving its contents.
                --preserve-snapshots=!: Enforces migration of the share snapshots to the destination.
                --writable=!: Enforces migration to keep the share writable while contents are being moved.
              completion:
                flag:
                    force-host-assisted-migration:
                        - "True"
                        - "False"
                    nondisruptive:
                        - "True"
                        - "False"
                    preserve-metadata:
                        - "True"
                        - "False"
                    preserve-snapshots:
                        - "True"
                        - "False"
                    writable:
                        - "True"
                        - "False"
              documentation:
                flag:
                    force-host-assisted-migration: Enforces the use of the host-assisted migration approach, which bypasses driver optimizations. Default=False.
                    new-share-network: Specify the new share network for the share. Do not specify this parameter if the migrating share has to be retained within its current share network.
                    new-share-type: Specify the new share type for the share. Do not specify this parameter if the migrating share has to be retained with its current share type.
                    nondisruptive: Enforces migration to be nondisruptive. If set to True, host-assisted migration will not be attempted.
                    preserve-metadata: Enforces migration to preserve all file metadata when moving its contents. If set to True, host-assisted migration will not be attempted.
                    preserve-snapshots: Enforces migration of the share snapshots to the destination. If set to True, host-assisted migration will not be attempted.
                    writable: Enforces migration to keep the share writable while contents are being moved. If set to True, host-assisted migration will not be attempted.
        - name: network
          commands:
            - name: create
              description: Create a share network
              flags:
                --availability-zone=: Name or ID of the avalilability zone to assign the specified network subnet parameters to.
                --description=: Add a description to the share network (Optional).
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Add a name to the share network (Optional)
                --neutron-net-id=: ID of the neutron network that must be associated with the share network (Optional).
                --neutron-subnet-id=: ID of the neutron sub-network that must be associated with the share network (Optional).
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    availability-zone: Name or ID of the avalilability zone to assign the specified network subnet parameters to. Must be used in conjunction with 'neutron-net-id' and 'neutron-subnet-id'. Do not specify this parameter if the network must be available across all availability zones ('default' share network subnet). Available only for microversion >= 2.51.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Add a description to the share network (Optional).
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Add a name to the share network (Optional)
                    neutron-net-id: ID of the neutron network that must be associated with the share network (Optional). The network specified will be associated with the 'default' share network subnet, unless 'availability-zone' is also specified.
                    neutron-subnet-id: ID of the neutron sub-network that must be associated with the share network (Optional). The subnet specified will be associated with the 'default' share network subnet, unless 'availability-zone' is also specified.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete one or more share networks
              flags:
                --wait: Wait for the share network(s) to be deleted
              documentation:
                flag:
                    wait: Wait for the share network(s) to be deleted
            - name: list
              description: List share networks
              flags:
                --all-projects: Include all projects (admin only)
                --cidr=: Filter share networks by the CIDR of network.
                --created-before=: Filter share networks by date they were created before.
                --created-since=: Filter share networks by date they were created after.
                --description=: Filter share networks by description.
                --description~=: Filter share networks by description-pattern.
                --detail: List share networks with details
                --fit-width: Fit the table to the display width.
                --ip-version=: Filter share networks by the IP Version of the network, either 4 or 6.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Filter share networks by name
                --name~=: Filter share networks by name-pattern.
                --network-type=: Filter share networks by the type of network.
                --neutron-net-id=: Filter share networks by the ID of a neutron network.
                --neutron-subnet-id=: Filter share networks by the ID of a neutron sub network.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter share networks by project (name or ID) (admin only)
                --quote=: when to include quotes, defaults to nonnumeric
                --security-service=: Filter share networks by the name or ID of a security service attached to the network.
                --segmentation-id=: Filter share networks by the segmentation ID of network.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    ip-version:
                        - "4"
                        - "6"
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all-projects: Include all projects (admin only)
                    cidr: Filter share networks by the CIDR of network.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    created-before: Filter share networks by date they were created before. The date can be in the format yyyy-mm-dd.
                    created-since: Filter share networks by date they were created after. The date can be in the format yyyy-mm-dd.
                    description: Filter share networks by description. Available only for microversion >= 2.36
                    description~: Filter share networks by description-pattern. Available only for microversion >= 2.36.
                    detail: List share networks with details
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    ip-version: Filter share networks by the IP Version of the network, either 4 or 6.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Filter share networks by name
                    name~: Filter share networks by name-pattern. Available only for microversion >= 2.36.
                    network-type: Filter share networks by the type of network. Examples include "flat", "vlan", "vxlan", "geneve", etc.
                    neutron-net-id: Filter share networks by the ID of a neutron network.
                    neutron-subnet-id: Filter share networks by the ID of a neutron sub network.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Filter share networks by project (name or ID) (admin only)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    security-service: Filter share networks by the name or ID of a security service attached to the network.
                    segmentation-id: Filter share networks by the segmentation ID of network. Relevant only for segmented networks such as "vlan", "vxlan", "geneve", etc.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set share network properties
              flags:
                --check-only: Run a dry-run of a security service replacement.
                --current-security-service=: Name or ID of a security service that is currently associated with a share network that must be replaced.
                --description=: Set a new description to the share network.
                --name=: Set a new name to the share network.
                --neutron-net-id=: Update the neutron network associated with the default share network subnet.
                --neutron-subnet-id=: Update the neutron subnetwork associated with the default share network subnet.
                --new-security-service=: Name or ID of a security service that must be associated with the share network.
                --restart-check: Restart a dry-run of a security service replacement.
                --status=: Assign a status to the share network (Admin only).
              completion:
                flag:
                    status:
                        - active
                        - error
                        - network_change
              documentation:
                flag:
                    check-only: Run a dry-run of a security service replacement. Available only for microversion >=2.63
                    current-security-service: Name or ID of a security service that is currently associated with a share network that must be replaced. Replacing a security service is only available for microversions >= 2.63.
                    description: Set a new description to the share network.
                    name: Set a new name to the share network.
                    neutron-net-id: Update the neutron network associated with the default share network subnet. If a default share network subnet is not present or if the share network is in use, setting this will fail.
                    neutron-subnet-id: Update the neutron subnetwork associated with the default share network subnet. If a default share network subnet is not present or if the share network is in use, setting this will fail.
                    new-security-service: Name or ID of a security service that must be associated with the share network. When replacing a security service, the current security service must also be provided with the '--current-security-service' option. Replacing a security service is only available for microversions >= 2.63.
                    restart-check: Restart a dry-run of a security service replacement. Helpful when check results are stale. Available only for microversion >=2.63.
                    status: 'Assign a status to the share network (Admin only). Options include : active, error, network_change. Available only for microversion >= 2.63.'
            - name: show
              description: Show details about a share network
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: subnet
              commands:
                - name: create
                  description: Create a share network subnet
                  flags:
                    --availability-zone=: Optional availability zone that the subnet is available within (Default=None).
                    --check-only: Run a dry-run of a share network subnet create.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --neutron-net-id=: Neutron network ID.
                    --neutron-subnet-id=: Neutron subnet ID.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --property=: Set a property to this share network subnet (repeat option to set multiple properties).
                    --restart-check: Restart a dry-run of a share network subnet create.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        availability-zone: Optional availability zone that the subnet is available within (Default=None). If None, the subnet will be considered as being available across all availability zones.
                        check-only: Run a dry-run of a share network subnet create. Available only for microversion >= 2.70.
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        neutron-net-id: Neutron network ID. Used to set up network for share servers (optional). Should be defined together with '--neutron-subnet-id'.
                        neutron-subnet-id: 'Neutron subnet ID. Used to set up network for share servers (optional). Should be defined together with ''--neutron-net-id'' to which this subnet belongs to. '
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        property: Set a property to this share network subnet (repeat option to set multiple properties). Available only for microversion >= 2.78.
                        restart-check: Restart a dry-run of a share network subnet create. Helpful when check results are stale. Available only for microversion >= 2.70.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a share network subnet
                - name: set
                  description: Set share network subnet properties
                  flags:
                    --property=: Set a property to this share network subnet (repeat option to set multiple properties).
                  documentation:
                    flag:
                        property: Set a property to this share network subnet (repeat option to set multiple properties). Available only for microversion >= 2.78.
                - name: show
                  description: Show share network subnet
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset a share network subnet property
                  flags:
                    --property=: Remove a property from share network subnet (repeat option to remove multiple properties).
                  documentation:
                    flag:
                        property: Remove a property from share network subnet (repeat option to remove multiple properties). Available only for microversion >= 2.78.
            - name: unset
              description: Unset a share network property
              flags:
                --description: Unset share network description.
                --name: Unset share network name.
                --security-service=: Disassociate a security service from the share network.
              documentation:
                flag:
                    description: Unset share network description.
                    name: Unset share network name.
                    security-service: Disassociate a security service from the share network. This is only possible with unused share networks.
        - name: pool
          commands:
            - name: list
              description: List all backend storage pools known to the scheduler (Admin only).
              flags:
                --backend=: Filter results by backend name.
                --detail: Show detailed information about pools.
                --fit-width: Fit the table to the display width.
                --host=: Filter results by host name.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --pool=: Filter results by pool name.
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --share-type=: Filter results by share type name or ID.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    backend: Filter results by backend name. Regular expressions are supported.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    detail: Show detailed information about pools.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Filter results by host name. Regular expressions are supported.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    pool: Filter results by pool name. Regular expressions are supported.
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    share-type: Filter results by share type name or ID. Available only for microversion >= 2.23
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
        - name: properties
          commands:
            - name: show
              description: Show share properties
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: qos
          commands:
            - name: type
              commands:
                - name: create
                  description: Create new qos type
                  flags:
                    --description=: QoS type description.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --spec=: Spec key and value of QoS type that will be used for QoS type creation.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: QoS type description.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        spec: 'Spec key and value of QoS type that will be used for QoS type creation. OPTIONAL: Default=None. Example: --spec qos_type=''fixed'' --spec peak_iops=300.'
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a qos type
                - name: list
                  description: List qos types
                  flags:
                    --description=: Filter results by description.
                    --description~=: Filter results matching a qos description pattern.
                    --fit-width: Fit the table to the display width.
                    --limit=: Limit the number of qos types returned.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: Filter results by name.
                    --name~=: 'Filter results matching a qos name pattern. '
                    --noindent: whether to disable indenting the JSON
                    --offset=: Start position of qos type records listing.
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    --sort_dir=: Sort direction, available values are ('asc', 'desc').
                    --sort_key=: Key to be sorted with, available keys are ('id', 'name', 'created_at', 'updated_at').
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Filter results by description. Default=None.
                        description~: Filter results matching a qos description pattern.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        limit: Limit the number of qos types returned. Default=None.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: Filter results by name. Default=None.
                        name~: 'Filter results matching a qos name pattern. '
                        noindent: whether to disable indenting the JSON
                        offset: Start position of qos type records listing.
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                        sort_dir: 'Sort direction, available values are (''asc'', ''desc''). OPTIONAL: Default=None.'
                        sort_key: Key to be sorted with, available keys are ('id', 'name', 'created_at', 'updated_at'). Default=None.
                - name: set
                  description: Set qos type description or specs
                  flags:
                    --description=: New description of qos type.
                    --spec=: Spec key and value of qos type that will be used for QoS type.
                  documentation:
                    flag:
                        description: New description of qos type.
                        spec: 'Spec key and value of qos type that will be used for QoS type. OPTIONAL: Default=None. For example --spec qos_type=''fixed'' --spec peak_iops=300'
                - name: show
                  description: Display qos type details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset qos type specs
                  flags:
                    --description: Unset qos type description.
                    --spec=: Remove specified spec from this qos type
                  documentation:
                    flag:
                        description: Unset qos type description.
                        spec: Remove specified spec from this qos type
        - name: quota
          commands:
            - name: delete
              description: Delete Quota
              flags:
                --share-type=: Name or ID of a share type to delete the quotas for.
                --user=: Name or ID of user to delete the quotas for.
              documentation:
                flag:
                    share-type: Name or ID of a share type to delete the quotas for. Optional. Mutually exclusive with '--user'. Available only for microversion >= 2.39
                    user: Name or ID of user to delete the quotas for. Optional. Mutually exclusive with '--share-type'.
            - name: set
              description: Set Quota for a project, or project/user or project/share-type or a class.
              flags:
                --class: Update class quota to all projects.
                --encryption-keys=: New value for the "encryption-keys" quota.
                --force: Force update the quota.
                --gigabytes=: New value for the "gigabytes" quota.
                --per-share-gigabytes=: New value for the "per-share-gigabytes" quota.
                --replica-gigabytes=: Capacity of share replicas in total.
                --share-group-snapshots=: New value for the "share-group-snapshots" quota.
                --share-groups=: New value for the "share-groups" quota.
                --share-networks=: New value for the "share-networks" quota.
                --share-replicas=: Number of share replicas.
                --share-type=: Name or ID of a share type to set the quotas for.
                --shares=: New value for the "shares" quota.
                --snapshot-gigabytes=: New value for the "snapshot-gigabytes" quota.
                --snapshots=: New value for the "snapshots" quota.
                --user=: Name or ID of a user to set the quotas for.
              documentation:
                flag:
                    class: Update class quota to all projects. Mutually exclusive with '--user' and '--share-type'.
                    encryption-keys: New value for the "encryption-keys" quota. Available only for microversion >= 2.90
                    force: Force update the quota. Not applicable for class update.
                    gigabytes: New value for the "gigabytes" quota.
                    per-share-gigabytes: New value for the "per-share-gigabytes" quota. Available only for microversion >= 2.62
                    replica-gigabytes: Capacity of share replicas in total. Available only for microversion >= 2.53
                    share-group-snapshots: New value for the "share-group-snapshots" quota. Available only for microversion >= 2.40
                    share-groups: New value for the "share-groups" quota. Available only for microversion >= 2.40
                    share-networks: New value for the "share-networks" quota.
                    share-replicas: Number of share replicas. Available only for microversion >= 2.53
                    share-type: Name or ID of a share type to set the quotas for. Mutually exclusive with '--user' and '--class'. Available only for microversion >= 2.39
                    shares: New value for the "shares" quota.
                    snapshot-gigabytes: New value for the "snapshot-gigabytes" quota.
                    snapshots: New value for the "snapshots" quota.
                    user: Name or ID of a user to set the quotas for. Mutually exclusive with '--share-type' and '--class'.
            - name: show
              description: Show Quota
              flags:
                --defaults: Show the default quotas for the project.
                --detail: Optional flag to indicate whether to show quota in detail.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --share-type=: Name or ID of a share type to list the quotas for.
                --user=: Name or ID of user to list the quotas for.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    defaults: Show the default quotas for the project.
                    detail: Optional flag to indicate whether to show quota in detail. Default false, available only for microversion >= 2.25.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    share-type: Name or ID of a share type to list the quotas for. Optional. Mutually exclusive with '--user'. Available only for microversion >= 2.39
                    user: Name or ID of user to list the quotas for. Optional. Mutually exclusive with '--share-type'.
                    variable: ==SUPPRESS==
        - name: replica
          commands:
            - name: create
              description: Create a replica of the given share
              flags:
                --availability-zone=: Availability zone in which the replica should be created.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --property=: Set a property to this replica (repeat option to set multiple properties).
                --scheduler-hint=: Scheduler hint for the share replica as key=value pairs, Supported key is only_host.
                --share-network=: Optional network info ID or name.
                --variable=: ==SUPPRESS==
                --wait: Wait for replica creation
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    availability-zone: Availability zone in which the replica should be created.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    property: Set a property to this replica (repeat option to set multiple properties). Available only for microversion >= 2.95
                    scheduler-hint: Scheduler hint for the share replica as key=value pairs, Supported key is only_host. Available for microversion >= 2.67.
                    share-network: Optional network info ID or name. Available for microversion >= 2.72
                    variable: ==SUPPRESS==
                    wait: Wait for replica creation
            - name: delete
              description: Delete one or more share replicas
              flags:
                --force: Attempt to force delete a replica on its backend.
                --wait: Wait for share replica deletion
              documentation:
                flag:
                    force: 'Attempt to force delete a replica on its backend. Using this option will purge the replica from Manila even if it is not cleaned up on the backend. '
                    wait: Wait for share replica deletion
            - name: export
              commands:
                - name: location
                  commands:
                    - name: list
                      description: List export locations of a share replica.
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: show
                      description: Show details of a share replica's export location.
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
            - name: list
              description: List share replicas
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --property=: Filter replicas having a given metadata key=value property.
                --quote=: when to include quotes, defaults to nonnumeric
                --share=: Name or ID of the share to list replicas for.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    property: Filter replicas having a given metadata key=value property. (repeat option to filter by multiple properties)
                    quote: when to include quotes, defaults to nonnumeric
                    share: Name or ID of the share to list replicas for.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: promote
              description: Promote specified replica to 'active' replica_state.
              flags:
                --quiesce-wait-time=: Quiesce wait time in seconds.
                --wait: Wait for share replica promotion
              documentation:
                flag:
                    quiesce-wait-time: Quiesce wait time in seconds. Available for microversion >= 2.75
                    wait: Wait for share replica promotion
            - name: resync
              description: Attempt to update the share replica with its 'active' mirror.
            - name: set
              description: Explicitly set share replica status and/or replica-state and/or property
              flags:
                --property=: Set a property to this replica (repeat option to set multiple properties).
                --replica-state=: Indicate which replica_state to assign the replica.
                --status=: Indicate which status to assign the replica.
              completion:
                flag:
                    replica-state:
                        - in_sync
                        - out_of_sync
                        - active
                        - error
                    status:
                        - available
                        - error
                        - creating
                        - deleting
                        - error_deleting
              documentation:
                flag:
                    property: Set a property to this replica (repeat option to set multiple properties). Available only for microversion >= 2.95
                    replica-state: Indicate which replica_state to assign the replica. Options include in_sync, out_of_sync, active and error.
                    status: Indicate which status to assign the replica. Options include available, error, creating, deleting and error_deleting.
            - name: show
              description: Show details about a replica
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset a share replica property
              flags:
                --property=: Remove a property from replica (repeat option to remove multiple properties)
              documentation:
                flag:
                    property: Remove a property from replica (repeat option to remove multiple properties)
        - name: resize
          description: Resize a share
          flags:
            --force: Only applicable when increasing the size of the share，only available with microversion 2.64 and higher.
            --wait: Wait for share resize
          documentation:
            flag:
                force: Only applicable when increasing the size of the share，only available with microversion 2.64 and higher. (admin only)
                wait: Wait for share resize
        - name: restore
          description: Restores this share or more shares from the recycle bin
        - name: revert
          description: Revert a share to the specified snapshot.
          flags:
            --wait: Wait for share revert
          documentation:
            flag:
                wait: Wait for share revert
        - name: security
          commands:
            - name: service
              commands:
                - name: create
                  description: Create security service used by project.
                  flags:
                    --default-ad-site=: Default AD site.
                    --description=: Security service description.
                    --dns-ip=: DNS IP address of the security service used inside project's network.
                    --domain=: Security service domain.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: Security service name.
                    --noindent: whether to disable indenting the JSON
                    --ou=: Security service OU (Organizational Unit).
                    --password=: Password used by user.
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --server=: Security service IP address or hostname.
                    --user=: Security service user or group used by project.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        default-ad-site: Default AD site. Available only for microversion >= 2.76. Can be provided in the place of '--server' but not along with it.
                        description: Security service description.
                        dns-ip: DNS IP address of the security service used inside project's network.
                        domain: Security service domain.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: Security service name.
                        noindent: whether to disable indenting the JSON
                        ou: Security service OU (Organizational Unit). Available only for microversion >= 2.44.
                        password: Password used by user.
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        server: Security service IP address or hostname.
                        user: Security service user or group used by project.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete one or more security services.
                - name: list
                  description: List security services.
                  flags:
                    --all-projects: Display information from all projects (Admin only).
                    --default-ad-site=: Filter results by security service default_ad_site.
                    --detail: Show detailed information about filtered security services.
                    --dns-ip=: Filter results by DNS IP address used inside project's network.
                    --domain=: Filter results by security service domain.
                    --fit-width: Fit the table to the display width.
                    --limit=: Limit the number of security services returned
                    --marker=: The last security service ID of the previous page
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: Filter results by security service name.
                    --noindent: whether to disable indenting the JSON
                    --ou=: Filter results by security service OU (Organizational Unit).
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --server=: Filter results by security service IP address or hostname.
                    --share-network=: Filter results by share network name or ID.
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    --status=: Filter results by status.
                    --type=: Filter results by security service type.
                    --user=: Filter results by security service user or group used by project.
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        all-projects: Display information from all projects (Admin only).
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        default-ad-site: Filter results by security service default_ad_site. Available only for microversion >= 2.76.
                        detail: Show detailed information about filtered security services.
                        dns-ip: Filter results by DNS IP address used inside project's network.
                        domain: Filter results by security service domain.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        limit: Limit the number of security services returned
                        marker: The last security service ID of the previous page
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: Filter results by security service name.
                        noindent: whether to disable indenting the JSON
                        ou: Filter results by security service OU (Organizational Unit). Available only for microversion >= 2.44.
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        server: Filter results by security service IP address or hostname.
                        share-network: Filter results by share network name or ID.
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                        status: Filter results by status.
                        type: Filter results by security service type.
                        user: Filter results by security service user or group used by project.
                - name: set
                  description: Set security service.
                  flags:
                    --default-ad-site=: Default AD site.
                    --description=: Set security service description.
                    --dns-ip=: Set DNS IP address used inside project's network.
                    --domain=: Set security service domain.
                    --name=: Set security service name.
                    --ou=: Set security service OU (Organizational Unit).
                    --password=: Set password used by user.
                    --server=: Set security service IP address or hostname.
                    --user=: Set security service user or group used by project.
                  documentation:
                    flag:
                        default-ad-site: Default AD site. Available only for microversion >= 2.76.
                        description: Set security service description.
                        dns-ip: Set DNS IP address used inside project's network.
                        domain: Set security service domain.
                        name: Set security service name.
                        ou: Set security service OU (Organizational Unit). Available only for microversion >= 2.44.
                        password: Set password used by user.
                        server: Set security service IP address or hostname.
                        user: Set security service user or group used by project.
                - name: show
                  description: Show security service.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: unset
                  description: Unset security service.
                  flags:
                    --default-ad-site: Default AD site.
                    --description: Unset security service description.
                    --dns-ip: Unset DNS IP address used inside project's network.
                    --domain: Unset security service domain.
                    --name: Unset security service name.
                    --ou: Unset security service OU (Organizational Unit).
                    --password: Unset password used by user.
                    --server: Unset security service IP address or hostname.
                    --user: Unset security service user or group used by project.
                  documentation:
                    flag:
                        default-ad-site: Default AD site. Available only for microversion >= 2.76.
                        description: Unset security service description.
                        dns-ip: Unset DNS IP address used inside project's network.
                        domain: Unset security service domain.
                        name: Unset security service name.
                        ou: Unset security service OU (Organizational Unit). Available only for microversion >= 2.44.
                        password: Unset password used by user.
                        server: Unset security service IP address or hostname.
                        user: Unset security service user or group used by project.
        - name: server
          commands:
            - name: abandon
              description: Remove one or more share server(s) (Admin only).
              flags:
                --force: Enforces the unmanage share server operation, even if the backend driver does not support it.
                --wait: Wait until share server is abandoned
              documentation:
                flag:
                    force: Enforces the unmanage share server operation, even if the backend driver does not support it.
                    wait: Wait until share server is abandoned
            - name: adopt
              description: Adopt share server not handled by Manila (Admin only).
              flags:
                --driver-options=: One or more driver-specific key=value pairs that may be necessary to manage the share server (Optional, Default=None).
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --share-network-subnet=: Share network subnet where share server has network  allocations in.The default subnet will be used if it's not specified.
                --variable=: ==SUPPRESS==
                --wait: Wait until share server is adopted
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    driver-options: One or more driver-specific key=value pairs that may be necessary to manage the share server (Optional, Default=None).
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    share-network-subnet: Share network subnet where share server has network  allocations in.The default subnet will be used if it's not specified. Available for microversion >= 2.51 (Optional, Default=None).
                    variable: ==SUPPRESS==
                    wait: Wait until share server is adopted
            - name: delete
              description: Delete one or more share servers
              flags:
                --wait: Wait for share server deletion.
              documentation:
                flag:
                    wait: Wait for share server deletion.
            - name: list
              description: List all share servers (Admin only).
              flags:
                --fit-width: Fit the table to the display width.
                --host=: Filter results by name of host.
                --identifier=: Identifier of the share server in the share back end.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter results by project name or ID.
                --quote=: when to include quotes, defaults to nonnumeric
                --share-network-subnet=: Filter results by share network subnet that the share server's network allocation exists within.
                --share-network=: Filter results by share network name or ID.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --source-share-server-id=: Share server ID to be used as a filter.
                --status=: Filter results by status.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Filter results by name of host.
                    identifier: Identifier of the share server in the share back end. Available for microversion >= 2.49 (Optional, Default=None)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Filter results by project name or ID.
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    share-network: Filter results by share network name or ID.
                    share-network-subnet: Filter results by share network subnet that the share server's network allocation exists within. Available for microversion >= 2.51 (Optional, Default=None)
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    source-share-server-id: Share server ID to be used as a filter. Available for microversion >= 2.57 (Optional, Default=None)
                    status: Filter results by status.
            - name: migration
              commands:
                - name: cancel
                  description: Cancels migration of a given share server when copying
                - name: complete
                  description: Completes migration for a given share server
                - name: show
                  description: Gets migration progress of a given share server when copying
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: start
                  description: Migrates share server to a new host.
                  flags:
                    --check-only: 'Run a dry-run of the share server migration. '
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --new-share-network=: Specify a new share network for the share server.
                    --noindent: whether to disable indenting the JSON
                    --nondisruptive=!: Enforces migration to be nondisruptive.
                    --prefix=: add a prefix to all variable names
                    --preserve-snapshots=!: Set to True if snapshots must be preserved at the migration destination.
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    --writable=!: Enforces migration to keep all its shares writable while contents are being moved.
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                        nondisruptive:
                            - "True"
                            - "False"
                        preserve-snapshots:
                            - "True"
                            - "False"
                        writable:
                            - "True"
                            - "False"
                  documentation:
                    flag:
                        check-only: 'Run a dry-run of the share server migration. '
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        new-share-network: Specify a new share network for the share server. Do not specify this parameter if the migrating share server has to be retained within its current share network.
                        noindent: whether to disable indenting the JSON
                        nondisruptive: Enforces migration to be nondisruptive.
                        prefix: add a prefix to all variable names
                        preserve-snapshots: Set to True if snapshots must be preserved at the migration destination.
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                        writable: Enforces migration to keep all its shares writable while contents are being moved.
            - name: set
              description: Set share server properties (Admin only).
              flags:
                --status=: Assign a status to the share server.
                --task-state=: Indicate which task state to assign the share server.
              documentation:
                flag:
                    status: 'Assign a status to the share server. Options include: unmanage_starting, server_migrating_to, error, unmanage_error, manage_error, inactive, active, server_migrating, manage_starting, deleting, network_change. If no state is provided, active will be used.'
                    task-state: 'Indicate which task state to assign the share server. Options include migration_starting, migration_in_progress, migration_completing, migration_success, migration_error, migration_cancelled, migration_driver_in_progress, migration_driver_phase1_done, data_copying_starting, data_copying_in_progress, data_copying_completing, data_copying_completed, data_copying_cancelled, data_copying_error. '
            - name: show
              description: Show details about a share server (Admin only).
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: service
          commands:
            - name: ensure
              commands:
                - name: shares
                  description: Run ensure shares in a back end (Admin only).
            - name: list
              description: List share services (Admin only).
              flags:
                --binary=: Filter services by the name of the service.
                --ensuring=: Filter services running ensure shares or not.
                --fit-width: Fit the table to the display width.
                --host=: Filter services by name of the host.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --state=: Filter results by state.
                --status=: Filter results by status.
                --zone=: Filter services by their availability zone.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    ensuring:
                        - "True"
                        - "False"
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    state:
                        - up
                        - down
              documentation:
                flag:
                    binary: Filter services by the name of the service.
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    ensuring: Filter services running ensure shares or not.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Filter services by name of the host.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    state: Filter results by state.
                    status: Filter results by status.
                    zone: Filter services by their availability zone.
            - name: set
              description: Enable/Disable share service (Admin only).
              flags:
                --disable: Disable share service
                --disable-reason=: Reason for disabling the service (should be used with --disable option)
                --enable: Enable share service
              documentation:
                flag:
                    disable: Disable share service
                    disable-reason: Reason for disabling the service (should be used with --disable option)
                    enable: Enable share service
        - name: set
          description: Set share properties
          flags:
            --description=: New share description.
            --name=: New share name.
            --property=: Set a property to this share (repeat option to set multiple properties)
            --public=: Level of visibility for share.
            --status=: Explicitly update the status of a share (Admin only).
            --task-state=: Indicate which task state to assign the share.
          documentation:
            flag:
                description: New share description. (Default=None)
                name: New share name. (Default=None)
                property: Set a property to this share (repeat option to set multiple properties)
                public: 'Level of visibility for share. Defines whether other tenants are able to see it or not. '
                status: 'Explicitly update the status of a share (Admin only). Examples include: available, error, creating, deleting, error_deleting.'
                task-state: 'Indicate which task state to assign the share. Options include migration_starting, migration_in_progress, migration_completing, migration_success, migration_error, migration_cancelled, migration_driver_in_progress, migration_driver_phase1_done, data_copying_starting, data_copying_in_progress, data_copying_completing, data_copying_completed, data_copying_cancelled, data_copying_error. '
        - name: show
          description: Display share details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: snapshot
          commands:
            - name: abandon
              description: Abandon share snapshot(s)
              flags:
                --wait: Wait until share snapshot is abandoned
              documentation:
                flag:
                    wait: Wait until share snapshot is abandoned
            - name: access
              commands:
                - name: create
                  description: Allow access to a snapshot
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete access to a snapshot
                - name: list
                  description: Show access list for a snapshot
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
            - name: adopt
              description: Adopt a share snapshot
              flags:
                --description=: Optional snapshot description (Default=None).
                --driver-option=: Set driver options as key=value pairs.(repeat
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Optional snapshot name (Default=None).
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                --wait: Wait until share snapshot is adopted
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Optional snapshot description (Default=None).
                    driver-option: Set driver options as key=value pairs.(repeat option to set multiple key=value pairs)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Optional snapshot name (Default=None).
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
                    wait: Wait until share snapshot is adopted
            - name: create
              description: Create a snapshot of the given share
              flags:
                --description=: Add a description to the snapshot (Optional).
                --fit-width: Fit the table to the display width.
                --force: Optional flag to indicate whether to snapshot a share even if it's busy.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Add a name to the snapshot (Optional).
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --property=: Set a property to this snapshot (repeat option to set multiple properties).Available only for microversion >= 2.73
                --variable=: ==SUPPRESS==
                --wait: Wait for share snapshot creation
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Add a description to the snapshot (Optional).
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    force: Optional flag to indicate whether to snapshot a share even if it's busy. (Default=False)
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Add a name to the snapshot (Optional).
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    property: Set a property to this snapshot (repeat option to set multiple properties).Available only for microversion >= 2.73
                    variable: ==SUPPRESS==
                    wait: Wait for share snapshot creation
            - name: delete
              description: Delete one or more share snapshots
              flags:
                --force: Delete the snapshot(s) ignoring the current state.
                --wait: Wait for share snapshot deletion
              documentation:
                flag:
                    force: Delete the snapshot(s) ignoring the current state.
                    wait: Wait for share snapshot deletion
            - name: export
              commands:
                - name: location
                  commands:
                    - name: list
                      description: List export locations of a given snapshot
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: show
                      description: Show export location of the share snapshot
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
            - name: instance
              commands:
                - name: export
                  commands:
                    - name: location
                      commands:
                        - name: list
                          description: List export locations from a share snapshot instance.
                          flags:
                            --fit-width: Fit the table to the display width.
                            --max-width=: Maximum display width, <1 to disable.
                            --noindent: whether to disable indenting the JSON
                            --print-empty: Print empty table if there is no data to show.
                            --quote=: when to include quotes, defaults to nonnumeric
                            --sort-ascending: sort the column(s) in ascending order
                            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            --sort-descending: sort the column(s) in descending order
                            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                            -f, --format=: the output format, defaults to table
                          completion:
                            flag:
                                format:
                                    - csv
                                    - json
                                    - table
                                    - value
                                    - yaml
                                quote:
                                    - all
                                    - minimal
                                    - none
                                    - nonnumeric
                          documentation:
                            flag:
                                column: specify the column(s) to include, can be repeated to show multiple columns
                                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                                format: the output format, defaults to table
                                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                                noindent: whether to disable indenting the JSON
                                print-empty: Print empty table if there is no data to show.
                                quote: when to include quotes, defaults to nonnumeric
                                sort-ascending: sort the column(s) in ascending order
                                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                                sort-descending: sort the column(s) in descending order
                        - name: show
                          description: Show export location of the share snapshot instance.
                          flags:
                            --fit-width: Fit the table to the display width.
                            --max-width=: Maximum display width, <1 to disable.
                            --noindent: whether to disable indenting the JSON
                            --prefix=: add a prefix to all variable names
                            --print-empty: Print empty table if there is no data to show.
                            --variable=: ==SUPPRESS==
                            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                            -f, --format=: the output format, defaults to table
                          completion:
                            flag:
                                format:
                                    - json
                                    - shell
                                    - table
                                    - value
                                    - yaml
                          documentation:
                            flag:
                                column: specify the column(s) to include, can be repeated to show multiple columns
                                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                                format: the output format, defaults to table
                                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                                noindent: whether to disable indenting the JSON
                                prefix: add a prefix to all variable names
                                print-empty: Print empty table if there is no data to show.
                                variable: ==SUPPRESS==
                - name: list
                  description: List all share snapshot instances
                  flags:
                    --detailed: 'Show detailed information about snapshot instances. '
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --snapshot=: Filter results by share snapshot ID.
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        detailed: 'Show detailed information about snapshot instances. '
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        snapshot: Filter results by share snapshot ID.
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Explicitly update the state of a share snapshot instance.
                  flags:
                    --status=: Indicate state to update the snapshot instance to.
                  completion:
                    flag:
                        status:
                            - available
                            - error
                            - creating
                            - deleting
                            - error_deleting
                  documentation:
                    flag:
                        status: Indicate state to update the snapshot instance to. Default is available.
                - name: show
                  description: Show details about a share snapshot instance.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: list
              description: List snapshots
              flags:
                --all-projects: Display snapshots from all projects (Admin only).
                --count: The total count of share snapshots before pagination is applied.
                --description=: Filter results by description.
                --description~=: Filter results matching a share snapshot description pattern.
                --detail: List share snapshots with details
                --fit-width: Fit the table to the display width.
                --limit=: Limit the number of snapshots returned
                --marker=: The last share ID of the previous page
                --max-width=: Maximum display width, <1 to disable.
                --name=: Filter results by name.
                --name~=: Filter results matching a share snapshot name pattern.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --property=: Filter snapshots having a given metadata key=value property.
                --quote=: when to include quotes, defaults to nonnumeric
                --share=: Name or ID of a share to filter results by.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --sort=: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                --status=: Filter results by status
                --usage=: Option to filter snapshots by usage.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    usage:
                        - used
                        - unused
              documentation:
                flag:
                    all-projects: Display snapshots from all projects (Admin only).
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    count: The total count of share snapshots before pagination is applied. This parameter is useful when applying pagination parameters '--limit' and '--offset'. Available only for microversion >= 2.79.
                    description: Filter results by description. Available only for microversion >= 2.36.
                    description~: Filter results matching a share snapshot description pattern. Available only for microversion >= 2.36.
                    detail: List share snapshots with details
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: Limit the number of snapshots returned
                    marker: The last share ID of the previous page
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Filter results by name.
                    name~: Filter results matching a share snapshot name pattern. Available only for microversion >= 2.36.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    property: Filter snapshots having a given metadata key=value property. (repeat option to filter by multiple properties)
                    quote: when to include quotes, defaults to nonnumeric
                    share: Name or ID of a share to filter results by.
                    sort: 'Sort output by selected keys and directions(asc or desc) (default: name:asc), multiple keys and directions can be specified separated by comma'
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    status: Filter results by status
                    usage: Option to filter snapshots by usage.
            - name: set
              description: Set share snapshot properties
              flags:
                --description=: Set a description to the snapshot.
                --name=: Set a name to the snapshot.
                --property=: Set a property to this snapshot (repeat option to set multiple properties)
                --status=: Assign a status to the snapshot (Admin only).
              completion:
                flag:
                    status:
                        - available
                        - error
                        - creating
                        - deleting
                        - manage_starting
                        - manage_error
                        - unmanage_starting
                        - unmanage_error
                        - error_deleting
              documentation:
                flag:
                    description: Set a description to the snapshot.
                    name: Set a name to the snapshot.
                    property: Set a property to this snapshot (repeat option to set multiple properties)
                    status: 'Assign a status to the snapshot (Admin only). Options include : available, error, creating, deleting, manage_starting, manage_error, unmanage_starting, unmanage_error, error_deleting.'
            - name: show
              description: Show details about a share snapshot
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset a share snapshot property
              flags:
                --description: Unset snapshot description.
                --name: Unset snapshot name.
                --property=: Remove a property from snapshot (repeat option to remove multiple properties)
              documentation:
                flag:
                    description: Unset snapshot description.
                    name: Unset snapshot name.
                    property: Remove a property from snapshot (repeat option to remove multiple properties)
        - name: transfer
          commands:
            - name: accept
              description: Accepts a share transfer
              flags:
                --clear_rules: Whether manila should clean up the access rules after the transfer is complete.
              documentation:
                flag:
                    clear_rules: Whether manila should clean up the access rules after the transfer is complete. (Default=False)
            - name: create
              description: Create a new share transfer
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Transfer name.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Transfer name. Default=None.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Remove one or more transfers
            - name: list
              description: Lists all transfers
              flags:
                --all-projects: Shows details for all tenants.
                --detailed=: Show detailed information about filtered share transfers.
                --fit-width: Fit the table to the display width.
                --id=: Filter share transfers by ID.
                --limit=: Maximum number of transfer records to return.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Filter share transfers by name.
                --noindent: whether to disable indenting the JSON
                --offset=: Start position of transfer records listing.
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --resource_id=: Filter share transfers by resource ID.
                --resource_type=: Filter share transfers by resource type, which can be share.
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --sort_dir=: Sort direction, available values are ('asc', 'desc').
                --sort_key=: Key to be sorted, available keys are ('id', 'resource_type', 'resource_id', 'name', 'source_project_id', 'destination_project_id', 'created_at', 'expires_at').
                --source_project_id=: Filter share transfers by ID of the Project that initiated the transfer.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all-projects: Shows details for all tenants. (Admin only).
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    detailed: Show detailed information about filtered share transfers.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    id: Filter share transfers by ID. Default=None.
                    limit: Maximum number of transfer records to return. (Default=None)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Filter share transfers by name. Default=None.
                    noindent: whether to disable indenting the JSON
                    offset: Start position of transfer records listing.
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    resource_id: Filter share transfers by resource ID. Default=None.
                    resource_type: Filter share transfers by resource type, which can be share. Default=None.
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    sort_dir: 'Sort direction, available values are (''asc'', ''desc''). OPTIONAL: Default=None.'
                    sort_key: Key to be sorted, available keys are ('id', 'resource_type', 'resource_id', 'name', 'source_project_id', 'destination_project_id', 'created_at', 'expires_at'). Default=None.
                    source_project_id: Filter share transfers by ID of the Project that initiated the transfer. Default=None.
            - name: show
              description: Show details about a share transfer
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: type
          commands:
            - name: access
              commands:
                - name: create
                  description: Add access for share type
                - name: delete
                  description: Delete access from share type
                - name: list
                  description: Get access list for share type
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
            - name: create
              description: Create new share type
              flags:
                --create-share-from-snapshot-support=: Boolean extra spec used for filtering of back ends by their capability to create shares from snapshots.
                --description=: Share type description.
                --extra-specs=:
                    description: Extra specs key and value of share type that will be used for share type creation.
                    nargs: -1
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --mount-snapshot-support=: Boolean extra spec used for filtering of back ends by their capability to mount share snapshots.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --public=: Make type accessible to the public (default true).
                --revert-to-snapshot-support=: Boolean extra spec used for filtering of back ends by their capability to revert shares to snapshots.
                --snapshot-support=: Boolean extra spec used for filtering of back ends by their capability to create share snapshots.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    create-share-from-snapshot-support: Boolean extra spec used for filtering of back ends by their capability to create shares from snapshots.
                    description: Share type description. Available only for microversion >= 2.41.
                    extra-specs: 'Extra specs key and value of share type that will be used for share type creation. OPTIONAL: Default=None. example --extra-specs  thin_provisioning=''<is> True'', replication_type=readable.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    mount-snapshot-support: Boolean extra spec used for filtering of back ends by their capability to mount share snapshots. (Default is False).
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    public: Make type accessible to the public (default true).
                    revert-to-snapshot-support: Boolean extra spec used for filtering of back ends by their capability to revert shares to snapshots. (Default is False).
                    snapshot-support: Boolean extra spec used for filtering of back ends by their capability to create share snapshots.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete a share type
            - name: list
              description: List share types
              flags:
                --all: Display all share types whatever public or private.
                --extra-specs=:
                    description: Filter share types with extra specs (key=value).
                    nargs: -1
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all: Display all share types whatever public or private. Default=False. (Admin only)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    extra-specs: 'Filter share types with extra specs (key=value). Available only for API microversion >= 2.43. OPTIONAL: Default=None.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set share type properties
              flags:
                --description=: New description of share type.
                --extra-specs=:
                    description: Extra specs key and value of share type that will be used for share type creation.
                    nargs: -1
                --name=: New name of share type.
                --public=: New visibility of the share type.
              documentation:
                flag:
                    description: New description of share type. Available only for microversion >= 2.50
                    extra-specs: 'Extra specs key and value of share type that will be used for share type creation. OPTIONAL: Default=None. example --extra-specs  thin_provisioning=''<is> True'', replication_type=readable.'
                    name: New name of share type. Available only for microversion >= 2.50
                    public: New visibility of the share type. If set to True, share type will be available to all projects in the cloud. Available only for microversion >= 2.50
            - name: show
              description: Display share type details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset share type extra specs
        - name: unset
          description: Unset share properties
          flags:
            --description: Unset share description.
            --name: Unset share name.
            --property=: Remove a property from share (repeat option to remove multiple properties)
          documentation:
            flag:
                description: Unset share description.
                name: Unset share name.
                property: Remove a property from share (repeat option to remove multiple properties)
    - name: subnet
      commands:
        - name: create
          description: Create a subnet
          flags:
            --allocation-pool=: Allocation pool IP addresses for this subnet, for example, start=192.168.199.2,end=192.168.199.254 (repeat option to add multiple IP addresses)
            --description=: Set subnet description
            --dhcp: Enable DHCP (default)
            --dns-nameserver=: DNS server for this subnet (repeat option to set multiple DNS servers)
            --dns-publish-fixed-ip: Enable publishing fixed IPs in DNS
            --extra-property=: Additional parameters can be passed using this property.
            --fit-width: Fit the table to the display width.
            --gateway=: Specify a gateway for the subnet.
            --host-route=: 'Additional route for this subnet, for example, destination=10.10.0.0/16,gateway=192.168.71.254 destination: destination subnet (in CIDR notation) gateway: next-hop IP address (repeat option to add multiple routes)'
            --ip-version=: IP version (default is 4).
            --ipv6-address-mode=: 'IPv6 address mode, valid modes: [dhcpv6-stateful, dhcpv6-stateless, slaac]'
            --ipv6-ra-mode=: 'IPv6 RA (Router Advertisement) mode, valid modes: [dhcpv6-stateful, dhcpv6-stateless, slaac]'
            --max-width=: Maximum display width, <1 to disable.
            --network-segment=: Network segment to associate with this subnet (name or ID)
            --network=!: Network this subnet belongs to (name or ID)
            --no-dhcp: Disable DHCP
            --no-dns-publish-fixed-ip: Disable publishing fixed IPs in DNS (default)
            --no-tag: No tags associated with the subnet
            --noindent: whether to disable indenting the JSON
            --prefix-length=: Prefix length for subnet allocation from subnet pool
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Owner's project (name or ID)
            --service-type=: Service type for this subnet, for example, network:floatingip_agent_gateway.
            --subnet-pool=: Subnet pool from which this subnet will obtain a CIDR (Name or ID)
            --subnet-range=: Subnet range in CIDR notation (required if --subnet-pool is not specified, optional otherwise)
            --tag=: Tag to be added to the subnet (repeat option to set multiple tags)
            --use-default-subnet-pool: Use default subnet pool for --ip-version
            --use-prefix-delegation: Use 'prefix-delegation' if IP is IPv6 format and IP would be delegated externally
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
                ip-version:
                    - "4"
                    - "6"
                ipv6-address-mode:
                    - dhcpv6-stateful
                    - dhcpv6-stateless
                    - slaac
                ipv6-ra-mode:
                    - dhcpv6-stateful
                    - dhcpv6-stateless
                    - slaac
          documentation:
            flag:
                allocation-pool: Allocation pool IP addresses for this subnet, for example, start=192.168.199.2,end=192.168.199.254 (repeat option to add multiple IP addresses)
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: Set subnet description
                dhcp: Enable DHCP (default)
                dns-nameserver: DNS server for this subnet (repeat option to set multiple DNS servers)
                dns-publish-fixed-ip: Enable publishing fixed IPs in DNS
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                gateway: 'Specify a gateway for the subnet.  The three options are: <ip-address>: Specific IP address to use as the gateway, ''auto'': Gateway address should automatically be chosen from within the subnet itself, ''none'': This subnet will not use a gateway. For example, --gateway 192.168.9.1, --gateway auto or --gateway none (default is ''auto'').'
                host-route: 'Additional route for this subnet, for example, destination=10.10.0.0/16,gateway=192.168.71.254 destination: destination subnet (in CIDR notation) gateway: next-hop IP address (repeat option to add multiple routes)'
                ip-version: IP version (default is 4). Note that when subnet pool is specified, IP version is determined from the subnet pool and this option is ignored.
                ipv6-address-mode: 'IPv6 address mode, valid modes: [dhcpv6-stateful, dhcpv6-stateless, slaac]'
                ipv6-ra-mode: 'IPv6 RA (Router Advertisement) mode, valid modes: [dhcpv6-stateful, dhcpv6-stateless, slaac]'
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                network: Network this subnet belongs to (name or ID)
                network-segment: Network segment to associate with this subnet (name or ID)
                no-dhcp: Disable DHCP
                no-dns-publish-fixed-ip: Disable publishing fixed IPs in DNS (default)
                no-tag: No tags associated with the subnet
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                prefix-length: Prefix length for subnet allocation from subnet pool
                print-empty: Print empty table if there is no data to show.
                project: Owner's project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                service-type: Service type for this subnet, for example, network:floatingip_agent_gateway. Must be a valid device owner value for a network port (repeat option to set multiple service types)
                subnet-pool: Subnet pool from which this subnet will obtain a CIDR (Name or ID)
                subnet-range: Subnet range in CIDR notation (required if --subnet-pool is not specified, optional otherwise)
                tag: Tag to be added to the subnet (repeat option to set multiple tags)
                use-default-subnet-pool: Use default subnet pool for --ip-version
                use-prefix-delegation: Use 'prefix-delegation' if IP is IPv6 format and IP would be delegated externally
                variable: ==SUPPRESS==
        - name: delete
          description: Delete subnet(s)
        - name: list
          description: List subnets
          flags:
            --any-tags=: List subnets which have any given tag(s) (Comma-separated list of tags)
            --dhcp: List only subnets which have DHCP enabled
            --fit-width: Fit the table to the display width.
            --gateway=: List only subnets with the specified gateway IP
            --ip-version=: List only subnets with the specified IP version.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --name=: List only subnets with the specified name
            --network=: List only subnets which belong to the specified network (name or ID)
            --no-dhcp: List only subnets which have DHCP disabled
            --noindent: whether to disable indenting the JSON
            --not-any-tags=: Exclude subnets which have any given tag(s) (Comma-separated list of tags)
            --not-tags=: Exclude subnets which have all given tag(s) (Comma-separated list of tags)
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: List only subnets with the specified project (name or ID)
            --quote=: when to include quotes, defaults to nonnumeric
            --service-type=: List only subnets with the specified service type, for example, network:floatingip_agent_gateway.
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --subnet-pool=: List only subnets which belong to the specified subnet pool (name or ID)
            --subnet-range=: List only subnets with the specified subnet range (in CIDR notation).
            --tags=: List subnets which have all given tag(s) (Comma-separated list of tags)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                ip-version:
                    - "4"
                    - "6"
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                any-tags: List subnets which have any given tag(s) (Comma-separated list of tags)
                column: specify the column(s) to include, can be repeated to show multiple columns
                dhcp: List only subnets which have DHCP enabled
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                gateway: List only subnets with the specified gateway IP
                ip-version: List only subnets with the specified IP version. Allowed values for IP version are 4 and 6.
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: List only subnets with the specified name
                network: List only subnets which belong to the specified network (name or ID)
                no-dhcp: List only subnets which have DHCP disabled
                noindent: whether to disable indenting the JSON
                not-any-tags: Exclude subnets which have any given tag(s) (Comma-separated list of tags)
                not-tags: Exclude subnets which have all given tag(s) (Comma-separated list of tags)
                print-empty: Print empty table if there is no data to show.
                project: List only subnets with the specified project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                quote: when to include quotes, defaults to nonnumeric
                service-type: List only subnets with the specified service type, for example, network:floatingip_agent_gateway. Must be a valid device owner value for a network port (repeat option to list multiple service types).
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                subnet-pool: List only subnets which belong to the specified subnet pool (name or ID)
                subnet-range: List only subnets with the specified subnet range (in CIDR notation). For example, --subnet-range 10.10.0.0/16
                tags: List subnets which have all given tag(s) (Comma-separated list of tags)
        - name: pool
          commands:
            - name: create
              description: Create subnet pool
              flags:
                --address-scope=: Set address scope associated with the subnet pool (name or ID), prefixes must be unique across address scopes
                --default: Set this as a default subnet pool
                --default-prefix-length=: Set subnet pool default prefix length
                --default-quota=: Set default per-project quota for this subnet pool as the number of IP addresses that can be allocated from the subnet pool
                --description=: Set subnet pool description
                --extra-property=: Additional parameters can be passed using this property.
                --fit-width: Fit the table to the display width.
                --max-prefix-length=: Set subnet pool maximum prefix length
                --max-width=: Maximum display width, <1 to disable.
                --min-prefix-length=: Set subnet pool minimum prefix length
                --no-default: Set this as a non-default subnet pool
                --no-share: Set this subnet pool as not shared
                --no-tag: No tags associated with the subnet pool
                --noindent: whether to disable indenting the JSON
                --pool-prefix=!: Set subnet pool prefixes (in CIDR notation) (repeat option to set multiple prefixes)
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --share: Set this subnet pool as shared
                --tag=: Tag to be added to the subnet pool (repeat option to set multiple tags)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    address-scope: Set address scope associated with the subnet pool (name or ID), prefixes must be unique across address scopes
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    default: Set this as a default subnet pool
                    default-prefix-length: Set subnet pool default prefix length
                    default-quota: Set default per-project quota for this subnet pool as the number of IP addresses that can be allocated from the subnet pool
                    description: Set subnet pool description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-prefix-length: Set subnet pool maximum prefix length
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    min-prefix-length: Set subnet pool minimum prefix length
                    no-default: Set this as a non-default subnet pool
                    no-share: Set this subnet pool as not shared
                    no-tag: No tags associated with the subnet pool
                    noindent: whether to disable indenting the JSON
                    pool-prefix: Set subnet pool prefixes (in CIDR notation) (repeat option to set multiple prefixes)
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    share: Set this subnet pool as shared
                    tag: Tag to be added to the subnet pool (repeat option to set multiple tags)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete subnet pool(s)
            - name: list
              description: List subnet pools
              flags:
                --address-scope=: List only subnet pools with the specified address scope (name or ID)
                --any-tags=: List subnet pools which have any given tag(s) (Comma-separated list of tags)
                --default: List only subnet pools used as the default external subnet pool
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --name=: List only subnet pools with the specified name
                --no-default: List only subnet pools not used as the default external subnet pool
                --no-share: List only subnet pools not shared between projects
                --noindent: whether to disable indenting the JSON
                --not-any-tags=: Exclude subnet pools which have any given tag(s) (Comma-separated list of tags)
                --not-tags=: Exclude subnet pools which have all given tag(s) (Comma-separated list of tags)
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: List only subnet pools with the specified project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --share: List only subnet pools shared between projects
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --tags=: List subnet pools which have all given tag(s) (Comma-separated list of tags)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    address-scope: List only subnet pools with the specified address scope (name or ID)
                    any-tags: List subnet pools which have any given tag(s) (Comma-separated list of tags)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    default: List only subnet pools used as the default external subnet pool
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: List only subnet pools with the specified name
                    no-default: List only subnet pools not used as the default external subnet pool
                    no-share: List only subnet pools not shared between projects
                    noindent: whether to disable indenting the JSON
                    not-any-tags: Exclude subnet pools which have any given tag(s) (Comma-separated list of tags)
                    not-tags: Exclude subnet pools which have all given tag(s) (Comma-separated list of tags)
                    print-empty: Print empty table if there is no data to show.
                    project: List only subnet pools with the specified project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    share: List only subnet pools shared between projects
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    tags: List subnet pools which have all given tag(s) (Comma-separated list of tags)
            - name: set
              description: Set subnet pool properties
              flags:
                --address-scope=: Set address scope associated with the subnet pool (name or ID), prefixes must be unique across address scopes
                --default: Set this as a default subnet pool
                --default-prefix-length=: Set subnet pool default prefix length
                --default-quota=: Set default per-project quota for this subnet pool as the number of IP addresses that can be allocated from the subnet pool
                --description=: Set subnet pool description
                --extra-property=: Additional parameters can be passed using this property.
                --max-prefix-length=: Set subnet pool maximum prefix length
                --min-prefix-length=: Set subnet pool minimum prefix length
                --name=: Set subnet pool name
                --no-address-scope: Remove address scope associated with the subnet pool
                --no-default: Set this as a non-default subnet pool
                --no-tag: Clear tags associated with the subnet pool.
                --pool-prefix=: Set subnet pool prefixes (in CIDR notation) (repeat option to set multiple prefixes)
                --tag=: Tag to be added to the subnet pool (repeat option to set multiple tags)
              documentation:
                flag:
                    address-scope: Set address scope associated with the subnet pool (name or ID), prefixes must be unique across address scopes
                    default: Set this as a default subnet pool
                    default-prefix-length: Set subnet pool default prefix length
                    default-quota: Set default per-project quota for this subnet pool as the number of IP addresses that can be allocated from the subnet pool
                    description: Set subnet pool description
                    extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                    max-prefix-length: Set subnet pool maximum prefix length
                    min-prefix-length: Set subnet pool minimum prefix length
                    name: Set subnet pool name
                    no-address-scope: Remove address scope associated with the subnet pool
                    no-default: Set this as a non-default subnet pool
                    no-tag: Clear tags associated with the subnet pool. Specify both --tag and --no-tag to overwrite current tags
                    pool-prefix: Set subnet pool prefixes (in CIDR notation) (repeat option to set multiple prefixes)
                    tag: Tag to be added to the subnet pool (repeat option to set multiple tags)
            - name: show
              description: Display subnet pool details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset subnet pool properties
              flags:
                --all-tag: Clear all tags associated with the subnet pool
                --tag=: Tag to be removed from the subnet pool (repeat option to remove multiple tags)
              documentation:
                flag:
                    all-tag: Clear all tags associated with the subnet pool
                    tag: Tag to be removed from the subnet pool (repeat option to remove multiple tags)
        - name: set
          description: Set subnet properties
          flags:
            --allocation-pool=: Allocation pool IP addresses for this subnet, for example, start=192.168.199.2,end=192.168.199.254 (repeat option to add multiple IP addresses)
            --description=: Set subnet description
            --dhcp: Enable DHCP
            --dns-nameserver=: DNS server for this subnet (repeat option to set multiple DNS servers)
            --dns-publish-fixed-ip: Enable publishing fixed IPs in DNS
            --extra-property=: Additional parameters can be passed using this property.
            --gateway=: Specify a gateway for the subnet.
            --host-route=: 'Additional route for this subnet, for example, destination=10.10.0.0/16,gateway=192.168.71.254 destination: destination subnet (in CIDR notation) gateway: next-hop IP address (repeat option to add multiple routes)'
            --leak-routes: Leak subnet routes to the underlay BGP fabric (ovn-bgp extension required)
            --name=: Updated name of the subnet
            --network-segment=: Network segment to associate with this subnet (name or ID).
            --no-allocation-pool: Clear associated allocation-pools from the subnet.
            --no-dhcp: Disable DHCP
            --no-dns-nameservers: Clear existing information of DNS Nameservers.
            --no-dns-publish-fixed-ip: Disable publishing fixed IPs in DNS
            --no-host-route: Clear associated host-routes from the subnet.
            --no-leak-routes: Do not leak subnet routes to the underlay BGP fabric (ovn-bgp extension required)
            --no-tag: Clear tags associated with the subnet.
            --service-type=: Service type for this subnet, for example, network:floatingip_agent_gateway.
            --tag=: Tag to be added to the subnet (repeat option to set multiple tags)
          documentation:
            flag:
                allocation-pool: Allocation pool IP addresses for this subnet, for example, start=192.168.199.2,end=192.168.199.254 (repeat option to add multiple IP addresses)
                description: Set subnet description
                dhcp: Enable DHCP
                dns-nameserver: DNS server for this subnet (repeat option to set multiple DNS servers)
                dns-publish-fixed-ip: Enable publishing fixed IPs in DNS
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                gateway: 'Specify a gateway for the subnet. The options are: <ip-address>: Specific IP address to use as the gateway, ''none'': This subnet will not use a gateway. For example, --gateway 192.168.9.1 or --gateway none.'
                host-route: 'Additional route for this subnet, for example, destination=10.10.0.0/16,gateway=192.168.71.254 destination: destination subnet (in CIDR notation) gateway: next-hop IP address (repeat option to add multiple routes)'
                leak-routes: Leak subnet routes to the underlay BGP fabric (ovn-bgp extension required)
                name: Updated name of the subnet
                network-segment: Network segment to associate with this subnet (name or ID). It is only allowed to set the segment if the current value is `None`. The network must also have only one segment and only one subnet can exist on the network.
                no-allocation-pool: Clear associated allocation-pools from the subnet. Specify both --allocation-pool and --no-allocation-pool to overwrite the current allocation pool information.
                no-dhcp: Disable DHCP
                no-dns-nameservers: Clear existing information of DNS Nameservers. Specify both --dns-nameserver and --no-dns-nameserver to overwrite the current DNS Nameserver information.
                no-dns-publish-fixed-ip: Disable publishing fixed IPs in DNS
                no-host-route: Clear associated host-routes from the subnet. Specify both --host-route and --no-host-route to overwrite the current host route information.
                no-leak-routes: Do not leak subnet routes to the underlay BGP fabric (ovn-bgp extension required)
                no-tag: Clear tags associated with the subnet. Specify both --tag and --no-tag to overwrite current tags
                service-type: Service type for this subnet, for example, network:floatingip_agent_gateway. Must be a valid device owner value for a network port (repeat option to set multiple service types)
                tag: Tag to be added to the subnet (repeat option to set multiple tags)
        - name: show
          description: Display subnet details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: unset
          description: Unset subnet properties
          flags:
            --all-tag: Clear all tags associated with the subnet
            --allocation-pool=: Allocation pool IP addresses to be removed from this subnet, for example, start=192.168.199.2,end=192.168.199.254 (repeat option to unset multiple allocation pools)
            --dns-nameserver=: DNS server to be removed from this subnet (repeat option to unset multiple DNS servers)
            --extra-property=: Additional parameters can be passed using this property.
            --gateway: Remove gateway IP from this subnet
            --host-route=: 'Route to be removed from this subnet, for example, destination=10.10.0.0/16,gateway=192.168.71.254 destination: destination subnet (in CIDR notation) gateway: next-hop IP address (repeat option to unset multiple host routes)'
            --service-type=: Service type to be removed from this subnet, for example, network:floatingip_agent_gateway.
            --tag=: Tag to be removed from the subnet (repeat option to remove multiple tags)
          documentation:
            flag:
                all-tag: Clear all tags associated with the subnet
                allocation-pool: Allocation pool IP addresses to be removed from this subnet, for example, start=192.168.199.2,end=192.168.199.254 (repeat option to unset multiple allocation pools)
                dns-nameserver: DNS server to be removed from this subnet (repeat option to unset multiple DNS servers)
                extra-property: 'Additional parameters can be passed using this property. Default type of the extra property is string (''str''), but other types can be used as well. Available types are: ''dict'', ''list'', ''str'', ''bool'', ''int''. In case of ''list'' type, ''value'' can be semicolon-separated list of values. For ''dict'' value is semicolon-separated list of the key:value pairs.'
                gateway: Remove gateway IP from this subnet
                host-route: 'Route to be removed from this subnet, for example, destination=10.10.0.0/16,gateway=192.168.71.254 destination: destination subnet (in CIDR notation) gateway: next-hop IP address (repeat option to unset multiple host routes)'
                service-type: Service type to be removed from this subnet, for example, network:floatingip_agent_gateway. Must be a valid device owner value for a network port (repeat option to unset multiple service types)
                tag: Tag to be removed from the subnet (repeat option to remove multiple tags)
    - name: tap
      commands:
        - name: flow
          commands:
            - name: create
              description: Create a new tap flow.
              flags:
                --description=: Description of the tap flow.
                --direction=!: Direction of the Tap flow.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Name of the tap flow.
                --noindent: whether to disable indenting the JSON
                --port=!: Source port (name or ID) to monitor.
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --tap-service=!: Tap service (name or ID) to associate with this tap flow.
                --variable=: ==SUPPRESS==
                --vlan-filter=: VLAN IDs to mirror in the form of range string.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    direction:
                        - IN
                        - OUT
                        - BOTH
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the tap flow.
                    direction: 'Direction of the Tap flow. Valid options are: IN, OUT and BOTH'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Name of the tap flow.
                    noindent: whether to disable indenting the JSON
                    port: Source port (name or ID) to monitor.
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    tap-service: Tap service (name or ID) to associate with this tap flow.
                    variable: ==SUPPRESS==
                    vlan-filter: VLAN IDs to mirror in the form of range string.
            - name: delete
              description: Delete a tap flow.
            - name: list
              description: List tap flows.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show tap flow details.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: update
              description: Update a tap flow.
              flags:
                --description=: Description of the tap flow.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Name of the tap flow.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the tap flow.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Name of the tap flow.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: mirror
          commands:
            - name: create
              description: Create a new tap mirror.
              flags:
                --description=: Description of the tap service.
                --directions=!: Dictionary of direction and tunnel_id.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --mirror-type=!: Mirror type.
                --name=: Name of the tap service.
                --noindent: whether to disable indenting the JSON
                --port=!: Port (name or ID) to which the Tap Mirror is connected.
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --remote-ip=!: Remote IP address for the tap mirror (remote end of the GRE or ERSPAN v1 tunnel).
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the tap service.
                    directions: 'Dictionary of direction and tunnel_id. Valid directions are: IN and OUT.'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    mirror-type: 'Mirror type. Valid values are: gre and erspanv1.'
                    name: Name of the tap service.
                    noindent: whether to disable indenting the JSON
                    port: Port (name or ID) to which the Tap Mirror is connected.
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    remote-ip: Remote IP address for the tap mirror (remote end of the GRE or ERSPAN v1 tunnel).
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete a tap mirror.
            - name: list
              description: List tap mirrors.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show tap mirror details.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: update
              description: Update a tap mirror.
              flags:
                --description=: Description of the tap service.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Name of the tap service.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the tap service.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Name of the tap service.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: service
          commands:
            - name: create
              description: Create a new tap service.
              flags:
                --description=: Description of the tap service.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Name of the tap service.
                --noindent: whether to disable indenting the JSON
                --port=!: Port (name or ID) to connect to the tap service.
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the tap service.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Name of the tap service.
                    noindent: whether to disable indenting the JSON
                    port: Port (name or ID) to connect to the tap service.
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete a tap service.
            - name: list
              description: List tap services.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show tap service details.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: update
              description: Update a tap service.
              flags:
                --description=: Description of the tap service.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Name of the tap service.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the tap service.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Name of the tap service.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
    - name: token
      commands:
        - name: issue
          description: Issue new token
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: revoke
          description: Revoke existing token
    - name: trust
      commands:
        - name: create
          description: Create new trust
          flags:
            --expiration=: Sets an expiration date for the trust (format of YYYY-mm-ddTHH:MM:SS)
            --fit-width: Fit the table to the display width.
            --impersonate: Tokens generated from the trust will represent <trustor> (defaults to False)
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=!: Project being delegated (name or ID) (required)
            --role=!: Roles to authorize (name or ID) (repeat option to set multiple values, required)
            --trustee-domain=: Domain that contains <trustee> (name or ID)
            --trustor-domain=: Domain that contains <trustor> (name or ID)
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                expiration: Sets an expiration date for the trust (format of YYYY-mm-ddTHH:MM:SS)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                impersonate: Tokens generated from the trust will represent <trustor> (defaults to False)
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Project being delegated (name or ID) (required)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                role: Roles to authorize (name or ID) (repeat option to set multiple values, required)
                trustee-domain: Domain that contains <trustee> (name or ID)
                trustor-domain: Domain that contains <trustor> (name or ID)
                variable: ==SUPPRESS==
        - name: delete
          description: Delete trust(s)
        - name: list
          description: List trusts
          flags:
            --auth-user: Only list trusts related to the authenticated user
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --trustee-domain=: Domain that contains <trustee> (name or ID)
            --trustee=: Trustee user to filter (name or ID)
            --trustor-domain=: Domain that contains <trustor> (name or ID)
            --trustor=: Trustor user to filter (name or ID)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                auth-user: Only list trusts related to the authenticated user
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                trustee: Trustee user to filter (name or ID)
                trustee-domain: Domain that contains <trustee> (name or ID)
                trustor: Trustor user to filter (name or ID)
                trustor-domain: Domain that contains <trustor> (name or ID)
        - name: show
          description: Display trust details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: usage
      commands:
        - name: list
          description: List resource usage per project
          flags:
            --end=: 'Usage range end date, ex 2012-01-20 (default: tomorrow)'
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --start=: 'Usage range start date, ex 2012-01-20 (default: 4 weeks ago)'
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                end: 'Usage range end date, ex 2012-01-20 (default: tomorrow)'
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                start: 'Usage range start date, ex 2012-01-20 (default: 4 weeks ago)'
        - name: show
          description: Show resource usage for a single project
          flags:
            --end=: 'Usage range end date, ex 2012-01-20 (default: tomorrow)'
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Name or ID of project to show usage for
            --start=: 'Usage range start date, ex 2012-01-20 (default: 4 weeks ago)'
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                end: 'Usage range end date, ex 2012-01-20 (default: tomorrow)'
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Name or ID of project to show usage for
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                start: 'Usage range start date, ex 2012-01-20 (default: 4 weeks ago)'
                variable: ==SUPPRESS==
    - name: user
      commands:
        - name: create
          description: Create new user
          flags:
            --description=: User description
            --disable: Disable user
            --disable-lock-password: Enables the ability for a user to change its password through self-service APIs
            --disable-multi-factor-auth: Disables the MFA (Multi Factor Auth)
            --domain=: Default domain (name or ID)
            --email=: Set user email address
            --enable: Enable user (default)
            --enable-lock-password: Disables the ability for a user to change its password through self-service APIs
            --enable-multi-factor-auth: Enables the MFA (Multi Factor Auth)
            --fit-width: Fit the table to the display width.
            --ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time.
            --ignore-lockout-failure-attempts: Opt into ignoring the number of times a user has authenticated and locking out the user as a result
            --ignore-password-expiry: Opt into allowing user to continue using passwords that may be expired
            --max-width=: Maximum display width, <1 to disable.
            --multi-factor-auth-rule=: Set multi-factor auth rules.
            --no-ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time.
            --no-ignore-lockout-failure-attempts: Opt out of ignoring the number of times a user has authenticated and locking out the user as a result
            --no-ignore-password-expiry: Opt out of allowing user to continue using passwords that may be expired
            --noindent: whether to disable indenting the JSON
            --or-show: Return existing user
            --password-prompt: Prompt interactively for password
            --password=: Set user password
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Default project (name or ID)
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                description: User description
                disable: Disable user
                disable-lock-password: Enables the ability for a user to change its password through self-service APIs
                disable-multi-factor-auth: Disables the MFA (Multi Factor Auth)
                domain: Default domain (name or ID)
                email: Set user email address
                enable: Enable user (default)
                enable-lock-password: Disables the ability for a user to change its password through self-service APIs
                enable-multi-factor-auth: Enables the MFA (Multi Factor Auth)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time. Opt into ignoring the user to change their password during first time login in keystone
                ignore-lockout-failure-attempts: Opt into ignoring the number of times a user has authenticated and locking out the user as a result
                ignore-password-expiry: Opt into allowing user to continue using passwords that may be expired
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                multi-factor-auth-rule: 'Set multi-factor auth rules. For example, to set a rule requiring the "password" and "totp" auth methods to be provided, use: "--multi-factor-auth-rule password,totp". May be provided multiple times to set different rule combinations.'
                no-ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time. Opt out of ignoring the user to change their password during first time login in keystone
                no-ignore-lockout-failure-attempts: Opt out of ignoring the number of times a user has authenticated and locking out the user as a result
                no-ignore-password-expiry: Opt out of allowing user to continue using passwords that may be expired
                noindent: whether to disable indenting the JSON
                or-show: Return existing user
                password: Set user password
                password-prompt: Prompt interactively for password
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                project: Default project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                variable: ==SUPPRESS==
        - name: delete
          description: Delete user(s)
          flags:
            --domain=: Domain owning <user> (name or ID)
          documentation:
            flag:
                domain: Domain owning <user> (name or ID)
        - name: list
          description: List users
          flags:
            --disabled: List only disabled users, does nothing with --project and --group
            --domain=: Filter users by <domain> (name or ID)
            --enabled: List only enabled users, does nothing with --project and --group
            --fit-width: Fit the table to the display width.
            --group=: Filter users by <group> membership (name or ID)
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project=: Filter users by <project> (name or ID)
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                disabled: List only disabled users, does nothing with --project and --group
                domain: Filter users by <domain> (name or ID)
                enabled: List only enabled users, does nothing with --project and --group
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                group: Filter users by <group> membership (name or ID)
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: Filter users by <project> (name or ID)
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
        - name: password
          commands:
            - name: set
              description: Change current user password
              flags:
                --original-password=: Original user password
                --password=: New user password
              documentation:
                flag:
                    original-password: Original user password
                    password: New user password
        - name: set
          description: Set user properties
          flags:
            --description=: Set user description
            --disable: Disable user
            --disable-lock-password: Enables the ability for a user to change its password through self-service APIs
            --disable-multi-factor-auth: Disables the MFA (Multi Factor Auth)
            --domain=: Domain the user belongs to (name or ID).
            --email=: Set user email address
            --enable: Enable user (default)
            --enable-lock-password: Disables the ability for a user to change its password through self-service APIs
            --enable-multi-factor-auth: Enables the MFA (Multi Factor Auth)
            --ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time.
            --ignore-lockout-failure-attempts: Opt into ignoring the number of times a user has authenticated and locking out the user as a result
            --ignore-password-expiry: Opt into allowing user to continue using passwords that may be expired
            --multi-factor-auth-rule=: Set multi-factor auth rules.
            --name=: Set user name
            --no-ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time.
            --no-ignore-lockout-failure-attempts: Opt out of ignoring the number of times a user has authenticated and locking out the user as a result
            --no-ignore-password-expiry: Opt out of allowing user to continue using passwords that may be expired
            --password-prompt: Prompt interactively for password
            --password=: Set user password
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Set default project (name or ID)
          documentation:
            flag:
                description: Set user description
                disable: Disable user
                disable-lock-password: Enables the ability for a user to change its password through self-service APIs
                disable-multi-factor-auth: Disables the MFA (Multi Factor Auth)
                domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
                email: Set user email address
                enable: Enable user (default)
                enable-lock-password: Disables the ability for a user to change its password through self-service APIs
                enable-multi-factor-auth: Enables the MFA (Multi Factor Auth)
                ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time. Opt into ignoring the user to change their password during first time login in keystone
                ignore-lockout-failure-attempts: Opt into ignoring the number of times a user has authenticated and locking out the user as a result
                ignore-password-expiry: Opt into allowing user to continue using passwords that may be expired
                multi-factor-auth-rule: 'Set multi-factor auth rules. For example, to set a rule requiring the "password" and "totp" auth methods to be provided, use: "--multi-factor-auth-rule password,totp". May be provided multiple times to set different rule combinations.'
                name: Set user name
                no-ignore-change-password-upon-first-use: Control if a user should be forced to change their password immediately after they log into keystone for the first time. Opt out of ignoring the user to change their password during first time login in keystone
                no-ignore-lockout-failure-attempts: Opt out of ignoring the number of times a user has authenticated and locking out the user as a result
                no-ignore-password-expiry: Opt out of allowing user to continue using passwords that may be expired
                password: Set user password
                password-prompt: Prompt interactively for password
                project: Set default project (name or ID)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
        - name: show
          description: Display user details
          flags:
            --domain=: Domain owning <user> (name or ID)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                domain: Domain owning <user> (name or ID)
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
    - name: versions
      commands:
        - name: show
          description: Show available versions of services
          flags:
            --all-interfaces: Show values for all interfaces
            --fit-width: Fit the table to the display width.
            --interface=: Show versions for a specific interface.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --quote=: when to include quotes, defaults to nonnumeric
            --region-name=: Show versions for a specific region.
            --service=: Show versions for a specific service.
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --status=: Show versions for a specific status.
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all-interfaces: Show values for all interfaces
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                interface: Show versions for a specific interface.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                quote: when to include quotes, defaults to nonnumeric
                region-name: Show versions for a specific region.
                service: Show versions for a specific service. The argument should be either an exact match to what is in the catalog or a known official value or alias from service-types-authority (https://service-types.openstack.org/)
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                status: |-
                    Show versions for a specific status. Valid values are:

                    - SUPPORTED
                    - CURRENT
                    - DEPRECATED
                    - EXPERIMENTAL
    - name: volume
      commands:
        - name: attachment
          commands:
            - name: complete
              description: Complete an attachment for a volume.
            - name: create
              description: Create an attachment for a volume.
              flags:
                --connect: Make an active connection using provided connector info
                --fit-width: Fit the table to the display width.
                --host=: Name of the host attaching to
                --initiator=: IQN of the initiator attaching to
                --ip=: IP of the system attaching to
                --max-width=: Maximum display width, <1 to disable.
                --mode=: Mode of volume attachment, rw, ro and null, where null indicates we want to honor any existing admin-metadata settings (supported by --os-volume-api-version 3.54 or later)
                --mountpoint=: Mountpoint volume will be attached at
                --multipath: Use multipath
                --no-connect: Do not make an active connection using provided connector info
                --no-multipath: Use multipath
                --noindent: whether to disable indenting the JSON
                --os-type=: OS type
                --platform=: Platform type
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    connect: Make an active connection using provided connector info
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Name of the host attaching to
                    initiator: IQN of the initiator attaching to
                    ip: IP of the system attaching to
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    mode: Mode of volume attachment, rw, ro and null, where null indicates we want to honor any existing admin-metadata settings (supported by --os-volume-api-version 3.54 or later)
                    mountpoint: Mountpoint volume will be attached at
                    multipath: Use multipath
                    no-connect: Do not make an active connection using provided connector info
                    no-multipath: Use multipath
                    noindent: whether to disable indenting the JSON
                    os-type: OS type
                    platform: Platform type
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete an attachment for a volume.
            - name: list
              description: Lists all volume attachments.
              flags:
                --all-projects: Shows details for all projects (admin only).
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter results by project (name or ID) (admin only)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --status=: Filters results by a status.
                --volume-id=: Filters results by a volume ID.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all-projects: Shows details for all projects (admin only).
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Filter results by project (name or ID) (admin only)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    status: Filters results by a status. This option is deprecated. Consider using the '--filters' option which was introduced in microversion 3.33 instead.
                    volume-id: Filters results by a volume ID. This option is deprecated. Consider using the '--filters' option which was introduced in microversion 3.33 instead.
            - name: set
              description: Update an attachment for a volume.
              flags:
                --fit-width: Fit the table to the display width.
                --host=: Name of the host attaching to
                --initiator=: IQN of the initiator attaching to
                --ip=: IP of the system attaching to
                --max-width=: Maximum display width, <1 to disable.
                --mountpoint=: Mountpoint volume will be attached at
                --multipath: Use multipath
                --no-multipath: Use multipath
                --noindent: whether to disable indenting the JSON
                --os-type=: OS type
                --platform=: Platform type
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: Name of the host attaching to
                    initiator: IQN of the initiator attaching to
                    ip: IP of the system attaching to
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    mountpoint: Mountpoint volume will be attached at
                    multipath: Use multipath
                    no-multipath: Use multipath
                    noindent: whether to disable indenting the JSON
                    os-type: OS type
                    platform: Platform type
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: show
              description: Show detailed information for a volume attachment.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: backend
          commands:
            - name: capability
              commands:
                - name: show
                  description: Show capability command
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
            - name: pool
              commands:
                - name: list
                  description: List pool command
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: Show detailed information about pools.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: Show detailed information about pools.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
        - name: backup
          commands:
            - name: create
              description: Create new volume backup
              flags:
                --availability-zone=: AZ where the backup should be stored; by default it will be the same as the source (supported by --os-volume-api-version 3.51 or above)
                --container=: Optional backup container name
                --description=: Description of the backup
                --fit-width: Fit the table to the display width.
                --force: Allow to back up an in-use volume
                --incremental: Perform an incremental backup
                --max-width=: Maximum display width, <1 to disable.
                --name=: Name of the backup
                --no-incremental: Do not perform an incremental backup
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --property=: Set a property on this backup (repeat option to remove multiple values) (supported by --os-volume-api-version 3.43 or above)
                --snapshot=: Snapshot to backup (name or ID)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    availability-zone: AZ where the backup should be stored; by default it will be the same as the source (supported by --os-volume-api-version 3.51 or above)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    container: Optional backup container name
                    description: Description of the backup
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    force: Allow to back up an in-use volume
                    format: the output format, defaults to table
                    incremental: Perform an incremental backup
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Name of the backup
                    no-incremental: Do not perform an incremental backup
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    property: Set a property on this backup (repeat option to remove multiple values) (supported by --os-volume-api-version 3.43 or above)
                    snapshot: Snapshot to backup (name or ID)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete volume backup(s)
              flags:
                --force: Allow delete in state other than error or available
              documentation:
                flag:
                    force: Allow delete in state other than error or available
            - name: list
              description: List volume backups
              flags:
                --all-projects: Include all projects (admin only)
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Filters results by the backup name
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project=: Filter results by project (name or ID) (admin only)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --status=: 'Filters results by the backup status, one of: creating, available, deleting, error, restoring or error_restoring'
                --volume=: Filters results by the volume which they backup (name or ID)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    status:
                        - creating
                        - available
                        - deleting
                        - error
                        - restoring
                        - error_restoring
              documentation:
                flag:
                    all-projects: Include all projects (admin only)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Filters results by the backup name
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Filter results by project (name or ID) (admin only)
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    status: 'Filters results by the backup status, one of: creating, available, deleting, error, restoring or error_restoring'
                    volume: Filters results by the volume which they backup (name or ID)
            - name: record
              commands:
                - name: export
                  description: Export volume backup details.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: import
                  description: Import volume backup details.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: restore
              description: Restore volume backup
              flags:
                --fit-width: Fit the table to the display width.
                --force: Restore the backup to an existing volume (default to False)
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    force: Restore the backup to an existing volume (default to False)
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: set
              description: Set volume backup properties
              flags:
                --description=: New backup description (supported by --os-volume-api-version 3.9 or above)
                --name=: New backup name (supported by --os-volume-api-version 3.9 or above)
                --no-property: Remove all properties from this backup (specify both --no-property and --property to remove the current properties before setting new properties)
                --property=: Set a property on this backup (repeat option to set multiple values) (supported by --os-volume-api-version 3.43 or above)
                --state=: New backup state ("available" or "error") (admin only) (This option simply changes the state of the backup in the database with no regard to actual status; exercise caution when using)
              completion:
                flag:
                    state:
                        - available
                        - error
              documentation:
                flag:
                    description: New backup description (supported by --os-volume-api-version 3.9 or above)
                    name: New backup name (supported by --os-volume-api-version 3.9 or above)
                    no-property: Remove all properties from this backup (specify both --no-property and --property to remove the current properties before setting new properties)
                    property: Set a property on this backup (repeat option to set multiple values) (supported by --os-volume-api-version 3.43 or above)
                    state: New backup state ("available" or "error") (admin only) (This option simply changes the state of the backup in the database with no regard to actual status; exercise caution when using)
            - name: show
              description: Display volume backup details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset volume backup properties.
              flags:
                --property=: 'Property to remove from this backup (repeat option to unset multiple values) '
              documentation:
                flag:
                    property: 'Property to remove from this backup (repeat option to unset multiple values) '
        - name: create
          description: Create new volume
          flags:
            --availability-zone=: Create volume in <availability-zone>
            --backup=: Restore backup to a volume (name or ID) (supported by --os-volume-api-version 3.47 or later)
            --bootable: Mark volume as bootable
            --cluster=: 'Cinder cluster on which the existing volume resides; takes the form: cluster@backend-name#pool.'
            --consistency-group=: Consistency group where the new volume belongs to
            --description=: Volume description
            --fit-width: Fit the table to the display width.
            --hint=: Arbitrary scheduler hint key-value pairs to help creating a volume.
            --host=: 'Cinder host on which the existing volume resides; takes the form: host@backend-name#pool.'
            --image=: Use <image> as source of volume (name or ID)
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --non-bootable: Mark volume as non-bootable (default)
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --property=: Set a property to this volume (repeat option to set multiple properties)
            --read-only: Set volume to read-only access mode
            --read-write: Set volume to read-write access mode (default)
            --remote-source=: 'The attribute(s) of the existing remote volume (admin required) (repeat option to specify multiple attributes, e.g.: ''--remote-source source-name=test_name --remote-source source-id=test_id'')'
            --size=: Volume size in GB (required unless --snapshot or --source specified)
            --snapshot=: Use <snapshot> as source of volume (name or ID)
            --source-replicated=: ==SUPPRESS==
            --source=: Volume to clone (name or ID)
            --type=: Set the type of volume
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                availability-zone: Create volume in <availability-zone>
                backup: Restore backup to a volume (name or ID) (supported by --os-volume-api-version 3.47 or later)
                bootable: Mark volume as bootable
                cluster: 'Cinder cluster on which the existing volume resides; takes the form: cluster@backend-name#pool. This is only used along with the --remote-source option. (supported by --os-volume-api-version 3.16 or above)'
                column: specify the column(s) to include, can be repeated to show multiple columns
                consistency-group: Consistency group where the new volume belongs to
                description: Volume description
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                hint: Arbitrary scheduler hint key-value pairs to help creating a volume. Repeat the option to set multiple hints. 'same_host' and 'different_host' get values appended when repeated, all other keys take the last given value
                host: 'Cinder host on which the existing volume resides; takes the form: host@backend-name#pool. This is only used along with the --remote-source option.'
                image: Use <image> as source of volume (name or ID)
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                non-bootable: Mark volume as non-bootable (default)
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                property: Set a property to this volume (repeat option to set multiple properties)
                read-only: Set volume to read-only access mode
                read-write: Set volume to read-write access mode (default)
                remote-source: 'The attribute(s) of the existing remote volume (admin required) (repeat option to specify multiple attributes, e.g.: ''--remote-source source-name=test_name --remote-source source-id=test_id'')'
                size: Volume size in GB (required unless --snapshot or --source specified)
                snapshot: Use <snapshot> as source of volume (name or ID)
                source: Volume to clone (name or ID)
                source-replicated: ==SUPPRESS==
                type: Set the type of volume
                variable: ==SUPPRESS==
        - name: delete
          description: Delete volume(s)
          flags:
            --cascade: Remove any snapshots along with volume(s) (defaults to False)
            --force: Attempt forced removal of volume(s), regardless of state (defaults to False)
            --purge: ==SUPPRESS==
            --remote: Specify this parameter to unmanage a volume.
          documentation:
            flag:
                cascade: Remove any snapshots along with volume(s) (defaults to False)
                force: Attempt forced removal of volume(s), regardless of state (defaults to False)
                purge: ==SUPPRESS==
                remote: Specify this parameter to unmanage a volume.
        - name: group
          commands:
            - name: create
              description: Create a volume group.
              flags:
                --availability-zone=: Availability zone for volume group.
                --description=: Description of a volume group.
                --fit-width: Fit the table to the display width.
                --group-snapshot=: Existing group snapshot to use (name or ID) (supported by --os-volume-api-version 3.14 or later)
                --max-width=: Maximum display width, <1 to disable.
                --name=: Name of the volume group.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --source-group=: Existing volume group to use (name or ID) (supported by --os-volume-api-version 3.14 or later)
                --variable=: ==SUPPRESS==
                --volume-group-type=: Volume group type to use (name or ID)
                --volume-type=: Volume type(s) to use (name or ID) (required with --volume-group-type)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    availability-zone: Availability zone for volume group. (not available if creating group from source)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of a volume group.
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    group-snapshot: Existing group snapshot to use (name or ID) (supported by --os-volume-api-version 3.14 or later)
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Name of the volume group.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    source-group: Existing volume group to use (name or ID) (supported by --os-volume-api-version 3.14 or later)
                    variable: ==SUPPRESS==
                    volume-group-type: Volume group type to use (name or ID)
                    volume-type: Volume type(s) to use (name or ID) (required with --volume-group-type)
            - name: delete
              description: Delete a volume group.
              flags:
                --force: Delete the volume group even if it contains volumes.
              documentation:
                flag:
                    force: Delete the volume group even if it contains volumes. This will delete any remaining volumes in the group.
            - name: failover
              description: Failover replication for a volume group.
              flags:
                --allow-attached-volume: Allow group with attached volumes to be failed over.
                --disallow-attached-volume: Disallow group with attached volumes to be failed over.
                --secondary-backend-id=: Secondary backend ID.
              documentation:
                flag:
                    allow-attached-volume: Allow group with attached volumes to be failed over.
                    disallow-attached-volume: Disallow group with attached volumes to be failed over.
                    secondary-backend-id: Secondary backend ID.
            - name: list
              description: Lists all volume groups.
              flags:
                --all-projects: Shows details for all projects (admin only).
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    all-projects: Shows details for all projects (admin only).
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Update a volume group.
              flags:
                --description=: New description for group.
                --disable-replication: Disable replication for group.
                --enable-replication: Enable replication for group.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --name=: New name for group.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: New description for group.
                    disable-replication: Disable replication for group. (supported by --os-volume-api-version 3.38 or above)
                    enable-replication: Enable replication for group. (supported by --os-volume-api-version 3.38 or above)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: New name for group.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: show
              description: Show detailed information for a volume group.
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --no-replication-targets: Do not show replication targets for the group.
                --no-volumes: Do not show volumes included in the group.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --replication-targets: Show replication targets for the group.
                --variable=: ==SUPPRESS==
                --volumes: Show volumes included in the group.
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    no-replication-targets: Do not show replication targets for the group. (supported by --os-volume-api-version 3.38 or above)
                    no-volumes: Do not show volumes included in the group. (supported by --os-volume-api-version 3.25 or above)
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    replication-targets: Show replication targets for the group. (supported by --os-volume-api-version 3.38 or above)
                    variable: ==SUPPRESS==
                    volumes: Show volumes included in the group. (supported by --os-volume-api-version 3.25 or above)
            - name: snapshot
              commands:
                - name: create
                  description: Create a volume group snapshot.
                  flags:
                    --description=: Description of a volume group snapshot.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: Name of the volume group snapshot.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description of a volume group snapshot.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: Name of the volume group snapshot.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a volume group snapshot.
                - name: list
                  description: Lists all volume group snapshot.
                  flags:
                    --all-projects: Shows details for all projects (admin only).
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        all-projects: Shows details for all projects (admin only).
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: show
                  description: Show detailed information for a volume group snapshot.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: type
              commands:
                - name: create
                  description: Create a volume group type.
                  flags:
                    --description=: Description of the volume group type.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --private: Volume group type is not available to other projects
                    --public: Volume group type is available to other projects (default)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description of the volume group type.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        private: Volume group type is not available to other projects
                        public: Volume group type is available to other projects (default)
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete a volume group type.
                - name: list
                  description: Lists all volume group types.
                  flags:
                    --default: List the default volume group type.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        default: List the default volume group type.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Update a volume group type.
                  flags:
                    --description=: New description for volume group type.
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: New name for volume group type.
                    --no-property: Remove all properties from this volume group type (specify both --no-property and --property to remove the current properties before setting new properties)
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --private: Make volume group type unavailable to other projects.
                    --property=: Property to add or modify for this volume group type (repeat option to set multiple properties)
                    --public: Make volume group type available to other projects.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: New description for volume group type.
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: New name for volume group type.
                        no-property: Remove all properties from this volume group type (specify both --no-property and --property to remove the current properties before setting new properties)
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        private: Make volume group type unavailable to other projects.
                        property: Property to add or modify for this volume group type (repeat option to set multiple properties)
                        public: Make volume group type available to other projects.
                        variable: ==SUPPRESS==
                - name: show
                  description: Show detailed information for a volume group type.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
        - name: host
          commands:
            - name: failover
              description: Failover volume host to different backend
              flags:
                --volume-backend=!: The ID of the volume backend replication target where the host will failover to (required)
              documentation:
                flag:
                    volume-backend: The ID of the volume backend replication target where the host will failover to (required)
            - name: set
              description: Set volume host properties
              flags:
                --disable: Freeze and disable the specified volume host
                --enable: Thaw and enable the specified volume host
              documentation:
                flag:
                    disable: Freeze and disable the specified volume host
                    enable: Thaw and enable the specified volume host
        - name: list
          description: List volumes
          flags:
            --all-projects: Include all projects (admin only)
            --fit-width: Fit the table to the display width.
            --limit=: The maximum number of entries to return per page.
            --long: List additional fields in output
            --marker=: The first position in the collection to return results from.
            --max-width=: Maximum display width, <1 to disable.
            --name=: Filter results by volume name
            --noindent: whether to disable indenting the JSON
            --print-empty: Print empty table if there is no data to show.
            --project-domain=: Domain the project belongs to (name or ID).
            --project=: Filter results by project (name or ID) (admin only)
            --property=: 'Filter by a property on the volume list (repeat option to filter by multiple properties) '
            --quote=: when to include quotes, defaults to nonnumeric
            --sort-ascending: sort the column(s) in ascending order
            --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
            --sort-descending: sort the column(s) in descending order
            --status=: Filter results by status
            --user-domain=: Domain the user belongs to (name or ID).
            --user=: Filter results by user (name or ID) (admin only)
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - csv
                    - json
                    - table
                    - value
                    - yaml
                quote:
                    - all
                    - minimal
                    - none
                    - nonnumeric
          documentation:
            flag:
                all-projects: Include all projects (admin only)
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                long: List additional fields in output
                marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                name: Filter results by volume name
                noindent: whether to disable indenting the JSON
                print-empty: Print empty table if there is no data to show.
                project: Filter results by project (name or ID) (admin only)
                project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                property: 'Filter by a property on the volume list (repeat option to filter by multiple properties) '
                quote: when to include quotes, defaults to nonnumeric
                sort-ascending: sort the column(s) in ascending order
                sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                sort-descending: sort the column(s) in descending order
                status: Filter results by status
                user: Filter results by user (name or ID) (admin only)
                user-domain: Domain the user belongs to (name or ID). This can be used in case collisions between user names exist.
        - name: message
          commands:
            - name: delete
              description: Delete a volume failure message
            - name: list
              description: List volume failure messages
              flags:
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --marker=: The first position in the collection to return results from.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter results by project (name or ID) (admin only)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Filter results by project (name or ID) (admin only)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: show
              description: Show a volume failure message
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
        - name: migrate
          description: Migrate volume to a new host
          flags:
            --cluster=: Destination cluster to migrate the volume to (requires --os-volume-api-version 3.16 or higher)
            --force-host-copy: Enable generic host-based force-migration, which bypasses driver optimizations
            --host=: 'Destination host (takes the form: host@backend-name#pool)'
            --lock-volume: If specified, the volume state will be locked and will not allow a migration to be aborted (possibly by another operation)
          documentation:
            flag:
                cluster: Destination cluster to migrate the volume to (requires --os-volume-api-version 3.16 or higher)
                force-host-copy: Enable generic host-based force-migration, which bypasses driver optimizations
                host: 'Destination host (takes the form: host@backend-name#pool)'
                lock-volume: If specified, the volume state will be locked and will not allow a migration to be aborted (possibly by another operation)
        - name: qos
          commands:
            - name: associate
              description: Associate a QoS specification to a volume type
            - name: create
              description: Create new QoS specification
              flags:
                --consumer=: Consumer of the QoS.
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --property=: Set a QoS specification property (repeat option to set multiple properties)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    consumer:
                        - front-end
                        - back-end
                        - both
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    consumer: 'Consumer of the QoS. Valid consumers: back-end, both, front-end (defaults to ''both'')'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    property: Set a QoS specification property (repeat option to set multiple properties)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete QoS specification
              flags:
                --force: Allow to delete in-use QoS specification(s)
              documentation:
                flag:
                    force: Allow to delete in-use QoS specification(s)
            - name: disassociate
              description: Disassociate a QoS specification from a volume type
              flags:
                --all: Disassociate the QoS from every volume type
                --volume-type=: Volume type to disassociate the QoS from (name or ID)
              documentation:
                flag:
                    all: Disassociate the QoS from every volume type
                    volume-type: Volume type to disassociate the QoS from (name or ID)
            - name: list
              description: List QoS specifications
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set QoS specification properties
              flags:
                --no-property: Remove all properties from <qos-spec> (specify both --no-property and --property to remove the current properties before setting new properties)
                --property=: Property to add or modify for this QoS specification (repeat option to set multiple properties)
              documentation:
                flag:
                    no-property: Remove all properties from <qos-spec> (specify both --no-property and --property to remove the current properties before setting new properties)
                    property: Property to add or modify for this QoS specification (repeat option to set multiple properties)
            - name: show
              description: Display QoS specification details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset QoS specification properties
              flags:
                --property=: Property to remove from the QoS specification.
              documentation:
                flag:
                    property: Property to remove from the QoS specification. (repeat option to unset multiple properties)
        - name: revert
          description: Revert a volume to a snapshot.
        - name: service
          commands:
            - name: list
              description: List service command
              flags:
                --fit-width: Fit the table to the display width.
                --host=: List services on specified host (name only)
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --service=: List only specified service (name only)
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    host: List services on specified host (name only)
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    service: List only specified service (name only)
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set volume service properties
              flags:
                --disable: Disable volume service
                --disable-reason=: Reason for disabling the service (should be used with --disable option)
                --enable: Enable volume service
              documentation:
                flag:
                    disable: Disable volume service
                    disable-reason: Reason for disabling the service (should be used with --disable option)
                    enable: Enable volume service
        - name: set
          description: Set volume properties
          flags:
            --attached: Set volume attachment status to "attached" (admin only) (This option simply changes the state of the volume in the database with no regard to actual status, exercise caution when using)
            --bootable: Mark volume as bootable
            --description=: New volume description
            --detached: Set volume attachment status to "detached" (admin only) (This option simply changes the state of the volume in the database with no regard to actual status, exercise caution when using)
            --image-property=: Set an image property on this volume (repeat option to set multiple image properties)
            --migration-policy=: Migration policy while re-typing volume ("never" or "on-demand", default is "never" ) (available only when --type option is specified)
            --name=: New volume name
            --no-property: Remove all properties from <volume> (specify both --no-property and --property to remove the current properties before setting new properties.)
            --non-bootable: Mark volume as non-bootable
            --property=: Set a property on this volume (repeat option to set multiple properties)
            --read-only: Set volume to read-only access mode
            --read-write: Set volume to read-write access mode
            --retype-policy=: ==SUPPRESS==
            --size=: Extend volume size in GB
            --state=: New volume state ("available", "error", "creating", "deleting", "in-use", "attaching", "detaching", "error_deleting" or "maintenance") (admin only) (This option simply changes the state of the volume in the database with no regard to actual status, exercise caution when using)
            --type=: New volume type (name or ID)
          completion:
            flag:
                migration-policy:
                    - never
                    - on-demand
                retype-policy:
                    - never
                    - on-demand
                state:
                    - available
                    - error
                    - creating
                    - deleting
                    - in-use
                    - attaching
                    - detaching
                    - error_deleting
                    - maintenance
          documentation:
            flag:
                attached: Set volume attachment status to "attached" (admin only) (This option simply changes the state of the volume in the database with no regard to actual status, exercise caution when using)
                bootable: Mark volume as bootable
                description: New volume description
                detached: Set volume attachment status to "detached" (admin only) (This option simply changes the state of the volume in the database with no regard to actual status, exercise caution when using)
                image-property: Set an image property on this volume (repeat option to set multiple image properties)
                migration-policy: Migration policy while re-typing volume ("never" or "on-demand", default is "never" ) (available only when --type option is specified)
                name: New volume name
                no-property: Remove all properties from <volume> (specify both --no-property and --property to remove the current properties before setting new properties.)
                non-bootable: Mark volume as non-bootable
                property: Set a property on this volume (repeat option to set multiple properties)
                read-only: Set volume to read-only access mode
                read-write: Set volume to read-write access mode
                retype-policy: ==SUPPRESS==
                size: Extend volume size in GB
                state: New volume state ("available", "error", "creating", "deleting", "in-use", "attaching", "detaching", "error_deleting" or "maintenance") (admin only) (This option simply changes the state of the volume in the database with no regard to actual status, exercise caution when using)
                type: New volume type (name or ID)
        - name: show
          description: Display volume details
          flags:
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: snapshot
          commands:
            - name: create
              description: Create new volume snapshot
              flags:
                --description=: Description of the snapshot
                --fit-width: Fit the table to the display width.
                --force: Allow snapshot of in-use (attached) volume.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --property=: Set a property to this snapshot (repeat option to set multiple properties)
                --remote-source=: 'The attribute(s) of the existing remote volume snapshot (admin required) (repeat option to specify multiple attributes) e.g.: ''--remote-source source-name=test_name --remote-source source-id=test_id'''
                --variable=: ==SUPPRESS==
                --volume=: Volume to snapshot (name or ID) (default is <snapshot-name>)
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description of the snapshot
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    force: Allow snapshot of in-use (attached) volume. Only needed for microversions prior to 3.66; ignored for 3.66+
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    property: Set a property to this snapshot (repeat option to set multiple properties)
                    remote-source: 'The attribute(s) of the existing remote volume snapshot (admin required) (repeat option to specify multiple attributes) e.g.: ''--remote-source source-name=test_name --remote-source source-id=test_id'''
                    variable: ==SUPPRESS==
                    volume: Volume to snapshot (name or ID) (default is <snapshot-name>)
            - name: delete
              description: Delete volume snapshot(s)
              flags:
                --force: Attempt forced removal of snapshot(s), regardless of state (defaults to False)
                --remote: Unmanage the snapshot, removing it from the Block Storage service management but not from the backend.
              documentation:
                flag:
                    force: Attempt forced removal of snapshot(s), regardless of state (defaults to False)
                    remote: Unmanage the snapshot, removing it from the Block Storage service management but not from the backend.
            - name: list
              description: List volume snapshots
              flags:
                --all-projects: Include all projects (admin only)
                --fit-width: Fit the table to the display width.
                --limit=: The maximum number of entries to return per page.
                --long: List additional fields in output
                --marker=: The first position in the collection to return results from.
                --max-items=: The maximum number of entries to return in total, paging through multiple requests if needed.
                --max-width=: Maximum display width, <1 to disable.
                --name=: Filters results by a name.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Filter results by project (name or ID) (admin only)
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                --status=: Filters results by a status.
                --volume=: Filters results by a volume (name or ID).
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
                    status:
                        - available
                        - error
                        - creating
                        - deleting
                        - error_deleting
              documentation:
                flag:
                    all-projects: Include all projects (admin only)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    limit: The maximum number of entries to return per page. If the value exceeds the server-defined maximum, then the server-defined value will be used. Note that this controls the page size, not the total number of entries returned. Use --max-items to limit the total number of entries returned.
                    long: List additional fields in output
                    marker: The first position in the collection to return results from. This should be a value that was returned in a previous request.
                    max-items: The maximum number of entries to return in total, paging through multiple requests if needed. Use --limit to control the page size.
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    name: Filters results by a name.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    project: Filter results by project (name or ID) (admin only)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
                    status: Filters results by a status. ('available', 'error', 'creating', 'deleting' or 'error_deleting')
                    volume: Filters results by a volume (name or ID).
            - name: set
              description: Set volume snapshot properties
              flags:
                --description=: New snapshot description
                --name=: New snapshot name
                --no-property: Remove all properties from <snapshot> (specify both --no-property and --property to remove the current properties before setting new properties.)
                --property=: Property to add/change for this snapshot (repeat option to set multiple properties)
                --state=: New snapshot state.
              completion:
                flag:
                    state:
                        - available
                        - error
                        - creating
                        - deleting
                        - error_deleting
              documentation:
                flag:
                    description: New snapshot description
                    name: New snapshot name
                    no-property: Remove all properties from <snapshot> (specify both --no-property and --property to remove the current properties before setting new properties.)
                    property: Property to add/change for this snapshot (repeat option to set multiple properties)
                    state: New snapshot state. ("available", "error", "creating", "deleting", or "error_deleting") (admin only) (This option simply changes the state of the snapshot in the database with no regard to actual status, exercise caution when using)
            - name: show
              description: Display volume snapshot details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset volume snapshot properties
              flags:
                --property=: Property to remove from snapshot (repeat option to remove multiple properties)
              documentation:
                flag:
                    property: Property to remove from snapshot (repeat option to remove multiple properties)
        - name: summary
          description: Show a summary of all volumes in this deployment.
          flags:
            --all-projects: Include all projects (admin only)
            --fit-width: Fit the table to the display width.
            --max-width=: Maximum display width, <1 to disable.
            --noindent: whether to disable indenting the JSON
            --prefix=: add a prefix to all variable names
            --print-empty: Print empty table if there is no data to show.
            --variable=: ==SUPPRESS==
            -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
            -f, --format=: the output format, defaults to table
          completion:
            flag:
                format:
                    - json
                    - shell
                    - table
                    - value
                    - yaml
          documentation:
            flag:
                all-projects: Include all projects (admin only)
                column: specify the column(s) to include, can be repeated to show multiple columns
                fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                format: the output format, defaults to table
                max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                noindent: whether to disable indenting the JSON
                prefix: add a prefix to all variable names
                print-empty: Print empty table if there is no data to show.
                variable: ==SUPPRESS==
        - name: transfer
          commands:
            - name: request
              commands:
                - name: accept
                  description: Accept volume transfer request.
                  flags:
                    --auth-key=!: Volume transfer request authentication key
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        auth-key: Volume transfer request authentication key
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
                - name: create
                  description: Create volume transfer request.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --name=: New transfer request name (default to None)
                    --no-snapshots: Do not transfer volume snapshots (supported by --os-volume-api-version 3.55 or later)
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --snapshots: Also transfer volume snapshots (default) (supported by --os-volume-api-version 3.55 or later)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        name: New transfer request name (default to None)
                        no-snapshots: Do not transfer volume snapshots (supported by --os-volume-api-version 3.55 or later)
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        snapshots: Also transfer volume snapshots (default) (supported by --os-volume-api-version 3.55 or later)
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete volume transfer request(s).
                - name: list
                  description: Lists all volume transfer requests.
                  flags:
                    --all-projects: Include all projects (admin only)
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        all-projects: Include all projects (admin only)
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: show
                  description: Show volume transfer request details.
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
        - name: type
          commands:
            - name: create
              description: Create new volume type
              flags:
                --availability-zone=: Set an availability zone for this volume type (this is an alias for '--property RESKEY:availability_zones:<az>') (repeat option to set multiple availability zones)
                --cacheable: Enable caching for this volume type (this is an alias for '--property cacheable=<is> True') (requires driver support)
                --description=: Volume type description
                --encryption-cipher=: Set the encryption algorithm or mode for this volume type (e.g "aes-xts-plain64") (admin only)
                --encryption-control-location=: Set the notional service where the encryption is performed ("front-end" or "back-end") (admin only) (The default value for this option is "front-end" when setting encryption type of a volume.
                --encryption-key-size=: Set the size of the encryption key of this volume type (e.g "128" or "256") (admin only)
                --encryption-provider=: 'Set the encryption provider format for this volume type (e.g "luks" or "plain") (admin only) (this option is required when setting encryption type of a volume; consider using other encryption options such as: "--encryption-cipher", "--encryption-key-size" and "--encryption-control-location")'
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --multiattach: Enable multi-attach for this volume type (this is an alias for '--property multiattach=<is> True') (requires driver support)
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --private: Volume type is not accessible to the public
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Allow <project> to access private type (name or ID) (must be used with --private option)
                --property=: Set a property on this volume type (repeat option to set multiple properties)
                --public: Volume type is accessible to the public
                --replicated: Enabled replication for this volume type (this is an alias for '--property replication_enabled=<is> True') (requires driver support)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    encryption-control-location:
                        - front-end
                        - back-end
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    availability-zone: Set an availability zone for this volume type (this is an alias for '--property RESKEY:availability_zones:<az>') (repeat option to set multiple availability zones)
                    cacheable: Enable caching for this volume type (this is an alias for '--property cacheable=<is> True') (requires driver support)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Volume type description
                    encryption-cipher: Set the encryption algorithm or mode for this volume type (e.g "aes-xts-plain64") (admin only)
                    encryption-control-location: 'Set the notional service where the encryption is performed ("front-end" or "back-end") (admin only) (The default value for this option is "front-end" when setting encryption type of a volume. Consider using other encryption options such as: "--encryption-cipher", "--encryption-key-size" and "--encryption-provider")'
                    encryption-key-size: Set the size of the encryption key of this volume type (e.g "128" or "256") (admin only)
                    encryption-provider: 'Set the encryption provider format for this volume type (e.g "luks" or "plain") (admin only) (this option is required when setting encryption type of a volume; consider using other encryption options such as: "--encryption-cipher", "--encryption-key-size" and "--encryption-control-location")'
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    multiattach: Enable multi-attach for this volume type (this is an alias for '--property multiattach=<is> True') (requires driver support)
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    private: Volume type is not accessible to the public
                    project: Allow <project> to access private type (name or ID) (must be used with --private option)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    property: Set a property on this volume type (repeat option to set multiple properties)
                    public: Volume type is accessible to the public
                    replicated: Enabled replication for this volume type (this is an alias for '--property replication_enabled=<is> True') (requires driver support)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete volume type(s)
            - name: list
              description: List volume types
              flags:
                --availability-zone=: List only volume types with this availability configured (this is an alias for '--property RESKEY:availability_zones:<az>') (repeat option to filter on multiple availability zones)
                --cacheable: List only volume types with caching enabled (this is an alias for '--property cacheable=<is> True') (admin only) (supported by --os-volume-api-version 3.52 or above)
                --default: List the default volume type
                --encryption-type: Display encryption information for each volume type (admin only)
                --fit-width: Fit the table to the display width.
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --multiattach: List only volume types with multi-attach enabled (this is an alias for '--property multiattach=<is> True') (supported by --os-volume-api-version 3.52 or above)
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --private: List only private types (admin only)
                --property=: Filter by a property on the volume types (repeat option to filter by multiple properties) (admin only except for user-visible extra specs) (supported by --os-volume-api-version 3.52 or above)
                --public: List only public types
                --quote=: when to include quotes, defaults to nonnumeric
                --replicated: List only volume types with replication enabled (this is an alias for '--property replication_enabled=<is> True') (supported by --os-volume-api-version 3.52 or above)
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    availability-zone: List only volume types with this availability configured (this is an alias for '--property RESKEY:availability_zones:<az>') (repeat option to filter on multiple availability zones)
                    cacheable: List only volume types with caching enabled (this is an alias for '--property cacheable=<is> True') (admin only) (supported by --os-volume-api-version 3.52 or above)
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    default: List the default volume type
                    encryption-type: Display encryption information for each volume type (admin only)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    multiattach: List only volume types with multi-attach enabled (this is an alias for '--property multiattach=<is> True') (supported by --os-volume-api-version 3.52 or above)
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    private: List only private types (admin only)
                    property: Filter by a property on the volume types (repeat option to filter by multiple properties) (admin only except for user-visible extra specs) (supported by --os-volume-api-version 3.52 or above)
                    public: List only public types
                    quote: when to include quotes, defaults to nonnumeric
                    replicated: List only volume types with replication enabled (this is an alias for '--property replication_enabled=<is> True') (supported by --os-volume-api-version 3.52 or above)
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set volume type properties
              flags:
                --availability-zone=: Set an availability zone for this volume type (this is an alias for '--property RESKEY:availability_zones:<az>') (repeat option to set multiple availability zones)
                --cacheable: Enable caching for this volume type (this is an alias for '--property cacheable=<is> True') (requires driver support)
                --description=: Set volume type description
                --encryption-cipher=: Set the encryption algorithm or mode for this volume type (e.g "aes-xts-plain64") (admin only)
                --encryption-control-location=: Set the notional service where the encryption is performed ("front-end" or "back-end") (admin only) (The default value for this option is "front-end" when setting encryption type of a volume for the first time.
                --encryption-key-size=: Set the size of the encryption key of this volume type (e.g "128" or "256") (admin only)
                --encryption-provider=: Set the encryption provider format for this volume type (e.g "luks" or "plain") (admin only) (This option is required when setting encryption type of a volume for the first time.
                --multiattach: Enable multi-attach for this volume type (this is an alias for '--property multiattach=<is> True') (requires driver support)
                --name=: Set volume type name
                --private: Volume type is not accessible to the public
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Set volume type access to project (name or ID) (admin only)
                --property=: Set a property on this volume type (repeat option to set multiple properties)
                --public: Volume type is accessible to the public
                --replicated: Enabled replication for this volume type (this is an alias for '--property replication_enabled=<is> True') (requires driver support)
              completion:
                flag:
                    encryption-control-location:
                        - front-end
                        - back-end
              documentation:
                flag:
                    availability-zone: Set an availability zone for this volume type (this is an alias for '--property RESKEY:availability_zones:<az>') (repeat option to set multiple availability zones)
                    cacheable: Enable caching for this volume type (this is an alias for '--property cacheable=<is> True') (requires driver support)
                    description: Set volume type description
                    encryption-cipher: Set the encryption algorithm or mode for this volume type (e.g "aes-xts-plain64") (admin only)
                    encryption-control-location: 'Set the notional service where the encryption is performed ("front-end" or "back-end") (admin only) (The default value for this option is "front-end" when setting encryption type of a volume for the first time. Consider using other encryption options such as: "--encryption-cipher", "--encryption-key-size" and "--encryption-provider")'
                    encryption-key-size: Set the size of the encryption key of this volume type (e.g "128" or "256") (admin only)
                    encryption-provider: 'Set the encryption provider format for this volume type (e.g "luks" or "plain") (admin only) (This option is required when setting encryption type of a volume for the first time. Consider using other encryption options such as: "--encryption-cipher", "--encryption-key-size" and "--encryption-control-location")'
                    multiattach: Enable multi-attach for this volume type (this is an alias for '--property multiattach=<is> True') (requires driver support)
                    name: Set volume type name
                    private: Volume type is not accessible to the public
                    project: Set volume type access to project (name or ID) (admin only)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    property: Set a property on this volume type (repeat option to set multiple properties)
                    public: Volume type is accessible to the public
                    replicated: Enabled replication for this volume type (this is an alias for '--property replication_enabled=<is> True') (requires driver support)
            - name: show
              description: Display volume type details
              flags:
                --encryption-type: Display encryption information of this volume type (admin only)
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    encryption-type: Display encryption information of this volume type (admin only)
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
            - name: unset
              description: Unset volume type properties
              flags:
                --encryption-type: Remove the encryption type for this volume type (admin only)
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Removes volume type access to project (name or ID) (admin only)
                --property=: Remove a property from this volume type (repeat option to remove multiple properties)
              documentation:
                flag:
                    encryption-type: Remove the encryption type for this volume type (admin only)
                    project: Removes volume type access to project (name or ID) (admin only)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    property: Remove a property from this volume type (repeat option to remove multiple properties)
        - name: unset
          description: Unset volume properties
          flags:
            --image-property=: Remove an image property from volume (repeat option to remove multiple image properties)
            --property=: Remove a property from volume (repeat option to remove multiple properties)
          documentation:
            flag:
                image-property: Remove an image property from volume (repeat option to remove multiple image properties)
                property: Remove a property from volume (repeat option to remove multiple properties)
    - name: vpn
      commands:
        - name: endpoint
          commands:
            - name: group
              commands:
                - name: create
                  description: Create an endpoint group
                  flags:
                    --description=: Description for the endpoint group
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --type=!: Type of endpoints in group (e.g. subnet, cidr, network, router).
                    --value=!: Endpoint(s) for the group.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                        type:
                            - subnet
                            - cidr
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description for the endpoint group
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        type: Type of endpoints in group (e.g. subnet, cidr, network, router). Currently only subnet and cidr are supported.
                        value: Endpoint(s) for the group. Must all be of the same type. (--value) option can be repeated
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete endpoint group(s)
                - name: list
                  description: List endpoint groups that belong to a given project
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set endpoint group properties
                  flags:
                    --description=: Description for the endpoint group
                    --name=: Set a name for the endpoint group
                  documentation:
                    flag:
                        description: Description for the endpoint group
                        name: Set a name for the endpoint group
                - name: show
                  description: Display endpoint group details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
        - name: ike
          commands:
            - name: policy
              commands:
                - name: create
                  description: Create an IKE policy
                  flags:
                    --auth-algorithm=: Authentication algorithm
                    --description=: Description of the IKE policy
                    --encryption-algorithm=: Encryption algorithm
                    --fit-width: Fit the table to the display width.
                    --ike-version=: IKE version for the policy
                    --lifetime=: IKE lifetime attributes.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --pfs=: Perfect Forward Secrecy
                    --phase1-negotiation-mode=: IKE Phase1 negotiation mode
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        auth-algorithm:
                            - sha1
                            - sha256
                            - sha384
                            - sha512
                            - aes-xcbc
                            - aes-cmac
                        encryption-algorithm:
                            - 3des
                            - aes-128
                            - aes-192
                            - aes-256
                            - aes-128-ccm-8
                            - aes-192-ccm-8
                            - aes-256-ccm-8
                            - aes-128-ccm-12
                            - aes-192-ccm-12
                            - aes-256-ccm-12
                            - aes-128-ccm-16
                            - aes-192-ccm-16
                            - aes-256-ccm-16
                            - aes-128-gcm-8
                            - aes-192-gcm-8
                            - aes-256-gcm-8
                            - aes-128-gcm-12
                            - aes-192-gcm-12
                            - aes-256-gcm-12
                            - aes-128-gcm-16
                            - aes-192-gcm-16
                            - aes-256-gcm-16
                            - aes-128-ctr
                            - aes-192-ctr
                            - aes-256-ctr
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                        ike-version:
                            - v1
                            - v2
                        pfs:
                            - group2
                            - group5
                            - group14
                            - group15
                            - group16
                            - group17
                            - group18
                            - group19
                            - group20
                            - group21
                            - group22
                            - group23
                            - group24
                            - group25
                            - group26
                            - group27
                            - group28
                            - group29
                            - group30
                            - group31
                        phase1-negotiation-mode:
                            - main
                            - aggressive
                  documentation:
                    flag:
                        auth-algorithm: Authentication algorithm
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description of the IKE policy
                        encryption-algorithm: Encryption algorithm
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        ike-version: IKE version for the policy
                        lifetime: IKE lifetime attributes. 'units'-seconds, default:seconds. 'value'-non negative integer, default:3600.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        pfs: Perfect Forward Secrecy
                        phase1-negotiation-mode: IKE Phase1 negotiation mode
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete IKE policy (policies)
                - name: list
                  description: List IKE policies that belong to a given project
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set IKE policy properties
                  flags:
                    --auth-algorithm=: Authentication algorithm
                    --description=: Description of the IKE policy
                    --encryption-algorithm=: Encryption algorithm
                    --ike-version=: IKE version for the policy
                    --lifetime=: IKE lifetime attributes.
                    --name=: Name of the IKE policy
                    --pfs=: Perfect Forward Secrecy
                    --phase1-negotiation-mode=: IKE Phase1 negotiation mode
                  completion:
                    flag:
                        auth-algorithm:
                            - sha1
                            - sha256
                            - sha384
                            - sha512
                            - aes-xcbc
                            - aes-cmac
                        encryption-algorithm:
                            - 3des
                            - aes-128
                            - aes-192
                            - aes-256
                            - aes-128-ccm-8
                            - aes-192-ccm-8
                            - aes-256-ccm-8
                            - aes-128-ccm-12
                            - aes-192-ccm-12
                            - aes-256-ccm-12
                            - aes-128-ccm-16
                            - aes-192-ccm-16
                            - aes-256-ccm-16
                            - aes-128-gcm-8
                            - aes-192-gcm-8
                            - aes-256-gcm-8
                            - aes-128-gcm-12
                            - aes-192-gcm-12
                            - aes-256-gcm-12
                            - aes-128-gcm-16
                            - aes-192-gcm-16
                            - aes-256-gcm-16
                            - aes-128-ctr
                            - aes-192-ctr
                            - aes-256-ctr
                        ike-version:
                            - v1
                            - v2
                        pfs:
                            - group2
                            - group5
                            - group14
                            - group15
                            - group16
                            - group17
                            - group18
                            - group19
                            - group20
                            - group21
                            - group22
                            - group23
                            - group24
                            - group25
                            - group26
                            - group27
                            - group28
                            - group29
                            - group30
                            - group31
                        phase1-negotiation-mode:
                            - main
                            - aggressive
                  documentation:
                    flag:
                        auth-algorithm: Authentication algorithm
                        description: Description of the IKE policy
                        encryption-algorithm: Encryption algorithm
                        ike-version: IKE version for the policy
                        lifetime: IKE lifetime attributes. 'units'-seconds, default:seconds. 'value'-non negative integer, default:3600.
                        name: Name of the IKE policy
                        pfs: Perfect Forward Secrecy
                        phase1-negotiation-mode: IKE Phase1 negotiation mode
                - name: show
                  description: Display IKE policy details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
        - name: ipsec
          commands:
            - name: policy
              commands:
                - name: create
                  description: Create an IPsec policy
                  flags:
                    --auth-algorithm=: Authentication algorithm for IPsec policy
                    --description=: Description of the IPsec policy
                    --encapsulation-mode=: Encapsulation mode for IPsec policy
                    --encryption-algorithm=: Encryption algorithm for IPsec policy
                    --fit-width: Fit the table to the display width.
                    --lifetime=: IPsec lifetime attributes.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --pfs=: Perfect Forward Secrecy for IPsec policy
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --project-domain=: Domain the project belongs to (name or ID).
                    --project=: Owner's project (name or ID)
                    --transform-protocol=: Transform protocol for IPsec policy
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        auth-algorithm:
                            - sha1
                            - sha256
                            - sha384
                            - sha512
                            - aes-xcbc
                            - aes-cmac
                        encapsulation-mode:
                            - tunnel
                            - transport
                        encryption-algorithm:
                            - 3des
                            - aes-128
                            - aes-192
                            - aes-256
                            - aes-128-ccm-8
                            - aes-192-ccm-8
                            - aes-256-ccm-8
                            - aes-128-ccm-12
                            - aes-192-ccm-12
                            - aes-256-ccm-12
                            - aes-128-ccm-16
                            - aes-192-ccm-16
                            - aes-256-ccm-16
                            - aes-128-gcm-8
                            - aes-192-gcm-8
                            - aes-256-gcm-8
                            - aes-128-gcm-12
                            - aes-192-gcm-12
                            - aes-256-gcm-12
                            - aes-128-gcm-16
                            - aes-192-gcm-16
                            - aes-256-gcm-16
                            - aes-128-ctr
                            - aes-192-ctr
                            - aes-256-ctr
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                        pfs:
                            - group2
                            - group5
                            - group14
                            - group15
                            - group16
                            - group17
                            - group18
                            - group19
                            - group20
                            - group21
                            - group22
                            - group23
                            - group24
                            - group25
                            - group26
                            - group27
                            - group28
                            - group29
                            - group30
                            - group31
                        transform-protocol:
                            - esp
                            - ah
                            - ah-esp
                  documentation:
                    flag:
                        auth-algorithm: Authentication algorithm for IPsec policy
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        description: Description of the IPsec policy
                        encapsulation-mode: Encapsulation mode for IPsec policy
                        encryption-algorithm: Encryption algorithm for IPsec policy
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        lifetime: IPsec lifetime attributes. 'units'-seconds, default:seconds. 'value'-non negative integer, default:3600.
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        pfs: Perfect Forward Secrecy for IPsec policy
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        project: Owner's project (name or ID)
                        project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                        transform-protocol: Transform protocol for IPsec policy
                        variable: ==SUPPRESS==
                - name: delete
                  description: Delete IPsec policy(policies)
                - name: list
                  description: List IPsec policies that belong to a given project
                  flags:
                    --fit-width: Fit the table to the display width.
                    --long: List additional fields in output
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --print-empty: Print empty table if there is no data to show.
                    --quote=: when to include quotes, defaults to nonnumeric
                    --sort-ascending: sort the column(s) in ascending order
                    --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    --sort-descending: sort the column(s) in descending order
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - csv
                            - json
                            - table
                            - value
                            - yaml
                        quote:
                            - all
                            - minimal
                            - none
                            - nonnumeric
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        long: List additional fields in output
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        print-empty: Print empty table if there is no data to show.
                        quote: when to include quotes, defaults to nonnumeric
                        sort-ascending: sort the column(s) in ascending order
                        sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        sort-descending: sort the column(s) in descending order
                - name: set
                  description: Set IPsec policy properties
                  flags:
                    --auth-algorithm=: Authentication algorithm for IPsec policy
                    --description=: Description of the IPsec policy
                    --encapsulation-mode=: Encapsulation mode for IPsec policy
                    --encryption-algorithm=: Encryption algorithm for IPsec policy
                    --lifetime=: IPsec lifetime attributes.
                    --name=: Name of the IPsec policy
                    --pfs=: Perfect Forward Secrecy for IPsec policy
                    --transform-protocol=: Transform protocol for IPsec policy
                  completion:
                    flag:
                        auth-algorithm:
                            - sha1
                            - sha256
                            - sha384
                            - sha512
                            - aes-xcbc
                            - aes-cmac
                        encapsulation-mode:
                            - tunnel
                            - transport
                        encryption-algorithm:
                            - 3des
                            - aes-128
                            - aes-192
                            - aes-256
                            - aes-128-ccm-8
                            - aes-192-ccm-8
                            - aes-256-ccm-8
                            - aes-128-ccm-12
                            - aes-192-ccm-12
                            - aes-256-ccm-12
                            - aes-128-ccm-16
                            - aes-192-ccm-16
                            - aes-256-ccm-16
                            - aes-128-gcm-8
                            - aes-192-gcm-8
                            - aes-256-gcm-8
                            - aes-128-gcm-12
                            - aes-192-gcm-12
                            - aes-256-gcm-12
                            - aes-128-gcm-16
                            - aes-192-gcm-16
                            - aes-256-gcm-16
                            - aes-128-ctr
                            - aes-192-ctr
                            - aes-256-ctr
                        pfs:
                            - group2
                            - group5
                            - group14
                            - group15
                            - group16
                            - group17
                            - group18
                            - group19
                            - group20
                            - group21
                            - group22
                            - group23
                            - group24
                            - group25
                            - group26
                            - group27
                            - group28
                            - group29
                            - group30
                            - group31
                        transform-protocol:
                            - esp
                            - ah
                            - ah-esp
                  documentation:
                    flag:
                        auth-algorithm: Authentication algorithm for IPsec policy
                        description: Description of the IPsec policy
                        encapsulation-mode: Encapsulation mode for IPsec policy
                        encryption-algorithm: Encryption algorithm for IPsec policy
                        lifetime: IPsec lifetime attributes. 'units'-seconds, default:seconds. 'value'-non negative integer, default:3600.
                        name: Name of the IPsec policy
                        pfs: Perfect Forward Secrecy for IPsec policy
                        transform-protocol: Transform protocol for IPsec policy
                - name: show
                  description: Display IPsec policy details
                  flags:
                    --fit-width: Fit the table to the display width.
                    --max-width=: Maximum display width, <1 to disable.
                    --noindent: whether to disable indenting the JSON
                    --prefix=: add a prefix to all variable names
                    --print-empty: Print empty table if there is no data to show.
                    --variable=: ==SUPPRESS==
                    -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                    -f, --format=: the output format, defaults to table
                  completion:
                    flag:
                        format:
                            - json
                            - shell
                            - table
                            - value
                            - yaml
                  documentation:
                    flag:
                        column: specify the column(s) to include, can be repeated to show multiple columns
                        fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                        format: the output format, defaults to table
                        max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                        noindent: whether to disable indenting the JSON
                        prefix: add a prefix to all variable names
                        print-empty: Print empty table if there is no data to show.
                        variable: ==SUPPRESS==
            - name: site
              commands:
                - name: connection
                  commands:
                    - name: create
                      description: Create an IPsec site connection
                      flags:
                        --description=: Description for the connection
                        --disable: Disable IPSec site connection
                        --dpd=: IPSec Connection Dead Peer Detection attributes.
                        --enable: Enable IPSec site connection
                        --fit-width: Fit the table to the display width.
                        --ikepolicy=!: IKE policy associated with this connection (name or ID)
                        --initiator=: Initiator state
                        --ipsecpolicy=!: IPsec policy associated with this connection (name or ID)
                        --local-endpoint-group=: Local endpoint group (name or ID) with subnet(s) for IPsec connection
                        --local-id=: An ID to be used instead of the external IP address for a virtual router
                        --max-width=: Maximum display width, <1 to disable.
                        --mtu=: MTU size for the connection
                        --noindent: whether to disable indenting the JSON
                        --peer-address=!: Peer gateway public IPv4/IPv6 address or FQDN
                        --peer-cidr=: Remote subnet(s) in CIDR format.
                        --peer-endpoint-group=: Peer endpoint group (name or ID) with CIDR(s) for IPSec connection
                        --peer-id=!: Peer router identity for authentication.
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --project-domain=: Domain the project belongs to (name or ID).
                        --project=: Owner's project (name or ID)
                        --psk=!: Pre-shared key string.
                        --variable=: ==SUPPRESS==
                        --vpnservice=!: VPN service instance associated with this connection (name or ID)
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                            initiator:
                                - bi-directional
                                - response-only
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            description: Description for the connection
                            disable: Disable IPSec site connection
                            dpd: IPSec Connection Dead Peer Detection attributes. 'action'-hold,clear,disabled,restart,restart-by-peer. 'interval' and 'timeout' are non negative integers.  'interval' should be less than 'timeout' value. 'action', default:hold 'interval', default:30, 'timeout', default:120.
                            enable: Enable IPSec site connection
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            ikepolicy: IKE policy associated with this connection (name or ID)
                            initiator: Initiator state
                            ipsecpolicy: IPsec policy associated with this connection (name or ID)
                            local-endpoint-group: Local endpoint group (name or ID) with subnet(s) for IPsec connection
                            local-id: An ID to be used instead of the external IP address for a virtual router
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            mtu: MTU size for the connection
                            noindent: whether to disable indenting the JSON
                            peer-address: Peer gateway public IPv4/IPv6 address or FQDN
                            peer-cidr: Remote subnet(s) in CIDR format. Cannot be specified when using endpoint groups. Only applicable, if subnet provided for VPN service.
                            peer-endpoint-group: Peer endpoint group (name or ID) with CIDR(s) for IPSec connection
                            peer-id: Peer router identity for authentication. Can be IPv4/IPv6 address, e-mail address, key id, or FQDN
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            project: Owner's project (name or ID)
                            project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                            psk: Pre-shared key string.
                            variable: ==SUPPRESS==
                            vpnservice: VPN service instance associated with this connection (name or ID)
                    - name: delete
                      description: Delete IPsec site connection(s)
                    - name: list
                      description: List IPsec site connections that belong to a given project
                      flags:
                        --fit-width: Fit the table to the display width.
                        --long: List additional fields in output
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --print-empty: Print empty table if there is no data to show.
                        --quote=: when to include quotes, defaults to nonnumeric
                        --sort-ascending: sort the column(s) in ascending order
                        --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                        --sort-descending: sort the column(s) in descending order
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - csv
                                - json
                                - table
                                - value
                                - yaml
                            quote:
                                - all
                                - minimal
                                - none
                                - nonnumeric
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            long: List additional fields in output
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            print-empty: Print empty table if there is no data to show.
                            quote: when to include quotes, defaults to nonnumeric
                            sort-ascending: sort the column(s) in ascending order
                            sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                            sort-descending: sort the column(s) in descending order
                    - name: set
                      description: Set IPsec site connection properties
                      flags:
                        --description=: Description for the connection
                        --disable: Disable IPSec site connection
                        --dpd=: IPSec Connection Dead Peer Detection attributes.
                        --enable: Enable IPSec site connection
                        --initiator=: Initiator state
                        --local-endpoint-group=: Local endpoint group (name or ID) with subnet(s) for IPsec connection
                        --local-id=: An ID to be used instead of the external IP address for a virtual router
                        --mtu=: MTU size for the connection
                        --name=: Set friendly name for the connection
                        --peer-address=: Peer gateway public IPv4/IPv6 address or FQDN
                        --peer-cidr=: Remote subnet(s) in CIDR format.
                        --peer-endpoint-group=: Peer endpoint group (name or ID) with CIDR(s) for IPSec connection
                        --peer-id=: Peer router identity for authentication.
                      completion:
                        flag:
                            initiator:
                                - bi-directional
                                - response-only
                      documentation:
                        flag:
                            description: Description for the connection
                            disable: Disable IPSec site connection
                            dpd: IPSec Connection Dead Peer Detection attributes. 'action'-hold,clear,disabled,restart,restart-by-peer. 'interval' and 'timeout' are non negative integers.  'interval' should be less than 'timeout' value. 'action', default:hold 'interval', default:30, 'timeout', default:120.
                            enable: Enable IPSec site connection
                            initiator: Initiator state
                            local-endpoint-group: Local endpoint group (name or ID) with subnet(s) for IPsec connection
                            local-id: An ID to be used instead of the external IP address for a virtual router
                            mtu: MTU size for the connection
                            name: Set friendly name for the connection
                            peer-address: Peer gateway public IPv4/IPv6 address or FQDN
                            peer-cidr: Remote subnet(s) in CIDR format. Cannot be specified when using endpoint groups. Only applicable, if subnet provided for VPN service.
                            peer-endpoint-group: Peer endpoint group (name or ID) with CIDR(s) for IPSec connection
                            peer-id: Peer router identity for authentication. Can be IPv4/IPv6 address, e-mail address, key id, or FQDN
                    - name: show
                      description: Show information of a given IPsec site connection
                      flags:
                        --fit-width: Fit the table to the display width.
                        --max-width=: Maximum display width, <1 to disable.
                        --noindent: whether to disable indenting the JSON
                        --prefix=: add a prefix to all variable names
                        --print-empty: Print empty table if there is no data to show.
                        --variable=: ==SUPPRESS==
                        -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                        -f, --format=: the output format, defaults to table
                      completion:
                        flag:
                            format:
                                - json
                                - shell
                                - table
                                - value
                                - yaml
                      documentation:
                        flag:
                            column: specify the column(s) to include, can be repeated to show multiple columns
                            fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                            format: the output format, defaults to table
                            max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                            noindent: whether to disable indenting the JSON
                            prefix: add a prefix to all variable names
                            print-empty: Print empty table if there is no data to show.
                            variable: ==SUPPRESS==
        - name: service
          commands:
            - name: create
              description: Create an VPN service
              flags:
                --description=: Description for the VPN service
                --disable: Disable VPN service
                --enable: Enable VPN service
                --fit-width: Fit the table to the display width.
                --flavor=: Flavor for the VPN service (name or ID)
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --project-domain=: Domain the project belongs to (name or ID).
                --project=: Owner's project (name or ID)
                --router=!: Router for the VPN service (name or ID)
                --subnet=: Local private subnet (name or ID)
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    description: Description for the VPN service
                    disable: Disable VPN service
                    enable: Enable VPN service
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    flavor: Flavor for the VPN service (name or ID)
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    project: Owner's project (name or ID)
                    project-domain: Domain the project belongs to (name or ID). This can be used in case collisions between project names exist.
                    router: Router for the VPN service (name or ID)
                    subnet: Local private subnet (name or ID)
                    variable: ==SUPPRESS==
            - name: delete
              description: Delete VPN service(s)
            - name: list
              description: List VPN services that belong to a given project
              flags:
                --fit-width: Fit the table to the display width.
                --long: List additional fields in output
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --print-empty: Print empty table if there is no data to show.
                --quote=: when to include quotes, defaults to nonnumeric
                --sort-ascending: sort the column(s) in ascending order
                --sort-column=: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                --sort-descending: sort the column(s) in descending order
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - csv
                        - json
                        - table
                        - value
                        - yaml
                    quote:
                        - all
                        - minimal
                        - none
                        - nonnumeric
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    long: List additional fields in output
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    print-empty: Print empty table if there is no data to show.
                    quote: when to include quotes, defaults to nonnumeric
                    sort-ascending: sort the column(s) in ascending order
                    sort-column: specify the column(s) to sort the data (columns specified first have a priority, non-existing columns are ignored), can be repeated
                    sort-descending: sort the column(s) in descending order
            - name: set
              description: Set VPN service properties
              flags:
                --description=: Description for the VPN service
                --disable: Disable VPN service
                --enable: Enable VPN service
                --flavor=: Flavor for the VPN service (name or ID)
                --name=: Name for the VPN service
                --subnet=: Local private subnet (name or ID)
              documentation:
                flag:
                    description: Description for the VPN service
                    disable: Disable VPN service
                    enable: Enable VPN service
                    flavor: Flavor for the VPN service (name or ID)
                    name: Name for the VPN service
                    subnet: Local private subnet (name or ID)
            - name: show
              description: Display VPN service details
              flags:
                --fit-width: Fit the table to the display width.
                --max-width=: Maximum display width, <1 to disable.
                --noindent: whether to disable indenting the JSON
                --prefix=: add a prefix to all variable names
                --print-empty: Print empty table if there is no data to show.
                --variable=: ==SUPPRESS==
                -c, --column=: specify the column(s) to include, can be repeated to show multiple columns
                -f, --format=: the output format, defaults to table
              completion:
                flag:
                    format:
                        - json
                        - shell
                        - table
                        - value
                        - yaml
              documentation:
                flag:
                    column: specify the column(s) to include, can be repeated to show multiple columns
                    fit-width: Fit the table to the display width. Implied if --max-width greater than 0. Set the environment variable CLIFF_FIT_WIDTH=1 to always enable
                    format: the output format, defaults to table
                    max-width: Maximum display width, <1 to disable. You can also use the CLIFF_MAX_TERM_WIDTH environment variable, but the parameter takes precedence.
                    noindent: whether to disable indenting the JSON
                    prefix: add a prefix to all variable names
                    print-empty: Print empty table if there is no data to show.
                    variable: ==SUPPRESS==
documentation:
    flag:
        debug: Show tracebacks on errors.
        insecure: Disable server certificate verification
        log-file: Specify a file to log output. Disabled by default.
        os-access-token: |-
            With v3oidcaccesstoken: OAuth 2.0 Access Token

            (Env: OS_ACCESS_TOKEN)
        os-access-token-endpoint: |-
            With v3oidcclientcredentials: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.
            With v3oidcpassword: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.
            With v3oidcauthcode: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.
            With v3oidcdeviceauthz: OpenID Connect Provider Token Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.

            (Env: OS_ACCESS_TOKEN_ENDPOINT)
        os-access-token-type: |-
            With v3oidcclientcredentials: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"
            With v3oidcpassword: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"
            With v3oidcauthcode: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"
            With v3oidcdeviceauthz: OAuth 2.0 Authorization Server Introspection token type, it is used to decide which type of token will be used when processing token introspection. Valid values are: "access_token" or "id_token"

            (Env: OS_ACCESS_TOKEN_TYPE)
        os-application-credential-id: |-
            With v3applicationcredential: Application credential ID

            (Env: OS_APPLICATION_CREDENTIAL_ID)
        os-application-credential-name: |-
            With v3applicationcredential: Application credential name

            (Env: OS_APPLICATION_CREDENTIAL_NAME)
        os-application-credential-secret: |-
            With v3applicationcredential: Application credential auth secret

            (Env: OS_APPLICATION_CREDENTIAL_SECRET)
        os-auth-methods: |-
            With v3multifactor: Methods to authenticate with.

            (Env: OS_AUTH_METHODS)
        os-auth-type: 'Select an authentication type. Available types: v3token, v3tokenlessauth, v2token, v3oidcclientcredentials, none, v2password, v3multifactor, v3oidcpassword, v3oidcaccesstoken, v3oidcauthcode, v3websso, v3oauth2mtlsclientcredential, v3oidcdeviceauthz, v3totp, v3applicationcredential, admin_token, v3oauth2clientcredential, http_basic, token, password, v3password. Default: selected based on --os-username/--os-token (Env: OS_AUTH_TYPE)'
        os-auth-url: |-
            With v3token: Authentication URL
            With v3tokenlessauth: Authentication URL
            With v2token: Authentication URL
            With v3oidcclientcredentials: Authentication URL
            With v2password: Authentication URL
            With v3multifactor: Authentication URL
            With v3oidcpassword: Authentication URL
            With v3oidcaccesstoken: Authentication URL
            With v3oidcauthcode: Authentication URL
            With v3websso: Authentication URL
            With v3oauth2mtlsclientcredential: Authentication URL
            With v3oidcdeviceauthz: Authentication URL
            With v3totp: Authentication URL
            With v3applicationcredential: Authentication URL
            With v3oauth2clientcredential: Authentication URL
            With token: Authentication URL
            With password: Authentication URL
            With v3password: Authentication URL

            (Env: OS_AUTH_URL)
        os-beta-command: Enable beta commands which are subject to change
        os-cacert: 'CA certificate bundle file (Env: OS_CACERT)'
        os-cert: 'Client certificate bundle file (Env: OS_CERT)'
        os-client-id: |-
            With v3oidcclientcredentials: OAuth 2.0 Client ID
            With v3oidcpassword: OAuth 2.0 Client ID
            With v3oidcauthcode: OAuth 2.0 Client ID
            With v3oidcdeviceauthz: OAuth 2.0 Client ID

            (Env: OS_CLIENT_ID)
        os-client-secret: |-
            With v3oidcclientcredentials: OAuth 2.0 Client Secret
            With v3oidcpassword: OAuth 2.0 Client Secret
            With v3oidcauthcode: OAuth 2.0 Client Secret
            With v3oidcdeviceauthz: OAuth 2.0 Client Secret

            (Env: OS_CLIENT_SECRET)
        os-cloud: 'Cloud name in clouds.yaml (Env: OS_CLOUD)'
        os-code: |-
            With v3oidcauthcode: OAuth 2.0 Authorization Code

            (Env: OS_CODE)
        os-code-challenge-method: |-
            With v3oidcdeviceauthz: PKCE Challenge Method (RFC 7636)

            (Env: OS_CODE_CHALLENGE_METHOD)
        os-compute-api-version: 'Compute API version, default=2.1 (Env: OS_COMPUTE_API_VERSION)'
        os-default-domain: 'Default domain ID, default=default. (Env: OS_DEFAULT_DOMAIN)'
        os-default-domain-id: |-
            With token: Optional domain ID to use with v3 and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.
            With password: Optional domain ID to use with v3 and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.

            (Env: OS_DEFAULT_DOMAIN_ID)
        os-default-domain-name: |-
            With token: Optional domain name to use with v3 API and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.
            With password: Optional domain name to use with v3 API and v2 parameters. It will be used for both the user and project domain in v3 and ignored in v2 authentication.

            (Env: OS_DEFAULT_DOMAIN_NAME)
        os-device-authorization-endpoint: |-
            With v3oidcdeviceauthz: OAuth 2.0 Device Authorization Endpoint. Note that if a discovery document is being passed this option will override the endpoint provided by the server in the discovery document.

            (Env: OS_DEVICE_AUTHORIZATION_ENDPOINT)
        os-discovery-endpoint: |-
            With v3oidcclientcredentials: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration
            With v3oidcpassword: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration
            With v3oidcauthcode: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration
            With v3oidcdeviceauthz: OpenID Connect Discovery Document URL. The discovery document will be used to obtain the values of the access token endpoint and the authentication endpoint. This URL should look like https://idp.example.org/.well-known/openid-configuration

            (Env: OS_DISCOVERY_ENDPOINT)
        os-domain-id: |-
            With v3token: Domain ID to scope to
            With v3tokenlessauth: Domain ID to scope to
            With v3oidcclientcredentials: Domain ID to scope to
            With v3multifactor: Domain ID to scope to
            With v3oidcpassword: Domain ID to scope to
            With v3oidcaccesstoken: Domain ID to scope to
            With v3oidcauthcode: Domain ID to scope to
            With v3websso: Domain ID to scope to
            With v3oauth2mtlsclientcredential: Domain ID to scope to
            With v3oidcdeviceauthz: Domain ID to scope to
            With v3totp: Domain ID to scope to
            With v3applicationcredential: Domain ID to scope to
            With v3oauth2clientcredential: Domain ID to scope to
            With token: Domain ID to scope to
            With password: Domain ID to scope to
            With v3password: Domain ID to scope to

            (Env: OS_DOMAIN_ID)
        os-domain-name: |-
            With v3token: Domain name to scope to
            With v3tokenlessauth: Domain name to scope to
            With v3oidcclientcredentials: Domain name to scope to
            With v3multifactor: Domain name to scope to
            With v3oidcpassword: Domain name to scope to
            With v3oidcaccesstoken: Domain name to scope to
            With v3oidcauthcode: Domain name to scope to
            With v3websso: Domain name to scope to
            With v3oauth2mtlsclientcredential: Domain name to scope to
            With v3oidcdeviceauthz: Domain name to scope to
            With v3totp: Domain name to scope to
            With v3applicationcredential: Domain name to scope to
            With v3oauth2clientcredential: Domain name to scope to
            With token: Domain name to scope to
            With password: Domain name to scope to
            With v3password: Domain name to scope to

            (Env: OS_DOMAIN_NAME)
        os-endpoint: |-
            With none: The endpoint that will always be used
            With admin_token: The endpoint that will always be used
            With http_basic: The endpoint that will always be used

            (Env: OS_ENDPOINT)
        os-endpoint-override: Use this API endpoint instead of the Service Catalog. Defaults to env[OS_SHARED_FILE_SYSTEM_ENDPOINT_OVERRIDE].
        os-identity-api-version: 'Identity API version, default=3 (Env: OS_IDENTITY_API_VERSION)'
        os-identity-provider: |-
            With v3oidcclientcredentials: Identity Provider's name
            With v3oidcpassword: Identity Provider's name
            With v3oidcaccesstoken: Identity Provider's name
            With v3oidcauthcode: Identity Provider's name
            With v3websso: Identity Provider's name
            With v3oidcdeviceauthz: Identity Provider's name

            (Env: OS_IDENTITY_PROVIDER)
        os-idp-otp-key: |-
            With v3oidcpassword: A key to be used in the Identity Provider access token endpoint to pass the OTP value. E.g. totp

            (Env: OS_IDP_OTP_KEY)
        os-image-api-version: 'Image API version, default=2 (Env: OS_IMAGE_API_VERSION)'
        os-interface: 'Select an interface type. Valid interface types: [admin, public, internal]. default=public, (Env: OS_INTERFACE)'
        os-key: 'Client certificate key file (Env: OS_KEY)'
        os-login-timeout: |-
            With v3websso: Seconds to wait for the user to finish authenticating in the browser before giving up. Raise it when a first login or MFA prompt is likely to take longer than the default.

            (Env: OS_LOGIN_TIMEOUT)
        os-network-api-version: 'Network API version, default=2.0 (Env: OS_NETWORK_API_VERSION)'
        os-oauth2-client-id: |-
            With v3oauth2mtlsclientcredential: Client credential ID for OAuth2.0 Mutual-TLS Authorization
            With v3oauth2clientcredential: Client id for OAuth2.0

            (Env: OS_OAUTH2_CLIENT_ID)
        os-oauth2-client-secret: |-
            With v3oauth2clientcredential: Client secret for OAuth2.0

            (Env: OS_OAUTH2_CLIENT_SECRET)
        os-oauth2-endpoint: |-
            With v3oauth2mtlsclientcredential: Endpoint for OAuth2.0 Mutual-TLS Authorization
            With v3oauth2clientcredential: Endpoint for OAuth2.0

            (Env: OS_OAUTH2_ENDPOINT)
        os-object-api-version: 'Object API version, default=1 (Env: OS_OBJECT_API_VERSION)'
        os-openid-scope: |-
            With v3oidcclientcredentials: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.
            With v3oidcpassword: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.
            With v3oidcauthcode: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.
            With v3oidcdeviceauthz: OpenID Connect scope that is requested from authorization server. Note that the OpenID Connect specification states that "openid" must be always specified.

            (Env: OS_OPENID_SCOPE)
        os-passcode: |-
            With v3totp: User's TOTP passcode

            (Env: OS_PASSCODE)
        os-password: |-
            With v2password: Password to use
            With v3oidcpassword: Password
            With http_basic: User's password
            With password: User's password
            With v3password: User's password

            (Env: OS_PASSWORD)
        os-project-domain-id: |-
            With v3token: Domain ID containing project
            With v3tokenlessauth: Domain ID containing project
            With v3oidcclientcredentials: Domain ID containing project
            With v3multifactor: Domain ID containing project
            With v3oidcpassword: Domain ID containing project
            With v3oidcaccesstoken: Domain ID containing project
            With v3oidcauthcode: Domain ID containing project
            With v3websso: Domain ID containing project
            With v3oauth2mtlsclientcredential: Domain ID containing project
            With v3oidcdeviceauthz: Domain ID containing project
            With v3totp: Domain ID containing project
            With v3applicationcredential: Domain ID containing project
            With v3oauth2clientcredential: Domain ID containing project
            With token: Domain ID containing project
            With password: Domain ID containing project
            With v3password: Domain ID containing project

            (Env: OS_PROJECT_DOMAIN_ID)
        os-project-domain-name: |-
            With v3token: Domain name containing project
            With v3tokenlessauth: Domain name containing project
            With v3oidcclientcredentials: Domain name containing project
            With v3multifactor: Domain name containing project
            With v3oidcpassword: Domain name containing project
            With v3oidcaccesstoken: Domain name containing project
            With v3oidcauthcode: Domain name containing project
            With v3websso: Domain name containing project
            With v3oauth2mtlsclientcredential: Domain name containing project
            With v3oidcdeviceauthz: Domain name containing project
            With v3totp: Domain name containing project
            With v3applicationcredential: Domain name containing project
            With v3oauth2clientcredential: Domain name containing project
            With token: Domain name containing project
            With password: Domain name containing project
            With v3password: Domain name containing project

            (Env: OS_PROJECT_DOMAIN_NAME)
        os-project-id: |-
            With v3token: Project ID to scope to
            With v3tokenlessauth: Project ID to scope to
            With v3oidcclientcredentials: Project ID to scope to
            With v3multifactor: Project ID to scope to
            With v3oidcpassword: Project ID to scope to
            With v3oidcaccesstoken: Project ID to scope to
            With v3oidcauthcode: Project ID to scope to
            With v3websso: Project ID to scope to
            With v3oauth2mtlsclientcredential: Project ID to scope to
            With v3oidcdeviceauthz: Project ID to scope to
            With v3totp: Project ID to scope to
            With v3applicationcredential: Project ID to scope to
            With v3oauth2clientcredential: Project ID to scope to
            With token: Project ID to scope to
            With password: Project ID to scope to
            With v3password: Project ID to scope to

            (Env: OS_PROJECT_ID)
        os-project-name: |-
            With v3token: Project name to scope to
            With v3tokenlessauth: Project name to scope to
            With v3oidcclientcredentials: Project name to scope to
            With v3multifactor: Project name to scope to
            With v3oidcpassword: Project name to scope to
            With v3oidcaccesstoken: Project name to scope to
            With v3oidcauthcode: Project name to scope to
            With v3websso: Project name to scope to
            With v3oauth2mtlsclientcredential: Project name to scope to
            With v3oidcdeviceauthz: Project name to scope to
            With v3totp: Project name to scope to
            With v3applicationcredential: Project name to scope to
            With v3oauth2clientcredential: Project name to scope to
            With token: Project name to scope to
            With password: Project name to scope to
            With v3password: Project name to scope to

            (Env: OS_PROJECT_NAME)
        os-protocol: |-
            With v3oidcclientcredentials: Protocol for federated plugin
            With v3oidcpassword: Protocol for federated plugin
            With v3oidcaccesstoken: Protocol for federated plugin
            With v3oidcauthcode: Protocol for federated plugin
            With v3websso: Protocol for federated plugin
            With v3oidcdeviceauthz: Protocol for federated plugin

            (Env: OS_PROTOCOL)
        os-redirect-host: |-
            With v3websso: Host the callback server listens on. This must be a loopback address.

            (Env: OS_REDIRECT_HOST)
        os-redirect-port: |-
            With v3websso: Port the callback server listens on. The resulting callback URL, http://<redirect-host>:<redirect-port>/auth/websso/, must be listed in the identity service's [federation] trusted_dashboard option.

            (Env: OS_REDIRECT_PORT)
        os-redirect-uri: |-
            With v3oidcauthcode: OpenID Connect Redirect URL

            (Env: OS_REDIRECT_URI)
        os-region-name: 'Authentication region name (Env: OS_REGION_NAME)'
        os-remote-project-domain-id: Domain ID of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
        os-remote-project-domain-name: Domain name of the project when authenticating to a service provider if using Keystone-to-Keystone federation.
        os-remote-project-id: Project ID when authenticating to a service provider if using Keystone-to-Keystone federation.
        os-remote-project-name: Project name when authenticating to a service provider if using Keystone-to-Keystone federation.
        os-service-provider: Authenticate with and perform the command on a service provider using Keystone-to-keystone federation. Must also specify the remote project option.
        os-share-api-version: 'Shared File System API version, default=2.98 (version supported by both the client and the server) (Env: OS_SHARE_API_VERSION)'
        os-system-scope: |-
            With v3token: Scope for system operations
            With v3oidcclientcredentials: Scope for system operations
            With v3multifactor: Scope for system operations
            With v3oidcpassword: Scope for system operations
            With v3oidcaccesstoken: Scope for system operations
            With v3oidcauthcode: Scope for system operations
            With v3websso: Scope for system operations
            With v3oauth2mtlsclientcredential: Scope for system operations
            With v3oidcdeviceauthz: Scope for system operations
            With v3totp: Scope for system operations
            With v3applicationcredential: Scope for system operations
            With v3oauth2clientcredential: Scope for system operations
            With token: Scope for system operations
            With password: Scope for system operations
            With v3password: Scope for system operations

            (Env: OS_SYSTEM_SCOPE)
        os-tenant-id: ==SUPPRESS==
        os-tenant-name: ==SUPPRESS==
        os-token: |-
            With v3token: Token to authenticate with
            With v2token: Token
            With admin_token: The token that will always be used
            With token: Token to authenticate with

            (Env: OS_TOKEN)
        os-trust-id: |-
            With v3token: ID of the trust to use as a trustee use
            With v2token: ID of the trust to use as a trustee use
            With v3oidcclientcredentials: ID of the trust to use as a trustee use
            With v2password: ID of the trust to use as a trustee use
            With v3multifactor: ID of the trust to use as a trustee use
            With v3oidcpassword: ID of the trust to use as a trustee use
            With v3oidcaccesstoken: ID of the trust to use as a trustee use
            With v3oidcauthcode: ID of the trust to use as a trustee use
            With v3websso: ID of the trust to use as a trustee use
            With v3oauth2mtlsclientcredential: ID of the trust to use as a trustee use
            With v3oidcdeviceauthz: ID of the trust to use as a trustee use
            With v3totp: ID of the trust to use as a trustee use
            With v3applicationcredential: ID of the trust to use as a trustee use
            With v3oauth2clientcredential: ID of the trust to use as a trustee use
            With token: ID of the trust to use as a trustee use
            With password: ID of the trust to use as a trustee use
            With v3password: ID of the trust to use as a trustee use

            (Env: OS_TRUST_ID)
        os-user-domain-id: |-
            With v3totp: User's domain ID
            With v3applicationcredential: User's domain ID
            With password: User's domain id
            With v3password: User's domain ID

            (Env: OS_USER_DOMAIN_ID)
        os-user-domain-name: |-
            With v3totp: User's domain name
            With v3applicationcredential: User's domain name
            With password: User's domain name
            With v3password: User's domain name

            (Env: OS_USER_DOMAIN_NAME)
        os-user-id: |-
            With v2password: User ID to login with
            With v3totp: User's user ID
            With v3applicationcredential: User's user ID
            With password: User id
            With v3password: User's user ID

            (Env: OS_USER_ID)
        os-username: |-
            With v2password: Username to login with
            With v3oidcpassword: Username
            With v3websso: Optional label used only to keep cached tokens for different SSO accounts apart. It is not used to authenticate; the identity is chosen in the browser. Set a distinct value per account, or leave it unset if only one account is used.
            With v3totp: User's username
            With v3applicationcredential: User's username
            With http_basic: Username
            With password: Username
            With v3password: User's username

            (Env: OS_USERNAME)
        os-volume-api-version: 'Volume API version, default=3 (Env: OS_VOLUME_API_VERSION)'
        quiet: Suppress output except warnings and errors.
        timing: Print API call timing info
        verbose: Increase verbosity of output. Can be repeated.
        verify: Verify server certificate (default)
        version: show program's version number and exit
