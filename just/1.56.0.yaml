# yaml-language-server: $schema=https://carapace.sh/schemas/command.json
name: just
description: 🤖 Just a command runner - https://github.com/casey/just
flags:
  --alias-style=:
    description: Set list command alias display style
    default: right
  --allow-missing: Ignore missing recipe and module errors
  --ceiling=: Do not ascend above <CEILING> directory when searching for a justfile.
  --changelog: Print changelog
  --check: Run `--fmt` in 'check' mode. Exits with 0 if justfile is formatted correctly. Exits with 1 and prints a diff if formatting is required.
  --choose: Select one or more recipes to run using a binary chooser. If `--chooser` is not passed the chooser defaults to the value of $JUST_CHOOSER, falling back to `fzf`
  --chooser=: Override binary invoked by `--choose`
  --clean=*:
    description: Clear recipe cache, optionally restricted to recipes whose path begins with <RECIPE_PATH>
    nargs: -1
  --clear-shell-args: Clear shell arguments
  --color=:
    description: Print colorful output
    default: auto
  -c, --command=*:
    description: Run an arbitrary command with the working directory, `.env`, overrides, and exports set
    nargs: -1
  --command-color=: Echo recipe lines in <COMMAND-COLOR>
  --complete-aliases: Auto-complete recipe aliases
  --completions=: Print shell completion script for <SHELL>
  --cygpath=:
    description: Use binary at <CYGPATH> to convert between unix and Windows paths
    default: cygpath
  --default-list: List recipes when no arguments are provided
  --dotenv-command=*: Run <COMMAND> and load its output as an environment file
  -F, --dotenv-filename=*: Search for an environment file named <DOTENV-FILENAME> instead of `.env`
  -E, --dotenv-path=*: Load <DOTENV-PATH> as an environment file instead of searching for one
  -n, --dry-run: Print what just would do without doing it
  --dump: Print justfile
  --dump-format=:
    description: Dump justfile as <FORMAT>
    default: just
  -e, --edit: Edit justfile with editor given by $VISUAL or $EDITOR, falling back to `vim`
  --evaluate: Evaluate and print all variables. If a variable name is given as an argument, only print that variable's value.
  --eval&: Evaluate and print all variables. If a variable name is given as an argument, only print that variable's value.
  --evaluate-format=:
    description: Print evaluated variables in <FORMAT>
    default: just
  --explain: Print recipe doc comment before running it
  --fmt: Format and overwrite justfile
  --format&: Format and overwrite justfile
  -g, --global-justfile: Use global justfile
  --group=*: Only list recipes in <GROUP>
  --groups: List recipe groups
  -h, --help: Print help
  --highlight: Highlight echoed recipe lines in bold
  --indentation=: Indent recipes bodies with <INDENTATION>
  --init: Initialize new justfile in project root
  --initialize&: Initialize new justfile in project root
  --jobs=: Run at most <N> recipes simultaneously with the [parallel] attribute
  --json: Print justfile as JSON
  -f, --justfile=: Use <JUSTFILE> as justfile or `-` to read from standard input
  --justfile-name=*: Search for justfile named <NAME>
  -l, --list=*:
    description: List available recipes in <MODULE> or root if omitted
    nargs: -1
  --list-heading=:
    description: Print <TEXT> before list
    default: |
      Available recipes:
  --list-prefix=:
    description: Print <TEXT> before each list item
    default: '    '
  --list-submodules: List recipes in submodules
  --man: Print man page
  --no-aliases: Don't show aliases in list
  --no-cache: Bypass recipe cache
  --no-deps: Don't run recipe dependencies
  --no-dependencies&: Don't run recipe dependencies
  --no-dotenv: Don't load `.env` file
  --no-highlight: Don't highlight echoed recipe lines in bold
  --one: Forbid multiple recipes from being invoked on the command line
  -q, --quiet: Suppress all output
  --set=*:
    description: Override <VARIABLE> with <VALUE>
    nargs: 2
  --shell=: Invoke <SHELL> to run recipes
  --shell-arg=*: Invoke shell with <SHELL-ARG> as an argument
  --shell-command: Invoke <COMMAND> with the shell used to run recipe lines and backticks
  -s, --show=*:
    description: Show recipe at <RECIPE_PATH>
    nargs: -1
  --summary: List names of available recipes
  --tempdir=: Save temporary files to <TEMPDIR>.
  --time: Print recipe execution time
  --timestamp: Print recipe command timestamps
  --timestamp-format=:
    description: Timestamp format string
    default: '%H:%M:%S'
  -u, --unsorted: Return list and summary entries in source order
  --unstable: Enable unstable features
  --usage=*:
    description: Print usage information for recipe at <RECIPE_PATH>
    nargs: -1
  --variables: List names of variables
  -v, --verbose*: Use verbose output
  -V, --version: Print version
  -d, --working-directory=: Use <WORKING-DIRECTORY> as working directory. --justfile must also be set
  --yes: Automatically confirm all recipes.
completion:
  flag:
    alias-style:
    - left
    - right
    - separate
    ceiling:
    - $files
    chooser:
    - $files
    color:
    - always
    - auto
    - never
    command-color:
    - black
    - blue
    - cyan
    - green
    - purple
    - red
    - yellow
    completions:
    - bash
    - elvish
    - fish
    - nushell
    - powershell
    - zsh
    cygpath:
    - $files
    dump-format:
    - json
    - just
    evaluate-format:
    - just
    - shell
    justfile:
    - $files
    tempdir:
    - $files
    working-directory:
    - $files
