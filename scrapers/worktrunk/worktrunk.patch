diff --git a/src/main.rs b/src/main.rs
index 5711e6188..653c41f18 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -2,6 +2,10 @@ use std::collections::HashSet;
 use std::io::Write;
 use std::path::Path;
 
+use carapace_spec_clap::Spec;
+use clap_complete::generate;
+use std::io;
+
 use anyhow::Context;
 use clap::FromArgMatches;
 use clap::error::ErrorKind as ClapErrorKind;
@@ -1530,6 +1534,11 @@ fn print_help_to_stderr() {
 }
 
 fn main() {
+    generate(Spec, &mut cli::build_command(), "wt", &mut io::stdout());
+    if true {
+        return;
+    }
+
     // Capture the startup working directory before anything else. This is
     // used by shell_exec to resolve relative `GIT_*` path variables inherited
     // from a parent `git` (e.g. when invoked via `git wt ...` with
