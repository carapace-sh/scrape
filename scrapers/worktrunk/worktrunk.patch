diff --git a/src/main.rs b/src/main.rs
index e36f21c..390a290 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -12,6 +12,10 @@ use worktrunk::styling::{
 
 use commands::hooks::HookAnnouncer;
 
+use carapace_spec_clap::Spec;
+use clap_complete::generate;
+use std::io;
+
 mod cli;
 mod commands;
 mod completion;
@@ -1061,6 +1065,11 @@ fn print_help_to_stderr() {
 }
 
 fn main() {
+    generate(Spec, &mut cli::build_command(), "wt", &mut io::stdout());
+    if true {
+        return;
+    }
+
     // Capture startup state before anything else: the working directory
     // (used by shell_exec to resolve relative `GIT_*` path variables
     // inherited from a parent `git` — e.g. when invoked via `git wt ...`
