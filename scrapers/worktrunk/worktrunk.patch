diff --git a/src/main.rs b/src/main.rs
index 77f7cab..37374c3 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -12,6 +12,10 @@ use worktrunk::styling::{
 
 use commands::hooks::HookAnnouncer;
 
+use carapace_spec_clap::Spec;
+use clap_complete::generate;
+use std::io;
+
 mod cli;
 mod commands;
 mod completion;
@@ -1088,6 +1092,11 @@ fn print_help_to_stderr() {
 }
 
 fn main() {
+    generate(Spec, &mut cli::build_command(), "wt", &mut io::stdout());
+    if true {
+        return;
+    }
+
     // Mock-command playback for the test suite: when the harness linked this
     // binary onto PATH under another name (`gh`, `glab`, …), play back the
     // mock config and exit instead of running wt. Inert without
