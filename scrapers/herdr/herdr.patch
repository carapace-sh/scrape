diff --git a/src/cli.rs b/src/cli.rs
index 154e0c9..5723d10 100644
--- a/src/cli.rs
+++ b/src/cli.rs
@@ -34,7 +34,7 @@ mod protocol_guard;
 mod runtime;
 mod server;
 mod server_not_running;
-mod spec;
+pub(crate) mod spec;
 mod status;
 mod tab;
 mod target;
diff --git a/src/cli/spec.rs b/src/cli/spec.rs
index 9465dcc..9713c0a 100644
--- a/src/cli/spec.rs
+++ b/src/cli/spec.rs
@@ -5,7 +5,7 @@ use clap::{Arg, ArgAction, ArgGroup, Command, ValueHint};
 mod completion;
 mod machine;
 
-pub(super) fn command() -> Command {
+pub(crate) fn command() -> Command {
     let command = Command::new("herdr")
         .about("terminal workspace manager for AI coding agents")
         .disable_help_flag(true)
diff --git a/src/main.rs b/src/main.rs
index 8cfe9b0..52700a1 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -1,3 +1,5 @@
+use carapace_spec_clap::Spec;
+use clap_complete::generate;
 use std::io;
 
 pub(crate) const HERDR_ENV_VAR: &str = "HERDR_ENV";
@@ -501,6 +503,11 @@ fn finish_cli(outcome: io::Result<cli::CommandOutcome>) -> io::Result<()> {
 }
 
 fn main() -> io::Result<()> {
+    generate(Spec, &mut cli::spec::command(), "herdr", &mut io::stdout());
+    if true {
+        return Ok(());
+    }
+
     let raw_args: Vec<String> = match args_as_utf8(std::env::args_os()) {
         Ok(args) => args,
         Err(err) => {
