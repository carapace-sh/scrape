diff --git a/src/main.rs b/src/main.rs
index 244dd37..3c6ee94 100644
--- a/src/main.rs
+++ b/src/main.rs
@@ -11,8 +11,13 @@ use std::io::IsTerminal;
 use std::path::PathBuf;
 use std::time::Duration;
 
+use clap::CommandFactory;
 use clap::Parser;
 
+use carapace_spec_clap::Spec;
+use clap_complete::generate;
+use std::io;
+
 use config::Config;
 use git::Source;
 
@@ -73,6 +78,10 @@ struct Cli {
 }
 
 fn main() {
+    generate(Spec, &mut Cli::command(), "drift", &mut io::stdout());
+    if true {
+        return;
+    }
     if let Err(e) = run() {
         eprintln!("drift: {e}");
         std::process::exit(1);
